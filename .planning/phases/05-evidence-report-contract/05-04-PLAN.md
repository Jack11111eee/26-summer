---
phase: 05-evidence-report-contract
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - server/db.py
  - server/api/assessment.py
  - server/api/admin/feedback.py
  - server/services/report.py
  - server/test_phase5_feedback.py
autonomous: true
requirements: [REF-7.3]

must_haves:
  truths:
    - "候选人异议提交落 user_id 并留 REVIEW_FEEDBACK_RECEIVED 事件（一切留痕 D-003）"
    - "管理员 review/bad-case 的 note + reviewer_id + reviewed_at 持久化，不再静默丢弃"
    - "逐题回顾 question_reviews 携带 item_id（前端 itemReason 可锚定能力项）"
  artifacts:
    - path: "server/api/assessment.py"
      provides: "submit_feedback 落 user_id + 同事务 REVIEW_FEEDBACK_RECEIVED 事件"
      contains: "REVIEW_FEEDBACK_RECEIVED"
    - path: "server/api/admin/feedback.py"
      provides: "review_feedback/mark_bad_case 持久化 note/reviewer/reviewed_at"
      contains: "reviewed_at"
    - path: "server/services/report.py"
      provides: "_load_question_reviews 补 item_id"
      contains: "qs.item_id"
    - path: "server/db.py"
      provides: "feedback 表 + user_id/review_note/reviewer_id/reviewed_at 迁移"
      contains: "_migrate_feedback_phase5"
  key_links:
    - from: "server/api/assessment.py submit_feedback"
      to: "assessment_state_event"
      via: "append_event 同事务（commit 前）"
      pattern: "REVIEW_FEEDBACK_RECEIVED"
    - from: "server/api/admin/feedback.py review_feedback"
      to: "feedback.reviewer_id"
      via: "admin[\"user_id\"]"
      pattern: "reviewer_id"
    - from: "server/services/report.py _load_question_reviews"
      to: "question_score.item_id"
      via: "SELECT 加 qs.item_id"
      pattern: "qs\\.item_id"
---

<objective>
补全 feedback 审计链（REF-7.3）：feedback 表 + user_id/review_note/reviewer_id/reviewed_at；submit_feedback 落 user_id 且同事务写 REVIEW_FEEDBACK_RECEIVED 事件（D-003 一切留痕）；admin review/bad-case 持久化 note + reviewer_id + reviewed_at（废除现状「body.note 丢弃」）；_load_question_reviews 补 qs.item_id 使前端 itemReason 可锚定能力项。

Purpose: REF-7.3 SC 项「反馈可回溯、admin 处理留痕、逐题回顾锚定能力项」；D-003「一切留痕」、D-060 反馈契约锁定项。
Output: feedback 四列迁移 + 提交留痕事件 + admin 处理持久化 + question_reviews 补 item_id。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-evidence-report-contract/05-CONTEXT.md
@.planning/phases/05-evidence-report-contract/05-RESEARCH.md
@.planning/phases/05-evidence-report-contract/05-VALIDATION.md

<interfaces>
<!-- 本计划使用的既有契约 -->

From server/api/assessment.py:
- `submit_feedback(report_id, body, user)`（assessment.py:1114-1141）：INSERT INTO feedback(feedback_id, report_id, item_id, feedback_text, status, created_at)，无 user_id、无事件；`load_owned_report(conn, report_id, user)` 返回值未捕获（session_id 在返回 dict 中，本计划捕获用于事件）
- WR-06 JOIN 校验（1125-1133）：item_id 须属本报告会话锚定模型，`it is None → 404 "能力项不存在"`（保留不动）

From server/api/admin/feedback.py:
- `router = APIRouter(prefix="/api/admin/feedback", tags=["feedback"], dependencies=[Depends(require_admin)])`（feedback.py:9）
- `_ReviewBody(BaseModel): note: str = ""`（30-31）
- `review_feedback(feedback_id, body)`（34-44）：仅 `UPDATE feedback SET status='reviewed'`，丢弃 body.note
- `mark_bad_case(feedback_id, body)`（47-57）：仅 `UPDATE feedback SET status='bad_case'`，丢弃 body.note

From server/services/report.py:
- `_load_question_reviews(session_id)`（35-69）：SELECT 列表 qs.question_id/score_live/score_final/score_state/evidence_quote/reason + b.stem/qtype/std_name/category，缺 qs.item_id

From server/services/state_events.py:
- `append_event(conn, *, session_id, event_type, from_state=None, to_state=None, actor_type="system", actor_id=None, assessment_question_id=None, assessment_message_id=None, payload=None) -> None`（不 commit，调用者持事务；actor_type 校验 candidate/system/admin）

From server/core/security.py:
- `require_admin(user) -> user`（security.py:57-60，非 admin 403）
- `load_owned_report(conn, report_id, user)`（84-99）：返回 dict(r) 含 session_id

From server/db.py:
- feedback 表 DDL（db.py:283-290）：feedback_id/report_id/item_id/feedback_text/status(CHECK pending/reviewed/bad_case)/created_at
- `_migrate_question_bank_v2` ALTER ADD COLUMN 幂等形态（433-467）；init_db 注册点（680-699）

From server/test_m7_backend.py:
- `_ensure_admin`（28-46）、`_seed_report_and_item`（59-99）：本计划 test 文件 admin/seed helpers 照此形态内联
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 测试脚手架（test_phase5_feedback.py 三条 REF-7.3 断言）</name>
  <files>server/test_phase5_feedback.py</files>
  <read_first>
    - server/test_m5_backend.py（1-38 行三件套 + _q；43-73 行 _seed_position_with_confirmed_model）
    - server/test_m7_backend.py（28-56 行 admin helpers；59-99 行 _seed_report_and_item）
    - .planning/phases/05-evidence-report-contract/05-VALIDATION.md（58-60 行 REF-7.3 测试名）
  </read_first>
  <behavior>
    - test_feedback_audit_fields：submit_feedback 成功后 feedback 行 user_id == 候选人 user_id 非空；assessment_state_event 存在 event_type='REVIEW_FEEDBACK_RECEIVED' 且 actor_type='candidate'
    - test_admin_note_persisted：admin 调 /api/admin/feedback/{id}/review 传 note → feedback 行 review_note==note、reviewer_id==admin user_id、reviewed_at 非空；bad-case 同理 status=='bad_case'
    - test_question_reviews_has_item_id：造 session 后调 report._load_question_reviews(session_id)，返回 list 每条含 item_id 键且非空
  </behavior>
  <action>新建 server/test_phase5_feedback.py，复制 test_m5_backend.py 三件套头部（tempfile DB_PATH + LLM_PROVIDER=mock + JWT_SECRET + init_db + TestClient）+ _q 助手；import `from server.services.report import _load_question_reviews`。三条 test 按 05-VALIDATION.md 58-60 行命名（test_feedback_audit_fields / test_admin_note_persisted / test_question_reviews_has_item_id）。admin 侧 _ensure_admin/_admin_token/_auth/_seed_report_and_item 照 test_m7_backend.py 内联。逐文件命令 `cd server && python -m pytest test_phase5_feedback.py -v`（禁止同进程多文件）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_feedback.py -v 2>&1 | grep -E "KeyError|OperationalError|no such column|ImportError|failed" ; echo "RED 预期：feedback.user_id/review_note 列不存在 + REVIEW_FEEDBACK_RECEIVED 未写 + qs.item_id 未 select 导致失败"</automated>
  </verify>
  <acceptance_criteria>
    - test_phase5_feedback.py 三条 test_* 函数名与 05-VALIDATION.md 58-60 行一致
    - pytest 因「no such column: feedback.user_id / reviewer_id」或 REVIEW_FEEDBACK_RECEIVED 断言失败而 RED
    - 三件套头部 + _q 形态与 M5/M7 一致（无 conftest）
  </acceptance_criteria>
  <done>test_phase5_feedback.py 建好三条断言，当前 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: feedback 表四列迁移 + submit_feedback 落 user_id + REVIEW_FEEDBACK_RECEIVED 事件</name>
  <files>server/db.py, server/api/assessment.py</files>
  <read_first>
    - server/db.py（283-290 feedback DDL；433-467 ALTER ADD COLUMN 幂等形态；680-699 init_db 注册点）
    - server/api/assessment.py（1114-1141 submit_feedback 全文）
    - server/services/state_events.py（14-48 append_event 签名）
  </read_first>
  <behavior>
    - _migrate_feedback_phase5 幂等：PRAGMA table_info 嗅探，逐列 ALTER ADD COLUMN user_id/review_note/reviewer_id/reviewed_at（全可空，无 DB CHECK）
    - submit_feedback INSERT 带 user_id=user["user_id"]，同事务 append_event(REVIEW_FEEDBACK_RECEIVED, actor_type='candidate')
    - 新行 user_id 非空；存量行 user_id NULL（迁移不虚构用户）
  </behavior>
  <action>server/db.py 新增 _migrate_feedback_phase5(conn)：`PRAGMA table_info(feedback)` 拿现有列名集合，缺失则 `ALTER TABLE feedback ADD COLUMN user_id TEXT` / `ADD COLUMN review_note TEXT` / `ADD COLUMN reviewer_id TEXT` / `ADD COLUMN reviewed_at TEXT`（四个 if 独立、幂等、可重复执行）；注册进 init_db 加在 _migrate_report_phase5 之后。改 server/api/assessment.py submit_feedback：`rpt = load_owned_report(conn, report_id, user)`（捕获返回 dict 拿 rpt["session_id"]）替换原未捕获调用；feedback INSERT 列扩展为 `(feedback_id, report_id, item_id, feedback_text, status, created_at, user_id)`，VALUES 追加 `user["user_id"]`；在 `conn.commit()` 之前调 `append_event(conn, session_id=rpt["session_id"], event_type="REVIEW_FEEDBACK_RECEIVED", actor_type="candidate", actor_id=user["user_id"], payload={"report_id": report_id, "item_id": item_id, "feedback_id": feedback_id})`；顶部 import 补 `from ...services.state_events import append_event`。保留 WR-06 JOIN 校验与返回体 `{"feedback_id": ..., "status": "pending"}` 不动。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_feedback.py::test_feedback_audit_fields -v</automated>
  </verify>
  <acceptance_criteria>
    - feedback 表含 user_id/review_note/reviewer_id/reviewed_at 四列（PRAGMA 确认）
    - submit_feedback INSERT 含 user_id；同事务 REVIEW_FEEDBACK_RECEIVED 事件（actor_type=candidate）
    - test_feedback_audit_fields 绿
  </acceptance_criteria>
  <done>feedback 四列迁移 + user_id 落库 + REVIEW_FEEDBACK_RECEIVED 事件完成，test_feedback_audit_fields 绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: admin review/bad-case note 持久化 + question_reviews 补 item_id</name>
  <files>server/api/admin/feedback.py, server/services/report.py</files>
  <read_first>
    - server/api/admin/feedback.py（9/30-57 review_feedback/mark_bad_case 丢弃 note 现状）
    - server/services/report.py（35-48 _load_question_reviews SELECT 列表）
  </read_first>
  <behavior>
    - review_feedback：UPDATE status='reviewed' + review_note=body.note + reviewer_id=admin["user_id"] + reviewed_at=now_iso()，rowcount==0 → 404
    - mark_bad_case：UPDATE status='bad_case' + 同三列持久化，rowcount==0 → 404
    - _load_question_reviews SELECT 补 qs.item_id，返回 dict 含 item_id
  </behavior>
  <action>改 server/api/admin/feedback.py：两个函数签名追加 `admin: dict = Depends(require_admin)`（拿 admin["user_id"]）。review_feedback 的 UPDATE 改为 `UPDATE feedback SET status='reviewed', review_note=?, reviewer_id=?, reviewed_at=? WHERE feedback_id=?`，参数 `(body.note, admin["user_id"], now_iso(), feedback_id)`；mark_bad_case 同形改为 `SET status='bad_case', review_note=?, reviewer_id=?, reviewed_at=?`；两者保留 `if cur.rowcount == 0: raise HTTPException(404, ...)`（置 commit 后判定）。`now_iso` 已从 `...services.pipeline import now_iso`（feedback.py:7 已有）。改 server/services/report.py `_load_question_reviews`：SELECT 列表 qs.question_id 之前补 `qs.item_id,`（列序无影响，dict(r) 自动透出 item_id 键）；其余（answer 回捞、evidence_quote）不动。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_feedback.py::test_admin_note_persisted test_phase5_feedback.py::test_question_reviews_has_item_id -v</automated>
  </verify>
  <acceptance_criteria>
    - review_feedback/mark_bad_case UPDATE 含 review_note/reviewer_id/reviewed_at（grep "reviewed_at" 于 admin/feedback.py）
    - 两函数签名含 `admin: dict = Depends(require_admin)`
    - _load_question_reviews SELECT 含 qs.item_id（grep "qs.item_id" 于 services/report.py）
    - test_admin_note_persisted / test_question_reviews_has_item_id 绿
  </acceptance_criteria>
  <done>admin note 持久化 + question_reviews item_id 完成，两条测试绿；test_phase5_feedback.py 三全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→服务端 | submit_feedback 走 require_login + load_owned_report 所有权校验；admin review 走 require_admin |
| 服务层→DB | feedback.user_id/reviewer_id 落库留痕；REVIEW_FEEDBACK_RECEIVED 事件 append-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Elevation of Privilege | server/api/admin/feedback.py review/bad-case | mitigate | router 级 dependencies=[Depends(require_admin)] + 函数签名 admin 注入（security.py:57-60 非 admin 403） |
| T-05-12 | Tampering | server/api/assessment.py submit_feedback | mitigate | 保留 WR-06 JOIN report→session→competency_item 校验（assessment.py:1125-1133），挂无关 model 的 item → 404 |
| T-05-13 | Repudiation | server/services/state_events.py | mitigate | submit_feedback 同事务 append_event REVIEW_FEEDBACK_RECEIVED（actor_type='candidate', actor_id=user_id），一切留痕 D-003 |
| T-05-14 | Tampering | server/api/admin/feedback.py note 丢弃 | mitigate | review_note/reviewer_id/reviewed_at 持久化，废除 body.note 静默丢弃 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
`cd server && python -m pytest test_phase5_feedback.py -v` 三全绿；回归 test_m7_backend.py（feedback/trace admin 侧）与 test_phase5_report.py（publish）绿。
</verification>

<success_criteria>
feedback 审计链闭环：user_id 落库 + REVIEW_FEEDBACK_RECEIVED 事件 + admin note/reviewer/reviewed_at 持久化 + question_reviews item_id；test_phase5_feedback.py 三全绿，test_m7 回归绿。
</success_criteria>

## Open Items for Hard Gate A

无新增开放项。本计划触及的既有锁定决策均直接落地：D-003 一切留痕（REVIEW_FEEDBACK_RECEIVED 事件 + reviewer_id/reviewed_at）；D-060 feedback 字段契约；N11 枚举校验（feedback.status 既有 CHECK 不动——迁移仅加可空审计列，无新 CHECK）。存量 feedback 行的 user_id 置 NULL（迁移不虚构用户，只有未来新提交才有 user_id 溯源）。

<output>
Create `.planning/phases/05-evidence-report-contract/05-04-SUMMARY.md` when done
</output>
