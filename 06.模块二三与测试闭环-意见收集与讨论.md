# 模块二/三与测试闭环 — 意见收集与讨论稿

- 版本：**v0.2**（2026-08-30，第一轮用户反馈消化版）
- 状态：**讨论中**（U 表未清空前不动代码）
- 范围：03 文档的模块 2（AI 动态测评与多轮对话）、模块 3（立体人才画像）、模块 4（测试/迭代闭环）
- 输入契约：模块一已实现（05 文档 + M1~M3 已闭环），本文档消费其 confirmed 模型快照

> **v0.2 修订记录**
> 1. 用户首轮反馈全部消化：各决策点已回填「用户决定」；
> 2. **D3 改写**：按用户「题库驱动」方案（岗位题库 + 通用题库），取代我原「一次出整卷」建议；
> 3. 新增 **§0.5 权重口径澄清**（两层权重到底怎么用，U3）；
> 4. 新增 **N1~N9**：用户提出的新需求及落地方案（难度递进、岗位背景绑定、题库双轨、流式、表单工具、上下文管理、题目可溯、题库接口、岗位粒度）；
> 5. 新增 **§U 未决冲突表**：U1~U4，需要用户拍板的仅剩这 4 条；
> 6. D17 排期按用户意见改为「联调全绿后最后做」；D19 改为「四模块合并成总设计文档持续维护」；
> 7. 新增 **§P Prompt 清单**：全部 prompt 登记在册，按用户要求本期先不实现，留待专题讨论。

---

## 0. 现状快照：模块一留下的硬契约（共识区，无需讨论）

| 契约 | 内容 | 对模块二/三的意义 |
|---|---|---|
| 输入 | `GET /api/assessment/positions/{id}/model` → confirmed 模型快照 | 测评的唯一依据 |
| 模型项结构 | `std_name / category(4类) / required_level(1~5，门槛项 NULL) / importance(3档) / weight(Σ=1) / years / gate / level_reason / occurrence / evidence` | 出题、打分共用的"尺子" |
| 门槛项 | `gate=true`（qualification 全部 + experience 年限项），二值判定语义已在 04/05 定义 | 模块三代码判定，不进对话 |
| 可配置常量 | `CATEGORY_RATIO / IMPORTANCE_COEF / 阈值 / LLM_RETRY` 在 config.py | 新常量沿用同一机制 |
| llm_trace 表 | 每次 LLM 调用 prompt+response 落库，call_type 枚举 | 模块二/三扩展新 call_type 即可 |
| 预留接口 | `POST /api/assessment/sessions` → 当前返回 501 | 模块二从此接手 |
| 工程惯例 | mock 模式（离线可演示全流程）、异步任务+轮询、JWT 双角色、Element Plus | 模块二/三直接继承 |

**继承的核心设计原则**：① LLM 不碰数字（权重代码算 → 分数算术也代码算）；② 人工是唯一权威（模型不被静默覆盖 → 分数可异议但不做"AI 自动改分"）；③ 一切留痕。

---

## 0.5 关键澄清：模块一的「权重」到底是哪两层（重要，先对齐）

模块一聚合时，权重的生成是**两层相乘、一次合成**：

```
第一层（类目间）：硬技能 : 软技能 : 经验 : 门槛 = 5.5 : 2 : 2 : 0.5  → 55% / 20% / 20% / 5%
第二层（类内）  ：类目份额按各项 importance 系数分摊（required 1.0 / preferred 0.6 / plus 0.3）
                ↓ 合成结果
最终 item.weight（Σ=1，已存进 competency_item.weight）
```

**由此推出两个对齐结论**（用户在 D9/D10/D11 要求对齐的就是这里）：

1. 模块三算总分，**直接复用已合成的 `item.weight`**，不得再乘一次 5.5:2:2:0.5——再乘就是双重计费。
2. 用户提的「权重高题多、递进慢」里的"权重"，建议落到**类目权重**上（题量按 55/20/20/5 分配），难度递进速度也按类目分档（见 N1）。若落到单个能力项上，经验类只有 2 题，难度阶梯展不开。

> 此条即 **U3**，请确认或纠正。

---

# Part I 主链路：测评（模块二）与画像（模块三）

## 1. 出题与题库

### D1. 题型与能力类目的映射 —— ✅ 已定：A 继承

| category | 题型 | 测评方式 |
|---|---|---|
| hard_skill | 技术问答 / 场景小设计（不上代码评测器） | 对话中考察 |
| soft_skill | 情景行为题（STAR） | 对话中考察 |
| experience | 经历追问（项目深挖） + 客观事实题 | 对话 + 表单 |
| qualification | **不进对话**，结构化表单填报 + 代码校验 | 见 D8/N5 |

补充（用户提出）：hard_skill 内含**客观题**（有精确答案，如字段认读、概念辨析），此类题题干带 `answer_key`，评分走代码（正则/算法匹配），不经 LLM——见 D9。

### D2. 题量策略 —— ✅ 已定：B 按权重分配（联动 N1）

总题数 10~12 题，**按类目权重分配**：硬技能约 6~7 题、软技能约 2~3 题、经验约 2 题、门槛走表单不占题。required 项必考，preferred/plus 按类内权重竞争题位。

### D3. 出题与题库模式 —— ✅ 按用户方案改写（题库驱动）

取代我原版"测评开始时一次出整卷"，采用用户方案：

```
岗位模型 confirmed（人审动作）
   → 异步触发「题库锁定/生成」：LLM 批量出题，prompt 携带
     [岗位名 + 模型项定义 + required_level 锚点 + 难度档位]
   → 产出该岗位的【岗位题库】（hard_skill / soft_skill，position_id 绑定）
   + 复用【通用题库】（experience / qualification，跨岗位，position_id=NULL）
对话推进中：
   → 用户选定岗位 → 题库范围 = 该岗位题库 ∪ 通用题库
   → 每轮 D4 决策 action=next 时，【代码】按选题规则取下一题：
     当前 item 完成度 + 难度递进（N1）+ 类目剩余题量
   → LLM 可对题面做岗位化/口语化轻包装，但 question_id 落库绑定，可溯
```

设计要点：
- **选题是代码做的**（确定性规则），LLM 只负责话术与追问——选题依据可审计，避免"AI 为什么问这题"无法解释；
- 题目与能力项**强绑定**（N7），无绑定的题不允许进入对话；
- 题库行的 `question_id` .supports 后续替换为人工/外部真实题库（N8）。

### N1. 难度体系与递进策略（用户提出，我的落地）

- **难度分档按类目差异化**（用户原意：权重小快递进或保持低难度；experience/qualification 是事实问题无需难度）：

| 类目 | 档位 | 递进策略 |
|---|---|---|
| hard_skill | 简单 / 中等 / 困难 | **慢递进**：答对当前档 → 同能力项升一档；答错/含糊 → 保持或降一档；权重越高的项走完的档位越多 |
| soft_skill | 简单 / 困难（2 档即可） | 快递进：1 题基础情景达标后可直接进压力情景 |
| experience | 无难度 | 事实深挖（追问项目细节来自动加深，不设显式档位） |
| qualification | 无难度 | 表单 + 代码校验，不进对话 |

- 递进状态记在每个 session 的 `(item, current_difficulty)` 上，选题规则（D3）消费它。
- **我的说明**：这与 D5 的追问上限不冲突——追问是"同一题内挖深"，递进是"换更难的题"，两个维度。

### N2. 岗位背景绑定（用户提出，已并入 D3/N3）

核心论点：**同一能力在不同岗位的要求不同**。精通 Python 对"AI Agent 开发工程师"和"数据分析师"必须出不同的题。落地方式：
- 岗位题库的题，出题 prompt 强制携带岗位名 + 该岗位模型中此项的 `required_level / definition / evidence`，题目必须体现岗位场景；
- 评分 prompt 同理携带岗位背景——同一回答在不同岗位语境下得分可以不同（这是合理的，而非 bug）；
- 通用题库只装真正与岗位无关的事实题（年限、学历、证书）。

### N3. 题库双轨设计（schema 草案）

```sql
question_bank(
  question_id TEXT PRIMARY KEY,
  scope       TEXT NOT NULL CHECK(scope IN ('position','general')),
  position_id TEXT REFERENCES position,        -- scope=general 时为 NULL
  std_name TEXT NOT NULL, category TEXT NOT NULL,   -- 绑定能力项（N7）
  difficulty  TEXT CHECK(difficulty IN ('easy','medium','hard')),  -- N1；经验/门槛为 NULL
  qtype       TEXT NOT NULL CHECK(qtype IN ('objective','subjective')),
  stem TEXT NOT NULL,                          -- 题面
  answer_key TEXT,                             -- 客观题答案（代码判分用）
  rubric TEXT,                                 -- 主观题评分要点（给评分 prompt 用）
  source TEXT NOT NULL CHECK(source IN ('llm_seed','imported','human')),
  status TEXT NOT NULL DEFAULT 'active',       -- admin 可禁用
  created_at TEXT NOT NULL
)
```

### N7. 题目—模型可溯约束（用户提出，硬规则）

**每道题必须四要素齐全才允许进入对话**：能力项绑定（std_name+category）✔、难度档（或无难度的类目标记）✔、题型（客观/主观）✔、岗位归属（岗位题库 or 通用）✔。用户原话"每一条都有依据、有指向"落成这条校验规则，出题入库时强校验。

### N8. 题库导入接口（留口）

`POST /api/admin/question-bank/import`（JSONL，每行一题，字段同 N3 schema）。本期演示数据 = LLM 种子生成（U4 待定）；后续找到真实题库走此接口导入，`source=imported`。

## 2. 多轮对话

### D4. 对话控制模式 —— ✅ 已定：C 结构化 action

LLM 每轮返回 `{action: followup|next|finish, reason, reply}`，action 枚举、reason 必填落库，后端校验执行；`finish` 由规则触发（题量完成），LLM 不自主结束全场。

### D5. 追问规则 —— ✅ 已定（存储形态挂 U1）

- 每题硬上限 2 次追问（config 常量），LLM 可提前 `next`；
- **问题链条**（用户提出）：题库行可加 `chain_key / chain_seq` 字段表达"哪几题是同一链条、按什么顺序问"，递进规则优先沿链条走；链条缺失时退回 `(item, difficulty)` 通用递进。——链条字段放关系表即可，见 U1。

### D6 / N6. 上下文管理系统（用户提出，方案落地；触发频率挂 U2）

用户设计已采纳，落地为：

```sql
context_raw(raw_id TEXT PRIMARY KEY, hash TEXT UNIQUE NOT NULL,
            full_text TEXT NOT NULL, created_at TEXT NOT NULL)
-- message 表增列：
--   content_refined TEXT        -- 入对话历史的（可能是精炼版）
--   raw_hash TEXT NULL          -- NULL=未精炼（原文即全部）；非空→指向 context_raw
```

- **精炼**：LLM（P-refine）提取与测评相关的事实（经历/技能/数字/项目细节），丢弃客套、重复、无关内容；精炼版进对话历史，原文按哈希归档，**留痕且可回溯**；
- **防污染**：精炼同时承担注入防护（用户粘贴的长文本里的指令性内容在进历史前被过滤）——用户在 D6 提的"防 prompt 污染"由此覆盖；
- **评分不受损**（我补充的关键机制，化解"精炼丢证据"的担心）：逐题评分时，评分器按 `raw_hash` **回捞原文**输入，对话里用精炼版、打分时用原文版，两全；
- **触发频率**：用户设想"每次输入都触发"，我的折中=**阈值触发**（长度 > ~500 token 才走 LLM 精炼，短输入原文直存）——理由是短回答（"我做过电商项目"）被精炼会丢评分原话、且每场测评多烧约 36 次调用。**此条即 U2**。

### N4. 流式输出（用户提出，本期实现）

SSE 逐 token 推送。与 D4 结构化决策的兼容协议：**首行 JSON（action+reason） + 正文话术**——后端先解析首行落库，再将正文流式转发前端。一次调用，流式与可审计兼得。

### N5. 表单工具 / function call（用户提出，形态适配）

用户设想的"分发、记录、收回表单"的 function call，落地为**后端工具**（不引入 MCP 进程，MCP 留作后续扩展）：

- 工具 `render_form(form_type, fields[])` 注册在后端；面试官系统指令（P-interviewer）约定触发时机：**开场简历表**、gate 项缺信息时；
- LLM 输出标记 `📎[form:form_id]` → 前端识别渲染卡片表单 → 提交直传 `POST /api/assessment/forms/submit` → 存 `form_submission(form_id, session_id, user_id, payload_json, created_at)`；
- 提交后表单结论回注对话历史（精炼版），原始 payload 落库；与 D8 的 `user_profile` 打同一张表（已填过自动带入）；
- 典型表单：简历表（开场）、学历/年限/证书（门槛项）。

### D7. 会话持久化 —— ✅ 已定：照做

`assessment_session / assessment_question / assessment_message` 三表 + **session 锚定 model_id+version**；中断恢复；24h 未完成→abandoned；副产物"我的测评"历史页（P9）。message 表按 N6 增列 `content_refined / raw_hash`。

## 3. 门槛项采集（D8）—— ✅ 已定：C 为主 A 兜底

`user_profile` 表（不动模块一 user 表）；规则=该岗位模型 gate 项→生成填报项；走 N5 表单通道；**字段是事实声明，报告如实标注"基于用户自述"**；判定纯代码（比大小），不进 LLM。

## 4. 模块三：打分与报告

### D9. 打分架构 —— ✅ 已定：B 分步 + 用户补充（客观题代码判分）

| 阶段 | 执行者 | 干什么 |
|---|---|---|
| ① 逐题评分 | **客观题：代码**（answer_key 正则/算法匹配 → 对/错 → 映射分档）；**主观题：LLM**（temperature≈0，输出 `{item_id, score(1~5), evidence_quote, reason}`） | question_score |
| ② 代码聚合 | item 得分=该项各题均分（客观+主观题混合时按 qtype 分别归一后合并，细则实现期定）；gap=required−actual；**总分=Σ(item.weight × actual/5)×100**（权重口径见 §0.5/U3）；gate 项代码二值判定 | item_score + total |
| ③ 报告文字 | LLM：分数+差距排序+评分理由摘录 → 优势/短板/建议 | report |

### D10. 打分刻度 —— ✅ 已定：B 1~5 Dreyfus 同锚点

评分 prompt 复用模块一的 5 级行为锚点；`gap=required−actual` 直接可解释（"要求 Lv4 实测 Lv2，差 2 级"）。

### D11. 总分口径与门槛 —— ✅ 已定，数字对比如下（待 U3 确认后锁定）

以"后端开发工程师"模型为例（默认配比 55/20/20/5，Σ=100）：

| 项 | category | required | importance | weight | 计分 |
|---|---|---|---|---|---|
| Python | hard_skill | Lv4 | required | 19% | 19 × actual/5（实测 Lv3 → 11.4 分） |
| Go | hard_skill | Lv3 | preferred | 11% | 11 × actual/5 |
| …硬技能其余… | | | | 25% | |
| 沟通协作 | soft_skill | Lv3 | required | 12% | 12 × actual/5 |
| …软技能其余… | | | | 8% | |
| 后端开发经验(3年) | experience | —(gate) | required | 8% | 年限≥3→8 分；<3→**0 分** |
| …经验其余… | | | | 12% | |
| 本科 | qualification | —(gate) | required | 5% | 达标→5 分；不达标→0 分 |

- 门槛项无 actual_level，二值制（关闭 04 遗留 TBD：不做中间档）；
- UI 门槛达标情况单列红绿标签，不让 5% 分值掩盖硬信号。

### D12. 优势短板 —— ✅ 已定：代码排序 → LLM 措辞

优势=gap≥0 中权重最大前 3；短板=gap<0 中 |gap|×weight 最大前 3；LLM 只写自然语言段落。

### D13. 报告页 —— ✅ 已定：五段式

①总分+门槛标签 ②雷达图 required vs actual（ECharts）③逐项明细表（理由可展开）④优劣文字段 ⑤逐题回顾（问答+score+reason 可折叠溯回原文）。第 3、5 段是可审计的 UI 实体。

### D14. 建议生成 —— ✅ 已定：B 绑证据

输入=短板项+问答摘录+锚点描述；prompt 约束只能基于给到的短板项提建议。

---

# Part II 测试闭环（模块四）

用户重申三标准：**①整个软件可审计 ②反馈机制/可回溯 ③有测评标准，可迁移**。落点如下，均已拍板：

### D15. 全链路 trace —— ✅ 已定

llm_trace 扩展 4 个 call_type：`question_gen / interview_decide / score_item / report_gen`（N6 精炼再加 `refine`）；锚定链 report→session→(model,version)→question→message→trace；admin 加 trace 查看器（P8 tab）。

### D16. 反馈机制 —— ✅ 已定

candidate 报告页每分数旁「异议」按钮→feedback 表→admin 待办；admin 「标 bad case + 批注」；**不做改分**（分数是历史事实）；bad case 沉淀为评测素材。

### D17. 测评标准 —— ✅ 已定，排期按用户调整

b（打分一致性：同 transcript 复跑断言分差≤1）+ c（虚拟考生三档脚本端到端）必做；a（模块一黄金集）推后。**排期 = 联调之后、全功能正常之后最后做**。"可迁移"要求落到：评测脚本与断言配置独立成 `eval/` 目录，换岗位/换模型可直接复用。

### D18. 测试 UI —— ✅ 已定

P8 三 tab：评测运行 / trace 查看器 / 反馈·bad case。

### D19. 文档治理 —— ✅ 按用户意见修订

讨论完全收敛、细节无异议后，**将模块一/二/三/四设计合并为一份总设计文档**（04+05+06+模块四），持续维护；合并后总文档成为新的单一事实来源，05 转为档案。届时需在 CLAUDE.md 同步更新"单一事实来源"的指向。

---

# §U 未决冲突表（仅剩这 4 条，拍板即收敛）

| # | 冲突点 | 我的建议 | 备选 | 状态 |
|---|---|---|---|---|
| **U1** | 题库/问题链条存储形态 | **关系表**：`question_bank` + `chain_key/chain_seq` 字段，递进查询一条 SQL | 图谱（Neo4j）：违背 04 已拍板的"不上图谱"，引入第二套存储 | 待拍板 |
| **U2** | 上下文精炼触发频率 | **阈值触发**：>500 token 才走 LLM 精炼，短输入直存（评分按 raw_hash 回捞原文，证据无损） | 每次输入都触发：多烧 ~36 次/场，短回答丢原话 | 待拍板 |
| **U3** | 「两层权重」口径 | §0.5 的理解：两层已合成 item.weight，模块三**直接复用、不再二次乘**；D11 数字表按此口径 | 若有第三种理解请指出 | **待确认（最需要你回"对/不对"）** |
| **U4** | 演示期题库来源 | LLM 种子生成（`source=llm_seed`，N3 状态字段已预留，admin 可禁用差题） | 纯留 N8 接口等真实题库——但联调期无题可用 | 待拍板 |

---

# §P Prompt 清单（登记在册，本期不实现，留待专题讨论）

| 编号 | 用途 | 调用时机 | 状态 |
|---|---|---|---|
| P-extract / P-disambiguate / P-aggregate-level | 模块一三次调用 | — | ✅ 已定稿（05 附录 A） |
| P-question-gen | 题库批量出题（岗位背景+锚点+难度档） | 模型 confirmed 时 | 📝 待专题讨论 |
| P-interviewer | 面试官系统指令（角色、追问策略、action 协议、表单触发时机） | 每轮对话 | 📝 待专题讨论 |
| P-refine | 用户长输入精炼（事实提取+注入防护） | N6 触发时 | 📝 待专题讨论 |
| P-score | 逐题主观评分（锚点+evidence_quote 要求） | 测评结束评分阶段 | 📝 待专题讨论 |
| P-report | 报告文字（优劣/建议，绑证据约束） | 报告生成时 | 📝 待专题讨论 |

---

# 开放问题（实现阶段定）

1. ~~流式渲染~~ → 已定：本期 SSE（N4）；
2. mock 深度：模块二/三 mock 用 D17-c 三档虚拟考生脚本兼任，还是各写各的；
3. hard_skill 主观题是否允许贴代码作答（textarea 代码块）及对评分 prompt 的影响；
4. 报告是否可导出（PDF/Markdown）；
5. 里程碑：建议 M5 题库+对话 → M6 评分+报告 → M7 测试闭环（D17 含在 M7 尾部）；
6. 类内"客观题+主观题混合"时 item 分的合并细则（D9 阶段②括号内）。

---

# 决策汇总表（v0.2，含用户决定）

| 编号 | 决策点 | 结论 |
|---|---|---|
| D1 | 题型×类目 | ✅ A 继承（含 hard 客观题） |
| D2 | 题量 | ✅ B 按类目权重，联动 N1 递进 |
| D3 | 出题模式 | ✅ **用户题库方案**（岗位库+通用库，代码选题） |
| D4 | 对话控制 | ✅ C 结构化 action |
| D5 | 追问规则 | ✅ 上限 2 + 链条字段；存储→U1 |
| D6 | 上下文 | ✅ 用户精炼+哈希方案；频率→U2 |
| D7 | 会话持久化 | ✅ 三表+版本锚定，24h abandoned |
| D8 | 门槛采集 | ✅ C+A，走 N5 表单 |
| D9 | 打分架构 | ✅ B 分步；客观题代码判分；权重→U3 |
| D10 | 刻度 | ✅ B 1~5 Dreyfus |
| D11 | 总分/门槛 | ✅ 继承 04，数字表待 U3 确认锁定 |
| D12 | 优劣 | ✅ 代码排序→LLM 措辞 |
| D13 | 报告页 | ✅ 五段式 |
| D14 | 建议 | ✅ B 绑证据 |
| D15 | trace | ✅ 扩展 5 个 call_type + 查看器 |
| D16 | 反馈 | ✅ 双端最小闭环，不改分 |
| D17 | 测评标准 | ✅ b+c，排联调后最后做；eval/ 目录保证可迁移 |
| D18 | 测试 UI | ✅ P8 三 tab |
| D19 | 文档治理 | ✅ 收敛后合并四模块总文档，SSOT 迁移 |
