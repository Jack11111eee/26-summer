---
phase: 05-evidence-report-contract
plan: 05-05
title: 前端 IMPUTED 标记 + 覆盖率展示收口（gap closure）
gap_closure: true
type: frontend
source: 05-VERIFICATION.md gaps
status: complete
---

# 05-05 计划：IMPUTED 特殊视觉标记 + 覆盖率展示（前端收口）

## Objective

闭合 Phase 5 唯一阻断缺口（SC #2 前端展示 / REF-5.5 PARTIAL）：`web/src/views/assessment/Report.vue` 渲染 IMPUTED 特殊视觉标记 + 展示 coverage 字典；顺带修复 REVIEW IN-02（`itemReason` 改 item_id 匹配）。

后端已完整落地并透传，属「数据已就位、前端未接线」的纯展示缺口，非计算缺口。

## Interfaces（后端已透传的字段契约，本计划只消费、不新增写路径）

| 字段 | 形状 | 来源 |
|------|------|------|
| `report.item_details[]` 每项 | `{item_id, std_name, category, gate, no_data, imputed, human_review, required_level, actual_level, gap, weight, score, ...}` | `aggregation.item_scores` → `report_data.item_details` |
| `report.coverage` | `{observed_count, imputed_count, total_measureable, coverage_ratio(0~1), missing_reasons:[{item_id, std_name, reason}]}` | `aggregation.coverage` → `report_data.coverage` |
| `report.radar_data.indicators[]` | `{name, max}`（本计划 +`imputed` 标志） | `report.py` 雷达构造 |
| `report.question_reviews[]` 每项 | 含 `item_id`、`std_name`、`reason`（已 SELECT qs.item_id） | `_load_question_reviews` |

## Tasks（tdd：改后跑后端回归 + 前端目验）

### Task 1 — 后端：雷达 indicators 补 imputed 标志（1 行）
- **文件**：`server/services/report.py:182-186`（`radar_data` 构造）。
- **改**：`"indicators": [{"name": it["std_name"], "max": 5, "imputed": bool(it.get("imputed"))} for it in radar_items]`
- **verify**：`report.radar_data.indicators[i]` 带 `imputed`；`cd server && python -m pytest test_phase5_report.py -v` 仍 11 passed（形状为增量新增，不改既有断言）。

### Task 2 — 前端：明细表 IMPUTED 标记 + coverage 展示
- **文件**：`web/src/views/assessment/Report.vue`
- **2a 标记**：明细表「能力项」单元格，现为 `gate`/`no_data` 二选一子行（L107-108），新增互斥 `imputed` 分支：
  ```html
  <div v-else-if="it.imputed" class="cell-sub imputed-tag">补算 · 加权估算</div>
  ```
  加 `.imputed-tag` 样式（amber 色，区别于真实观察）。`imputed` 与 `gate`/`no_data` 互斥（后端保证 imputed 项 gate=false、no_data=false），故 `v-else-if` 链安全。
- **2b coverage 展示**：SECTION 3「逐项明细」标题下加一行覆盖率摘要（`coverage.total_measureable > 0` 时才渲染）：
  - 观察覆盖率 = `coverage_ratio × 100`（取整）；真实观察 `observed_count` / 可测量 `total_measureable`；补算 `imputed_count`。
  - `missing_reasons` 非空时逐条展示「std_name：reason」。
- **verify**：含补算 item 的报告显示「补算」徽标；覆盖率数字与后端 coverage 一致。

### Task 3 — 前端：雷达图 IMPUTED 轴标记
- **文件**：`web/src/views/assessment/Report.vue` `renderRadar()`。
- **改**：`indicator.name` 为 imputed 的轴追加「（补算）」后缀（ECharts 逐 indicator name 生效）。
- **verify**：imputed 轴标签可区分于真实观察轴。

### Task 4 — 前端：IN-02 itemReason 改 item_id 匹配
- **文件**：`web/src/views/assessment/Report.vue:316-323`。
- **改**：`const hit = reviews.find((q) => q.item_id === itemId && q.reason)`；删除陈旧注释「question_reviews 未携带 item_id」。
- **verify**：多题 item 理由取对，不再靠 std_name 反查。

## Threat Model（前端只读展示，无安全面）

- 无新端点、无新数据写路径、无用户可控文本注入（纯 `{{ }}` 插值，Vue 自动转义）。
- 风险①：imputed 标记与 gate/no_data 子行冲突 → 用 `v-else-if` 互斥链规避。
- 风险②：`coverage`/`missing_reasons` 可能为空 → 用 `v-if` 守卫（`coverage?.total_measureable`、`missing_reasons?.length`）。

## Verification

1. `cd server && python -m pytest test_phase5_report.py -v` → 仍全绿（backend 无行为变更）。
2. 前端目验（无前端单测基建，靠人工点验）：补算 item 有徽标 + 覆盖率展示 + 雷达轴标记。
3. 不触碰 SSOT、不触碰业务库、不改后端计算逻辑。

## Success Criteria

- SC #2 前端闭合：imputed 项与真实观察项视觉可区分；覆盖率/真实观察数/补算数/缺失原因可见。
- IN-02 修复：`itemReason` 按 `item_id` 匹配。
- 后端测试全绿，前端无回归（FAILED/GENERATING/ready 三分支、发布/异议流不变）。

## Output

- 修改 `server/services/report.py`（1 行）+ `web/src/views/assessment/Report.vue`，原子 commit。
- 更新 `05-DECISIONS.md` 记 gap 闭合；`05-05-SUMMARY.md`。
