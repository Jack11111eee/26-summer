---
phase: 03-sse
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - server/db.py
  - server/services/idempotency.py
  - server/services/question_selection.py
  - server/api/assessment.py
  - server/test_phase3_idempotency.py
  - server/test_phase3_forms.py
autonomous: true
requirements: [REF-4.9]
must_haves:
  truths:
    - "同 (session_id, endpoint, idempotency_key) 重复 answer 请求返回 200 + 首次持久化结果 JSON（response_snapshot 回放——sse.js 形态 B 自动消费），消息表无新增行（SC-4）"
    - "重复表单提交（submit-v2 携 key）返回第一次结果——form_instance 不重复 INSERT、gate 行不重复写"
    - "PENDING 态命中（并发双发）→ 409 REQUEST_IN_PROGRESS；COMMITTED 命中且 request_hash 一致 → 快照直返，COMMITTED 命中且 request_hash 不匹配 → 409 IDEMPOTENCY_KEY_REUSED（命中先比 hash——W1 定谳）；幂等在 StreamingResponse 之前（Anti-pattern 2）"
    - "assessment_question.revision 列落地（INTEGER NOT NULL DEFAULT 1）；带 expected_revision 的提交走乐观锁 UPDATE WHERE revision=? —— rowcount==0 → 409 QUESTION_REVISION_CONFLICT"
    - "无 idempotency_key 的请求零行为变化（A/B 兼容——缺省不启用；sse.js/eval/全部既有测试路径不受影响）"
    - "request_hash = sha256(json.dumps({payload, extra}, sort_keys=True, ensure_ascii=False))——答题付三键（question_instance_id/expected_question_revision/client_attempt_id）并入 extra"
    - "响应快照只存决策结果 dict（action/reply/question_id/next_question_id/score_live/answer_state/evidence_sufficient——不含候选人输入原文——A1）"
  artifacts:
    - path: "server/db.py"
      provides: "idempotency_record 新表 DDL（UNIQUE(session_id, endpoint, idempotency_key) 三键 + status/created_at 索引）+ assessment_question 加 revision 列（并入 _migrate_assessment_question_v2 + _DDL 双轨 + _instantiate INSERT 补列）"
      contains: "idempotency_record"
    - path: "server/services/idempotency.py"
      provides: "check_idempotency（两阶段 INSERT PENDING → UPDATE COMMITTED；COMMITTED 命中先比 request_hash——不匹配 409 IDEMPOTENCY_KEY_REUSED）+ request_hash_of + IntegrityError 捕获重试环 + finalize_idempotency"
      contains: "def check_idempotency"
    - path: "server/api/assessment.py"
      provides: "submit_answer 前置区挂三键检查（load_owned_session 后）+ 主链尾快照 finalize + submit-v2 挂幂等"
      contains: "check_idempotency"
    - path: "server/test_phase3_idempotency.py"
      provides: "三键回放/revision 冲突/PENDING 并发/hash 不匹配 409 IDEMPOTENCY_KEY_REUSED/无 key 兼容/表单幂等全部断言"
      contains: "test_idempotent"
  key_links:
    - from: "submit_answer 前置区（03-02 预留位）"
      to: "server/services/idempotency.py check_idempotency"
      via: "带 key 时入口先查——COMMITTED 且 hash 一致直接 200 JSON 快照（不进 SSE 链）；COMMITTED 且 hash 不一致 409 IDEMPOTENCY_KEY_REUSED；PENDING 409；None 走两阶段占位"
      pattern: "check_idempotency\\("
    - from: "submit_answer 尾段（:357 终态 commit 后、return 前）"
      to: "finalize_idempotency"
      via: "全部持久化完成 → UPDATE status='COMMITTED', response_snapshot=决策 dict JSON → 独立小事务"
      pattern: "finalize_idempotency"
    - from: "answer 封存 UPDATE（:213-216 asked_at 或独立语句）"
      to: "assessment_question.revision"
      via: "带 expected_revision 时 UPDATE 扩 WHERE revision=? + SET revision=revision+1；rowcount==0 → 409"
      pattern: "revision=revision\\+1"
---

<objective>
幂等与并发防护（SSOT §13.4 / D-36~D-38）：idempotency_record 新表（三键唯一 + 两阶段 PENDING/COMMITTED + 响应快照 JSON）+ assessment_question.revision 乐观锁（带 expected_revision 才走）+ answer/submit-v2 两端点接入 + 409 三态 error_code（REQUEST_IN_PROGRESS/QUESTION_REVISION_CONFLICT/IDEMPOTENCY_KEY_REUSED——最后者为同 key 异 payload 命中维度）。

Purpose: REF-4.9——SC-4「携 idempotency_key 的重复请求返回第一次持久化结果，不重复写消息/计 followup/扣题量；并发双写由事务+乐观版本号防护」。幂等键可选（缺省不启用）——无 key 前端零影响（D-36 锁定）。
Output: db.py DDL + idempotency.py 新服务 + question_selection.py _instantiate 补 revision 列 + assessment.py 两端点接入 + 1 新测试文件 + test_phase3_forms 补表单幂等断言。

上下文预算注记：3 任务中等密度（DDL+服务+接入）；test_phase3_forms 的追加断言是 03-01 文件的最小增补（表单幂等跨 plan 归属——文件级冲突已以 wave 串行消解）。files_modified 全集 = Task 1-3 实改文件并集（含 question_selection.py——_instantiate INSERT 补列落本计划 Task 2，W2 自列不豁免）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sse/03-CONTEXT.md
@.planning/phases/03-sse/03-PATTERNS.md
@.planning/phases/03-sse/03-01-SUMMARY.md
@.planning/phases/03-sse/03-02-SUMMARY.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

## idempotency_record DDL（db.py——PATTERNS 新表惯例）
CREATE TABLE IF NOT EXISTS idempotency_record (
  id               TEXT PRIMARY KEY,
  session_id       TEXT NOT NULL,
  endpoint         TEXT NOT NULL,           -- 'answer' | 'form_submit'
  idempotency_key  TEXT NOT NULL,
  request_hash     TEXT,                    -- sha256 规范化 JSON（Claude 裁量）
  status           TEXT NOT NULL,           -- PENDING/COMMITTED 代码校验（N11）
  response_snapshot TEXT,                   -- COMMITTED 后填——决策结果 dict JSON
  created_at       TEXT NOT NULL,
  UNIQUE(session_id, endpoint, idempotency_key)
);
CREATE INDEX IF NOT EXISTS idx_idem_created ON idempotency_record(created_at);
（组合唯一放 CREATE 表尾——db.py:57 competency_model 形态；created_at 普通索引语句形态照 db.py:167——D-38 Phase 6 数据治理预留位，注释注明「清理策略不实现」。）
assessment_question.revision：并入 _migrate_assessment_question_v2 的 new_cols（("revision", "INTEGER NOT NULL DEFAULT 1")）+ _DDL v2 列区注释块同步 + question_selection.py _instantiate INSERT 列清单补（revision=1 显式——新实例默认）。_instantiate 属 server/services/question_selection.py——**已列本计划 files_modified**（Task 2 实改第三文件——W2：执行前列全，不靠执行期扩列豁免）。

## check_idempotency 两阶段（services/idempotency.py——refine.py 短服务形态 + difficulty 接 conn 契约）
def check_idempotency(conn, *, session_id: str, endpoint: str, key: str, request_hash: str) -> dict | None:
    # 无行 → INSERT PENDING 占位（并发双发时 UNIQUE 拦截 IntegrityError）→ return None
    # 命中 PENDING → raise HTTPException(409, detail={"error_code": "REQUEST_IN_PROGRESS", ...})——WR-01 三态
    # 命中 COMMITTED → **先比 request_hash**（W1）：
    #   row["request_hash"] == request_hash（且 snapshot 可解析）→ 返回 json.loads(response_snapshot) 快照 dict（API 层直接 200 JSON——sse.js 形态 B）
    #   row["request_hash"] != request_hash → raise HTTPException(409,
    #       detail={"error_code": "IDEMPOTENCY_KEY_REUSED", "message": "同幂等键已被不同请求体使用"})
    # IntegrityError 捕获语义：INSERT PENDING 放 try 内，except sqlite3.IntegrityError → 再查一次行
    #   （此时对方已完成或进行中——按 COMMITTED/PENDING 分支处理，COMMITTED 同样先比 hash）
def finalize_idempotency(conn, *, session_id: str, endpoint: str, key: str, snapshot: dict) -> None:
    # UPDATE status='COMMITTED', response_snapshot=json.dumps(snapshot, ensure_ascii=False)
    # WHERE 三键 —— 接 conn 不 commit（answer 主链 :357 commit 后的独立小事务由本函数自 conn.commit() 或
    # 调用方新开连接执行——推荐：本函数自 get_conn 开新连接（主链已 commit，新事务干净）——服务层自取连接惯例例外，docstring 注明原因（answer 返回 StreamingResponse 前的最后写点）
def request_hash_of(payload: dict, extra: dict | None = None) -> str:
    # hashlib.sha256(json.dumps({"payload": payload, "extra": extra or {}},
    #           ensure_ascii=False, sort_keys=True).encode("utf-8")).hexdigest()  —— refine.py:30 sha256 用法照抄

## answer 接入形态（assessment.py——A4 时序：幂等最先，超时点检 03-04 后插）
前置区（load_owned_session :186 后、消息 INSERT :207 前）：
  if body.idempotency_key:                                   # D-36 可选——无 key 零路径
      snap = check_idempotency(conn, session_id=..., endpoint="answer",
                               key=body.idempotency_key,
                               request_hash=request_hash_of(body.model_dump(), {
                                   "question_instance_id": question_id,
                                   "expected_question_revision": body.expected_revision,
                                   "client_attempt_id": body.client_attempt_id}))
      if snap is not None:
          return JSONResponse(content=snap, status_code=200)  # 200 application/json——sse.js 形态 B
乐观锁（带 expected_revision 时）：
  落点 :213-216 asked_at COALESCE UPDATE 扩展 or 语句前独立 UPDATE——实现：若 body.expected_revision 不为 None：
      cur = conn.execute("UPDATE assessment_question SET revision=revision+1 WHERE question_id=? AND revision=?",
                         (question_id, body.expected_revision))
      if cur.rowcount == 0: raise 409 QUESTION_REVISION_CONFLICT（WR-01 detail 三态）
  不带 expected_revision：零行为变化（跳过——A/B 兼容）。
尾段（:357 最终 commit 后、return StreamingResponse 前）：
  if body.idempotency_key:
      finalize_idempotency(conn 邻位新连接, ..., snapshot={
          "action": action, "reply": decision["reply"], "question_id": question_id,
          "next_question_id": next_question_id, "score_live": decision["score_live"],
          "answer_state": decision["answer_state"], "evidence_sufficient": decision["evidence_sufficient"]})
  （快照 = 决策结果 dict——A1/Pitfall 5：不含 SSE 事件序、不含候选人输入原文；sse.js 形态 B 消费 data.action/data.reply/data.next_question_id 全对齐——:46-53 分支核对）

## submit-v2 接入形态（03-01 端点 + FormSubmitRequest.idempotency_key）
load_owned_session 后同构前置检查（endpoint="form_submit"）；命中且 hash 一致 → 快照直接 200 返回（form 的快照 dict = submit-v2 首次返回体 {action, form_instance_id, status, gate_results}）；finalize 调用在 validate_and_submit 的 conn.commit() 后。FORM_ALREADY_SUBMITTED（03-01 状态维 409）与幂等 200 回放的边界：带 key 重发 → 幂等层先命中返回 200 首次结果（不进六维——状态维不触发）；不带 key 重发 → 六维状态维 409（既有语义不变——两者共存正确：幂等层在闸门外围）。

## 无 key 面（A/B 兼容断言面）
- sse.js streamAnswer body 无 idempotency_key（前端零改动——后端 Optional 承接）
- eval/virtual_candidates.py 直插链（无 answer API——RESEARCH 已核零影响）
- 全部既有测试（m5/p0×2/phase2×5/phase3_forms）无 key 调用 → 走原路径（无快照 INSERT——表零行增长可断言）

## 现行代码锚点
- assessment.py：AnswerRequest 消费（03-02 后）；前置检查区 :185-202；asked_at UPDATE :213-216；终态 commit :357；submit-v2 端点（03-01 后形态——B1 后含 finish/next 触发器，快照 dict 照该响应体）
- db.py：assessment_question v2 迁移 :404-433 new_cols（revision 并入）；_DDL :145-167 v2 列区；idempotency_record 追加位（question_bank_task :276-291 邻位后）
- question_selection.py：_instantiate INSERT :576-583（补 revision 列——本计划 files_modified 已列，Task 2 实改）
  UNIQUE 既有先例：db.py:57 / :267 / :167
- 测试：test_p0_chain.py:188+ _seed_completed_session_direct（时间旅行直插形态——PENDING 态构造参照）；aggregation.py:41-44（json 解析防御 try/except JSONDecodeError 形态）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test_phase3_idempotency.py 幂等全断言（先红）</name>
  <files>server/test_phase3_idempotency.py</files>
  <read_first>
    - server/test_p0_chain.py（:30-57 头 + :188+ 直插种子手法 + :48-54 _q）
    - server/test_phase3_sse.py（03-02 产物 _stream_answer helper——复制形态；本文件流式消费同构）
    - .planning/phases/03-sse/03-RESEARCH.md（Code Examples #5 + Pitfall 5 + A1/A4）
  </read_first>
  <behavior>
    - test_same_key_returns_first_snapshot：带 key 答题（流式消费完）→ 再次同 key 同 payload → 响应 200 + Content-Type application/json（非 SSE）+ dict 与首次组回 dict 五键一致（action/reply/next_question_id/score_live/question_id）+ _q 消息表 COUNT 不增（user/assistant 各不加行）+ 事件表 COUNT 不增
    - test_hash_sensitivity：同 key 不同 payload（answer 文本不同——hash 必异）→ 409 且 error_code=='IDEMPOTENCY_KEY_REUSED'（COMMITTED 命中先比 request_hash——不匹配即 409，W1 定谳口径；不回放首次快照）
    - test_pending_returns_409：手工直插 PENDING 态行（test_p0_chain 直插手法）→ 带 key 请求 → 409 error_code=='REQUEST_IN_PROGRESS'
    - test_distinct_keys_independent：同 session 两个不同 key → 各自首次处理（消息表各追加行）
    - test_cross_endpoint_isolation：answer 的 key 与 form_submit 同串 key → 互不干扰（各端点独立处理——实验 4 语义）
    - test_revision_optimistic_lock：带 expected_revision=1 答题 → 200 + _q 该行 revision==2；带 expected_revision=1（stale）再答次题 → 409 error_code=='QUESTION_REVISION_CONFLICT'
    - test_revision_absent_no_lock：不带 expected_revision（sse.js 现状）→ 全程无 409（兼容面）
    - test_no_key_no_records：无 key 三连答 → idempotency_record COUNT==0（缺省不启用）
    - test_concurrent_double_insert（单进程模拟）：两个并发首达（线程或直接连续两次 INSERT PENDING 检验 UNIQUE——最简：直插一行 PENDING 后再走 check_idempotency 语义断言 409/回放，不引真线程）——断言 UNIQUE 索引存在（sqlite_master uq 查询）+ 双 INSERT 第二个 IntegrityError（或 409）
    - test_form_submit_idempotent：池耗尽触发 form → submit-v2 带 key 成功 → 重发同 key → 200 首次 gate_results 快照 + _q form_instance COUNT 不增 + question_score gate 行 COUNT 不增
    - test_snapshot_schema：COMMITTED 行 response_snapshot 可 json.loads 且键集合 ⊆ {action, reply, question_id, next_question_id, score_live, answer_state, evidence_sufficient}（不含 answer 原文——A1 断言：快照字符串不含提交的原文文案片段）
  </behavior>
  <action>新建 server/test_phase3_idempotency.py（三件套头 + 种子复制 + _stream_answer 复制自 03-02 形态——带 key 请求经 stream 消费首次、JSON .json() 消费重放）。PENDING 直插/COMMITTED 直插参照 _seed_completed_session_direct 直插手法。test_hash_sensitivity 断言口径：409 + error_code IDEMPOTENCY_KEY_REUSED（实现按 interfaces 命中比 hash——W1）。本任务提交红（idempotency_record 表/check_idempotency 不存在）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase3_idempotency.py -v 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    - 回放/PENDING 409/hash 不匹配 IDEMPOTENCY_KEY_REUSED/键隔离核心断言 + revision 乐观锁三组 + 无 key 兼容两组 + 表单幂等 + 快照白名单
    - 快照不含原文断言（A1——字符串级检查）
    - 本任务提交红，Task 2/3 后转绿
  </acceptance_criteria>
  <done>幂等与乐观锁全部行为断言就位（同 key 异 payload 409 口径定谳），红测证明现状无幂等层</done>
</task>

<task type="auto">
  <name>Task 2: db.py idempotency_record + revision 列；services/idempotency.py 两阶段服务；question_selection.py 补列</name>
  <files>server/db.py, server/services/idempotency.py, server/services/question_selection.py</files>
  <read_first>
    - server/db.py（:145-167 _DDL assessment_question v2 列区、:404-433 _migrate_assessment_question_v2、:276-291 新表分节惯例、:57/:167/:267 唯一索引先例、:462-477 init_db）
    - server/services/refine.py（:25-44 短服务 + :30 sha256）
    - server/services/question_selection.py（:576-583 _instantiate INSERT 列清单）
    - .planning/phases/03-sse/03-RESEARCH.md（Code Examples #5 + Pattern 2）
  </read_first>
  <action>(1) db.py：_DDL 追加 idempotency_record CREATE（<interfaces> DDL——分节注释含 D-38 「清理策略不实现（Phase 6 数据治理）」注记）+ idx_idem_created 索引；new 迁移函数 _migrate_idempotency_record（CREATE IF NOT EXISTS 幂等——照 _migrate_form_instance 形态）register 进 init_db；assessment_question 的 revision 双轨（_migrate_assessment_question_v2 new_cols 追加 ("revision", "INTEGER NOT NULL DEFAULT 1") + _DDL v2 列区注释块同步加行）。(2) 新建 server/services/idempotency.py：模块 docstring（SSOT §13.4 + D-36~D-38 出处 + 事务边界声明——check 接调用方主 conn、finalize 自取新连接因主链已 commit）；check_idempotency（两阶段——<interfaces> 形态；**COMMITTED 命中先比 request_hash**：一致返回快照 / 不一致 raise 409 IDEMPOTENCY_KEY_REUSED（W1）；IntegrityError 环：INSERT 失败→重查行→按 COMMITTED/PENDING 分支（COMMITTED 分支同样过 hash 比对）；json 解析防御照 aggregation.py:41-44 try/except JSONDecodeError——解析失败视为快照损坏，走 409 REQUEST_IN_PROGRESS 防回放脏数据）；request_hash_of（sha256 sort_keys——<interfaces> 形态）；finalize_idempotency（UPDATE status/response_snapshot 三键 WHERE——自取 get_conn 新连接 + commit，docstring 注明原因）。(3) question_selection.py _instantiate INSERT 列清单补 revision（显式 1——新实例；:576-583 列名/值两清单各加一项；该文件已列本计划 files_modified——W2 口径，无执行期扩列豁免）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -c "
import os, tempfile, sys
os.environ['DB_PATH'] = os.path.join(tempfile.mkdtemp(), 'p3_idem_smoke.db')
os.environ['LLM_PROVIDER'] = 'mock'
sys.path.insert(0, os.path.dirname(os.getcwd()))
from server.db import init_db, get_conn
init_db(); init_db()
c = get_conn()
c.execute(\"INSERT INTO idempotency_record(id, session_id, endpoint, idempotency_key, status, created_at) VALUES('i1','s1','answer','k1','PENDING','t')\")
c.commit()
cols = [r[1] for r in c.execute('PRAGMA table_info(assessment_question)')]
assert 'revision' in cols
import sqlite3
try:
    c.execute(\"INSERT INTO idempotency_record(id, session_id, endpoint, idempotency_key, status, created_at) VALUES('i2','s1','answer','k1','PENDING','t')\")
    raise SystemExit('UNIQUE not enforced')
except sqlite3.IntegrityError:
    pass
r = c.execute(\"SELECT id FROM idempotency_record WHERE session_id='s2' AND endpoint='answer' AND idempotency_key='k1'\")
print('idempotency DDL OK')" && python -m pytest test_phase3_idempotency.py -v 2>&1 | tail -6 && grep -n "revision" services/question_selection.py | head -3</automated>
  </verify>
  <acceptance_criteria>
    - idempotency_record 三键 UNIQUE 实测拦截（verify 内嵌断言）+ created_at 索引存在
    - assessment_question.revision 双轨（迁移 + _DDL）+ _instantiate INSERT 补列（grep revision question_selection.py 命中——verify 循环末段已加）
    - check_idempotency/finalize_idempotency/request_hash_of 三函数存在；finalize 自取连接（grep get_conn 在 finalize 段）
    - check_idempotency 含 hash 比对分支（源码 grep IDEMPOTENCY_KEY_REUSED 命中——W1 实现面）
    - init_db 二次调用幂等
    - test_phase3_idempotency.py 红项部分转绿（DDL 层断言全绿；API 层红待 Task 3）
  </acceptance_criteria>
  <done>幂等数据层与两阶段服务落地（COMMITTED 命中比 hash——异 payload 409），乐观锁列与 _instantiate 补列就位</done>
</task>

<task type="auto">
  <name>Task 3: assessment.py 前置检查 + 乐观锁 + 快照 finalize（answer 与 submit-v2 两端点）</name>
  <files>server/api/assessment.py, server/test_phase3_forms.py</files>
  <read_first>
    - server/api/assessment.py（03-02 后 submit_answer 全文——前置区/asked_at UPDATE/终态 commit 位 + 03-01 submit-v2 端点含 finish/next 触发器）
    - server/services/idempotency.py（Task 2 产物三函数签名）
    - server/test_phase3_forms.py（03-01 产物 + 03-02 流式适配后形态——表单幂等断言追加位）
    - .planning/phases/03-sse/03-RESEARCH.md（A4 时序——幂等最先；Pitfall 5）
  </read_first>
  <action>(1) submit_answer 前置区（load_owned_session 后）：if body.idempotency_key: 快照 check（<interfaces> 形态——request_hash_of 的 extra 传三键）→ 命中（check 内 hash 一致）return JSONResponse(snap)（from fastapi.responses import JSONResponse——sse.js 形态 B application/json 消费）→ 命中异 hash 由 check 内 409 IDEMPOTENCY_KEY_REUSED 抛出（endpoint 层不重复判）。(2) 乐观锁：消息 INSERT 前（asked_at UPDATE 邻位）：if body.expected_revision is not None: UPDATE ... WHERE question_id=? AND revision=? → rowcount==0 raise 409 QUESTION_REVISION_CONFLICT（WR-01 detail）。(3) 尾段（终态 commit 后 return 前）：if body.idempotency_key: finalize_idempotency(七键快照 dict——<interfaces> 清单)。(4) submit-v2（03-01 端点——含 B1 finish/next 触发器）：load_owned_session 后同构（endpoint='form_submit'——快照 = 首次返回体含 action 键）；validate_and_submit 的 commit 后 finalize。(5) test_phase3_forms.py 追加 test_form_submit_idempotent 兼容回归断言（与 test_phase3_idempotency 同名测试不冲突——各文件独立；本文件聚焦 form 面重放后 gate_results 与 action 键一致性）——最小三断言（重放 200/COUNT 不增/事件不增）。(6) A4 次序注释落 code：前置区行内注释「幂等先于单题超时点检（03-04 后插）——已超时+重复提交不先封存（A4）」。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase3_idempotency.py -v 2>&1 | tail -6 && python -m pytest test_phase3_forms.py -v 2>&1 | tail -4 && python -m pytest test_phase3_sse.py -v 2>&1 | tail -4 && python -m pytest test_m5_backend.py -v 2>&1 | tail -3 && python -m pytest test_p0_chain.py -v 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - assessment.py grep check_idempotency ≥ 2（answer + form 两点）+ finalize_idempotency ≥ 2
    - 乐观锁 UPDATE 含 revision=revision+1 与 WHERE revision=?（grep 双命中）；无 key 路径零改动（A/B 兼容——test_no_key_no_records/test_revision_absent_no_lock 断言）
    - 快照命中响应为 application/json 200（test_same_key_returns_first_snapshot）；同 key 异 payload 409 IDEMPOTENCY_KEY_REUSED（test_hash_sensitivity）
    - test_phase3_idempotency.py 全绿；test_phase3_forms/sse 全绿（追加不回归）；test_m5_backend/p0_chain 回归绿（无 key 面）
  </acceptance_criteria>
  <done>幂等两端点接入闭合（回放/hash 比对 409/两阶段/乐观锁/快照白名单），无 key 面零行为变化</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 候选人 → idempotency_key（客户端可控标识） | 客户端猜测/复用 key 试图回放他人结果——三键作用域 + 所有权前置 |
| 并发双发 → UNIQUE 索引 | 同 key 两请求并发 INSERT——DB 层原子拦 |
| 快照回放 → 敏感面 | response_snapshot 落库数据（含决策结论）——白名单键控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | 幂等键猜测回放他人结果 | mitigate | 三键含 session_id + load_owned_session 前置（key 与 session 域绑定——他人 key 在他人 session 内查不到本 session 行） |
| T-03-13 | Info disclosure | 快照含候选人输入原文 | mitigate | 快照键白名单（A1——七键不含 answer 原文；test_snapshot_schema 断言） |
| T-03-14 | DoS | 并发双发重复写消息/扣题量 | mitigate | UNIQUE 三键 IntegrityError 拦 + 两阶段 PENDING 409 快速失败（实验 4） |
| T-03-15 | Tampering | request_hash 碰撞（不同 payload 同快照回放变造） | mitigate | sha256 + sort_keys 规范化 + **COMMITTED 命中先比 hash**：不匹配 → 409 IDEMPOTENCY_KEY_REUSED 不回放（W1 定谳——异 payload 永不拿到首次结果；test_hash_sensitivity 断言） |
| T-03-16 | Tampering | 乐观锁缺位（TOCTOU 双写同题） | mitigate | UPDATE WHERE revision=? rowcount 原子判（实验 4——先 SELECT 后比对有窗口） |
| T-03-17 | DoS | 幂等表无限增长 | accept | D-38 锁定不实现（Phase 6 数据治理）；created_at 索引就位——演示期数据量级接受 |
</threat_model>

<verification>
1. `cd server && python -m pytest test_phase3_idempotency.py -v` — 回放/两阶段/hash 不匹配 409/乐观锁/兼容面全绿
2. `cd server && python -m pytest test_phase3_forms.py -v && python -m pytest test_phase3_sse.py -v`（逐文件）— phase3 三文件回归绿
3. `for f in test_m5_backend test_p0_chain test_p0_security; do python -m pytest server/$f.py -v | tail -2; done` — 无 key 主链回归
4. `grep -c "check_idempotency\|finalize_idempotency" server/api/assessment.py` — ≥ 4（两端点两阶段）
5. `python -c`（Task 2 verify 的 UNIQUE/revision 断言）— 通过
</verification>

<success_criteria>
- SC-4 达成：携 key 重复请求返回第一次结果（消息/实例/事件零重复写）+ 并发双写防护（UNIQUE + 乐观锁）+ 同 key 异 payload 409 IDEMPOTENCY_KEY_REUSED（W1）
- REF-4.9 落地（三键作用域/答题付三键/重复返回首次/事务+乐观版本号）
- 无 key 面（sse.js/eval/全部既有测试）零行为变化（A/B 兼容）
- 快照白名单七键（A1 ASSUMED 呈报项——关口包列明）
</success_criteria>

<output>
Create `.planning/phases/03-sse/03-03-SUMMARY.md` when done
</output>
