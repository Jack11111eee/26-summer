---
phase: 01-p0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/core/security.py
  - server/api/assessment.py
  - server/test_p0_security.py
  - server/test_m6_backend.py
  - web/src/router/index.js
autonomous: true
requirements: [REF-1.1, REF-1.2]
must_haves:
  truths:
    - "候选人 B 访问候选人 A 的 session/report/feedback 任意读或写路由，一律返回 404（不引入 403 分支，D-01）"
    - "admin 可读任意候选人资源（读豁免），但写操作（answer/forms/score/report/feedback）同样被拒（owner-only，D-03）"
    - "admin 访问自己创建的 session/report 正常 200（owner 判定恒优先于角色豁免，Pitfall 10）"
    - "admin 完成测评后可进入报告页，不再被前端 route guard 弹回（D-04）"
    - "候选人本人主链（建会话→答题→评分→报告→反馈）在越权修复后全部不回归"
  artifacts:
    - path: "server/core/security.py"
      provides: "load_owned_session / load_owned_report 所有权 helper"
      contains: "def load_owned_session"
    - path: "server/api/assessment.py"
      provides: "8 条候选人资源路由全部接入 helper + user 注入"
      contains: "load_owned_session"
    - path: "server/test_p0_security.py"
      provides: "越权测试矩阵（candidate↔candidate + admin 边界）"
      min_lines: 150
    - path: "web/src/router/index.js"
      provides: "route guard admin 例外修复"
      contains: "requiresAuth: true"
  key_links:
    - from: "server/api/assessment.py 8 条路由"
      to: "server/core/security.py helper"
      via: "路由首调 load_owned_session/load_owned_report"
      pattern: "load_owned_(session|report)\\("
    - from: "server/test_m6_backend.py _test_feedback_api"
      to: "submit_feedback 新签名（user 参数）"
      via: "直调适配"
      pattern: "submit_feedback\\(.*user"
---

<objective>
候选人资源级所有权校验（IDOR 修复）：api/assessment.py 8 条候选人资源路由全部接入 `load_owned_session` / `load_owned_report` 共享 helper（404 统一语义、admin 只读豁免），配套越权测试矩阵；顺带修复前端 route guard admin 例外（D-04）。

Purpose: REF-1.1/REF-1.2 是 SSOT §7 P0 上线阻断项——现状 8 条路由只查 ID 不查归属，任何候选人可读写他人 session/report/feedback。越权测试矩阵为上线阻断测试。
Output: helper + 8 路由接入 + test_p0_security.py + m6 直调适配 + router meta 修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0/01-CONTEXT.md
@.planning/phases/01-p0/01-PATTERNS.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

require_login 返回的 user dict 形状（server/core/security.py:47）:
  {"user_id": str, "username": str, "role": "admin"|"candidate", "is_active": 1}

8 条目标路由 × 读写 × admin 豁免矩阵（现行号，改后行号会移）:
| 路由 | 方法 | helper | allow_admin_read |
| get_session (:97) | GET | load_owned_session | 是（读） |
| submit_answer (:129) | POST | load_owned_session | 否（写） |
| submit_form (:209) | POST | load_owned_session | 否 |
| score_session_endpoint (:233) | POST | load_owned_session | 否 |
| request_report (:259) | POST | load_owned_session | 否 |
| get_report_by_session (:272) | GET | load_owned_report（按 session_id） | 是 |
| get_report (:285) | GET | load_owned_report（按 report_id） | 是 |
| submit_feedback (:297) | POST | load_owned_report（按 report_id，写） | 否 |

POST /sessions（create_session :59）与 GET /positions 不涉他人资源，不加 helper。
现行路由注入现状：仅 create_session 与 submit_form 有 `user: dict = Depends(require_login)`；其余 7 条（get_session/submit_answer/score_session_endpoint/request_report/get_report_by_session/get_report/submit_feedback）需逐条补加。

report 表列（server/db.py:189）: report_id, session_id, total_score, gate_passed, report_json, created_at
form_submission 落库已有 user["user_id"]（submit_form :226 可作参照）。

测试种子参照：
- test_m5_backend.py:43-127 _seed_position_with_confirmed_model + _seed_question_bank（岗位+confirmed 模型+题库直插，无 question_bank_task 行——01-04 readiness 必须容忍此形态，本计划不动 create_session 检查）
- test_m5_backend.py:130-135 _auth_headers（register+login 取 Bearer）
- test_m7_backend.py:28-46 _ensure_admin（CryptContext(schemes=["bcrypt"]) 直插 admin user 行）
- test_m5_backend.py:213-246 答题闭环（followup→next→finish 循环）
- test_m6_backend.py:263-270 _test_feedback_api 直调 submit_feedback（本计划 Task 3 需适配其签名）

D-09 预告（01-03 落地）：score_session 服务层将加状态护栏。本计划只加所有权（404），不动 score_session 服务层。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 创建 test_p0_security.py 越权测试矩阵（先红）</name>
  <files>server/test_p0_security.py</files>
  <read_first>
    - server/test_m5_backend.py（全文 307 行——头部模板 :6-38、_seed_position_with_confirmed_model :43-73、_seed_question_bank :76-127、_auth_headers :130-135、答题闭环 :213-246、_q :32-38）
    - server/test_m7_backend.py（:28-46 _ensure_admin 种子段）
    - server/api/assessment.py（8 条路由现状：只查 ID 不查归属）
    - .planning/phases/01-p0/01-RESEARCH.md（Code Examples 越权矩阵测试骨架 12 用例）
  </read_first>
  <behavior>
    - 矩阵用例 1-8（candidate B → A 的资源，写越权为 404 而非 409）：B GET /sessions/{A_sid} → 404；B POST /sessions/{A_sid}/answer → 404；B POST /sessions/{A_sid}/forms/submit → 404；B POST /sessions/{A_sid}/score → 404；B POST /sessions/{A_sid}/report → 404；B GET /reports/by-session/{A_sid} → 404；B GET /reports/{A_rid} → 404；B POST /reports/{A_rid}/feedback → 404
    - admin 读豁免：admin GET /sessions/{A_sid} → 200；admin GET /reports/by-session/{A_sid} → 200；admin GET /reports/{A_rid} → 200
    - admin 写拒绝：admin POST /sessions/{A_sid}/answer、/forms/submit、/score、/report、POST /reports/{A_rid}/feedback → 404（D-01 字面：统一 404，不引入 403）
    - owner 正常链不回归：A 本人 GET /sessions/{A_sid} → 200；A POST answer → 200；A POST score → 200（确定性要求：owner 的 score 断言必须用一个 **in_progress** 会话调 POST /score 并断言 200——01-03 completed 护栏落地后该断言不得翻红；不得改用"非 404 即可"的软性写法，也不得在答疑完成后（completed）调 score 再断 200）
    - admin 自己的资源：admin 建会话（后端允许任何登录用户 create_session）→ admin GET /sessions/{admin 自己的 sid} → 200（Pitfall 10）
    - admin trace：admin GET /api/admin/trace/by-session/{A_sid} → 200（既有路由，回归确认）
  </behavior>
  <action>新建 server/test_p0_security.py。头部照抄 M5 模板：模块顶先设 os.environ["DB_PATH"]=tempfile 临时文件、LLM_PROVIDER=mock、JWT_SECRET=test-secret，再 sys.path.insert 仓库根，import 带 # noqa: E402，模块级 init_db() 与 client = TestClient(app)，文件 docstring 写明"运行：cd server && python -m pytest test_p0_security.py -v"。种子复用 m5 模式自写 _seed_position_with_confirmed_model/_seed_question_bank（直插 question_bank 行、不走 confirm）、_auth_headers（register+login）；admin 用 m7 的 _ensure_admin 模式（CryptContext(schemes=["bcrypt"]) 直插 admin 用户行）。测试函数一律无参。矩阵用例按 <behavior> 分组成 4-6 个 test_* 函数（如 test_candidate_cannot_read_others、test_candidate_cannot_write_others、test_admin_read_exemption_write_denied、test_owner_main_chain_unaffected、test_admin_own_resources）。产出 A 的完整链（建会话→答完全场题到 finish 置 completed→直调 score_session 服务函数产生 question_score 行→POST /report 生成 report 行）以获得 A_sid 与 A_rid 两个越权目标；报告生成沿用 POST /report + TestClient 同步执行 background task 的特性直接断言 report 行已落库。种子链的 01-03 稳定性硬约束：产出 A_rid 的会话必须已 finish（completed）再 POST /report——01-03 落地后 in_progress 会话 POST /report 会被 409 拒绝；而仅为 owner score 断言另开一个只答 1 题、未 finish 的 in_progress 会话（见上）。注意：本计划只写测试先红（Task 2 实现 helper 后转绿），score 的 owner 断言避开 01-03 护栏的 completed 语义——确定性要求：owner score 用例用 in_progress 会话（未 finish）调 POST /score 断言 200，不得用 completed 会话调 score，不得软化为"非 404 即可"。单文件单进程，本文件不得 import 其他 test_* 模块。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_p0_security.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/test_p0_security.py 存在且头部含 os.environ["DB_PATH"] / LLM_PROVIDER=mock / JWT_SECRET 设置（位于任何 server.* import 之前）
    - 文件含针对 8 条路由的 candidate↔candidate 越权断言（B 访问 A 的资源断言 status_code == 404）
    - 含 admin 读豁免断言（200）与 admin 写拒绝断言（404）
    - 含 admin 访问自己 session 的 200 断言
    - 本 Task 提交时矩阵必须红（越权用例实际返回 200/201——先证明缺陷存在），Task 2 后转绿
    - python -m pytest test_p0_security.py -v 单文件可收集，无 fixture 报错
  </acceptance_criteria>
  <done>越权测试矩阵文件落地，红测证明现状 IDOR 缺陷；Task 2 完成后同一命令全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_owned_session / load_owned_report helper + 8 路由接入（转绿）</name>
  <files>server/core/security.py, server/api/assessment.py</files>
  <read_first>
    - server/core/security.py（全文 57 行——_current_user 查行→dict(row)→HTTPException 三段式 analog）
    - server/api/assessment.py（全文 318 行——8 条路由现状；:226 submit_form 已取 user["user_id"] 作参照）
    - .planning/phases/01-p0/01-PATTERNS.md（01-01 节：helper 形状 + 8 路由矩阵 + 判定顺序 Pitfall 10）
    - .planning/phases/01-p0/01-RESEARCH.md（Pattern 1 helper [VERIFIED] 样例）
  </read_first>
  <behavior>
    - load_owned_session(conn, session_id, user, *, allow_admin_read)：单查询 `WHERE session_id=? AND user_id=?`，admin 读豁免用 OR 条件；无行 raise HTTPException(404, "会话不存在")
    - load_owned_report(conn, report_id, user, *, allow_admin_read)：`SELECT r.* FROM report r JOIN assessment_session s ON s.session_id=r.session_id WHERE r.report_id=? AND s.user_id=?`；无行 raise HTTPException(404, "报告不存在")
    - 判定顺序：user_id == current 恒通过（admin 本人资源走通），仅非 owner 且 allow_admin_read 且 role=='admin' 才豁免；写路由不传 allow_admin_read
    - 8 条路由：B 访问 A 的任意读/写路由 → 404；admin 读 → 200；admin 写 → 404；owner → 原状态码
    - helper 内 SQL 全部 ? 参数化（防注入），返回 dict(row)
  </behavior>
  <action>在 server/core/security.py 追加两个模块级公开函数 load_owned_session / load_owned_report（放置选定 core/security.py——"鉴权语义集中在 core"，CONTEXT Claude's Discretion 二选一已定）。函数签名按 <behavior>；admin 读豁免实现为：owner 条件 user_id=? 命中即返回，未命中且 allow_admin_read and user["role"]=="admin" 时放宽为仅按资源 ID 查询（等价 OR 语义）。写路由调用时不传 allow_admin_read（默认 False）→ admin 与其他候选人同路径 404（D-03 owner-only）。然后改 server/api/assessment.py 8 条路由：(a) get_session/submit_answer/score_session_endpoint/request_report/get_report_by_session/get_report/submit_feedback 七条逐条加 `user: dict = Depends(require_login)` 参数（router 级 dependencies 只做 401 拦截不注入 user，这是前置必要改动）；(b) 各路由删掉原"只查 id"的 SELECT+404 块，改首调 helper（session 类用 load_owned_session(get_conn(), session_id, user, allow_admin_read=读或省略)；report 类 get_report_by_session 用 load_owned_report 变体按 session_id join 查询、get_report 按 report_id、submit_feedback 经 load_owned_report owner-only——Pitfall 5 最易漏的一条）；(c) get_session 后续统计查询沿用返回的 session 行（helper 返回 dict 含 status/position_id/model_version，原 SELECT 的列子集可由 dict 取用或补充查询，保持响应体字段不变）。原有 409 状态判定（submit_answer :143-144）保留不动。submit_feedback 的 item 存在性校验保留。中文错误消息沿用既有文案（"会话不存在"/"报告不存在"）。不改 create_session、GET /positions、服务层任何函数。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_p0_security.py -v 2>&1 | tail -15 && python -m pytest test_m5_backend.py -v 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - server/core/security.py 含 `def load_owned_session(` 与 `def load_owned_report(`
    - server/api/assessment.py 中 8 条路由函数体均出现 load_owned_session( 或 load_owned_report( 调用（grep -c 计 8 处路由调用点）
    - submit_feedback 使用 load_owned_report（report→session 所有权链），不再只查 report 存在性
    - test_p0_security.py 全绿（矩阵所有用例通过）
    - test_m5_backend.py 全绿（owner 主链不回归；单独跑不与 p0 文件同进程）
  </acceptance_criteria>
  <done>8 条候选人资源路由所有权校验生效，越权矩阵全绿，m5 回归通过</done>
</task>

<task type="auto">
  <name>Task 3: test_m6_backend.py 直调适配 + 前端 route guard 修复（D-04）</name>
  <files>server/test_m6_backend.py, web/src/router/index.js</files>
  <read_first>
    - server/test_m6_backend.py（:261-276 _test_feedback_api——submit_feedback 直调将因新 user 参数必挂；:22-32 check() 脚本骨架不得改）
    - web/src/router/index.js（:50-60 两条路由 meta.role:'candidate'；:37-42 /assessment/positions 的 requiresAuth: true 既有写法；:72-90 守卫逻辑）
    - .planning/phases/01-p0/01-PATTERNS.md（"修改现有测试"节 + 前端节）
  </read_first>
  <action>三处小改：(1) test_m6_backend.py 的 _test_feedback_api（:263-270）适配 submit_feedback 新签名——函数将新增 user 参数并 helper 首调，直调需补传种子用户 dict（形如 {"user_id": ctx["user_id"], "username": "...", "role": "candidate", "is_active": 1}，ctx["user_id"] 已在 _seed_full_chain 返回值中），只改调用与必要断言，不动 check() 脚本骨架与 __main__ 流程。(2) web/src/router/index.js :50-60 两条路由（AssessmentChat /assessment/session/:session_id、AssessmentReport /assessment/report/:session_id）的 meta: { role: 'candidate' } 改为 meta: { requiresAuth: true }（与 :37-42 /assessment/positions 一致）；守卫逻辑（:72-90）不动——to.meta.role 为空即放行，登录态由 auth.isLoggedIn 兜底。(3) 不改 PositionAssess.vue（409 提示属 01-04）。m6 的 _test_dual_scoring 直调 score_session 在 in_progress 会话上不受影响（本计划不动服务层护栏），但运行一次全脚本确认无其他直调受 user 注入影响。</action>
  <verify>
    <automated>set -o pipefail && cd server && python test_m6_backend.py 2>&1 | tail -5 && set -o pipefail && cd ../web && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_m6_backend.py 的 submit_feedback 直调传入了含 user_id 的 user dict（grep 到 submit_feedback( 调用带 user 关键字或新参数）
    - python test_m6_backend.py 退出码 0（PASS 全过、FAIL=0）
    - web/src/router/index.js 中 /assessment/session/:session_id 与 /assessment/report/:session_id 两条路由 meta 为 requiresAuth: true，文件内不再有 role: 'candidate'
    - cd web && npx vite build 成功（路由改动无语法错误）
  </acceptance_criteria>
  <done>m6 脚本在新签名下全过；admin 完成测评不再被 route guard 弹回（后端所有权校验仍是权威）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 候选人→API 路由 | 任意持有效 JWT 的登录用户携带他人 session_id/report_id 跨越此边界（本计划修复的 IDOR 面） |
| admin→API 路由 | 合法高权限角色；读豁免/写拒绝边界在本计划 helper 内统一 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure (IDOR) | api/assessment.py 8 条候选人资源路由 | mitigate | 单查询 WHERE user_id=current + 404 统一语义（D-01/D-02），Task 2 |
| T-01-02 | Elevation of Privilege | admin 写操作边界 | mitigate | 写路由 owner-only（不传 allow_admin_read），admin 写同路径 404（D-03），Task 2 矩阵断言 |
| T-01-03 | Information Disclosure（存在性枚举） | 越权返回码 | mitigate | 404 统一"不存在"，不引入 403 分支；ID 为 uuid4 hex 12 位不可枚举（D-01） |
| T-01-04 | Spoofing | JWT_SECRET 默认值（config.py:16） | accept | 已知安全债，REF-6.2 属 Phase 6，本阶段禁止顺手修（Surgical Changes）；测试设 JWT_SECRET=test-secret 规避 |
| T-01-05 | Tampering | SQL 注入面（新 helper 查询） | mitigate | 全部 ? 参数化占位符，禁止值插值（全库既有约定） |
| T-01-06 | Information Disclosure | 密码策略 1 字符下限（schemas.py:11） | accept | 已知安全债，不在本阶段范围（Phase 6） |
</threat_model>

<verification>
1. `cd server && python -m pytest test_p0_security.py -v` — 越权矩阵全绿（candidate↔candidate 8 路由 404、admin 读 200/写 404、admin 自有资源 200、owner 主链不回归）
2. `cd server && python -m pytest test_m5_backend.py -v` — owner 主链回归全绿（单独进程）
3. `cd server && python test_m6_backend.py` — 直调适配后全过（exit 0）
4. `cd web && npx vite build` — router meta 改动编译通过
</verification>

<success_criteria>
- 越权矩阵证明：B 对 A 的任何 session/report/feedback 读写 → 404；admin 读豁免/写拒绝按矩阵断言通过（成功标准 1）
- admin 完成测评可进报告页（D-04 修复）
- m5/m6 回归不破坏（m6 仅 Task 3 适配 _test_feedback_api，脚本体不动）
- D-01/D-02/D-03/D-04 四项决策全部落地（决策 ID 可追溯）
</success_criteria>

<output>
Create `.planning/phases/01-p0/01-01-SUMMARY.md` when done
</output>
