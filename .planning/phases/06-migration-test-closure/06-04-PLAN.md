---
phase: 06-migration-test-closure
plan: "06-04"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - server/test_e2e_full_chain.py
  - server/api/assessment.py
  - web/src/api/index.js
  - web/src/views/assessment/Chat.vue
  - web/src/components/FormCard.vue
autonomous: true
requirements: [REF-7.6]

must_haves:
  truths:
    - "候选人端完整 E2E 脚本通过：注册→登录→选岗→建 session→start→作答/追问→表单 submit-v2→完成→评分→报告→异议（D-73 脚本化 API 层，不引 Playwright）"
    - "刷新/断线/越权/超时四场景覆盖：刷新恢复历史对话、断线重试 SSE、越权访问他人 session 拒绝、超时（计时器到期）处理"
    - "get_session 返回 position_name + messages（当前缺这两字段，前端渲染契约断裂，须补）"
    - "FormCard 切到 submit-v2（form_instance_id + payload + expected_revision）；web/src/api/index.js 的 submitForm 改新端点（当前 47-48 行仍打旧 /forms/submit）"
    - "Chat.vue missing_reasons score_state→中文映射 + 报告失败重试 UI 补上（D-79 必做契约修复）"
  artifacts:
    - path: "server/test_e2e_full_chain.py"
      provides: "全链主链 + 四场景脚本化 E2E"
      contains: "test_"
    - path: "server/api/assessment.py"
      provides: "get_session 补 position_name + messages"
      contains: "position_name"
    - path: "web/src/api/index.js"
      provides: "submitForm 新端点 submit-v2"
      contains: "submit-v2"
  key_links:
    - from: "server/api/assessment.py get_session"
      to: "assessment_message 表"
      via: "JOIN 拉 messages + position name，返回给 Chat.vue load()"
      pattern: "position_name"
    - from: "web/src/components/FormCard.vue onSubmit"
      to: "web/src/api/index.js submitForm"
      via: "传 formInstanceId + payload 打 submit-v2"
      pattern: "submit-v2"
    - from: "server/test_e2e_full_chain.py"
      to: "test_m5_backend.py 的 _stream_answer/_auth_headers helper"
      via: "复用脚本化 API 层调用模式"
      pattern: "_stream_answer"
---

<objective>
候选人端完整 E2E 收口（D-73/REF-7.6）：新建 `server/test_e2e_full_chain.py` 覆盖主链（注册→登录→选岗→session→start→作答/追问→表单→完成→评分→报告→异议）加四场景（刷新恢复 / 断线重试 / 越权拒绝 / 超时）。同时补齐前端契约断裂（D-79 必做）：`get_session` 返回 `position_name` + `messages`、`FormCard` 切 submit-v2、`Chat.vue` 的 missing_reasons 中文映射与报告失败重试。

Purpose: Phase 1 修复后全链无人脚本化守护；前端契约漂移（get_session 缺字段、FormCard 打旧端点）导致真实 UI 流程断裂（[Phase 1 前置] blocker）。
Output: test_e2e_full_chain.py + assessment.py 契约修复 + 前端三文件契约修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-migration-test-closure/06-CONTEXT.md
@.planning/phases/06-migration-test-closure/06-RESEARCH.md
@.planning/phases/06-migration-test-closure/06-PATTERNS.md

<interfaces>
<!-- 本计划复用契约（全部 file:line 已核实） -->

From server/api/assessment.py（get_session 契约修复对象）:
- `get_session`（142-205 行）：当前返回 session_id/status/phase/position_id/model_version/current_question/answered_count/total_count/open_form——缺 position_name 与 messages（D-79 必做）
- `get_form`（906-921 行）/ `submit_form_v2`（924-991 行，body = form_instance_id/payload/expected_revision/idempotency_key）
- `request_report`（1087-1125 行）/ `get_report_by_session`（1128-1142 行）/ `submit_feedback`（1153-1185 行）

From server/test_m5_backend.py（脚本化 API 层 helper 复用样板）:
- `_q`（32-38）/ `_seed_position_with_confirmed_model`（43-73 返回 pid,mid）/ `_seed_question_bank`（76-127）/ `_auth_headers`（130-135）/ `_start`（138-141）/ `_stream_answer`（144-158）

From web/src/api/index.js（前端唯一 API 目标——注意无 server/api/index.js）:
- `submitForm`（47-48 行）仍打旧 `/forms/submit`——须改 submit-v2
- `getForm`（46 行）正确；axios interceptor（11-18 行）Bearer from localStorage

From web/src/components/FormCard.vue:
- `onSubmit`（101-118 行）调 `assessment.submitForm(sessionId, form_type, {...model})`——须改 submit-v2 签名（form_instance_id + payload）

From web/src/views/assessment/Chat.vue:
- `load()`（172-195 行）已消费 `data.messages` 与 `session.position_name`——backend 缺字段导致渲染断裂
- 缺 missing_reasons score_state→中文映射 + report-failure-retry（D-79）

From web/src/utils/sse.js:
- `streamAnswer`（21-28 行）`Authorization: Bearer localStorage` header——断线重试场景用
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_e2e_full_chain.py 全链主链 + 四场景脚本化 E2E（REF-7.6）</name>
  <files>server/test_e2e_full_chain.py</files>
  <read_first>
    - server/test_m5_backend.py（32-158 行全部 helper）
    - server/api/assessment.py（142-205 get_session / 924-991 submit_form_v2 / 1087-1185 report+feedback）
    - .planning/phases/06-migration-test-closure/06-RESEARCH.md（Pattern 3 E2E 链端点）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-73 脚本化 API 层，不引 Playwright）
  </read_first>
  <behavior>
    - 主链：注册→登录拿 token→建 position→建 model(confirmed)→建 session→start→逐题作答/追问→表单 submit-v2→完成→评分→request_report→轮询报告→submit_feedback→断言报告 status/coverage 非空
    - 刷新恢复：get_session 返回 messages 非空且含历史 role/content
    - 断线重试：SSE 流中断后重连 resume 端点不丢已答题
    - 越权拒绝：user B token 访问 user A session 返回 401/403
    - 超时：计时器到期后状态转 TIMEOUT（或等价降级分支）
    - 全部用脚本化 API 层（TestClient / 直接函数调用），不引 Playwright
  </behavior>
  <action>新建 server/test_e2e_full_chain.py，复用 test_m5_backend.py 的 `_q`/`_seed_position_with_confirmed_model`/`_seed_question_bank`/`_auth_headers`/`_start`/`_stream_answer` helper 模式（直接在本文件内复制/import 这些 helper，不跨测试模块 import——遵守「同一进程不 import 两测试模块」纪律）。主链与四场景各一条 `test_*`（共约 5 条）。mock 三件套由 06-01 conftest 提供（无需本文件设 env）。主链断言报告 status 与 coverage 非空、report_text 可序列化；四场景各自断言（刷新 messages 非空 / 重试不丢题 / 越权 401/403 / 超时状态）。</action>
  <verify>
    <automated>cd server && python -m pytest test_e2e_full_chain.py -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_" server/test_e2e_full_chain.py` ≥ 5（主链 + 四场景）
    - `grep -n "submit_v2\|request_report\|submit_feedback" server/test_e2e_full_chain.py` 命中主链调用
    - `grep -n "401\|403\|resume\|TIMEOUT\|messages" server/test_e2e_full_chain.py` 命中四场景断言
    - 上条 pytest 命令全部 passed
  </acceptance_criteria>
  <done>E2E 主链 + 四场景脚本化测试全绿（D-73 不引 Playwright）</done>
</task>

<task type="auto">
  <name>Task 2: get_session 补 position_name + messages（D-79 契约修复）</name>
  <files>server/api/assessment.py</files>
  <read_first>
    - server/api/assessment.py（142-205 get_session；906-991 form；1128-1142 get_report_by_session）
    - server/db.py（assessment_message 表 DDL + assessment_session.position_id）
    - .planning/phases/06-migration-test-closure/06-PATTERNS.md（get_session 契约修复样例）
  </read_first>
  <action>在 server/api/assessment.py 的 `get_session`（142-205）返回 dict 中补两个字段：`position_name`（JOIN position 表取 name，session.position_id → position.position_id；position_id 为空则返回 None 或 ""，不抛异常）与 `messages`（`SELECT role, content, created_at FROM assessment_message WHERE session_id=? ORDER BY sequence_no, created_at`，返回 list[dict]，空则 []）。不要改其余字段键名与既有测试断言依赖的字段。前端 Chat.vue `load()` 已消费 `data.messages` 与 `session.position_name`，此处是后端契约补齐而非前端改动。</action>
  <verify>
    <automated>cd server && python -m pytest test_e2e_full_chain.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "position_name\|messages" server/api/assessment.py` 命中 get_session 返回处
    - `grep -n "FROM assessment_message" server/api/assessment.py` 命中 messages 查询
    - E2E 刷新恢复场景（断言 messages 非空）通过
  </acceptance_criteria>
  <done>get_session 返回 position_name + messages；前端刷新恢复不再渲染断裂</done>
</task>

<task type="auto">
  <name>Task 3: 前端契约修复——submit-v2 + missing_reasons 中文映射 + 报告失败重试（D-79）</name>
  <files>web/src/api/index.js, web/src/components/FormCard.vue, web/src/views/assessment/Chat.vue</files>
  <read_first>
    - web/src/api/index.js（46-48 getForm/submitForm；11-18 interceptor）
    - web/src/components/FormCard.vue（101-118 onSubmit）
    - web/src/views/assessment/Chat.vue（172-195 load；缺 missing_reasons 映射与 report-failure-retry）
    - .planning/phases/06-migration-test-closure/06-PATTERNS.md（FormCard submit-v2 修复样例）
  </read_first>
  <action>三处修复。① web/src/api/index.js：`submitForm`（47-48）改打 `POST /assessment/sessions/{sessionId}/forms/submit-v2`，body 改为 `{ form_instance_id, payload, expected_revision }`（签名 `submitForm(sessionId, formInstanceId, payload, expectedRevision)`），保留 axios Bearer interceptor。② web/src/components/FormCard.vue：`onSubmit`（101-118）改为调 `assessment.submitForm(props.sessionId, props.formId, {...model}, props.expectedRevision)`（formId 即 form_instance_id；expected_revision 从组件 props 或 schema 取）。③ web/src/views/assessment/Chat.vue：补 missing_reasons 的 score_state→中文映射常量（INVALIDATED→无效、INCOMPLETE→未完成、CONFLICT→冲突、HUMAN_REVIEW_REQUIRED→需人工复核、其余英文原样回退），在报告渲染处应用；补报告失败重试 UI（报告 status=failed 时显示「重试」按钮，点击重新调 request_report）。不引新依赖，不重构无关代码。</action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "submit-v2" web/src/api/index.js` 命中新端点
    - `grep -n "submitForm(props.sessionId, props.formId" web/src/components/FormCard.vue` 命中新签名调用
    - `grep -n "INVALIDATED\|HUMAN_REVIEW_REQUIRED\|重试" web/src/views/assessment/Chat.vue` 命中映射与重试 UI
    - 上条 npm run build 成功（无编译错误）
  </acceptance_criteria>
  <done>前端三契约修复完成：submit-v2 接线 + missing_reasons 中文映射 + 报告失败重试；npm build 绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 候选人端 → API | 未授权 token 跨 session 访问，须越权拒绝（四场景之一） |
| 前端 → API | Bearer token 经 localStorage 传递，submit-v2 携带 form_instance_id 防错表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation | server/api/assessment.py get_session | mitigate | 复用 load_owned_session 校验；E2E 越权场景断言 401/403 |
| T-06-11 | Tampering | web/src/api/index.js submit-v2 | mitigate | form_instance_id 服务端校验 + expected_revision 幂等（submit_form_v2 已有） |
| T-06-12 | Info Disclosure | server/api/assessment.py messages | mitigate | messages 仅返回当前登录用户自己的 session（load_owned_session 保证） |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
逐文件：`cd server && python -m pytest test_e2e_full_chain.py -q` 全绿；`cd web && npm run build` 成功。全量收集：`cd server && python -m pytest . --collect-only -q 2>&1 | tail -20`（06-02 后应无 fixture error，新增 e2e 可收集）。
</verification>

<success_criteria>
E2E 主链 + 四场景脚本化测试全绿；get_session 补 position_name + messages；FormCard 切 submit-v2；Chat.vue 补 missing_reasons 中文映射与报告失败重试；npm build 绿；REF-7.6 候选人端完整 E2E 兑现。
</success_criteria>

<output>
Create `.planning/phases/06-migration-test-closure/06-04-SUMMARY.md` when done
</output>
