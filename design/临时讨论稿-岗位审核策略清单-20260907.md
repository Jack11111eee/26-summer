# 岗位审核策略清单

> 生成：2026-09-07，回填 --apply 与 draft 清理之后 · 状态快照只读导出
> 性质：**决策清单，未实施任何一项**。全部动作为建议，每节末尾标注「需用户裁决」项。

## 0. 当前状态快照（回填后）

| 范围 | 数量 | 说明 |
|---|---|---|
| pending_review（有 JD） | **537** | 回填新建，全部源 title 归一化名；n=1 占 502，n≥3 仅 7 |
| active（有 JD） | **90** | 老岗幸存者：名称与源归一化名恰好重合的（算法 114、图像算法 37、AI算法 30…
| active（空巢） | **353** | 900 条 JD 挪走后失血，无任何 JD 挂靠 |
| competency_model / item | 0 / 0 | 两个作废 draft 已删（5 个下游引用检查全 0，删除安全） |

完整性已验证：job_title 与源 1000/1000 对齐；归岗==源归一化名 902/1000（98 条差异全部为大小写归并，如 `ai算法`→`AI算法`，NOCASE 匹配的正确行为，非错误）。回退点：`data/backups/app_20260907_pre_backfill.db`。

---

## 1. 537 个 pending_review 的审核策略

### 事实基础
- **502 个 n=1 单例岗**（93.5%）：一条 JD 一个岗，审核内容 = 看一眼岗位名是否成立
- 7 个 n≥3 值得人工看：算法专家(8)、高级图像算法(5)、高级算法(5)、ai算法专家(4)、高级nlp算法(3)、高级ai算法(3)、资深算法(7→n=3)
- 28 个 n=2、1 个 n=4

### 建议策略（三档）

**A. 批量 approve 全部 537（含 n=1）** —— 忠实源 title，每个岗位独立存在
- 优点：零判断成本，与「源 title 为真值」的既定方向一致；聚合按岗独立，不互相污染
- 缺点：443+537≈980 个岗位对测评端岗位列表是灾难（候选人选岗页面要翻 900+ 卡片）
- 适配前提：**接受模块一轮询 5s 的列表膨胀**（P5 岗位列表仅显示 active+confirmed，聚合确认后可见）

**B. 单例岗暂缓 approve，按需激活** —— 只 approve n≥2 的 35 个
- 优点：测评端列表保持干净；单例岗的聚合天然是单 JD 镜像（此前的分析结论：人审成本≈复看一条 JD），等真实测评需求出现再激活
- 缺点：“按需”需要人工记得回来开；442 个潜在可用能力沉睡

**C. 单例岗并入家族骨架岗（合并降维）** —— 用 alias 表把 n=1 归入近义大岗（如「高级图像算法」5→并入「图像算法」37）
- 优点：岗位基数从 ~627 收敛到 ~120（估计值，取决于合并清单），聚合频次 n 变大、信号变强
- 缺点：**回到了「合并稀释特异性」的老问题**（此前讨论已否决过族池方案的数据基础）；且 alias 合并一旦做了，逆向拆分要人工逐条
- 注意：C 不必全量做——可只对「明显是同岗位不同后缀」的做（高级/资深/专家系列），保持语义零争议合并

**建议**：**B 起步 + C 的窄版（仅高级/资深/专家系列并入同主名）**。980→约 150 岗位量级，聚合信号从 n=1 起步的岗不虚耗 LLM#3。

> ⚠️ 需用户裁决：选 A / B / C（或组合）。C 范围（哪些系列并入）需另给清单。

### 单例岗的聚合与确认（若选 B）
- 单例岗聚合成 draft 无害（不进测评端），但 **confirm 按需**——此前裁决过：单例岗的确认价值 = 复看一条 JD，confirm 前置无意义
- draft 生成本身可以全量跑（105+35 岗位量级的 LLM#3 成本约为 0，冲突几乎全在多 JD 岗）

---

## 2. 353 个空巢 active 岗的处置

**事实**：这些是 LLM 重命名时期的岗位骨架，900 条 JD 已挪走，0 挂靠。其中含「SLAM算法工程师（定位方向）」（draft v1 宿主，已删模型）。

**选项**：
- **a. 批量下架（推荐）**：脚本将 status→inactive（不是删除）。岗位名留档，若未来新 JD 的源 title 归一化后再撞名，走「同名重新激活」或新建待审——两条路径都已存在
- **b. 批量 reject 删除**：彻底清除。风险：position 被引用的历史痕迹（若未来有 competency_model/question_bank/assessment_session 引用会触发 409——当前 0 引用，但 audit 痕迹消失）
- **c. 保留不动**：零成本，但 P1 岗位库列表长期背着 353 个死岗

> ⚠️ 需用户裁决：a / b / c。**建议 a**（下架可逆、删除不可逆、保留脏）。

**执行提醒**：353 个空巢岗的名称与 537 个 pending 岗的名称**无 lower 同名碰撞**（已查 0 组），下架不会与审核动作互踩。

---

## 3. 90 个幸存 active 岗的复核

回填后仍有 JD 的 90 个 active 老岗，其中 21 个 n≥4 是**天然聚合主力**（算法 114、图像算法 37、AI算法 30、NLP算法 19、视觉算法 14、SLAM算法 12…）。

**已具备直接批量聚合的条件**（无需任何审核动作——它们上一轮已 approve 过，status=active 不变）。

**建议**：这 90 个（尤其 n≥4 的 21 个）作为**质量验收的聚合样本**先行聚合，验证「干净输入上的 r/req + LLM#3」产出的模型质量——此前「计算机视觉算法 v1」暴露的长尾问题（144 项 plus 地板权重）在正确归岗后是否收敛，直接看这批。

---

## 4. 建议的完整执行序（审核轮之后）

```
审核轮（本清单裁决后）
  ├─ 537 pending 按 1 节策略处理
  ├─ 353 空巢按 2 节策略处理
  └─ 90 幸存 active 不动
→ 批量聚合（active 且有 JD 的岗位全量 run_aggregate；n≥4 的 21 岗先行质量验收）
→ 分层人审（n≥4 全审，n=2-3 抽检，单例按需）
→ confirm → 模块二题库生成
```

---

## 5. 数据附录（供裁决参考）

**幸存 active n≥4（21 个）**：算法 114 / 图像算法 37 / AI算法 30 / NLP算法 19 / 视觉算法 14 / SLAM算法 12 / 感知算法 7 / 大模型算法 7 / AI 7 / 路径规划算法 6 / 算法开发 6 / 机器视觉算法 6 / 音频算法 6 / 控制算法 5 / 计算机视觉算法 5 / 深度学习算法 5+ / 激光SLAM算法 5 / 数字图像处理算法 4 / 图像识别算法 4 / …（完整列表可从 DB 导出）

**pending n≥3（7 个）**：算法专家 8 / 高级图像算法 5 / 高级算法 5 / 资深算法 3 / 高级nlp算法 3 / 高级ai算法 3 / ai算法专家 4

**回填归岗摘要**：911 回写 / 900 换岗 / 新建 537 pending / 李 98 条大小写形态并岗（合规）
