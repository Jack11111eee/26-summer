---
phase: 03-sse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db.py
  - server/schemas.py
  - server/services/forms.py
  - server/services/aggregation.py
  - server/services/scoring.py
  - server/api/assessment.py
  - server/api/admin/forms.py
  - server/main.py
  - server/test_phase3_forms.py
  - server/test_p0_chain.py
  - server/test_p0_security.py
autonomous: true
requirements: [REF-2.4, REF-3.3, REF-4.7, REF-4.10]
must_haves:
  truths:
    - "池耗尽且存在未采集 gate item 时不再直接 finish，而是 INSERT form_instance（status='rendered'）+ assistant 消息含 📎[form:{form_instance_id}] 标记 + 返回 action='form'（D-30）——gate 全采集的会话池耗尽仍走原 finish 路径逐字不变"
    - "GET /forms/{form_instance_id} 只返回渲染白名单字段（form_type/title/fields[].name|label|type|required|options|placeholder），years 门槛值与 required_level 不出现在响应 JSON（SC-1）"
    - "表单提交经六维校验序（所有权→状态→revision→必填→枚举→长度）各失败路径有独立 error_code 409/404；重复提交同一 instance 返回第一次结果（409 FORM_ALREADY_SUBMITTED 幂等语义）"
    - "gate 判定写 question_score gate 行：gate_result/gate_status/gate_reason/evaluated_schema_version/evaluated_at 五列落值，question_id=NULL（§16.1 结构化结果）——由四步放宽法放开 NOT NULL（02-RESEARCH A2，ASSUMED 呈报项）"
    - "人工覆盖列就位且二次确认生效：无 override_reason 的覆盖请求被 422 拒绝（D-31）"
    - "同一 instance 修订 = INSERT 新行（revision+1，instance_id 不变）+ 旧行 status='superseded'——UPDATE 语句出现 payload_json/snapshot 列名即违规（Pitfall 3）"
    - "score_session 重算不再吞掉 gate 行（score_session 的 DELETE 现状会清空 session 全部 question_score）"
    - "experience/qualification 题不出现在动态选题终局（全 session aq 关联题库 category ⊆ {hard,soft}——SC-2 前半 02-02 已落，本计划 form 链补终局采集位）"
  artifacts:
    - path: "server/db.py"
      provides: "form_instance 新表 DDL + _migrate_form_instance + question_score gate 五列 + 覆盖四列 + question_id/score_state NOT NULL 四步放宽"
      contains: "form_instance"
    - path: "server/services/forms.py"
      provides: "FORM_SCHEMA_V1 常量 + FORM_STATUS 枚举 + render_form_instance（幂等：已有 open rendered 实例不重复建）+ validate 六维 + gate 结构化判定 + gate 行写入"
      contains: "def render_form_instance"
    - path: "server/api/assessment.py"
      provides: "池耗尽两处 render 插入（:323-334 主链 / :343-353 legacy）+ GET /forms/{id} + POST /sessions/{id}/forms/submit-v2"
      contains: "forms/submit-v2"
    - path: "server/services/aggregation.py"
      provides: "_gate_check 迁移为双源读（gate 行优先 → form_submission payload 兜底——过渡期 D-31）"
      contains: "gate_result"
    - path: "server/api/admin/forms.py"
      provides: "gate 人工覆盖端点（require_admin + override_reason 强制 + rowcount 404）"
      contains: "require_admin"
    - path: "server/test_phase3_forms.py"
      provides: "表单全链断言：DDL 迁移（旧库放宽路径）+ render→GET→submit→gate→revision 链 + 六维错误码 + admin 覆盖"
      contains: "test_form_"
  key_links:
    - from: "server/api/assessment.py picked is None 分支"
      to: "server/services/forms.py render_form_instance"
      via: "gate 未采集判定后同事务调用（FORM_RENDERED 事件 + assistant 消息）"
      pattern: "render_form_instance\\("
    - from: "server/api/assessment.py submit-v2"
      to: "server/services/forms.py 表单校验与 gate 写入"
      via: "六维校验 → gate 行 INSERT question_score → GATE_EVALUATED 事件 → 之后 select_next_question 继续（gate 已采集才放行 finish）"
      pattern: "GATE_EVALUATED"
    - from: "server/services/scoring.py score_session DELETE"
      to: "question_score gate 行"
      via: "DELETE 加 WHERE gate_result IS NULL 条件（gate 行非评分行，重算不得吞掉）"
      pattern: "gate_result IS NULL"
    - from: "server/services/aggregation.py _gate_check"
      to: "question_score gate 行 / form_submission payload"
      via: "双源读：先查 gate 行（新链，gate_result 列）→ 无行回退 _load_form_payload（旧链兼容）"
      pattern: "_load_form_payload"
---

<objective>
表单链落地（SSOT §16.1）：form_instance 新表（不可变 schema 快照 + status 生命周期 + 不可变 revision）+ question_score gate 结构化五列与人工覆盖四列（NOT NULL 四步放宽法——A2 ASSUMED 呈报项）+ 池耗尽两处扩展点插入 render 分支 + GET /forms/{form_instance_id} 渲染白名单 + submit 六维校验 + GATE_EVALUATED 事件 + admin 覆盖写入口 + _gate_check 双源迁移 + score_session 的 gate 行保留修正。

Purpose: REF-2.4/3.3/4.10/4.7——SC-1/SC-2（表单成为代码触发的真实链路；gate 判定不再从自由 payload 猜测）。D-29~D-32 落地。02-02 的「池耗尽即 finish」在本计划扩展为「池耗尽 → gate 采集 → 表单链 → 之后才 finish」；gate=0 会话（如 m5/p0_security 多数种子）行为逐字不变。
Output: db.py DDL/迁移 + forms.py 新服务 + assessment.py 三端点两插入点 + admin/forms.py + aggregation/scoring 消费迁移 + 全链测试（新建 test_phase3_forms.py + p0_chain/p0_security 答题闭链加表单步骤适配）。

上下文预算注记：本计划 4 auto 任务、11 文件，处 02-02 同密度水位；各任务原子契约绑定（DDL→服务→端点→回归），执行者按任务序推进，若进 DEGRADING 区（>50%）Task 4 的 p0 回归适配可请示拆后续计划——Task 1-3 自身闭合表单主体。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sse/03-CONTEXT.md
@.planning/phases/03-sse/03-PATTERNS.md
@.planning/phases/02-dynamic-selection/02-02-SUMMARY.md
@.planning/phases/02-dynamic-selection/02-04-SUMMARY.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

## form_instance DDL（db.py _DDL 追加 + _migrate_form_instance 双轨——PATTERNS db.py 惯例）
CREATE TABLE IF NOT EXISTS form_instance (
  form_instance_id TEXT PRIMARY KEY,
  session_id       TEXT NOT NULL REFERENCES assessment_session,
  form_type        TEXT NOT NULL,          -- 'experience' | 'qualification' | 'gate_combined'
  schema_version   TEXT NOT NULL,          -- "v1" 起字符串版本（D-29）
  schema_snapshot  TEXT NOT NULL,          -- 代码常量 JSON 整体落库（创建时固定）
  status           TEXT NOT NULL DEFAULT 'rendered',  -- rendered/submitted/superseded 代码校验（N11 无 CHECK）
  revision         INTEGER NOT NULL DEFAULT 1,        -- 不可变修订：修订=INSERT 新行 revision+1
  payload_json     TEXT,                   -- submit 后填（渲染列/内部判分列分栏——白名单见 GET）
  created_at       TEXT NOT NULL,
  submitted_at     TEXT
);
迁移函数 _migrate_form_instance(conn)：PRAGMA table_info 嗅探（照 02-01 _migrate_question_bank_v2 形态，新表只在存量库补建——CREATE IF NOT EXISTS 幂等即可）；register 加入 init_db 迁移序列（db.py:462-477 末尾，_migrate_question_score_v2 之后）。

## question_score gate 列 + NOT NULL 放宽（A2 四步放宽法——ASSUMED 呈报项，标注在迁移 docstring）
- gate 五列（照 if-not-in-ADD）：gate_result TEXT（bool JSON 串或 'true'/'false' 字面量——plan 定 'true'/'false'）、gate_status TEXT（'EVALUATED'）、gate_reason TEXT（现 _gate_check 的中文 reason 串）、evaluated_schema_version TEXT（"v1"）、evaluated_at TEXT。全部可空（gate 行才填，普通评分行 NULL）。
- 覆盖四列：automated_gate_result TEXT、human_override TEXT、override_reason TEXT、reviewer_id TEXT（D-31 全集）。
- question_id/score_state NOT NULL 放宽（gate 行这两列 NULL——研究实验 9/10 实测四步法）：ADD question_id_v2 TEXT → UPDATE question_score SET question_id_v2=question_id → DROP COLUMN question_id → ALTER RENAME（`ALTER TABLE question_score RENAME COLUMN question_id_v2 TO question_id`）；score_state 同法（score_state_v2，回填值保持 NOT NULL 时代值 'SCORED'）。放宽后两列可空——普通评分行照常写值，业务读路径（scoring/aggregation/report 的 SELECT 列清单）零改动。
- _DDL 双轨：CREATE TABLE question_score 的 question_id/score_state 声明同步去 NOT NULL + gate 五列与覆盖四列写进 _DDL（新库直建路径——02-01 双轨纪律）。
- ASSUMED 标记：迁移 docstring 注明「A2 四步放宽法——02-RESEARCH 实验验证；gate 行 question_id NULL 与 D-31 字面 'question_score gate 项行' 的兼容取舍，关口包呈报项」。

## FORM_SCHEMA_V1 常量（services/forms.py——Claude's Discretion 就近设计：字段名照 aggregation._gate_check 现行消费口径）
FORM_SCHEMA_VERSION = "v1"
FORM_SCHEMA = {
  "form_type": "gate_combined", "title": "资格核验",
  "fields": [
    {"name": "years_of_experience", "label": "工作年限（年）", "type": "number",
     "required": True, "max_len": 10},                       # _gate_check:56-63 消费
    # qualification items 动态展开：模型 gate=1 的 qualification item 各生成
    # {"name": std_name, "label": std_name, "type": "select", "required": True,
    #  "options": ["是", "否"]} —— _gate_check:66-69 消费（'是' 在真值表）
  ],  # 表达式写意：快照 = base 模板 + 会话模型 gate items 展开后的 dict，render 时 json.dumps
}
"fields 数组按会话模型动态展开" 的实例快照即 §16.1「instance 创建时存不可变 schema 快照」。白名单 GET 响应只含 form_type/title/fields（FormCard.vue:81 契约 `{form_type, title?, fields:[{name,label,type,required?,options?,placeholder?}]}`——PATTERNS 锁定）；years 门槛值（competency_item.years）是内部判分字段，不进快照 fields、不进 GET。

## 服务形态（services/forms.py——两个锚：question_selection._instantiate 写库型 + difficulty 接 conn 不 commit）
- render_form_instance(conn, session_id) -> dict：幂等——先查该 session 的 open 实例（status='rendered' 且 revision 最大）：存在直接返回（不重复 INSERT，防 UI 旧链提交后循环重渲染）；不存在 → 展开 gate items 构造快照 → INSERT form_instance(status='rendered', revision=1, schema_snapshot=json.dumps(..., ensure_ascii=False)) → append_event FORM_RENDERED（payload 含 form_instance_id/form_type/schema_version）→ return dict(form_instance_id, form_type, fields=渲染白名单)。接 conn 不 commit（调用者主事务统一——difficulty.py:27-28 契约照写模块 docstring）。
- _all_gate_items_collected(conn, session_id) -> bool：模型 gate=1 items（照 question_selection._uncovered_required_items :350-361 覆盖判定形态 JOIN form_instance）全部有 status='submitted' 实例 → True。无 gate item 模型恒 True（m5/p0_security gate=0 种子面不动）。
- validate_and_submit(conn, *, session_id, form_instance_id, payload, expected_revision, user) -> dict：六维序（①所有权 load_owned_session 前置在 API 层；②status=='rendered' 否则 409 FORM_ALREADY_SUBMITTED；③expected_revision 匹配否则 409 FORM_INSTANCE_REVISION_CONFLICT；④必填/⑤枚举/⑥长度——schema_snapshot 的 fields 逐项校验，失败 422 detail 列缺失字段）。成功：UPDATE status='submitted', submitted_at, payload_json（status/payload 是生命周期列不是内容列——Pitfall 3 只锁 schema_snapshot/payload_json 修订面）→ 每个已提交 gate item 写 gate 行（INSERT question_score(score 10 列 + gate 五列, question_id=NULL, score_state=NULL, gate_result='true'/'false', gate_reason=现 _gate_check 中文串, evaluated_schema_version='v1', evaluated_at)）→ append_event FORM_SUBMITTED + GATE_EVALUATED（每 gate item 一条，payload 含 item_id/gate_result/gate_reason）→ 接 conn 不 commit。
- 修订（admin 侧再触发 render）：INSERT 新行 revision=旧+1 同 form_instance_id + 旧行 UPDATE status='superseded'（仅 status 列）。

## 消费迁移
- scoring.py score_session（:243-251 executemany 前的 DELETE :243）：`DELETE FROM question_score WHERE session_id=? AND gate_result IS NULL`——gate 行非评分重算面，必须幸存（顺序链 _generate_report_task 全场超时收尾（03-04）与 UI request_report 都走 score_session）。
- aggregation.py _gate_check 迁移：aggregate_session_scores 的 gate 消费段（:116-134）改为双源——先查该 session+item 的 gate 行（SELECT gate_result, gate_reason FROM question_score WHERE session_id=? AND item_id=? AND gate_result IS NOT NULL）→ 有行取 gate_result=='true' 与 gate_reason；无行回退现行 _gate_check(item, form_payload)（旧链 form_submission payload——m6 脚本测试全部走此路径，行为不变）。:32-45 _load_form_payload 照抄保留。weight 公式与七键结构零改动（D-31）。_gate_check 签名保留（旧链兜底主体），新增 _gate_row(conn, session_id, item_id) 小函数。

## 端点契约（api/assessment.py + api/admin/forms.py）
- render 插入两处对称（主链 :323-334 与 legacy :343-353 的 picked/nxt is None 分支）：`picked is None` 后先 `_all_gate_items_collected` 判定 → False 走 render_form_instance + assistant 消息 INSERT（角色 assistant，content=决策 reply + f"请先填写资格核验表单 📎[form:{form_instance_id}]"——Chat.vue:159-160 extractFormId 正则 `/📎\[form:([^\]]+)\]/` 逐字对齐）+ conn.commit() + return {"action": "form", "reply": ..., "question_id": question_id, "next_question_id": None, "score_live": ...}；True 走原 finish 路径逐字不动（D-30 扩展不改 02-02 口径）。
- GET /forms/{form_instance_id}（web/src/api/index.js:46 锁定 URL `/api/assessment/forms/{formId}`——不带 /sessions 前缀）：双 404（instance 不存在 / 非 owner——session 归属经 form_instance.session_id 反查 load_owned_session）→ 200 渲染白名单 dict（FormCard.vue:81 契约）。骨架照 get_confirmed_model（:55-74）。
- POST /sessions/{session_id}/forms/submit-v2（Pydantic body FormSubmitRequest——schemas.py 新增：form_instance_id str、schema_version str、expected_revision int=1、payload dict、idempotency_key Optional（D-36 本期占位，03-03 接幂等链））：load_owned_session → validate_and_submit → conn.commit → return {"form_instance_id": ..., "status": "submitted", "gate_results": [{item_id, gate_result, gate_reason}...]}。骨架照 forms/submit 旧端点（:469-486）。旧端点 /forms/submit 逐字不动（D-29 兼容旧 UI——FormCard submitForm 仍调旧路由；新链后端契约完备，前端切换属 Phase 6 收口，注明非本 phase 面）。
- GET /sessions/{session_id} 附加（FormCard 渲染恢复所需最小面——Chat.vue refreshSession 消费会话）：get_session 响应加 "open_form": 白名单 dict 或 None（有 rendered 实例时返回）——前端 load() 已消费 current_question 同构路径，零前端改动的前提下刷新页面表单可恢复。
- admin 覆盖端点（api/admin/forms.py 新文件，照 admin/feedback.py:11-40 全套）：POST /api/admin/forms/gate-override，body={form_instance_id or question_score score_id, item_id, human_override: bool, override_reason: str = Field(min_length=1)——422 二次确认}; require_admin 路由级挂载；UPDATE question_score SET human_override=?, override_reason=?, reviewer_id=user["user_id"] WHERE ...（按 session_id+item_id 定位 gate 行，rowcount==0 → 404）+ append_event GATE_OVERRIDDEN（payload 含 item_id/override_reason/reviewer_id——§13.2 枚举 OVERRIDDEN 对应）。main.py 两行注册（:43-44 import 邻位 + :79-80 include 邻位）。

## 现行代码锚点（02-04 交付后形态）
- assessment.py：三相 commit（:217-218/:311/:316/:331/:350/:357）；池耗尽主链 :321-334、legacy :335-353；assistant INSERT :229-236；OBSERVATION_CLASSIFIED :238-243；forms 旧端点 :469-486；create_session :79-117；get_session :120-167（派发 :135-137）
- db.py：question_score DDL :199-215（question_id NOT NULL REFERENCES :203）；迁移 register :462-477；新表分节注释惯例 :276-291；_migrate_question_score_v2 :436-459
- aggregation.py：_gate_check :48-70（迁移对象——判定逻辑保留只换数据源）；gate 消费段 :116-134（结构不动）；_load_form_payload :32-45
- scoring.py：DELETE+executemany :243-251
- 测试：test_p0_chain.py 种子 gate=1（:76-78 后端开发经验）+ _answer_whole_session（:172-184 逐题 GET→POST 循环 + :182 action in ("next","finish") 断言）；test_p0_security.py 种子 gate=1（02-02 SUMMARY 记录）+ 答题闭链 :171-182 形态；迁移双路径模板 test_phase2_migration.py:12-60（_OLD_DDL 形态——复制不 import）
- 前端零改动核对锚（不落 files_modified）：FormCard.vue:81/91/109、api/index.js:46-48、Chat.vue:159-162
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test_phase3_forms.py 表单全链断言（先红）</name>
  <files>server/test_phase3_forms.py</files>
  <read_first>
    - server/test_p0_chain.py（:30-57 三件套文件头 + :59-143 种子 fixtures——gate=1 行 :76-78 形态 + :48-54 _q helper + :456-471 事件断言）
    - server/test_phase2_migration.py（:12-60 _OLD_DDL 旧库模拟双路径形态——复制不 import）
    - .planning/phases/03-sse/03-RESEARCH.md（Code Examples #2/#3 + Pitfall 3/4）
  </read_first>
  <behavior>
    - test_old_db_gateway_relaxation：老库模拟（_OLD_DDL 建 question_score 旧行含 NOT NULL 列 + 建无 form_instance 的 19 表旧结构）→ init_db → form_instance 表存在；PRAGMA table_info(question_score) 含 gate 五列 + 覆盖四列 + question_id/score_state 均可空（旧行 question_id 值保留——放宽不丢数据）
    - test_new_db_direct_path：init_db 新库直建 → form_instance 列集完整 + question_score 直建列集含 gate 九列
    - test_render_on_exhaustion：种子模型含 gate=1 经验 item → 答完普通题（答题闭链照 p0_chain 形态：逐题 GET current_question → POST answer）→ 响应 action=='form' + reply 含 📎[form: 前缀 + 正则提取 id；_q 查 form_instance 行 status='rendered' + schema_snapshot 可 json.loads 且含 fields；assistant 消息行存在（content 含标记串）；FORM_RENDERED 事件行存在
    - test_render_idempotent_open：同一会话再触发（再答/再进池耗尽）不产生第二个 rendered 实例（_q COUNT rendered==1）
    - test_get_form_whitelist：GET /api/assessment/forms/{form_instance_id} → 200 + 响应键集合 ⊆ {form_type, title, fields} 且 fields[i] 键 ⊆ {name,label,type,required,options,placeholder}；响应 JSON 串不含 competency 的 years 门槛值与 "required_level"（白名单分栏断言——.fields 内不出现数字门槛）
    - test_get_form_ownership：他人 token GET → 404（D-01 统一不存在语义）
    - test_submit_six_dimensions：submit-v2 成功路径（payload={years_of_experience:5, <qual_std_name>:"是"}）→ 201 + status='submitted' + gate_results 数组每项含 item_id/gate_result；缺必填 → 422；枚举外值 → 409/422 DISTINCT error_code；超长 → 422；status 非 rendered（重提交）→ 409 FORM_ALREADY_SUBMITTED 且返回首次结果行 payload_json 不变；revision 不匹配 → 409 FORM_INSTANCE_REVISION_CONFLICT
    - test_gate_row_written：提交后 _q 查 question_score：gate item 各一行（question_id IS NULL + score_state IS NULL + gate_result='true'/'false' 按构造 + gate_reason 非空 + evaluated_schema_version=='v1'）；GATE_EVALUATED 事件行数 == gate item 数
    - test_submit_unblocks_finish：表单提交后再答/再进 → 池耗尽 + gate 全采集 → finish（status='completed' + SESSION_COMPLETED——02-02 finish 语义恢复）
    - test_revision_immutable：手工触发修订路径（admin 或直插造修订——最小：直插 revision=2 同 instance_id 行 + 旧行 UPDATE superseded）→ _q 断言旧行 status='superseded' 且新行 revision=2 + schema_snapshot 未被 UPDATE（两行快照各自独立）；active 查询 ORDER BY revision DESC LIMIT 1 命中新行
    - test_admin_override_requires_reason：admin token POST gate-override 无 override_reason → 422；带 reason → 200 + question_score 该行 human_override/reviewer_id 落值 + GATE_OVERRIDDEN 事件；非 admin → 401/403
    - test_dual_source_precedence：会话 A 走新链（form_instance 提交，gate 行 gate_result='false'）+ 直插 form_submission payload 使旧判定 pass → aggregation 该 gate item passed==False（gate 行优先）；会话 B 只走旧链（form_submission）→ 旧 _gate_check 判定不变（m6 兼容断言）
    - test_score_session_preserves_gate_rows：提交表单后调 score_session（allow_completed 或 completed 会话经评分链）→ gate 行仍在（_q COUNT 不为 0）
    - test_exp_qual_not_in_selection：全 session aq JOIN question_bank 的 category 集合 ⊆ {'hard_skill','soft_skill'}（REF-3.3 终局断言）
  </behavior>
  <action>新建 server/test_phase3_forms.py，文件头照 test_p0_chain.py:30-57 三件套模板（env → tempfile DB → mock → TestClient；模块 docstring 注明 REF 号 + 运行命令；不 import 其他测试模块）。种子：_seed_position_with_confirmed_model 直复制后改造 items——gate=1 的 experience（years=3）与 qualification 各一 + 普通 hard×2/soft×1（照 p0_chain :76-78 gate 行形态；gate item 的题库题行照 02-04 形态挂 scope='general' 但 selection 不取——gate item 不进普通池由 ORDINARY_CATEGORIES 保证）。答题闭链照 p0_chain :172-184 复制。迁移双路径段：_OLD_DDL 旧结构照 test_phase2_migration.py:12-60 复制形态（独立临时库文件 + 子进程或独立 init_db 路径——同文件内以独立 sqlite3 连接建旧表后跑 init_db，照 test_phase2_migration 现成手法）。注意：本任务提交红（render 端点/forms 服务/gate 列不存在）——import 层可用（db 存量），断言层红。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase3_forms.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在且 pytest 可收集（单文件单进程）
    - DDL 迁移双路径断言（老库放宽保数据 + 新库直建列集）
    - render→GET 白名单→submit 六维→gate 行→GATE_EVALUATED→再答 finish 全链断言就位
    - render 幂等（open rendered 不重复建）+ revision 不可变断言
    - 双源优先级断言（gate 行 > form_submission payload）
    - admin 覆盖二次确认（无 reason 422）断言
    - score_session 保留 gate 行断言
    - 本任务提交红，Task 2-4 后转绿
  </acceptance_criteria>
  <done>表单链全部行为断言就位，红测证明现状池耗尽直接 finish 且无 form_instance 表</done>
</task>

<task type="auto">
  <name>Task 2: db.py — form_instance 新表 + question_score gate 列与四步放宽迁移</name>
  <files>server/db.py</files>
  <read_first>
    - server/db.py（:199-215 question_score DDL、:276-291 question_bank_task 新表分节注释惯例、:355-433 全部 _migrate_*、:436-459 _migrate_question_score_v2、:462-477 init_db 序列）
    - .planning/phases/03-sse/03-PATTERNS.md（db.py 条目——四步放宽法与双轨纪律）
    - .planning/phases/03-sse/03-RESEARCH.md（Pitfall 4——实验 9/10 依据）
  </read_first>
  <action>(1) _DDL 段追加 form_instance CREATE（<interfaces> DDL 逐列；分节注释四行：SSOT §16.1 出处 + status 枚举代码校验 N11 + render 触发时机 + GET/submit 消费面——照 question_bank_task :276-291 惯例）。(2) question_score 的 _DDL CREATE：question_id/score_state 两列声明去 NOT NULL（question_id 保留 REFERENCES）+ gate 五列与覆盖四列追加进 _DDL（新库直建路径）。(3) 新函数 _migrate_form_instance(conn)：CREATE IF NOT EXISTS form_instance（幂等）。(4) _migrate_question_score_phase3(conn)（新函数——不并入 _migrate_question_score_v2，四步放宽是独立节奏）：PRAGMA table_info 嗅探 → gate 五+四列 if-not-in-ADD → question_id 放宽（若 question_id NOT NULL：ADD question_id_v2 TEXT → UPDATE 拷 → DROP question_id → RENAME）→ score_state 同法（score_state_v2 回填 COALESCE 原值）；docstring 注明 A2 ASSUMED 呈报项 + 实验 9/10 验证。register：init_db 序列 :472-473 之后追加两行调用。(5) 全程不用 DB CHECK（N11）；json 列 TEXT。放宽后执行 sqlite_master 一致性：迁移函数末尾不 commit（init_db 统一 commit 既有形态）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -c "
import os, tempfile, sqlite3
os.environ['DB_PATH'] = os.path.join(tempfile.mkdtemp(), 'p3_form_smoke.db')
os.environ['LLM_PROVIDER'] = 'mock'
from server.db import init_db, get_conn
init_db(); init_db()
c = get_conn()
tables = {r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")}
assert 'form_instance' in tables
cols = [r[1] for r in c.execute('PRAGMA table_info(question_score)')]
for col in ['gate_result','gate_status','gate_reason','evaluated_schema_version','evaluated_at','automated_gate_result','human_override','override_reason','reviewer_id']:
    assert col in cols, col
c.execute(\"INSERT INTO question_score(score_id, session_id, item_id, created_at, gate_result) VALUES('s1','x','i1','t','true')\")
c.execute(\"INSERT INTO form_instance(form_instance_id, session_id, form_type, schema_version, schema_snapshot, created_at) VALUES('fi_1','x','gate_combined','v1','{}','t')\")
c.commit(); print('forms DDL OK')" && python -m pytest test_phase3_forms.py -v 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - grep form_instance 命中 _DDL 与 _migrate_form_instance 两处；grep RENAME COLUMN 在 _migrate_question_score_phase3 命中
    - gate 行可插（question_id NULL + score_state NULL 不再 IntegrityError——verify 脚本含插入断言）
    - _DDL 直建路径 question_score 含 gate 九列且新库两列可空
    - init_db 二次调用幂等（verify 脚本双跑）
    - 无新 DB CHECK 列（N11——PRAGMA 无 CHECK 新增）
  </acceptance_criteria>
  <done>form_instance 表 + gate 九列 + NOT NULL 放宽双轨迁移落地，存量数据零丢失</done>
</task>

<task type="auto">
  <name>Task 3: services/forms.py 新服务 + aggregation 双源迁移 + scoring gate 行保留</name>
  <files>server/services/forms.py, server/services/aggregation.py, server/services/scoring.py</files>
  <read_first>
    - server/services/scoring.py（:23-30 SCORE_STATES 枚举区 + :243-251 DELETE/executemany 段）
    - server/services/aggregation.py（全文 192 行——:32-45 _load_form_payload、:48-70 _gate_check、:116-134 gate 消费段）
    - server/services/question_selection.py（:350-361 _uncovered_required_items JOIN 判定形态 + :524-607 _instantiate 写库形态）
    - server/services/state_events.py（append_event 签名）
    - .planning/phases/03-sse/03-PATTERNS.md（forms.py 条目——双风格锚）
  </read_first>
  <action>(1) 新建 server/services/forms.py：模块 docstring 中文（SSOT §16.1 + D-29~D-32 出处 + 「事务边界（D-06）：render_form_instance/validate_and_submit 接 conn 但不 commit——实例写入与事件在调用者同一事务」difficulty.py:27-28 契约照写）。结构：①常量区（FORM_SCHEMA_VERSION "v1" + FORM_STATUS 三态 tuple + FORM_FIELD_BASE 模板 dict——<interfaces> FORM_SCHEMA 形态）；②render_form_instance（幂等 open 实例查——ORDER BY revision DESC LIMIT 1；快照 = base 模板 + 会话模型 gate items 展开 qualification fields——conn 查 competency_item gate=1；INSERT + FORM_RENDERED append_event；返回渲染白名单 dict）；③_all_gate_items_collected（gate item 全 submitted 判定）；④_validate_payload（六维的后三维纯函数：必填/枚举/长度——不持 conn 照 difficulty 纯函数区惯例，分区注释照写）；⑤validate_and_submit（状态/revision 查库校验 → _validate_payload → UPDATE status/submitted_at/payload_json → gate 行 INSERT question_score（10 列 + gate 五列——列清单一行写全照 _instantiate :576 形态，question_id/score_state 显式 NULL）→ FORM_SUBMITTED + GATE_EVALUATED（每 gate item 一条）append_event）；⑥revise_instance（修订：INSERT 新行 revision+1 + 旧行 UPDATE status='superseded' 仅 status 列）。(2) aggregation.py：新增 _gate_row(conn, session_id, item_id) -> tuple | None（SELECT gate_result, gate_reason FROM question_score WHERE session_id=? AND item_id=? AND gate_result IS NOT NULL LIMIT 1）；gate 消费段 :116-134 的 `_gate_check(item, form_payload)` 调用改为 `row = _gate_row(...); passed, reason = (row gating) if row else _gate_check(item, form_payload)`——weight 公式/七键结构/分母规则逐字不动（D-31）。(3) scoring.py :243 DELETE 改 `DELETE FROM question_score WHERE session_id=? AND gate_result IS NULL` + 行内注释（gate 行非重算面——03-01）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -c "import os; os.environ['DB_PATH']='/tmp/p3_svc_smoke.db'; os.environ['LLM_PROVIDER']='mock'; from server.db import init_db; init_db(); from server.services.forms import render_form_instance, validate_and_submit, _all_gate_items_collected; print('forms service OK')" && python -m pytest test_phase3_forms.py -v 2>&1 | tail -6 && grep -n "gate_result IS NULL" services/scoring.py && grep -c "_gate_row" services/aggregation.py</automated>
  </verify>
  <acceptance_criteria>
    - forms.py 四函数存在（render_form_instance/_all_gate_items_collected/validate_and_submit/_validate_payload）且无 conn.commit()（grep commit 本文件零命中——事务归调用者）
    - gate 行 INSERT 显式 NULL 两列 + gate_reason 中文串照 _gate_check 现行格式
    - scoring.py DELETE 含 gate_result IS NULL 条件（grep 命中）
    - aggregation.py 双源（_gate_row 优先 + _gate_check 兜底——grep 两函数并存）
    - FORM_RENDERED/FORM_SUBMITTED/GATE_EVALUATED/GATE_OVERRIDDEN 预留全部经 append_event（全文件 grep 手拼 INSERT INTO assessment_state_event 零命中）
    - test_phase3_forms.py 红项大幅转绿（render/submit/gate 行/双源/DELETE 保留五组）
  </acceptance_criteria>
  <done>表单服务三层落位（渲染/校验/gate 判定），gate 结果双源消费，评分重算不再吞 gate 行</done>
</task>

<task type="auto">
  <name>Task 4: assessment.py 三端点两插入点 + admin/forms.py + main.py + p0 回归适配</name>
  <files>server/api/assessment.py, server/schemas.py, server/api/admin/forms.py, server/main.py, server/test_p0_chain.py, server/test_p0_security.py</files>
  <read_first>
    - server/api/assessment.py（:321-353 池耗尽两分支、:229-236 assistant INSERT、:469-486 forms 旧端点、:55-74 get_confirmed_model GET 骨架、:120-167 get_session）
    - server/api/admin/feedback.py（:11-40 require_admin + Pydantic body + rowcount 404 全套先例）
    - server/main.py（:43-44 import 邻位 + :79-80 include 邻位）
    - server/schemas.py（:29-48 JdImportRequest/ExtractItem Field 形态）
    - server/test_p0_chain.py（:76-78 gate 种子、:172-184 答题闭链）；server/test_p0_security.py（:171-182 答题闭链 + 种子 gate=1 行）
    - .planning/phases/03-sse/03-PATTERNS.md（assessment.py 条目 + admin/forms.py 条目 + schemas 条目）
  </read_first>
  <action>(1) schemas.py 新增 FormSubmitRequest（照 ExtractItem 形态）：form_instance_id str Field(min_length=1)、schema_version str、expected_revision int Field(ge=1, default=1)、payload dict、idempotency_key Optional[str]（D-36 占位，03-03 接）；分节注释 `# ---- Phase 3 表单提交（SSOT §16.1/D-46） ----`。(2) assessment.py：(a) 池耗尽两处对称插入（主链 :323 picked is None 后 + legacy :343 nxt is None 后）——`if not _all_gate_items_collected(conn, session_id):` 走 render_form_instance + assistant 消息 INSERT（role='assistant', content=decision['reply'] + f" 请先填写资格核验表单 📎[form:{fi['form_instance_id']}]", action='form'）+ conn.commit() + return form dict（含 action='form'/reply/question_id/next_question_id=None/score_live）；else 分支原 finish 段逐字不动。import from ..services.forms import render_form_instance, validate_and_submit, _all_gate_items_collected（加在 :12 邻位）。(b) GET /forms/{form_instance_id} 新端点：反查 session_id → load_owned_session → 返回渲染白名单（快照 fields + form_type/title——gate_results 不出）；双 404（无行 404/非 owner 404 经 load_owned_session）。(c) POST /sessions/{session_id}/forms/submit-v2：body FormSubmitRequest → load_owned_session → validate_and_submit(...) → conn.commit() → 201 返回 dict。(d) get_session 响应加 open_form 键（rendered 实例存在时白名单 dict 否则 None）。(3) 新建 server/api/admin/forms.py：照 feedback.py 全套——APIRouter prefix="/api/admin/forms" dependencies=[Depends(require_admin)]；class GateOverrideBody(BaseModel): item_id str、human_override bool、override_reason str = Field(min_length=1)、session_id str；POST /gate-override：UPDATE question_score SET human_override=?, override_reason=?, reviewer_id=? WHERE session_id=? AND item_id=? AND gate_result IS NOT NULL（rowcount==0 → 404）+ GATE_OVERRIDDEN 事件（独立事务 append_event 形态照 feedback.py 的 UPDATE+事件同事务）+ commit + return dict。(4) main.py 两行注册（:43-44 邻位 import + :79-80 邻位 include_router——admin_forms 名与 admin_feedback 邻位同步）。(5) 回归适配（D-09 只改断言/步骤不改结构）：test_p0_chain.py _answer_whole_session 循环加表单步骤——action=='form' 时由测试提取 form_instance_id（reply 正则）→ POST submit-v2（payload 造 gate item 白名单值：years_of_experience=5 + qualification item std_name:"是"）→ 继续下一轮 GET/current_question（完成语义恢复 finish）；:182 断言 action in ("next","finish") 扩为 ("next","finish","form") 后循环消化。test_p0_security.py 同法（gate=1 种子——闭链 step 与 p0_chain 同构；只加提交 helper 不改测试结构）。两文件的 gate 提交 helper 写在各文件内（不 import test_phase3_forms——单文件单进程纪律）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase3_forms.py -v 2>&1 | tail -6 && python -m pytest test_p0_chain.py -v 2>&1 | tail -5 && python -m pytest test_p0_security.py -v 2>&1 | tail -5 && python -m pytest test_m5_backend.py -v 2>&1 | tail -4 && python -m pytest test_phase2_interview.py -v 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - assessment.py 两处插入点 grep render_form_instance ≥ 2 + GET /forms 路由 + submit-v2 路由存在
    - 第 36 类旧 finish 路径逐字保持（git diff 该段零改动——gate 全采集面无变化）
    - FormSubmitRequest Pydantic（grep class FormSubmitRequest schemas.py）+ admin GateOverrideBody override_reason min_length=1
    - main.py include_router(admin_forms) 两行
    - test_phase3_forms.py 全绿；test_p0_chain/test_p0_security 全绿（表单步骤插入后完成链恢复）；test_m5_backend 全绿（gate=0 面零影响回归）；test_phase2_interview 全绿（answer 链无 form 分支零影响回归）
  </acceptance_criteria>
  <done>表单链 API 面闭合（render 触发/只读 GET/submit-v2/admin 覆盖），p0 双文件答题闭链恢复绿，零前端改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 候选人 → GET /forms/{id} + submit-v2 | 只读 schema 与提交 payload 跨信任边界——所有权前置 + 渲染白名单 |
| 表单 payload → gate 判定 | 候选人自报数据（years/学历）——构造性不信任但 gate 语义即自报核验（§16） |
| admin → gate 覆盖端点 | 特权操作——二次确认 + 审计事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Info disclosure | GET /forms/{id} 暴露内部阈值（years 门槛/required_level） | mitigate | 渲染白名单分栏：fields 仅 name/label/type/required/options/placeholder——门槛值不进快照（测试白名单断言） |
| T-03-02 | Tampering | 越权读/提交他人表单 | mitigate | GET/submit-v2 全走 load_owned_session（D-01 统一 404）；test_get_form_ownership 断言 |
| T-03-03 | Repudiation | gate 判定不可审计 | mitigate | gate 行五列结构化 + FORM_RENDERED/FORM_SUBMITTED/GATE_EVALUATED 事件链（append_event 唯一入口） |
| T-03-04 | Tampering | 表单 payload 注入（超长/绕过枚举） | mitigate | 六维校验序（长度截断拒绝不静默 + 枚举白名单）+ FormSubmitRequest Pydantic |
| T-03-05 | Elevation | 人工覆盖无二次确认被滥用 | mitigate | override_reason Field(min_length=1) 422 + require_admin + GATE_OVERRIDDEN 事件（reviewer_id 落库） |
| T-03-06 | DoS | 评分重算吞 gate 行导致表单链死循环（gate 永不采集→finish 不可达） | mitigate | scoring.py DELETE 加 gate_result IS NULL（test_score_session_preserves_gate_rows 断言） |
</threat_model>

<verification>
1. `cd server && python -m pytest test_phase3_forms.py -v` — 全链（迁移/render/白名单/六维/gate 行/双源/覆盖/revision）
2. `cd server && python -m pytest test_p0_chain.py -v && python -m pytest test_p0_security.py -v`（逐文件）— 表单步骤适配后完成链恢复
3. `cd server && python -m pytest test_m5_backend.py -v && python -m pytest test_phase2_interview.py -v` — gate=0 会话零影响回归
4. `grep -n "gate_result IS NULL" services/scoring.py` — 命中（DELETE 保护就位）
5. `grep -c "RENAME COLUMN" db.py` — ≥ 2（四步放宽法落地）
6. `grep -rn "INSERT INTO assessment_state_event" server/services/forms.py server/api/assessment.py server/api/admin/forms.py` — 零命中（全走 append_event）
</verification>

<success_criteria>
- SC-1 达成：form_instance 生命周期实体（不可变快照/状态机/revision）+ GET 只读白名单 + 六维校验 + 重复提交返回第一次结果
- SC-2 达成：gate 结构化五列落 question_score + 人工覆盖四列二次确认；_gate_check 迁移双源（新链优先/旧链兜底）
- REF-3.3 表单链承接 experience/qualification 终局采集（dynamic selection 剔除 02-02 已落，互补闭合）
- D-29~D-32 全落地；02-02 finish 口径不破坏（gate 全采集面逐字不变）
- A2 四步放宽法 ASSUMED 呈报项在迁移 docstring 与关口包列明
</success_criteria>

<output>
Create `.planning/phases/03-sse/03-01-SUMMARY.md` when done
</output>
