# AI 驱动的岗位胜任力测评与人才画像系统 — 总设计文档

- 版本：**v1.0**（2026-08-30，由 04 概念设计 + 05 模块一实施设计 + 06 模块二三讨论稿合并而成）
- 状态：**实施中**（模块一 M1~M3 已完成；模块二/三/四待实施）
- 范围：全系统单一事实来源（SSOT）。任何设计变更，**先更新本文档（正文 + 变更日志），再动代码**。
- 上游需求：02（需求）、03（技术方案概述）；下游：全系统实施
- 历史档案：04 概念设计（理念来源）、05 模块一实施设计（已实现内容存档）、06 模块二三讨论稿（合并后弃用）

> **维护规则（继承 05）**
> 1. 任何设计变更、想法调整、范围增减，**先更新本文档（正文 + §13 变更日志），再实施**；
> 2. 变更日志条目格式：日期 / 变更内容 / 原因 / 影响面；
> 3. 与 04 概念设计的关键差异登记在 §3（合并后 04 转档案，仅答辩取材用）。

---

# 第一部分 总览

## 1. 系统定位与端到端链路

「AI 驱动的岗位胜任力测评与人才画像系统」：把非结构化 JD 转化为可测量的**岗位胜任力模型**（尺子），基于尺子对候选人进行**动态多轮测评**，产出**立体人才画像**（读数），并通过**测试闭环**保证全链路可审计、可回溯、有评测标准。

```
JD 文本 ──► 胜任力模型（模块一）──► 动态测评/出题（模块二）──► 人才画像/评分（模块三）
                                                                      │
                                            测试闭环模块（模块四）◄──┘
                                       可审计 / 反馈可回溯 / 测评标准（可迁移）
```

## 2. 三条强约束（全系统最低价定，不可谈判）

| # | 约束 | 落点 |
|---|---|---|
| ① | **LLM 不碰数字** | 权重代码算（模块一）；分数算术代码算（模块三） |
| ② | **人工是唯一权威** | 模型 confirmed 不被静默覆盖（模块一）；分数可异议但不做"AI 自动改分"（模块三） |
| ③ | **一切留痕** | 每道工序中间产物落库；每次 LLM 调用 trace 落库；答案原文哈希归档可回溯（模块二/四） |

## 3. 与 04 概念设计的差异登记（继承 05 D1~D6，新增 M2/M3 两条实施约束）

| # | 04 原设计 | 总文档 | 原因 |
|---|---|---|---|
| D1 | 三渠道含浏览器插件 | 仅粘贴/上传 | 范围收敛，插件砍掉 |
| D2 | 归岗未命中 → 轻量 LLM 判别称 | 直接建 pending_review 新岗位 | 简化，别名靠人审积累 |
| D3 | llm_trace_json 挂在 competency_model 一列 | 独立 llm_trace 表 | 统一检索/展示/评估 |
| D4 | competency_dict 无排除项/状态列 | 增 exclusions_json + status | 词典管理页需要 |
| D5 | 未涉及鉴权 | JWT 双角色 + user 表 | 两类操作者落地 |
| D6 | competency_model 无确认人字段 | 增 confirmed_by/confirmed_at | 人审留痕 |
| M2 | llm_trace.call_type 用 CHECK 枚举 | **新扩展不用 CHECK（FREE TEXT）** | SQLite CHECK 不可 ALTER（v1.1 M2） |
| M3 | 未区分异步模式 | **任务型用轮询、对话型用 SSE** | 题库/报告十秒级，对话秒级（v1.1 M3） |

## 4. 技术栈（全系统沿用）

| 层 | 选型 |
|---|---|
| 后端 | Python 3.11+ / FastAPI / Uvicorn |
| LLM 客户端 | `openai` SDK（base_url 切 DeepSeek/通义），JSON 模式 + **function call**（模块二新增） |
| 默认模型 | DeepSeek（备选通义），key 走环境变量 |
| 存储 | SQLite（单文件 data/app.db），不上图谱 |
| 鉴权 | python-jose(JWT) + passlib[bcrypt]，HS256，12h |
| 前端 | Vue 3 + Vite + Element Plus + Vue Router + Pinia + axios |
| 图表 | ECharts（雷达图，模块三） |
| 中文匹配 | pypinyin + difflib（词典候选 top10） |
| 部署 | vite build 产物由 FastAPI 挂静态文件，单进程 uvicorn；mock 模式离线可跑通全流程 |

## 5. 全局数据库清单（14 张表）

**模块一（8 张，已实现）**：`user / position / position_alias / jd_record / competency_model / competency_item / competency_dict / llm_trace`

**模块二/三新增（6 张）**：`assessment_session / assessment_question / assessment_message / question_bank / context_raw / form_submission`（`user_profile` 字段并入 user 表扩展列或独立小表，实施期定）

详表 schema：模块一 8 张见 §7.1；模块二三新增见 §7.2。

---

# 第二部分 模块一：JD 解析与胜任力模型构建（已实现）

> 本部分为 05 文档 v1.0 已实现内容的档案化摘录；实施期细节以 05 原文为准。

### 5.1 流水线（六工位，已实现）

```
①接入(粘贴/JSONL) → ②清洗(纯规则) → ③抽取(LLM#1) → ④归一消歧(词典+LLM#2)
→ ⑤聚合(代码频次+LLM#3裁决+代码算权重) → ⑥人审(P3审核页，确认升版本)
```

- 状态机：`imported → parsing → parsed/failed → aggregating → draft/stalled → confirmed`；confirmed 后新增 JD/重解析走 diff 审阅流升 v{n+1}；
- 归岗：job_title 规范化 → position 精确匹配 → 别名表 → 未命中建 pending_review 岗位（人工审激活）；
- 聚合：双比率（r/req）→ importance 阈值映射 → level 冲突交 LLM#3（无自动取众数）→ 权重代码计算；
- 三次 LLM 调用职责契约与提示词已定稿（见附录 A），全部落 llm_trace。

### 5.2 权重口径（U3 锁定，模块三直接复用）

```
第一层（类目间）：硬技能 : 软技能 : 经验 : 门槛 = 5.5 : 2 : 2 : 0.5  → 55% / 20% / 20% / 5%
第二层（类内）  ：类目份额按各项 importance 系数分摊（required 1.0 / preferred 0.6 / plus 0.3）
                ↓ 合成结果（已算好，存 competency_item.weight，Σ=1）
```

**模块三算总分 = 直接复用 item.weight，不再二次乘 5.5:2:2:0.5。**

### 5.3 关键设计原则（已实现）

- 门槛项 `gate=true`（qualification 全部 + experience 年限项）二值判定，不进 1~5 级评分；
- 分数是历史事实，confirmed 模型不被静默覆盖（人工唯一权威）；
- 每道工序中间产物（raw_items/std_items/cleaned_text）落 jd_record 可查。

---

# 第三部分 模块二：AI 动态测评（待实施）

## 6. 测评框架生成（题库 + 选题）

### 6.1 题库双轨设计（U1/U4）

```sql
question_bank(
  question_id TEXT PRIMARY KEY,
  scope TEXT NOT NULL CHECK(scope IN ('position','general')),
  position_id TEXT REFERENCES position,        -- general 时 NULL
  std_name TEXT NOT NULL, category TEXT NOT NULL,
  difficulty TEXT CHECK(difficulty IN ('easy','medium','hard')),  -- 经验/门槛为 NULL
  qtype TEXT NOT NULL CHECK(qtype IN ('objective','subjective')),
  stem TEXT NOT NULL,
  answer_key TEXT,                             -- 客观题代码判分用
  rubric TEXT,                                 -- 主观题评分要点
  chain_key TEXT, chain_seq INTEGER,           -- 问题链条（M1 补）
  source TEXT NOT NULL CHECK(source IN ('llm_seed','imported','human')),
  status TEXT NOT NULL DEFAULT 'active',
  created_at TEXT NOT NULL
)
```

- **岗位题库**：hard_skill/soft_skill，position_id 绑定；**通用题库**：experience/qualification，跨岗位；
- 演示期 = LLM 种子生成（U4，`source=llm_seed`）；演示与测试完成后真实题库走 N8 导入接口；
- **可溯硬约束（N7）**：四要素齐全（能力项绑定/难度/题型/岗位归属）才允许进入对话，出题入库强校验。

### 6.2 题库生成与题量分配

- 模型 confirmed 后**异步触发**题库生成（异步任务+轮询，M3）：prompt 携带岗位名+模型项定义+required_level 锚点+难度档；
- 题量按**类目权重**分配（D2）：总 10~12 题 ≈ 硬技能 6~7 / 软技能 2~3 / 经验 2 / 门槛走表单不占题；required 项必考；
- 每道题必须绑定 std_name（同一能力不同岗位不同题，N2 岗位背景绑定）。

### 6.3 选题规则（代码执行，可审计）

- 每轮 action=next 时，**代码**按规则取下一题：当前 item 完成度 + 难度递进（N1）+ 类目剩余题量；优先沿 chain_key 链条，缺失时退回 `(item, difficulty)` 通用递进；
- LLM 只对题面做岗位化/口语化轻包装，question_id 落库绑定。

## 7. 多轮对话（function call 驱动）

### 7.1 对话控制：结构化 action + function call 分两阶段（H2）

- 决策阶段（**非流式**）：function call `interview_step` 同步返回 `{action: followup|next|finish, reason, assessment}` → 后端**先落库**（action/reason/过程判分先于展示，可审计）；
- 话术阶段（**流式 SSE**）：基于已定 action 生成/转发 reply 逐 token 推送；
- `finish` 由规则触发（题量完成），模型不自主结束全场。

### 7.2 追问与递进

- 追问上限 2 次/题（config）；模型可提前 next；
- 递进（N1）：hard_skill 慢递进（简单→中等→困难，权重越高走完档位越多）、soft_skill 快递进（2 档）、经验/门槛无难度（事实深化）；
- 递进判分来自**过程判分 score_live**（H1）。

## 8. 上下文管理与表单（N5/N6）

```sql
context_raw(raw_id TEXT PRIMARY KEY, hash TEXT UNIQUE NOT NULL, full_text TEXT NOT NULL, created_at TEXT NOT NULL)
-- assessment_message 增列：content_refined TEXT, raw_hash TEXT NULL
```

- **精炼（U2 阈值触发）**：长度 > ~500 token（config `REFINE_MIN_TOKENS`）才走 P-refine，短输入原文直存；精炼版进对话历史，原文哈希归档可回溯；同时承担**注入防护**；
- **评分不受损**：终局判分（P-score）按 raw_hash 回捞原文输入；
- **表单工具**：`render_form(form_type, fields[])` 后端工具；开场简历表 / gate 项缺信息时触发；提交直传 `form_submission` 表；与 user_profile 打通（已填自动带入）；原始 payload 落库。

## 9. 会话持久化

三表 + **锚定 model_id+version**（报告永远对应某版尺子）；中断恢复；24h 未完成→abandoned；副产物"我的测评"历史页。

---

# 第四部分 模块三：立体人才画像（待实施）

## 10. 打分体系（H1 双分合成 + U3 权重复用）

### 10.1 两次评分的合成（H1 拍板）

| 评分 | 时机 | 驱动者 | 用途 |
|---|---|---|---|
| **过程判分 `score_live`** | 对话中每轮 | function call `interview_step` 附带 `assessment`（quality 1~5 + 一句依据） | 驱动追问/递进/切题 |
| **终局判分 `score_final`** | 测评结束后 | LLM 逐题评分（P-score，temperature≈0，含 evidence_quote+reason） | 终分基础 |

- **合成（每道主观题）**：`终分 = round(score_live × 0.5 + score_final × 0.5)`（1~5 区间）；
- 客观题：仅代码匹配分（无过程判分）；
- 双分严重背离（≥2 级）自动进 bad case 候选（D16）。

### 10.2 分步打分架构

| 阶段 | 执行者 | 干什么 |
|---|---|---|
| ① 逐题评分 | 客观题代码判 / 主观题 R1 双分合成 | question_score |
| ② 代码聚合 | item 得分=该项各题合成后均分；gap=required−actual；**总分=Σ(item.weight × actual/5)×100**（复用 item.weight，不再二次乘，U3）；gate 项代码二值判定 | item_score + total |
| ③ 报告文字 | LLM：分数+差距排序+评分理由摘录 → 优势/短板/建议 | report |

### 10.3 打分刻度（1~5 Dreyfus 同锚点）

过程判分与终局判分同尺度（否则 50/50 无意义）；gap=required−actual 直接可解释（"要求 Lv4 实测 Lv2，差 2 级"）。

### 10.4 总分口径与门槛（D11 示例锁定）

- 非门槛项：`weight × actual/5`；门槛项：达标拿满，不达标 0；UI 门槛达标情况单列红绿标签；
- 示例（后端开发工程师，55/20/20/5）：Python(19%, Lv4→Lv3 得 11.4) / 后端经验(8%, 3年→2年得 0) / 本科(5%, 不达标得 0)。

### 10.5 报告生成（五段式，异步生成按 M3）

①总分+门槛标签 ②雷达图 required vs actual（ECharts）③逐项明细表（理由可展开）④优劣文字段 ⑤逐题回顾（双分对照+理由，可溯回原文）。报告生成走异步任务+轮询（十秒级）。第 3、5 段是可审计的 UI 实体。

### 10.6 优势短板与建议（绑证据）

- 代码排序：优势=gap≥0 中权重最大前 3；短板=gap<0 中 |gap|×weight 最大前 3；LLM 只写自然语言；
- 建议绑证据：输入=短板项+问答摘录+锚点，prompt 约束只能基于给到的短板项提建议。

---

# 第五部分 模块四：测试闭环（待实施）

## 11. 三大标准落点

| 标准 | 落点 | 实现 |
|---|---|---|
| ①全链路可审计 | report→session→(model,version)→question→message→trace 锚定链 | llm_trace 扩 5 类型 + P8 trace 查看器 |
| ②反馈可回溯 | 异议/bad case 双端 | candidate 报告页每分数旁「异议」→feedback 表；admin「标 bad case」；不做改分；双分背离≥2 级自动候选 |
| ③有测评标准 | b+c 评测 | **排期：联调全绿后最后做**；eval/ 目录独立可迁移 |

## 12. 评测与 UI

- **一致性（b）**：固定 transcript 复跑断言 score_final 分差≤1（temperature=0；score_live 属过程值不在此断言范围）；
- **虚拟考生（c）**：三档脚本（强/中/弱）端到端，断言档位正确区分（强>中>弱）+ 短板定位符合预设；
- **黄金集（a，模块一）**：推后至真实数据收集后；
- **P8 测试中心**：三 tab = 评测运行 / trace 查看器 / 反馈·bad case。

---

# 第六部分 公共约束与开放问题

## 13. 变更日志

| 日期 | 变更 | 原因 | 影响面 |
|---|---|---|---|
| 2026-08-30 | **总文档 v1.0 创建**：04+05+06 合并；模块一已实现内容档案化；模块二/三/四按 06 v1.1 全部拍板项纳入 | 单一事实来源统一 | 全文 |
| 2026-08-30 | 历史决策继承：05 D1~D6、06 U1~U4/H1/H2/M1~M3/L1~L4、R1 双分合成 | 合并时全部保留 | §3/§5~§12 |
| 2026-08-30 | **M7 测试闭环后端落地**：①评测运行器 `/api/admin/eval/*`（一致性/虚拟考生异步 + eval_results 表）；② trace 查看器 `/api/admin/trace/*`；③ 反馈管理 `/api/admin/feedback/*`（status='bad_case' 扩展，CHECK 迁移同 llm_trace 套路）；④ eval/ 目录（consistency_test / virtual_candidates / assertions） | 模块四「可审计/可回溯/可迁移」落地 | §11 / §12 / 新增 admin API |
| 2026-08-30 | **M5 测评核心后端落地**：§7.2 七张表建库（assessment_session/question_bank/assessment_question/assessment_message/context_raw/form_submission/question_score）；会话/答题/表单/打分 API；P-interviewer/P-refine/P-score 提示词 v1；选题规则按 §6.3 代码实现（类目配额 hard6/soft2/exp2、required 优先、难度递进、chain 排序）；mock 模式规则兜底离线可跑通；`REFINE_MIN_TOKENS=500`、`FOLLOWUP_MAX=2` 入 config | 模块二 M5 任务 #1（会话+对话+评分核心） | §6~§10 实施落地；llm_trace.call_type CHECK 扩 4 类（question_gen/interviewer/refine/score，与 §3-M2 "新扩展不用 CHECK" 有偏差，因旧库已存在采用 migrate 式重建，登记待议） |
| 2026-08-30 | **M6 后端落地**：打分+聚合+报告生成；新增 `report` / `feedback` 两表（§5）；`llm_trace.call_type` 扩 `report` 类型（§11）；P-report 提示词定稿（附录 A）；门槛项代码判定规则明确：experience 走 `years_of_experience` 表单字段、qualification 走同名布尔字段（§10.2/§10.4） | 模块三「立体人才画像」后端实现 | §5/§10/§11/附录 A |
| 2026-08-30 | **M5 题库落地**：`question_bank` 表 + 生成服务（模型 confirmed 异步触发，hard/soft→岗位题带难度链，experience/qualification→通用题；幂等）；选题服务（required 优先→权重→chain_key 整链递进）；P-question-gen/P-interviewer/P-refine/P-score 四提示词定稿（`server/services/prompts/`，附录 A 状态转 ✅）；`llm_trace.call_type` CHECK 扩 7 类型（老库重建迁移） | M5 Task#2 实施 | §6/§附录A/§11 |
| 2026-08-30 | **M5 候选人测评前端**：岗位选择/模型预览/对话（作答 + 决策理由折叠 + 表单卡渲染）/报告占位五页落地；`utils/sse.js`（fetch+ReadableStream，**自适应两种后端形态**：SSE `text/event-stream` 解析 decision/reply/done；当前后端为单次 JSON，则映射为 decision+整段 reply+done）；`api/index.js` 增 `assessment` 命名空间；路由新增 `/assessment/session/:id`、`/assessment/report/:id`（candidate 角色守卫）；作答后经 `getSession` 拉取权威计数（followup 不计数）。候选人端视觉切换为 Grail×Notion 主题（`assets/grail-notion.css` 令牌引入）。**契约差异登记**：①§7.1 目标为 SSE 流式，当前后端 `/answer` 实现为单次 JSON（前端已兼容，后续可平滑切流式）；②`render_form`/`📎[form:]` 标记与 `GET /forms/{id}` schema 接口后端尚未实现，FormCard 为前向兼容、当前不触发；③会话状态后端用 `completed`（非 `finished`），前端已对齐 | M5 Task#3 实施 | §7/§8/§9 |
| 2026-08-30 | **M6 报告展示前端**：`Report.vue` 占位页 → §10.5 五段式落地（①总分 sigma 读数条+门槛红绿标签表 ②ECharts 雷达 required 灰 vs actual 蓝 ③逐项明细表（gap 绿/红着色、行内理由、逐行「异议」按钮）④优势/短板/建议 rr-card 三色顶边卡 ⑤逐题回顾 el-collapse 双分 tag+证据 quote）；异步生成 M3 模式（by-session 404 → POST 202 → 3s 轮询，40 次上限转失败态可重试）；异议走 el-dialog + feedback API，成功后按钮转「已提交」禁用；`api/index.js` 增 `generateReport/getReportBySession/getReport/submitFeedback`；新增依赖 echarts@6。**契约差异登记**：`question_reviews` 未携带 `item_id`（§10.5 未要求），明细行「理由」列按 `std_name` 反查逐题回顾首条带理由题目——同模型内 std_name 唯一（题库按 position+std_name+category 幂等去重），实践中安全；若后续同 std_name 跨 item 复用需后端在 reviews 中补 `item_id` | M6 Task#5 实施 | §10.5/§11② |
| 2026-08-30 | **M7 P8 测试中心前端落地**：`views/admin/TestCenter.vue` 三 tab（评测运行 / trace 查看器 / 反馈·bad case）按 §12 落地，Grail 三栏布局（rail-left 管理导航 / rail-center tabs / rail-right 系统状态卡——最近评测 + 待处理反馈 count）；评测运行支持 session_id+runs 触发一致性、position_id 触发虚拟考生，3s 轮询 `GET /admin/eval/results/{task_id}` 直至非 running；trace 查看器支持 call_type/ref_id/success 三筛选 + 点击行展开 prompt/response 详情（等宽 pre，max-height 400px 滚动）；反馈管理支持 status 筛选 + 「标记已处理」「标 bad case」操作 + 点击行展开报告上下文（session_id/item_id/总分）；`api/index.js` 增 `admin.eval/trace/feedback` 三命名空间；路由新增 `/admin/test-center`（admin 守卫），AdminNav 增「测试中心」入口 | M7 Task#7 实施 | §11/§12 |

## 14. 开放问题（实现阶段定）

1. mock 深度：模块二/三 mock 用虚拟考生三档脚本兼任，还是各写各的；
2. hard_skill 主观题是否允许贴代码作答（textarea 代码块）及评分 prompt 影响；
3. 报告是否可导出（PDF/Markdown）；
4. 里程碑切分：建议 M5 题库+对话 → M6 评分+报告 → M7 测试闭环；
5. score_live 落库表位（挂 assessment_message 增列 vs 独立 score 行）；
6. user_profile 字段并入 user 表扩展列 vs 独立小表；
7. `REFINE_MIN_TOKENS=500` 数值存疑、后续可调。

---

# 附录

## 附录 A 提示词清单（登记在册，模块一已定稿见 05；模块二三待专题讨论）

| 编号 | 用途 | 调用时机 | 状态 |
|---|---|---|---|
| P-extract / P-disambiguate / P-aggregate-level | 模块一 LLM#1/#2/#3 | — | ✅ 已定稿（05 附录 A，源码 `server/services/prompts/`） |
| P-question-gen | 题库批量出题（岗位背景+锚点+难度档） | 模型 confirmed 时 | ✅ 已定稿（源码 `server/services/prompts/question_gen.py`） |
| P-interviewer | 面试官系统指令（角色/追问策略/action 协议/表单触发/过程判分要求） | 每轮对话 | ✅ 已定稿（源码 `server/services/prompts/interviewer.py`） |
| P-refine | 长输入精炼（事实提取+注入防护） | N6 阈值触发时 | ✅ 已定稿（源码 `server/services/prompts/refine.py`） |
| P-score | 终局逐题主观评分（锚点+evidence_quote 要求） | 评分阶段 | ✅ 已定稿（源码 `server/services/prompts/score.py`） |
| P-report | 报告文字（优劣/建议，绑证据约束） | 报告生成时 | 📝 待专题讨论 |

## 附录 B 模块一已完成里程碑（存档）

- M1 鉴权 + 单 JD 解析链端到端（2026-08-30 完成）
- M2 聚合 + 人审（2026-08-30 完成）
- M3 外围页面：岗位库/测评视角/词典/版本 diff/用户管理（2026-08-30 完成）
- M4（保留）：黄金集 eval、浏览器插件、真实 JD 数据集收集导入

## 附录 C 文档档案关系

- 04（概念设计）：理念来源，差异见 §3 D1~D6；合并后转**档案**（答辩取材用）
- 05（模块一实施设计）：已实现内容存档，本文件 §5 为其摘录；合并后转**档案**
- 06（模块二三讨论稿）：v1.1 全部并入本文件；合并后**弃用**
- 本文档：全系统**单一事实来源**（SSOT）
