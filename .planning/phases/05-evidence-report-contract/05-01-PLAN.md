---
phase: 05-evidence-report-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db.py
  - server/services/scoring.py
  - server/services/trace_link.py
  - server/api/admin/trace.py
  - server/test_phase5_evidence.py
autonomous: true
requirements: [REF-2.10, REF-2.3, REF-8.7]

must_haves:
  truths:
    - "每个证据引用可结构化定位到原文（source_message_id + start_offset/end_offset Unicode code point + quote_hash）"
    - "定位失败的 quote 降级为 quote_hash only + source_message_id=NULL，不抛异常不静默丢弃"
    - "trace_link 表落地且 link_role 枚举代码校验（非法值 raise），审计链 report→session→model/version→question→message→score→trace 可达"
    - "存量 llm_trace.ref_id 单字段弱关联被迁移导入 trace_link（命中实体表拆 entity_type/entity_id，命不中保留 ref_id 原值）"
  artifacts:
    - path: "server/services/trace_link.py"
      provides: "link_entity 写入助手 + LINK_ROLES 枚举 + link_role 代码校验"
      exports: ["LINK_ROLES", "link_entity"]
    - path: "server/services/scoring.py"
      provides: "_locate_span 纯函数 + score_question/score_session 写 evidence_spans_json 列"
      contains: "_locate_span"
    - path: "server/db.py"
      provides: "trace_link 表 DDL + _migrate_trace_link（建表 + 旧 ref_id 导入）+ _migrate_question_score_phase5（+4 列）"
      contains: "_migrate_trace_link"
  key_links:
    - from: "server/services/scoring.py score_question"
      to: "_locate_span"
      via: "evidence_quote 文本定位 offset/hash"
      pattern: "_locate_span\\(answer_text"
    - from: "server/services/scoring.py score_session"
      to: "question_score.evidence_spans_json"
      via: "INSERT 列清单扩展"
      pattern: "evidence_spans_json"
    - from: "server/db.py _migrate_trace_link"
      to: "llm_trace.ref_id"
      via: "逐实体表命中探测"
      pattern: "SELECT 1 FROM"
---

<objective>
把模块三证据引用从「单字段 evidence_quote」升级为「结构化 span（source_message_id / source_content_type / start_offset / end_offset / quote_hash）」并落地 trace_link 统一审计表，闭合 report→session→model/version→question→message→score→trace 审计链，同时把存量 llm_trace.ref_id 单字段弱关联迁移导入 trace_link（REF-8.7）。

Purpose: D-003「LLM 不碰数字」——offset/hash 全代码计算，LLM 只产 evidence_quote 文本；D-020「trace_link 统一审计链」五要素必须可达。
Output: trace_link 表 + 旧 ref_id 导入迁移；question_score.evidence_spans_json 落库；_locate_span 定位算法；admin/trace.py 消费升级。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-evidence-report-contract/05-CONTEXT.md
@.planning/phases/05-evidence-report-contract/05-RESEARCH.md
@.planning/phases/05-evidence-report-contract/05-PATTERNS.md
@.planning/phases/05-evidence-report-contract/05-VALIDATION.md

<interfaces>
<!-- 本计划使用的既有契约（执行者直接用，无需探索代码库） -->

From server/db.py:
- `get_conn() -> sqlite3.Connection`（row_factory=Row + PRAGMA foreign_keys=ON，db.py:373-378）
- `init_db()` 迁移注册点（db.py:680-699，新迁移函数加在 `_migrate_session_phase3(conn)` 之后、`conn.executescript(_DDL)` 之前）
- 迁移形态 1（ALTER ADD COLUMN + PRAGMA 嗅探幂等）：`_migrate_question_bank_v2` db.py:433-467
- 迁移形态 2（CREATE IF NOT EXISTS 双轨）：`_migrate_form_instance` db.py:588-607
- `_DDL` 字符串尾部（db.py:9-370，trace_link 表需在 `_DDL` 与迁移函数双轨定义）

From server/services/scoring.py:
- `score_question(session_id, question_id) -> dict`（scoring.py:102-148；主观题返回 evidence_quote=result.get("evidence_quote","")，客观题 evidence_quote=answer_text[:60]）
- `_fetch_answer_text(session_id, question_id) -> str`（scoring.py:82-99；raw_hash→context_raw.full_text 回捞原文，多个 user 消息 "\n".join）
- `score_session` 单事务写库段（scoring.py:247-254；INSERT 列清单扩展点）
- `imports` 现有：`import json` / `import re` / `from ..db import get_conn` / `from .llm import call_llm_json` / `from .pipeline import new_id, now_iso`

From server/services/llm.py:
- `_record_trace(call_type, ref_id, attempt, prompt, response, success, error)`（llm.py:13-21；trace_id=new_id("t")，独立连接写 llm_trace 并 commit）

From server/api/admin/trace.py:
- `get_session_traces(session_id)`（trace.py:48-65；现为 ref_id IN (session_id + question_ids) 弱关联并集）

From server/services/pipeline.py:
- `new_id(prefix) -> f"{prefix}_{uuid4().hex[:12]}"` / `now_iso() -> str`

From server/test_m5_backend.py（测试三件套照抄形态）:
- 头部 11-29 行（tempfile + DB_PATH + LLM_PROVIDER=mock + JWT_SECRET + sys.path.insert + init_db + TestClient）
- `_q(sql, params)` 只读助手（32-38 行）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 测试脚手架（test_phase5_evidence.py 三件套 + 5 条 RED 断言）</name>
  <files>server/test_phase5_evidence.py</files>
  <read_first>
    - server/test_m5_backend.py（1-38 行：三件套头部 + _q 只读助手——照抄形态）
    - .planning/phases/05-evidence-report-contract/05-VALIDATION.md（REF-2.10/2.3/8.7 测试名与 Edge Cases）
    - server/services/scoring.py（102-148 行：score_question 返回契约——测试断言基准）
  </read_first>
  <behavior>
    - test_span_located_in_original：构造 answer_text 含子串 "性能问题"，断言 _locate_span 返回 dict 且 start_offset==answer_text.find("性能问题")、end_offset==start_offset+len("性能问题")、quote_hash==sha256("性能问题".encode("utf-8")).hexdigest()、source_content_type=="raw"
    - test_span_degrade_on_mock_quote：quote="mock quote" 不在原文中 → _locate_span 返回 None（调用方降级为 quote_hash only + source_message_id=NULL）
    - test_span_unicode_code_point：answer_text 含 "😀"（单 code point）与 CJK 生僻字，断言 answer_text[start:end]==quote 精确相等（code point 非 UTF-16）
    - test_trace_link_role_validation：LINK_ROLES 外传 link_role="bogus" → link_entity 抛 ValueError
    - test_ref_id_import_migration：造 llm_trace 行（ref_id 指向既有 assessment_session 行）+ 一条 ref_id 无命中的行 → 迁移后命中行拆 entity_type/entity_id 进 trace_link，未命中行保留 ref_id 原值
  </behavior>
  <action>新建 server/test_phase5_evidence.py：头部照抄 test_m5_backend.py 1-29 行三件套（tempfile.mkdtemp() 下 DB_PATH、LLM_PROVIDER=mock、JWT_SECRET=test-secret、sys.path.insert、init_db()、TestClient(app)）。定义 _q 只读助手（开连接读关，防持锁）。测试函数 import：from server.services.scoring import _locate_span；from server.services.trace_link import LINK_ROLES, link_entity；from server.db import init_db, get_conn。按 <behavior> 五条写断言，每条测试造数据用直插 SQL（assessment_session/llm_trace 行）与 now_iso()/new_id("t")。逐文件运行命令 `cd server && python -m pytest test_phase5_evidence.py -v`（禁止与其它测试文件同进程）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_evidence.py -v 2>&1 | grep -E "ImportError|ModuleNotFoundError|failed|error" ; echo "RED 预期：_locate_span / trace_link 模块不存在导致收集失败"</automated>
  </verify>
  <acceptance_criteria>
    - test_phase5_evidence.py 文件存在且模块级三件套（DB_PATH/LLM_PROVIDER/JWT_SECRET 在 import server 前设置）可 grep 确认
    - 5 个 test_* 函数名与 05-VALIDATION.md 完全一致：test_span_located_in_original / test_span_degrade_on_mock_quote / test_span_unicode_code_point / test_trace_link_role_validation / test_ref_id_import_migration
    - pytest 收集因 _locate_span / LINK_ROLES 不存在而失败（RED），非语法错误
  </acceptance_criteria>
  <done>test_phase5_evidence.py 落盘，5 条断言骨架可被 pytest 收集目标识别，当前 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: trace_link 服务 + 表 DDL + 旧 ref_id 导入迁移（REF-2.3/REF-8.7）</name>
  <files>server/services/trace_link.py, server/db.py</files>
  <read_first>
    - server/db.py（381-430 行 _migrate_llm_trace/_migrate_feedback_status 重建表形态；588-607 行 _migrate_form_instance 双轨形态；680-699 行 init_db 注册点）
    - server/services/state_events.py（11-35 行 _VALID_ACTOR_TYPES raise ValueError 形态——link_role 校验照此）
    - server/services/llm.py（13-21 行 _record_trace 审计行独立连接写形态）
    - server/api/admin/trace.py（48-65 行 get_session_traces 消费点）
  </read_first>
  <action>新建 server/services/trace_link.py：定义常量 LINK_ROLES = ("input", "output", "caused_by", "scored", "reported", "source")；定义 link_entity(conn, *, trace_id, entity_type, entity_id, link_role, created_at=None) -> None：先断言 link_role in LINK_ROLES 否则 raise ValueError(f"非法 link_role: {link_role}")（照 state_events._VALID_ACTOR_TYPES 形态），再 INSERT INTO trace_link(id, trace_id, entity_type, entity_id, link_role, created_at) VALUES(?,?,?,?,?,?)（id=new_id("tl")，created_at 缺省 now_iso()），不 commit（事务边界由调用者持有，照 append_event 形态）。server/db.py 做两件事：(1) 在 _DDL 字符串尾部加 trace_link 表 DDL（照 §13.3 逐字：id TEXT PRIMARY KEY, trace_id TEXT NOT NULL, entity_type TEXT NOT NULL, entity_id TEXT NOT NULL, link_role TEXT NOT NULL, created_at TEXT NOT NULL, UNIQUE(trace_id, entity_type, entity_id, link_role)）；(2) 新增 _migrate_trace_link(conn)：executescript 建 CREATE TABLE IF NOT EXISTS trace_link（与 _DDL 同串双轨），然后跑旧 ref_id 导入——按 call_type 逐 llm_trace 行映射实体表（extract/disambiguate/aggregate_level → jd_record/position/competency_model；question_gen/interviewer/refine/score → assessment_question/assessment_session；report → report），逐实体表 `SELECT 1 FROM <table> WHERE <pk>=ref_id` 命中即定 entity_type=表名 + entity_id=ref_id + link_role='source' + trace_id=llm_trace.trace_id，未命中跳过保留 ref_id 原值；导入 INSERT 幂等（INSERT OR IGNORE 或先查 UNIQUE 重复）。将 _migrate_trace_link 注册进 init_db：加在 _migrate_session_phase3(conn) 之后、conn.executescript(_DDL) 之前。改造 server/api/admin/trace.py get_session_traces：保留现有 ref_id IN 并集结果不破坏，另新增一段从 trace_link 反查（entity_type='assessment_session' AND entity_id=session_id → trace_id 集合 → JOIN llm_trace）合并去重后返回。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_evidence.py::test_trace_link_role_validation test_phase5_evidence.py::test_ref_id_import_migration -v</automated>
  </verify>
  <acceptance_criteria>
    - trace_link 表 DDL 在 _DDL 与 _migrate_trace_link 双轨出现，grep -n "CREATE TABLE IF NOT EXISTS trace_link" server/db.py 命中 >= 2 次
    - LINK_ROLES 常量六值齐全且 link_entity 对非法 link_role raise ValueError（test_trace_link_role_validation 绿）
    - _migrate_trace_link 已注册 init_db（grep "in init_db" 上下文含 _migrate_trace_link），旧 ref_id 命中行拆 entity_type/entity_id、未命中保留（test_ref_id_import_migration 绿）
    - link_entity 不 commit（同 append_event 形态）；trace.py get_session_traces 结果仍含原 ref_id IN 并集 + trace_link 反查合并
  </acceptance_criteria>
  <done>trace_link 表可建、link_role 校验生效、旧 ref_id 迁移导入可重放、trace.py 消费升级完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _locate_span 定位 + evidence_spans_json 落库 + 审计快照列（REF-2.10）</name>
  <files>server/services/scoring.py, server/db.py</files>
  <read_first>
    - server/services/scoring.py（14-20 行 imports；82-99 行 _fetch_answer_text；102-148 行 score_question；247-254 行 score_session 单事务写库段）
    - server/db.py（433-467 行 _migrate_question_bank_v2 ALTER ADD COLUMN 形态——_migrate_question_score_phase5 照此）
  </read_first>
  <behavior>
    - 定位成功：quote 在原文中 → span dict 含 source_message_id/source_content_type="raw"/start_offset/end_offset/quote_hash
    - 定位失败（quote 不在原文，如 mock "mock quote"）→ _locate_span 返回 None → score_question 降级返回 evidence_spans_json=[{"quote_hash": sha256(quote).hexdigest(), "source_message_id": None, "source_content_type": None, "start_offset": None, "end_offset": None}]
    - Unicode：start_offset/end_offset 按 Python str.find/len（code point 语义），emoji/CJK 处 answer_text[start:end]==quote 精确相等
  </behavior>
  <action>在 server/services/scoring.py 顶部 import 补 `import hashlib`。新增纯函数 _locate_span(answer_text: str, quote: str, source_message_id: str | None) -> dict | None：quote 空返回 None；idx = answer_text.find(quote)（多命中取最早，Python str 索引天然 code point）；idx==-1 返回 None；否则返回 dict {source_message_id, source_content_type:"raw", start_offset:idx, end_offset:idx+len(quote), quote_hash:hashlib.sha256(quote.encode("utf-8")).hexdigest()}。改造 score_question：主观题拿 result["evidence_quote"] 后调用 _locate_span(answer_text, evidence_quote, source_message_id=该题最新 user 消息 message_id)；客观题 evidence_quote=answer_text[:60] 同理定位；定位成功 → evidence_spans_json=json.dumps([span])，定位失败 → 降级 evidence_spans_json=json.dumps([{"quote_hash": sha256(evidence_quote).hexdigest(), "source_message_id": None, "source_content_type": None, "start_offset": None, "end_offset": None}])；score_question 返回 dict 增加键 evidence_spans_json（客观/主观两分支都补）。改造 score_session 单事务写库段：INSERT 列清单加 evidence_spans_json/rubric_version/scorer_version/measurement_target 四列——普通评分行填 evidence_spans_json=r["evidence_spans_json"]、rubric_version="v1"、scorer_version="p-score-1"、measurement_target=None；REFUSED/INVALIDATED 行四列填 None（rubric_version 仍 "v1"）。server/db.py 新增 _migrate_question_score_phase5(conn)：PRAGMA table_info 嗅探 question_score，若列存在跳过；逐列 ALTER ADD COLUMN（evidence_spans_json TEXT / measurement_target TEXT / rubric_version TEXT / scorer_version TEXT，全可空无 DB CHECK）；注册进 init_db 紧随 _migrate_trace_link 之后。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_evidence.py -v</automated>
  </verify>
  <acceptance_criteria>
    - scoring.py 存在 _locate_span 纯函数且 import hashlib（grep 确认）
    - evidence_spans_json 列在 _DDL 与 _migrate_question_score_phase5 双轨存在，迁移函数注册 init_db
    - score_session INSERT 列清单含 evidence_spans_json/rubric_version/scorer_version/measurement_target
    - test_phase5_evidence.py 全部 5 条绿（span 定位 / 降级 / Unicode / link_role 校验 / 迁移）
    - score_question 返回 dict 新增 evidence_spans_json 键，定位失败路径降级为 quote_hash only + source_message_id=None
  </acceptance_criteria>
  <done>evidence_spans 结构化权威落库、_locate_span 纯函数可独立测试、question_score 补 4 审计快照列，test_phase5_evidence.py 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→服务端 | 评分/报告请求的 quote 定位与 hash 计算全在服务端代码，不信任 LLM 产出的 offset/hash |
| 服务层→DB | trace_link/evidence_spans_json 写点经 get_conn()，link_role 枚举代码校验阻断非法值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | server/services/trace_link.py link_entity | mitigate | link_role ∈ LINK_ROLES 代码校验（N11），非法值 raise ValueError，不落库 |
| T-05-02 | Tampering | server/db.py _migrate_trace_link | mitigate | 旧 ref_id 导入逐实体表 SELECT 1 命中探测，未命中保留原值不臆造 entity_type |
| T-05-03 | Tampering | server/services/scoring.py _locate_span | mitigate | offset/hash 全代码计算（D-003 LLM 不碰数字），定位失败降级不抛异常 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包（stdlib hashlib/json/re/sqlite3），无供应链风险面 |
</threat_model>

<verification>
逐文件跑：`cd server && python -m pytest test_phase5_evidence.py -v` 全绿。禁止与其它测试文件同进程（DB_PATH import 冲突）。
</verification>

<success_criteria>
evidence_spans_json 结构化权威落库且含 source_message_id/start_offset/end_offset/quote_hash；定位失败降级 quote_hash only；trace_link 表 + 旧 ref_id 导入迁移可重放；link_role 枚举代码校验；test_phase5_evidence.py 5 条全绿。
</success_criteria>

## Open Items for Hard Gate A

本计划（05-01）不触及三处开放项（归一化公式 / §31-3 补算阈值 / 存量 report 回填默认值）——它们分属 05-02（前两项）与 05-03（第三项）。本计划唯一 Claude's Discretion 已定值：trace_id 取值语义（LLM 产生链接复用 llm_trace.trace_id，结构关联用 new_id("tl")）、quote_hash=sha256(quote.encode("utf-8")) 精确文本、多命中取最早、scorer_version 快照字符串 "p-score-1"。

<output>
Create `.planning/phases/05-evidence-report-contract/05-01-SUMMARY.md` when done
</output>
