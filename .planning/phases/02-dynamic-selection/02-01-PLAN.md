---
phase: 02-dynamic-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db.py
  - server/config.py
  - server/services/aggregate.py
  - server/test_phase2_migration.py
  - server/test_phase2_weights.py
autonomous: true
requirements: [REF-2.7, REF-2.9, REF-3.7, REF-5.7]
must_haves:
  truths:
    - "question_bank / assessment_question / question_score 三表新列在老库（ALTER）与新库（_DDL 直建）双路径全部存在（D-14/D-15）"
    - "锚点列按 difficulty 回填 §9.4 值：easy→[2,3] / medium→[3,4] / hard→[4,5]；difficulty NULL 的行保持 NULL"
    - "question_score 存量 final_score 值合并进 score_final（COALESCE 语义）；final_score 列在老库迁移与新库 _DDL 直建双路径均保留（wave 1-4 全程在位——scoring.py INSERT 到 02-05 才切列），DROP 属 02-05 消费点切换后"
    - "config.CATEGORY_RATIO 为 7:3 口径（hard 0.7 / soft 0.3 / exp 0.0 / qual 0.0）；新聚合模型的 Σhard weight≈0.7、Σsoft≈0.3、单类目岗位归一 1.0（D-16）"
    - "存量 confirmed 模型 weight 不被重算（分数是历史事实 D-003；无任何 UPDATE competency_item 的迁移代码）"
    - "(session_id, seq) 唯一索引存在（Q2 决议：沿用 seq 列承载 §12.2 sequence_no 语义，不加新列）"
  artifacts:
    - path: "server/db.py"
      provides: "三表新列 _DDL 同步 + _migrate_question_bank_v2/_migrate_assessment_question_v2/_migrate_question_score_v2 嗅探式 ALTER + 锚点回填 + score_final 合并 + uq_aq_session_seq 唯一索引"
      contains: "_migrate_question_bank_v2"
    - path: "server/config.py"
      provides: "CATEGORY_RATIO 7:3 新口径"
      contains: '"hard_skill": 0.7'
    - path: "server/test_phase2_migration.py"
      provides: "老库模拟迁移 + 新库直建双路径断言（Pitfall 2 双轨纪律）"
      contains: "test_old_db_migration"
    - path: "server/test_phase2_weights.py"
      provides: "7:3 三落点回归断言（_compute_weights Σ断言 + aggregation 不二次乘断言）"
      contains: "test_weight_73"
  key_links:
    - from: "server/db.py init_db"
      to: "三个 _migrate_question_*_v2"
      via: "既有迁移序列末尾追加（_migrate_llm_trace/_migrate_feedback_status 之后，executescript(_DDL) 之前）"
      pattern: "_migrate_question_bank_v2\\(conn\\)"
    - from: "server/services/aggregate.py _compute_weights"
      to: "server/config.py CATEGORY_RATIO"
      via: "total_ratio = sum(CATEGORY_RATIO[c] for c in by_cat)（零逻辑改动，仅新增 total_ratio==0 保护）"
      pattern: "CATEGORY_RATIO\\[c\\]"
    - from: "迁移函数 UPDATE question_score"
      to: "question_score.score_final"
      via: "UPDATE question_score SET score_final=COALESCE(final_score, score_final)"
      pattern: "COALESCE\\(final_score"
---

<objective>
Phase 2 表结构演进（三表 ALTER 内嵌迁移）+ 权重口径 7:3 修正三落点。本计划只落"列存在 + 数据兼容 + 权重公式就绪"，不动任何消费方逻辑（消费方切换分别属 02-02/02-04/02-05）。

Purpose: REF-2.7/2.9/3.7/5.7 的结构基础——后续四个计划的全部代码都依赖本计划的新列；7:3 是 SSOT §8.2 关键修正，与新模型聚合解耦（存量模型不重算，D-16）。
Output: db.py 双轨迁移 + config.py 7:3 + aggregate.py 零比率保护 + 两个新测试文件（迁移双路径 + 权重回归）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dynamic-selection/02-CONTEXT.md
@.planning/phases/02-dynamic-selection/02-PATTERNS.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

## 新列清单（D-14 锁定，全部 ALTER ADD COLUMN，无 DB CHECK——N11）

question_bank（现有 15 列 + 以下 10 列；REF-2.5 主体属 Phase 4，本计划只加列不填值）:
  model_id TEXT NULL, model_version INTEGER NULL, item_id TEXT NULL,
  question_type TEXT NOT NULL DEFAULT 'ordinary',
  measurement_stage TEXT NOT NULL DEFAULT 'ordinary',
  measurement_target TEXT NULL, evidence_requirement TEXT NULL,
  observable_level_max INTEGER NULL, observable_level_min INTEGER NULL,
  rubric_version TEXT NULL
  -- 注意锚点两列允许 NULL：difficulty 为 NULL 的 experience/qualification 通用题无锚点；
  -- 锚点回填用 UPDATE ... CASE difficulty 两步法（ADD COLUMN 不能带非常量 DEFAULT，SQLite 实测拒绝）

assessment_question（现有 6 列 + 以下 12 列）:
  question_type TEXT NOT NULL DEFAULT 'legacy',   -- 旧行='legacy' 不参与新选题路径（D-15）
  measurement_stage TEXT NULL, item_id TEXT NULL, difficulty TEXT NULL,
  status TEXT NULL,                                -- 旧行 NULL = legacy；新实例 'active'/'sealed'
  activated_at TEXT NULL, closed_at TEXT NULL,
  followup_count INTEGER NOT NULL DEFAULT 0,
  seal_reason TEXT NULL,                           -- answered/refused/timeout 枚举位，代码校验
  selection_reason TEXT NULL,                      -- D-18 结构化 JSON
  selection_policy_version TEXT NULL, path_state_snapshot TEXT NULL
  -- (session_id, sequence_no) 唯一 → CREATE UNIQUE INDEX IF NOT EXISTS uq_aq_session_seq
  --    ON assessment_question(session_id, seq)（Q2 决议：沿用 seq 列，不加 sequence_no 新列）

question_score（现有列不动 + 1 新列；human_override 系属 Phase 5 不加）:
  score_state TEXT NOT NULL DEFAULT 'SCORED'      -- 存量行回填 'SCORED' 语义正确（旧行均为正常评分）
  -- final_score 旧列保留（迁移合并其值进 score_final 后列本体不动——wave 1-4 期间
  -- scoring.py 的 INSERT 仍写 final_score，02-05 消费点全切后才 DROP + _DDL 去列）

## 锚点回填 SQL（§9.4，两步法——02-RESEARCH Pattern 1 实测路径）
UPDATE question_bank SET
  observable_level_max = CASE difficulty WHEN 'easy' THEN 3 WHEN 'medium' THEN 4 WHEN 'hard' THEN 5 ELSE observable_level_max END,
  observable_level_min = CASE difficulty WHEN 'easy' THEN 2 WHEN 'medium' THEN 3 WHEN 'hard' THEN 4 ELSE observable_level_min END
WHERE observable_level_max IS NULL AND difficulty IS NOT NULL

## score_final 合并（02-01 只合并不 DROP——A8 次序约束，DROP 属 02-05）
UPDATE question_score SET score_final=COALESCE(final_score, score_final)
（迁移函数内先嗅探 final_score 列存在才执行；DROP COLUMN final_score 严禁出现在本计划）

## 7:3 三落点（D-16）
1. server/config.py:25-30 CATEGORY_RATIO → {"hard_skill": 0.7, "soft_skill": 0.3, "experience": 0.0, "qualification": 0.0}
2. server/services/aggregate.py _compute_weights :87-98 —— 零逻辑改动自动跟随；唯一例外：新增
   total_ratio == 0 保护（7:3 下 experience/qualification 系数为 0，纯 gate 模型 sum=0 会除零，
   且现有尾差吸收块会把 drift=1.0 压给单个 gate item 使其 weight=1.0 → 达标即 +100 分，语义错误）。
   保护形态：if total_ratio == 0: 全部 it["weight"]=0.0 且跳过尾差吸收（注释注明 REF-5.7 与 §8.2 gate 不占权重池）
3. server/services/aggregation.py :118-121 —— 不改代码，仅回归断言"不二次乘大类比例"（weight × actual/5 × 100 无额外 CATEGORY_RATIO 因子）

## 现行代码锚点（已核对的行号）
- server/db.py：_DDL 三表 CREATE 在 :116-132（question_bank）/ :134-142（assessment_question）/ :174-185（question_score）；
  _migrate_llm_trace :277-302（sqlite_master 字符串嗅探先例）；
  新列嗅探改用 PRAGMA table_info(表名)（02-PATTERNS 推荐，直接取列名集合）；
  init_db 迁移注册点 :336-338（_migrate_llm_trace → _migrate_feedback_status → [新增三个] → executescript(_DDL)）
- server/config.py:23-33 常量区（CATEGORY_RATIO 现值 5.5/2.0/2.0/0.5）
- server/services/aggregate.py _compute_weights :81-98、category_weights 摘要 :165
- 测试文件头模板：server/test_p0_chain.py:30-57（env 先于 import server + init_db() + TestClient）；
  _q 只读 helper：server/test_p0_chain.py:48-54；老库直插种子参照：test_p0_chain.py:172-230 _seed_completed_session_direct

## 双轨纪律（Pitfall 2——_DDL 与 _migrate_* 必须同步加列）
- _DDL 的三个 CREATE 语句加同样新列（新库直建含新列——注意 _DDL 同样不得加 DB CHECK，与迁移形态一致；
  question_score 的 final_score 旧列在 _DDL 保留不动——「现有列不动」合同，02-05 才移除）；
- 迁移函数对已有表 PRAGMA table_info 嗅探缺列 ALTER——新库表已含新列，嗅探自然跳过（幂等）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test_phase2_migration.py 老库双路径断言（先红）</name>
  <files>server/test_phase2_migration.py</files>
  <read_first>
    - server/db.py（_DDL 现状 :9-266、init_db :329-341——本任务写测试时三表尚无新列，测试应红）
    - server/test_p0_chain.py（:30-57 文件头纪律模板 + :48-54 _q helper + :172-230 直插种子风格）
    - .planning/phases/02-dynamic-selection/02-RESEARCH.md（Pitfall 1/2、Pattern 1 迁移实测结论）
  </read_first>
  <behavior>
    - test_old_db_migration_adds_columns：手工用【旧版 DDL 字符串】（本测试文件内置旧 15 列 question_bank / 旧 6 列 assessment_question / 旧含 final_score 的 question_score 三段 CREATE）先建库并插旧行（question_bank 各 difficulty 一行含 difficulty=NULL 行、question_score 一行 final_score=3 与 score_final=2、assessment_question 一行），再调 init_db() → PRAGMA table_info 断言三表新列全在
    - test_anchor_backfill：老库路径下 easy/medium/hard 行的 observable_level_max/min == 3,2 / 4,3 / 5,4；difficulty NULL 行两锚点列仍为 NULL
    - test_score_final_merge：老库路径下合并后 score_final==3（COALESCE(final_score, score_final) 语义）且 final_score 列仍存在（不 DROP）
    - test_unique_index_created：sqlite_master 含 uq_aq_session_seq
    - test_new_db_direct_path：全新临时库 init_db() 后三表新列同样全在（Pitfall 2 双轨）——并断言 question_score 列集合含 final_score（_DDL 保留旧列——02-05 才去除）
    - test_legacy_columns_not_touched：迁移后 question_bank 旧 CHECK（category 三态）仍在（sqlite_master 的 CREATE 语句含旧 CHECK 文本）
  </behavior>
  <action>新建 server/test_phase2_migration.py，文件头照 test_p0_chain.py:30-57 模板（DB_PATH 指 tempfile.mkdtemp 临时库 + LLM_PROVIDER=mock + JWT_SECRET，env 必须在 import server 之前）。老库路径测试的关键形态：本测试文件须在 import server.db 之前先用 sqlite3 直连临时库建【旧结构】三表并插旧行——但 init_db 内 executescript(_DDL) 是 CREATE IF NOT EXISTS 不会动已有表，迁移函数才会 ALTER。注意各测试函数可用独立临时库路径（模块级一个主库 + 老库/新库双形态用两个测试函数各自 mkdtemp 会导致 config.DB_PATH 只读一次的问题——解法：老库形态用同进程内手工建"旧表名不同库"不可行时，可把老库模拟集中到一个测试函数内（模块级单库为主，首个测试先建旧结构再共享）；参考 test_p0_chain 单库全流程惯例，允许各测试用 new_id 唯一主键互不干扰。旧行构造直插风格照 _seed_completed_session_direct。断言全部走 _q 只读 helper。不得 import 其他测试模块。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase2_migration.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/test_phase2_migration.py 存在且 pytest 可收集（单文件单进程）
    - 含老库模拟（旧版 DDL 手工建表 + 插旧行 + init_db）与新库直建两条路径的断言
    - 锚点断言精确到 §9.4 表值（3,2/4,3/5,4）且 difficulty NULL 行锚点为 NULL
    - 合并断言精确验证 COALESCE 语义（final_score=3 覆盖 score_final=2 → 3）
    - 本任务提交时测试红（新列尚不存在），Task 2 后转绿
    - 测试库全部在 tempfile.mkdtemp() 下，grep 测试文件无 "data/app.db" 字样
  </acceptance_criteria>
  <done>迁移双路径断言就位，红测证明现状三表无新列</done>
</task>

<task type="auto">
  <name>Task 2: db.py 三表双轨迁移（_DDL 同步 + ALTER 嗅探 + 锚点回填 + 合并 + 唯一索引）</name>
  <files>server/db.py</files>
  <read_first>
    - server/db.py（全文——_DDL 三表块 :116-185、_migrate_llm_trace :277-302、_migrate_feedback_status :305-326、init_db :329-341）
    - .planning/phases/02-dynamic-selection/02-PATTERNS.md（计划 02-01 节 db.py 条目：PRAGMA table_info 嗅探骨架 + 双轨纪律）
    - design/final-design/总设计文档.md（§9.2 :177-199 question_bank 演进 DDL、§9.4 :211-217 锚点表、§12.2 :366-368、§12.4 :374-378）
  </read_first>
  <action>(1) _DDL 三表 CREATE 语句按 <interfaces> 新列清单同步加列（沿用各表列对齐风格；不加任何 DB CHECK——N11；question_score 的 CREATE 在既有列全保留的基础上新增 score_state TEXT NOT NULL DEFAULT 'SCORED'——final_score 与 score_final 两列照旧并存，列移除属 02-05 消费点切换后，删除将令 wave 1-4 期间 scoring.py 仍在写 final_score 的 INSERT 直接 OperationalError）。注意：_DDL 的 question_bank CREATE 保持既有 category/qtype/scope CHECK 原样不动不删。(2) 新增三个迁移函数（中文 docstring 注明 D-14/D-15 与 SSOT § 号，骨架同 _migrate_llm_trace 的"先查再动 + 幂等早退"，列存在性用 PRAGMA table_info(表名) 取列名集合而非 sqlite_master 字符串嗅探）：_migrate_question_bank_v2（10 新列逐列嗅探 ALTER——question_type/measurement_stage 带 NOT NULL DEFAULT 'ordinary'，其余裸 ADD；随后执行 <interfaces> 的锚点 CASE UPDATE 回填，回填幂等条件 WHERE observable_level_max IS NULL AND difficulty IS NOT NULL）、_migrate_assessment_question_v2（12 新列逐列嗅探 ALTER，question_type DEFAULT 'legacy'、followup_count DEFAULT 0、status/measurement_stage 等可 NULL 裸 ADD——measurement_stage 参照新列清单里允许 NULL；结尾 CREATE UNIQUE INDEX IF NOT EXISTS uq_aq_session_seq ON assessment_question(session_id, seq)，并先执行重复检测 SELECT session_id, seq, COUNT(*) ... GROUP BY 1,2 HAVING COUNT(*)>1，发现重复则 raise RuntimeError 附行明细——业务库为演示数据允许重跑生成，不做静默去重）、_migrate_question_score_v2（嗅探加 score_state TEXT NOT NULL DEFAULT 'SCORED'；嗅探 final_score 列存在则执行 UPDATE question_score SET score_final=COALESCE(final_score, score_final)——合并后【不 DROP】final_score，代码注释注明 DROP 属 02-05 消费点切换后）。(3) init_db 迁移序列在 _migrate_feedback_status 之后、executescript(_DDL) 之前依次注册三个新函数。全部 ? 参数化惯例不适用于 DDL 固定串（列名字面量拼 SQL 合法，本任务无用户输入）。</action>
  <verify>
    <automated>set -o pipefail && python -c "import os; os.environ['DB_PATH']='/tmp/p2_ddl_check.db'; from server.db import init_db; init_db(); init_db(); print('DDL idempotent OK')" && cd server && python -m pytest test_phase2_migration.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - db.py 含 _migrate_question_bank_v2 / _migrate_assessment_question_v2 / _migrate_question_score_v2 三个函数名（固定 _v2 命名，02-05 复用该函数追 DROP 段）且在 init_db 中按序调用
    - 新列全部无 DB CHECK（迁移函数与 _DDL 均 grep 不到新列上的 CHECK）
    - 锚点回填 SQL 为 UPDATE ... CASE difficulty 两步法（grep "observable_level_max = CASE difficulty"）
    - 合并 SQL 含 COALESCE(final_score, score_final) 且 db.py 无 "DROP COLUMN final_score"（本计划严禁 DROP）
    - _DDL 的 question_score CREATE 仍含 final_score 列（与 score_final 并存——PRAGMA 直查 /tmp 新库可证；02-05 消费点切换后随 DROP 一并去除）
    - 唯一索引 CREATE UNIQUE INDEX IF NOT EXISTS uq_aq_session_seq 存在且重复检测 raise 保护在先
    - 二次 init_db 幂等（新库 /tmp 路径直跑验证 OK）
    - test_phase2_migration.py 全绿
  </acceptance_criteria>
  <done>三表新列在老库 ALTER 与新库直建双路径就位；锚点/合并/唯一索引数据动作完成；无 CHECK 新增、无 final_score DROP</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 7:3 三落点 — config 更新 + aggregate 零比率保护 + 权重回归断言</name>
  <files>server/config.py, server/services/aggregate.py, server/test_phase2_weights.py</files>
  <read_first>
    - server/config.py（:23-33 CATEGORY_RATIO 现状 5.5/2.0/2.0/0.5）
    - server/services/aggregate.py（_compute_weights :81-98、category_weights 摘要 :165、gate 判定 :134）
    - server/services/aggregation.py（:118-121 总分公式——断言锚点，不改此文件）
    - .planning/phases/02-dynamic-selection/02-RESEARCH.md（Pattern 5 七三三层落点 + Warning 段 gate 项语义）
    - server/test_m5_backend.py（:1-57 文件头 + 断言组织风格）
  </read_first>
  <behavior>
    - test_weight_73_hard_soft：4 个 hard（importance 混合）+ 2 个 soft 的 items 过 _compute_weights 后 Σhard weight == 0.7（±0.0005 尾差）且 Σsoft == 0.3
    - test_weight_single_category：纯 soft 岗位（只 soft items）Σweight == 1.0（大类归一，§8.2）
    - test_weight_gate_items_zero：gate 项（experience/qualification）weight == 0.0 且非 gate 项不受影响；纯 gate 模型不抛 ZeroDivisionError 且全部 weight == 0.0（total_ratio==0 保护）
    - test_aggregation_no_double_scaling：构造一行式断言——aggregate 后总分公式只含 weight×(actual/5)×100，无 CATEGORY_RATIO 二次乘（可直调 aggregate_session_scores 或对 :118-121 源码 grep 断言 "actual / 5.0" 与不出现 "CATEGORY_RATIO" 于 aggregation.py import）
    - test_enum_ratio_value：config.CATEGORY_RATIO == {"hard_skill": 0.7, "soft_skill": 0.3, "experience": 0.0, "qualification": 0.0}
  </behavior>
  <action>(1) server/config.py：CATEGORY_RATIO 四值改 0.7/0.3/0.0/0.0（保留四键位——aggregate.py:87 只对出现类目求和，0.0 键不干扰归一；注释行更新为 SSOT §8.2 7:3 口径 + D-16 存量模型不重算说明）。(2) server/services/aggregate.py：仅 _compute_weights 内加 total_ratio==0 保护（if total_ratio == 0: 循环内 it["weight"]=0.0；尾差吸收块加条件跳过——sum 已为 0 时 drift 吸收逻辑不得执行；行内注释注明 REF-5.7、§8.2 gate 走事实核验不占权重池、7:3 零系数引入的除零边界）。其余零改动（category_weights 摘要 :165 的公式保持，回归断言与代码同式即可）。(3) 新建 server/test_phase2_weights.py：文件头照 test_m5_backend 模板（env 先于 import、mock、临时库、pytest 可收集、不 import 其他测试模块）；按 <behavior> 组织 5 个测试函数，_compute_weights 直调风格（import from server.services.aggregate import _compute_weights——items 用 list[dict] 直构，含 category/importance 键；gate 直构不必真插库，_compute_weights 是纯函数）。断言不用魔法数的地方写 0.7/0.3 字面量（SSOT §8.2 原文值）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_phase2_weights.py -v 2>&1 | tail -10 && python -m pytest test_phase2_migration.py -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - config.py CATEGORY_RATIO 值为 0.7/0.3/0.0/0.0 且四键保留
    - aggregate.py 除 total_ratio==0 保护外无其他逻辑改动（diff 只含保护块与注释）
    - aggregate.py 无任何 UPDATE competency_item / weight 重算存量语句（存量不重算，D-16）
    - test_phase2_weights.py 全绿且含纯 gate 模型不除零断言
    - aggregation.py 本任务零改动（grep git diff 不出现该文件）
  </acceptance_criteria>
  <done>7:3 口径在 config/aggregate/aggregation 三落点就位；零比率边界受保护；回归断言锁死口径</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 老业务库 data/app.db → 迁移函数 | 生产数据被 ALTER/UPDATE 修改（唯一会碰业务库的动作，D-15 授权范围） |
| 迁移函数 → 存量 confirmed 模型 | weight 属历史事实，迁移不得触碰（D-003/§8.3） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | _migrate_* 对业务库的 ALTER/UPDATE | mitigate | PRAGMA 嗅探幂等早退 + 唯一索引重复检测 raise（不静默去重）+ test_phase2_migration 双路径证明；正式运行业务库直跑前可 git checkout 双保险（原子 commit 已存证上一状态） |
| T-02-02 | Repudiation | score_final 合并丢数据 | mitigate | COALESCE(final_score, score_final) 保序（final_score 优先）合并 + final_score 列本计划保留（02-05 才 DROP），窗口内可审计对照 |
| T-02-03 | Tampering | 存量模型 weight 被 7:3 重算 | mitigate | aggregate.py 零 UPDATE；acceptance 明确 grep 无 weight 重算语句（D-003） |
| T-02-04 | DoS | 纯 gate 模型 total_ratio=0 除零 | mitigate | _compute_weights 零比率保护（7:3 引入的新边界，否则 gate-only 模型聚合崩） |
| T-02-05 | Tampering | 迁移中途失败留半态 | accept | init_db 每函数独立幂等（嗅探早退），重跑即收敛；SQLite ALTER 逐列原子性由事务提交粒度保证，演示数据允许重跑（D-15） |
</threat_model>

<verification>
1. `cd server && python -m pytest test_phase2_migration.py -v` — 双路径迁移断言全绿
2. `cd server && python -m pytest test_phase2_weights.py -v` — 7:3 回归全绿
3. 从仓库根：`python -c "import os; os.environ['DB_PATH']='/tmp/p2_ddl_check.db'; from server.db import init_db; init_db(); init_db(); print('OK')"` — 幂等（注意必须仓库根运行；DB_PATH env 前置于 import）
4. 既有回归不受影响：`cd server && python -m pytest test_m5_backend.py -v 2>&1 | tail -8` 与 `python test_m6_backend.py`（列新增不改行为——本计划零消费方改动，两套应全绿）
5. `grep -n "DROP COLUMN final_score" server/db.py` 零命中（DROP 属 02-05）
</verification>

<success_criteria>
- REF-2.7 结构件落地：assessment_question 12 新列 + (session_id, seq) 唯一索引（seq 承载 sequence_no 语义，Q2 决议）
- REF-2.9 主体落地：question_score.score_state 列 + final_score→score_final 数据合并（列本体保留至 02-05 消费点切换后原子 DROP——A8 完整次序：ADD score_state → UPDATE COALESCE 合并 →（02-05）DROP + _DDL 去列）
- REF-3.7 落地：observable_level_max/min 列 + §9.4 锚点回填
- REF-5.7 落地：CATEGORY_RATIO 7:3 + _compute_weights 跟随 + aggregation 不二次乘回归锁死 + gate 零权重边界
- D-14/D-15/D-16 三决策逐条满足（ALTER 无 CHECK / 旧数据 legacy 语义 / 存量不重算）
</success_criteria>

<output>
Create `.planning/phases/02-dynamic-selection/02-01-SUMMARY.md` when done
</output>
