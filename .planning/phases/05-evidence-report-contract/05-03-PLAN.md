---
phase: 05-evidence-report-contract
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - server/db.py
  - server/services/report.py
  - server/services/report_checks.py
  - server/api/assessment.py
  - server/api/admin/reports.py
  - server/main.py
  - web/src/views/assessment/Report.vue
  - web/src/views/admin/TestCenter.vue
  - server/test_phase5_report.py
  - server/test_m6_backend.py
autonomous: true
requirements: [REF-5.9, REF-8.3]

must_haves:
  truths:
    - "报告发布走完整状态机 GENERATING → PROVISIONAL|READY → PUBLISHED|FAILED，非法迁移被代码拒绝"
    - "发布前七项一致性校验由代码执行，任一失败 → report_status=FAILED（不生成正常报告）"
    - "管理员必须明确 POST /api/admin/reports/{report_id}/publish 才 PUBLISHED（候选人无法越权）"
    - "重复生成不再 DELETE 覆盖——同 session 多版本行，get_report_by_session 取最新，旧行保留（feedback FK 不悬空）"
    - "报告生成失败显式可见（FAILED 行 + TASK_FAILED 事件），前端确定性区分生成中/失败"
    - "报告生成运行时写 trace_link（generate_report 落报告后 link_entity link_role=reported/source），report→session→model/version→question→message→score→trace 对新报告闭合"
  artifacts:
    - path: "server/services/report.py"
      provides: "状态机推进 + 版本化 INSERT（废除 DELETE 覆盖）+ FAILED 行 + REVIEW_* 事件 + report→trace 运行时 trace_link 写点"
      contains: "REPORT_STATUSES"
    - path: "server/services/report_checks.py"
      provides: "七项一致性校验 + 录用判断词表常量"
      contains: "HIRING_REDLINE_WORDS"
    - path: "server/api/admin/reports.py"
      provides: "POST /reports/{report_id}/publish 端点（require_admin）"
      contains: "publish"
    - path: "server/db.py"
      provides: "report 表 +report_status/review_status/version + 发布字段 + 存量回填"
      contains: "_migrate_report_phase5"
  key_links:
    - from: "server/services/report.py generate_report"
      to: "_run_consistency_checks"
      via: "聚合后、版本化 INSERT 前"
      pattern: "_run_consistency_checks"
    - from: "server/api/assessment.py request_report"
      to: "report_status='GENERATING'"
      via: "入队前写 GENERATING 行"
      pattern: "REPORT_GENERATING"
    - from: "server/api/admin/reports.py publish"
      to: "REVIEW_REPORT_PUBLISH_CONFIRMED"
      via: "append_event 同事务"
      pattern: "REVIEW_REPORT_PUBLISH_CONFIRMED"
    - from: "server/services/report.py generate_report"
      to: "trace_link"
      via: "版本化 INSERT 后 link_entity"
      pattern: "link_entity\\(conn, trace_id=.*report"
---

<objective>
把报告发布收口为可审计契约：report 表加 report_status/review_status/version + 发布与人工复核字段；生成流程 = 聚合 → 七项一致性校验（任一失败 → FAILED 行）→ 版本化 INSERT（废除 DELETE 覆盖，旧版本保留、feedback FK 不悬空）→ 状态机 GENERATING→PROVISIONAL|READY→PUBLISHED|FAILED；管理员明确 POST publish 才 PUBLISHED；后台任务异常 → FAILED 行 + TASK_FAILED 事件，前端确定性区分「生成中/失败」；并落 report→session→…→trace 运行时 trace_link 写点（闭合 D-56 五要素）。

Purpose: D-025「报告状态机 + 七项校验 + 明确点击发布」；D-061~D-065 锁定决策；REF-5.9 SC-3「重复生成不再 DELETE 覆盖」；REF-8.3「失败显式可见」；D-56「审计链报告可达」（generate_report 落报告后 link_entity reported/source）。
Output: report 状态机 + 七项校验 + 版本化 INSERT + publish 端点 + FAILED 可见 + 前端最小化 + report→trace trace_link 写点。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-evidence-report-contract/05-CONTEXT.md
@.planning/phases/05-evidence-report-contract/05-RESEARCH.md
@.planning/phases/05-evidence-report-contract/05-PATTERNS.md
@.planning/phases/05-evidence-report-contract/05-VALIDATION.md

<interfaces>
<!-- 本计划使用的既有契约 -->

From server/services/report.py:
- `generate_report(session_id) -> dict`（report.py:89-153；DELETE+INSERT 在 144-152 行——版本化对象；agg = aggregate_session_scores 后组装 report_data）
- `_load_question_reviews(session_id)`（report.py:35-69；item_id 列在 05-04 补，本计划不碰）
- `_collect_evidence_quotes`（report.py:72-86，不动）

From server/services/state_events.py（只读复用，不在本计划 files_modified——append_event 按现状调用不修改）:
- `append_event(conn, *, session_id, event_type, from_state=None, to_state=None, actor_type="system", actor_id=None, assessment_question_id=None, assessment_message_id=None, payload=None) -> None`（不 commit，调用者持事务）

From server/services/trace_link.py（05-01 产出，本计划 report→trace 写点调用对象）:
- `link_entity(conn, *, trace_id, entity_type, entity_id, link_role, created_at=None) -> None`（link_role ∈ LINK_ROLES 代码校验；不 commit）

From server/services/llm.py（05-01 已透出 trace_id）:
- `call_llm_json(call_type, ref_id, system_prompt, user_prompt, mock_fn=None, trace_out=None) -> dict`（成功路径 trace_out.append(trace_id)——generate_report 用 trace_out 拿 report LLM 调的 trace_id）

From server/api/assessment.py:
- `request_report(session_id, background, user)`（assessment.py:1058-1087；现有「已存在 report 行 → 409 REPORT_ALREADY_EXISTS」改写对象）
- `_generate_report_task(session_id)`（assessment.py:1031-1055；except 只发 TASK_FAILED 静默——改写对象）
- `_append_task_event(session_id, event_type, ...)`（assessment.py:1018-1028；后台任务独立小事务事件）
- `get_report_by_session(session_id, user)`（assessment.py:1090-1103；ORDER BY created_at DESC LIMIT 1 取最新，需补返回 report_status）

From server/api/admin/models.py:
- `confirm_model`（126-159 行：require_admin + 状态迁移 + UPDATE + 同事务 + rowcount 404 判定——publish 端点照此骨架）
- router 声明 `APIRouter(prefix="/api/admin", tags=[...], dependencies=[Depends(require_admin)])`（models.py:13）
- `ModelUpdateBody`/`ModelItem` Pydantic BaseModel + Field 约束（models.py:16-33）

From server/api/admin/feedback.py:
- `_ReviewBody(BaseModel)`（feedback.py:30-31：note: str = ""）

From server/main.py:
- admin router 注册区（main.py:74-84；`from .api.admin import reports as admin_reports` + `app.include_router(admin_reports.router)`）

From server/core/security.py:
- `require_admin(user)`（security.py:57-60：非 admin 403）
- `load_owned_report(conn, report_id, user, allow_admin_read=False)`（security.py:84-99）

From server/db.py:
- `_migrate_question_bank_v2` ALTER ADD COLUMN 幂等形态（433-467 行）
- report 表 DDL（db.py:274-281：report_id/session_id/total_score/gate_passed/report_json/created_at）
- `get_conn()`（373-378）

From 05-02 产出:
- `aggregate_session_scores` 返回新增 review_status/observation_status/provisional/coverage 字段（05-03 状态机据此推进 PROVISIONAL vs READY）

From web/src/views/assessment/Report.vue:
- `phase` ref = 'generating'|'ready'|'failed'（256 行）；`bootstrap()` 先 getReportBySession 再 poll（380-407 行）；`poll()` 每 3s 轮询，404 或超时置 failed（409-424 行）；`getReportBySession` 现只返回 report_json 无 report_status

From server/test_m6_backend.py:
- 252-259 行幂等断言「重复生成幂等（同会话仅 1 行 report）n==1」+「重新生成 report_id 更新 rpt2 != rpt_id_1」——版本化后重写
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 测试脚手架（test_phase5_report.py 状态机/七项/版本化/publish/FAILED 断言）</name>
  <files>server/test_phase5_report.py</files>
  <read_first>
    - server/test_m5_backend.py（1-38 行三件套 + _q；43-73 行 _seed_position_with_confirmed_model）
    - server/test_m7_backend.py（28-56 行 _ensure_admin/_admin_token/_auth admin 侧 helpers；59-99 行 _seed_report_and_item 造外键全链）
    - .planning/phases/05-evidence-report-contract/05-VALIDATION.md（REF-5.9/8.3 测试名 + Edge Cases：状态机合法/非法迁移表、七项最小破坏、版本不可变 + FK 完整性）
  </read_first>
  <behavior>
    - test_status_transition_legality：合法迁移（GENERATING→PROVISIONAL/READY/FAILED；PROVISIONAL→PUBLISHED；READY→PUBLISHED；任意→FAILED）通过；非法迁移（PUBLISHED→READY、FAILED→PUBLISHED、PUBLISHED→PROVISIONAL）被 _assert_report_status/迁移校验 raise 拒绝
    - test_consistency_check_fails_to_failed：最小破坏构造（weight Σ≠1 或 question 引用不属于该 session 或文案含「建议录用」）→ _run_consistency_checks 返回非空 → generate_report 写 report_status='FAILED' 行（不生成正常报告）
    - test_version_immutability：重复触发 request_report 两次 → report 表 2 行、version 1/2、get_report_by_session 取 version 2、旧 version 1 行仍在；对 version 1 提 feedback 后 FK 不断裂（无 DELETE 触发）
    - test_publish_flow：admin POST /api/admin/reports/{id}/publish → report_status='PUBLISHED' + publish_confirmed_by + published_at + REVIEW_REPORT_PUBLISH_CONFIRMED 事件；候选人调用 → 403；review 未满足（review_status='HUMAN_REVIEW_REQUIRED' 且 review_outcome 非 CONFIRMED）→ 409
    - test_generate_failed_visible：后台任务抛异常 → report_status='FAILED' 行 + TASK_FAILED 事件；get_report_by_session 返回 report_status='FAILED'
  </behavior>
  <action>扩展 server/test_phase5_report.py（05-02 已建三件套与 _q 助手，本任务追加函数不重复建头部）：追加 5 条测试（test_status_transition_legality / test_consistency_check_fails_to_failed / test_version_immutability / test_publish_flow / test_generate_failed_visible）。import 补 from server.services.report import REPORT_STATUSES, REVIEW_STATUSES, _run_consistency_checks；from server.services.report_checks import HIRING_REDLINE_WORDS。admin 侧 helpers 照 test_m7_backend.py _ensure_admin/_admin_token/_auth/_seed_report_and_item 形态内联。逐文件命令 `cd server && python -m pytest test_phase5_report.py -v`（禁止同进程多文件）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py -v 2>&1 | grep -E "ImportError|ModuleNotFoundError|failed" ; echo "RED 预期：REPORT_STATUSES/_run_consistency_checks/HIRING_REDLINE_WORDS 不存在导致收集失败"</automated>
  </verify>
  <acceptance_criteria>
    - test_phase5_report.py 新增 5 个 test_* 函数名与 05-VALIDATION.md 一致
    - pytest 因 REPORT_STATUSES/_run_consistency_checks 不存在而 RED（非语法错误）
    - 05-02 的 4 条旧断言保留不删（adjudicate/IMPUTED/required/O=∅ 仍绿依赖 05-02 已实现）
  </acceptance_criteria>
  <done>test_phase5_report.py 扩展到 9 条断言（05-02 的 4 条 + 05-03 的 5 条），当前新增 5 条 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: report 表状态机列 + 枚举常量 + 七项校验 + 状态机推进 + 版本化 INSERT + report→trace 写点（REF-5.9）</name>
  <files>server/db.py, server/services/report.py, server/services/report_checks.py</files>
  <read_first>
    - server/db.py（274-281 行 report DDL；433-467 行 _migrate_question_bank_v2 ALTER ADD COLUMN 形态；680-699 行 init_db 注册点）
    - server/services/report.py（89-153 行 generate_report 全文；DELETE+INSERT 在 144-152 行）
    - design/final-design/总设计文档.md（562-571 行 §21.1 状态机 + 七项校验 + 发布流程）
    - server/services/scoring.py（22-30 行 SCORE_STATES 枚举常量形态——REPORT_STATUSES 照此）
    - server/services/trace_link.py（link_entity 签名——report→trace 写点调用对象）
  </read_first>
  <behavior>
    - REPORT_STATUSES = ("GENERATING","PROVISIONAL","READY","PUBLISHED","FAILED")；REVIEW_STATUSES = ("NONE","REQUIRED","IN_PROGRESS","CONFIRMED","CLOSED","HUMAN_REVIEW_REQUIRED")
    - _run_consistency_checks(agg, session_id) 七项全过返回 []，任一失败返回错误字符串列表
    - generate_report 聚合后先跑七项校验，失败 → 写 report_status='FAILED' 行（report_json 含 error 摘要）后 return，不生成正常报告
    - 版本化：不再 DELETE FROM report WHERE session_id=?；新版本 INSERT 新行，version = session 内 MAX(version)+1
    - report→trace：generate_report 落 report 行后 link_entity link_role='reported'（report 实体）+ 'source'（assessment_session 实体）
  </behavior>
  <action>server/db.py 新增 _migrate_report_phase5(conn)：PRAGMA table_info 嗅探 report 表，逐列 ALTER ADD COLUMN（全可空无 DB CHECK）：report_status TEXT / review_status TEXT / version INTEGER / review_request_reason TEXT / reviewer_id TEXT / review_note TEXT / review_outcome TEXT / reviewed_at TEXT / publish_confirmed_by TEXT / published_at TEXT；随后存量回填 UPDATE report SET report_status='PUBLISHED', review_status='NONE', version=1 WHERE report_status IS NULL（回填默认值已裁决 PUBLISHED+NONE+version=1，关口 A）；注册进 init_db 加在 _migrate_trace_link 之后。新建 server/services/report_checks.py：常量 HIRING_REDLINE_WORDS = ("建议录用","不予录用","排名第","推荐淘汰","拟录用","建议淘汰")（D-002 红线词表集中一处）；函数 _run_consistency_checks(agg: dict, session_id: str) -> list[str] 七项：①数字可重算（重算 total_score==agg["total_score"]，浮点差 < 0.01）②weight 总和一致（Σ item.weight ≈ 1.0 容差 0.005，含 gate）③引用 question/message 属于该 session（agg 内 item 的 question_id 反查 assessment_question WHERE session_id=session_id）④model/version 与快照一致（assessment_session.model_id/model_version 非空且与 agg 透传一致）⑤无效题/系统错误未进正常分母（重跑 _EXCLUDED_STATES 过滤比对 SCORED 行数）⑥IMPUTED/REFUSED/required 警告与结构化状态一致（agg["refusals"]/["missing_warnings"]/["review_status"] 自洽）⑦文案无录用判断表述（对 json.dumps(report_data) 全文含 HIRING_REDLINE_WORDS 任一 → 失败）。server/services/report.py 改造：顶部 import json 已有 + `from .report_checks import _run_consistency_checks` + `from .trace_link import link_entity`；定义 REPORT_STATUSES/REVIEW_STATUSES 常量 + _assert_report_status(s)（非法 raise ValueError）；generate_report 流程重排——聚合后 report_data 组装前先跑 `errors = _run_consistency_checks(agg, session_id)`，非空 → `_insert_report_row(session_id, status="FAILED", report_json={"error": "; ".join(errors)[:200]})` 后 return；七项通过 → report_status = "PROVISIONAL"（agg["provisional"] 为真或 review_status=="HUMAN_REVIEW_REQUIRED"）否则 "READY"；版本化 INSERT 替换 DELETE：`version = 1 + COALESCE((SELECT MAX(version) FROM report WHERE session_id=?), 0)`，INSERT INTO report(report_id, session_id, total_score, gate_passed, report_json, report_status, review_status, version, created_at)（发布字段本计划置 NULL，publish 端点填）；report LLM 调用 `trace_out = []` 传入 `call_llm_json("report", session_id, REPORT_SYSTEM, ..., mock_fn=_mock_report, trace_out=trace_out)`（trace_id = trace_out[0]），版本化 INSERT report 行之后、conn.commit() 之前写 `link_entity(conn, trace_id=trace_id, entity_type="report", entity_id=report_id, link_role="reported")` 与 `link_entity(conn, trace_id=trace_id, entity_type="assessment_session", entity_id=session_id, link_role="source")`；返回 report_data 附加 report_status/review_status/version 键。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py::test_status_transition_legality test_phase5_report.py::test_consistency_check_fails_to_failed test_phase5_report.py::test_version_immutability -v</automated>
  </verify>
  <acceptance_criteria>
    - REPORT_STATUSES/REVIEW_STATUSES 常量在 report.py，review_status 含 "HUMAN_REVIEW_REQUIRED"（grep 确认）
    - HIRING_REDLINE_WORDS 常量在 report_checks.py，_run_consistency_checks 存在且返回 list
    - generate_report 不再含 `DELETE FROM report WHERE session_id`（grep -n "DELETE FROM report" server/services/report.py 无命中）
    - _migrate_report_phase5 注册 init_db，report 表含 report_status/review_status/version 等 10 列，存量回填 UPDATE 存在
    - generate_report 写 trace_link（grep "link_entity" 命中 server/services/report.py，含 entity_type="report" link_role="reported" 与 entity_type="assessment_session" link_role="source"）
    - test_status_transition_legality / test_consistency_check_fails_to_failed / test_version_immutability 绿
  </acceptance_criteria>
  <done>报告状态机列 + 枚举 + 七项校验 + 状态机推进 + 版本化 INSERT + report→trace trace_link 写点完成，3 条测试绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: request_report 409 边界 + 生成失败可见 + publish 端点 + 前端最小化（REF-5.9/REF-8.3）</name>
  <files>server/api/assessment.py, server/api/admin/reports.py, server/main.py, web/src/views/assessment/Report.vue, web/src/views/admin/TestCenter.vue, server/test_m6_backend.py</files>
  <read_first>
    - server/api/assessment.py（1018-1087 行 _append_task_event/_generate_report_task/request_report/get_report_by_session）
    - server/api/admin/models.py（126-159 行 confirm_model 骨架；13 行 router 声明；16-33 行 Pydantic）
    - server/main.py（74-84 行 router 注册区）
    - web/src/views/assessment/Report.vue（256/380-424 行 phase/poll/bootstrap）
    - server/test_m6_backend.py（252-259 行幂等断言重写对象）
  </read_first>
  <behavior>
    - request_report 三分支：非 completed → 409 SESSION_NOT_COMPLETED；completed 且存在 report_status='GENERATING' 行 → 409 REPORT_GENERATING；其余（无行/已有 FAILED/READY/PUBLISHED 行）→ 写 GENERATING 行 + 202 入队
    - _generate_report_task 异常 → 写 report_status='FAILED' 行（report_json 含 {"error": str(e)[:200]}）+ TASK_FAILED 事件
    - publish 端点：admin → report_status='PUBLISHED' + publish_confirmed_by + published_at + REVIEW_REPORT_PUBLISH_CONFIRMED 事件；候选人 → 403；review 未满足 → 409
    - get_report_by_session 返回 report_status 字段；Report.vue 读 report_status 区分 GENERATING/FAILED/ready
  </behavior>
  <action>改写 server/api/assessment.py request_report：替换「已存在 report 行 → 409 REPORT_ALREADY_EXISTS」为三分支——`row = SELECT report_status FROM report WHERE session_id=? ORDER BY created_at DESC LIMIT 1`；(a) session["status"]!="completed" → 409 detail {error_code:"SESSION_NOT_COMPLETED"}；(b) 最新行 report_status=="GENERATING" → 409 detail {error_code:"REPORT_GENERATING"}；(c) 否则 INSERT 一行 report_status='GENERATING'、version=MAX(version)+1、report_json='{}'、total_score=0、gate_passed=0（GENERATING 占位行，generate_report 会 UPDATE 成终态）→ TASK_QUEUED 事件 → commit → add_task。改写 _generate_report_task：score_session 后 generate_report；except 分支写 `UPDATE report SET report_status='FAILED', report_json=? WHERE session_id=? AND report_status='GENERATING'`（无 GENERATING 行则 INSERT FAILED 行 version=MAX+1），report_json=json.dumps({"error": str(e)[:200]})，随后 _append_task_event("TASK_FAILED", payload={"error": str(e)[:200]})。改 get_report_by_session：返回 json.loads(r["report_json"]) 合并 {"report_status": r["report_status"], "version": r["version"]}。新建 server/api/admin/reports.py：`router = APIRouter(prefix="/api/admin", tags=["admin-reports"], dependencies=[Depends(require_admin)])`；`class _PublishBody(BaseModel): review_outcome: str = "CONFIRMED"; review_note: str = ""`；`@router.post("/reports/{report_id}/publish")` 函数签名 `(report_id: str, body: _PublishBody, admin: dict = Depends(require_admin))`——SELECT report 行，None → 404「报告不存在」；report_status=="PUBLISHED" → 409「报告已发布」；report_status in ("GENERATING","FAILED") → 409「当前状态不可发布」；review_status=="HUMAN_REVIEW_REQUIRED" 且 body.review_outcome!="CONFIRMED" → 409「需先完成人工复核」；否则 UPDATE report SET report_status='PUBLISHED', publish_confirmed_by=admin["user_id"], published_at=now_iso(), review_outcome=?, review_note=?, reviewer_id=admin["user_id"], reviewed_at=now_iso() WHERE report_id=?；同事务 append_event(conn, session_id=反查的 session_id, event_type="REVIEW_REPORT_PUBLISH_CONFIRMED", from_state=原状态, to_state="PUBLISHED", actor_type="admin", actor_id=admin["user_id"], payload={"report_id": report_id})；commit。server/main.py 加 `from .api.admin import reports as admin_reports` + `app.include_router(admin_reports.router)`。web Report.vue：bootstrap/poll 里 getReportBySession 返回 data 后读 data.report_status——"FAILED" → phase.value='failed'（停止轮询）；"GENERATING" → 继续轮询；其余（READY/PROVISIONAL/PUBLISHED）→ onReportReady(data)。web TestCenter.vue：feedback review 界面加「发布报告」按钮（调用 admin API 的 publish 端点，admin token 下）。重写 server/test_m6_backend.py 252-259 行断言：`n = SELECT COUNT(*) FROM report WHERE session_id=?`；`maxv = SELECT MAX(version)`；断言 n==2（重复生成 2 行）且 maxv==2 且最新行 report_id 与首行不同（保留「重新生成 report_id 更新」语义）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py -v</automated>
  </verify>
  <acceptance_criteria>
    - request_report 409 分支含 REPORT_GENERATING（grep），无 REPORT_ALREADY_EXISTS
    - _generate_report_task except 分支写 report_status='FAILED' 行（grep "FAILED" 于 except 上下文）
    - admin/reports.py 存在 publish 端点 require_admin；main.py 注册 admin_reports.router
    - get_report_by_session 返回含 report_status 键；Report.vue 读 data.report_status
    - test_publish_flow / test_generate_failed_visible 绿；test_version_immutability 绿（request_report 路径）
    - test_m6_backend.py 幂等断言重写为 version 计数（grep "version" 于 258 行附近）
    - test_phase5_report.py 全部 9 条绿
  </acceptance_criteria>
  <done>publish 端点 + 生成失败可见 + request_report 409 边界 + 前端最小化 + test_m6 断言重写完成，test_phase5_report.py 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→服务端 | publish 端点经 require_admin，候选人 token 无法越权；七项校验代码执行不信任 LLM 文案 |
| 服务层→DB | report_status/review_status 枚举代码校验，代码外直改 DB 被迁移合法性测试拦截 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Elevation of Privilege | server/api/admin/reports.py publish | mitigate | 路由级 Depends(require_admin)（security.py:57-60 非 admin 403）+ 函数签名 admin 注入 |
| T-05-08 | Tampering | server/services/report_checks.py | mitigate | 七项校验代码执行，HIRING_REDLINE_WORDS 精确词表匹配报告 JSON 全文，违则 FAILED（D-002 红线） |
| T-05-09 | Tampering | server/services/report.py 状态机 | mitigate | report_status/review_status 枚举代码校验（N11），非法迁移 raise；test_status_transition_legality 断言非法迁移拒绝 |
| T-05-10 | Tampering | server/api/assessment.py _generate_report_task | mitigate | 异常写 FAILED 行 + TASK_FAILED 事件，不再静默（REF-8.3） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
逐文件跑：`cd server && python -m pytest test_phase5_report.py -v` 全绿（9 条）；重写后的 test_m6_backend.py 断言绿；既有 test_m7_backend.py 绿（feedback/trace 回归）；`cd server && python -m pytest test_phase5_evidence.py::test_audit_chain_closure -v` 绿（report→trace 写点落地后审计链全链闭合）。
</verification>

<success_criteria>
报告状态机 + 七项校验 + 版本化 + publish 端点 + FAILED 可见 + 前端最小化全部落地；report→trace 运行时 trace_link 写点（reported/source）落地；test_phase5_report.py 9 条全绿；test_audit_chain_closure 绿；test_m6 幂等断言重写为版本化断言；候选人无法越权 publish（403）。
</success_criteria>

## Open Items for Hard Gate A

本计划触及一处 MUST-NOT-DECIDE 开放项 + 一处既有锁定的枚举张力：

1. **存量 report 行回填默认值（SSOT §21.1 迁移回填）— 已裁决：`PUBLISHED` + `NONE` + `version=1`**（关口 A 用户裁决）。现有 report 行无 report_status/review_status/version，迁移回填常量 `report_status='PUBLISHED'` + `review_status='NONE'` + `version=1`（旧报告已候选端可见，语义最接近终态）。

2. **review_status 枚举张力（D-60 vs §21.1）**：D-60 锁定 `review_status=HUMAN_REVIEW_REQUIRED`（required 缺失 / O=∅），而 §21.1 五值列表（NONE/REQUIRED/IN_PROGRESS/CONFIRMED/CLOSED）不含该字面值。本计划定义 `REVIEW_STATUSES = ("NONE","REQUIRED","IN_PROGRESS","CONFIRMED","CLOSED","HUMAN_REVIEW_REQUIRED")` 六值并集，D-60 场景用 `HUMAN_REVIEW_REQUIRED`，其余用 §21.1 五值。此为文档内部张力非硬关口，已按 D-60 锁定字面落地并在此标注。

3. **七项校验的「录用判断」精确词表**：SSOT 只给语义「文案无录用判断表述」，无精确词表。本计划用 RESEARCH Pitfall 5 的 D-002 红线词表 `HIRING_REDLINE_WORDS = ("建议录用","不予录用","排名第","推荐淘汰","拟录用","建议淘汰")` 集中一处（report_checks.py），词表过宽/过窄在 Phase 6 bad case 收口。

<output>
Create `.planning/phases/05-evidence-report-contract/05-03-SUMMARY.md` when done
</output>
