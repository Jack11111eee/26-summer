---
phase: 04-question-bank-version
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - server/api/admin/jds.py
  - server/api/admin/positions.py
  - server/api/admin/models.py
  - server/test_phase4_orphan.py
  - server/test_phase4_model_edit.py
autonomous: true
requirements: [REF-7.1, REF-7.2]
user_setup: []

must_haves:
  truths:
    - "管理员访问 GET /api/admin/jds/orphan 返回孤儿 JD 列表（非 404）—— orphan 字段口径为【待硬关口 A 裁定的 pending 决策】：选项 A（D-52 字面）= 字段同 list_jds、WHERE position_id IS NULL、无 status 过滤；选项 B（现有实现）= 字段子集 jd_id/job_title/company/source_type/status/created_at + WHERE position_id IS NULL AND status != 'failed'（与 get_todos 孤儿计数一致）。已锁定选项 B（[04-009] 用户裁决）"
    - "管理员编辑模型提交 NaN/∞ weight、required_level<1 或 >5、importance 非法枚举、years 负数被服务端 422 拒绝并返回明确错误"
    - "同 category 内重复 std_name 被服务端 400 拒绝（保留 Σ=100% 校验，weight 合计偏差 >0.005 仍 400）"
  artifacts:
    - path: "server/api/admin/jds.py"
      provides: "GET /jds/orphan 声明置于 /jds/{jd_id} 之前"
    - path: "server/api/admin/positions.py"
      provides: "移除 list_orphan_jds（迁入 jds.py，get_todos 的 orphan_jds 计数保留）"
    - path: "server/api/admin/models.py"
      provides: "ModelItem 字段级校验 + update_model 重复 std_name 拒绝"
  key_links:
    - from: "GET /jds/orphan"
      to: "jd_record WHERE position_id IS NULL AND status != 'failed'"
      via: "FastAPI 声明序（先于 /jds/{jd_id}）"
      pattern: "@router.get(\"/jds/orphan\")"
    - from: "PUT /models/{model_id}"
      to: "ModelItem.weight"
      via: "Field(ge=0, le=1, allow_inf_nan=False)"
      pattern: "allow_inf_nan=False"
---

<objective>
收口模块一管理端两处已知缺陷：① `/jds/orphan` 路由顺序修复——现状 `main.py` 先注册 admin_jds.router 再注册 admin_positions.router，`/jds/{jd_id}`（jds.py:79）把 `jd_id="orphan"` 吞掉致恒 404；把 orphan 路由迁入 jds.py 并置于 jd_detail 之前，同文件声明序可控（REF-7.1）。② 模型编辑字段级校验——ModelItem 拒绝 NaN/∞/越界/非法枚举，update_model 拒绝同 category 重复 std_name，保留 Σ=100%（REF-7.2）。

Purpose: 兑现 SSOT §28 第 4 步的「/jds/orphan 路由顺序修复 + 模型编辑字段校验 NaN/范围/重复」，消除管理端已知缺陷与 NaN 权重静默污染 competency_item 的隐患。
Output: jds.py 迁入 orphan 路由、positions.py 移除重复路由、models.py Pydantic 强化；新增 test_phase4_orphan.py / test_phase4_model_edit.py。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

设计权威（只读，不改）：@design/final-design/总设计文档.md §28 第 4 项（orphan 路由顺序修复 / 模型编辑字段校验 NaN/范围/重复）

改造对象（必读现状）：@server/api/admin/jds.py、@server/api/admin/positions.py、@server/api/admin/models.py、@server/main.py（只读佐证注册序，不改）

测试 analog：@server/test_phase2_selection.py（三件套头 lines 13-52）、@server/test_phase3_forms.py（admin _ensure_admin + admin 登录头模式 lines 164-174）

<interfaces>
<!-- 执行者直接使用的既有契约（已逐行核对源码） -->

server/api/admin/jds.py:
  - @router.get("/jds/{jd_id}") def jd_detail(jd_id: str) -> dict  # line 79，吞掉 orphan 的元凶
  - @router.get("/positions/{position_id}/jds") def list_jds(position_id: str) -> list[dict]  # line 68
  - router = APIRouter(prefix="/api/admin", tags=["admin-jds"], dependencies=[Depends(require_admin)])  # line 11

server/api/admin/positions.py:
  - @router.get("/jds/orphan") def list_orphan_jds() -> list[dict]  # lines 87-95，被遮蔽的现有实现（迁入源）
  - def get_todos() -> dict  # lines 11-33，其中 orphan_jds 计数 lines 21-23 保留
  - router = APIRouter(prefix="/api/admin", tags=["admin-positions"], dependencies=[Depends(require_admin)])  # line 8

server/api/admin/models.py:
  - class ModelItem(BaseModel)  # lines 15-28，现 weight=Field(ge=0)、required_level=None、importance=str|None、years=None
  - def update_model(model_id: str, body: ModelUpdateBody) -> dict  # lines 67-110，Σ=100% 校验 lines 84-88
  - from typing import Literal 需新增 import
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 新建 test_phase4_orphan.py + test_phase4_model_edit.py（先红）</name>
  <files>server/test_phase4_orphan.py, server/test_phase4_model_edit.py</files>
  <read_first>
    - server/test_phase2_selection.py（三件套头 lines 13-52 + _q helper lines 46-52）
    - server/test_phase3_forms.py（_ensure_admin lines 164-174 + admin 登录头）
    - server/api/admin/jds.py（jd_detail line 79 / list_jds line 68）
    - server/api/admin/positions.py（list_orphan_jds lines 87-95 / get_todos lines 11-33）
    - server/api/admin/models.py（ModelItem lines 15-28 / update_model lines 67-110）
  </read_first>
  <behavior>
    - test_phase4_orphan.py 覆盖 REF-7.1：种子 jd_record 孤儿行（position_id=NULL、status='imported'）与 failed 行（position_id=NULL、status='failed'）→ GET /api/admin/jds/orphan 返回 200 list（非 404），列表只含非 failed 孤儿，字段为 jd_id/job_title/company/source_type/status/created_at；并断言 /jds/orphan 先于 /jds/{jd_id} 匹配（200 而非 404「JD 不存在」）
    - test_phase4_model_edit.py 覆盖 REF-7.2：种子 draft 模型（status='draft'，非 confirmed）+ competency_item → PUT /api/admin/models/{mid} 分别提交 NaN weight（422）、weight=1.5（422）、required_level=6（422）、required_level=0（422）、importance='bad'（422）、years=-1（422）、同 category 重复 std_name 且权重合计 1.0（400）、权重合计 0.5（400 保留 Σ=100%）；合法提交（权重合计 1.0、字段合规）返回 200
  </behavior>
  <action>
    新建两个测试文件，严格沿用 test_phase2_selection.py 三件套（文件顶部 import server 之前设 os.environ["DB_PATH"]=tempfile、LLM_PROVIDER=mock、JWT_SECRET，sys.path.insert 仓库根，import TestClient/config/init_db/get_conn/app/new_id/now_iso，显式 init_db()，写 _q 只读 helper）。两个文件各自独立三件套、禁止同进程收集。
    test_phase4_orphan.py 需 admin 认证：复用 test_phase3_forms.py 的 _ensure_admin 模式（passlib CryptContext bcrypt 建 role='admin' 用户）→ POST /api/auth/login 取 Bearer token → GET /api/admin/jds/orphan 带 Authorization 头；断言 status_code==200、响应为 list、字段集合恰为 {jd_id,job_title,company,source_type,status,created_at}、failed 孤儿被排除、non-failed 孤儿被包含。
    test_phase4_model_edit.py 需 admin 认证（同上）；种子 competency_model 行 status='draft'（update_model 对 confirmed 409）与 competency_item 行；对每个非法 body 断言 status_code（422/400），对合法 body 断言 200 且 competency_item 表按 items 重建。NaN 用 float('nan') 构造（JSON 序列化需先验证 TestClient 对 NaN 的行为——若 json.dumps 抛错则改走 float('inf') 或直接对 ModelItem 做 parse_obj 单测 + 一个 HTTP 层 NaN 用例）。此文件当前运行必红（现有 ModelItem weight 无 le/allow_inf_nan、required_level 无范围、importance 无 Literal、无重复 std_name 校验；orphan 恒 404），不得为求绿放宽断言。
  </action>
  <acceptance_criteria>
    - 两文件存在，顶部含 tempfile + os.environ["DB_PATH"] + LLM_PROVIDER=mock + init_db() 三件套（grep 断言）
    - grep test_phase4_orphan.py 含 "/jds/orphan" 与 "status != 'failed'" 或 "failed" 排除断言
    - grep test_phase4_model_edit.py 含 "allow_inf_nan" 或 "nan" 与 "required_level" 与 "importance" 与 "duplicate"/"重复" 断言
    - cd server && python -m pytest test_phase4_orphan.py -v && python -m pytest test_phase4_model_edit.py -v 均收集成功且为 FAILED（红，非 collection error）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_orphan.py -v 2>&1 | tail -30 && python -m pytest test_phase4_model_edit.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>两个测试文件存在、pytest 可收集、当前为红（断言失败而非 import/collection error），admin 认证 + 三件套齐全</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /jds/orphan 路由迁移至 jds.py 并置于 jd_detail 之前 + 移除 positions.py 重复路由</name>
  <files>server/api/admin/jds.py, server/api/admin/positions.py</files>
  <read_first>
    - server/api/admin/jds.py（jd_detail lines 79-90 / list_jds lines 68-76 / list_positions lines 14-23）
    - server/api/admin/positions.py（list_orphan_jds lines 87-95 / get_todos lines 11-33）
    - server/main.py（lines 74-84 注册序佐证，不改）
    - server/test_phase4_orphan.py（Task 1 刚建）
  </read_first>
  <behavior>
    - jds.py 新增 @router.get("/jds/orphan") list_orphan_jds() 声明在 list_jds 之后、jd_detail（/jds/{jd_id}）之前
    - 查询字段/口径取现有 positions.py:87-95 实现：SELECT jd_id, job_title, company, source_type, status, created_at FROM jd_record WHERE position_id IS NULL AND status != 'failed' ORDER BY created_at DESC（字段口径已锁定【[04-009] 选项 B 现有实现】= 字段子集 + status != 'failed'）
    - positions.py 移除 list_orphan_jds（lines 87-95），保留 get_todos 的 orphan_jds 计数
  </behavior>
  <action>
    改两文件，main.py 零改动：
    1. server/api/admin/jds.py：在 list_jds（line 68-76）之后、jd_detail（line 79）之前，新增 @router.get("/jds/orphan") def list_orphan_jds() -> list[dict]，函数体从 positions.py:87-95 原样迁入（conn = get_conn()；SELECT jd_id, job_title, company, source_type, status, created_at FROM jd_record WHERE position_id IS NULL AND status != 'failed' ORDER BY created_at DESC；return [dict(r) for r in rows]）。orphan 字段口径已锁定【[04-009] 选项 B 现有实现】：字段子集 jd_id/job_title/company/source_type/status/created_at + WHERE position_id IS NULL AND status != 'failed'（与 get_todos 孤儿计数一致），迁入现有 positions.py:87-95 原样。
    2. server/api/admin/positions.py：删除 @router.get("/jds/orphan") list_orphan_jds（lines 87-95 整段），保留 get_todos 内的 orphan_jds 计数（lines 21-23 不动）。
    关键顺序约束：/jds/orphan 必须与 /jds/{jd_id} 同文件（jds.py）内声明在前，FastAPI 按声明序匹配，`jd_id="orphan"` 不再落到 jd_detail 返回 404。迁移后不得残留 positions.py 的同路径路由（否则双路由，首个声明生效、positions 版成死代码）。
  </action>
  <acceptance_criteria>
    - grep -n "@router.get(\"/jds/orphan\")" server/api/admin/jds.py 命中且其行号小于 jd_detail 的 @router.get("/jds/{jd_id}") 行号
    - grep -c "@router.get(\"/jds/orphan\")" server/api/admin/positions.py 为 0（已移除）
    - grep -n "position_id IS NULL AND status != 'failed'" server/api/admin/jds.py 命中 orphan 查询（此口径为选项 B「现有实现」，已锁定 [04-009]）
    - cd server && python -m pytest test_phase4_orphan.py -v 转绿
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_orphan.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>GET /api/admin/jds/orphan 返回孤儿 JD 列表（200 非 404），声明序先于 /jds/{jd_id}，positions.py 无重复 orphan 路由</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ModelItem 字段级校验强化 + update_model 重复 std_name 拒绝（保留 Σ=100%）</name>
  <files>server/api/admin/models.py</files>
  <read_first>
    - server/api/admin/models.py（ModelItem lines 15-28 / update_model lines 67-110 / Σ=100% lines 84-88）
    - server/test_phase4_model_edit.py（Task 1 刚建）
  </read_first>
  <behavior>
    - weight: float = Field(ge=0, le=1, allow_inf_nan=False)（拦 NaN/∞ + 单 item 越界）
    - required_level: int | None = Field(default=None, ge=1, le=5)
    - importance: Literal["required", "preferred", "plus"] | None = None
    - years: float | None = Field(default=None, ge=0, allow_inf_nan=False)
    - update_model 内 Σ=100% 校验之后、重建 competency_item 之前，加同 category 重复 std_name 拒绝（HTTPException 400）
  </behavior>
  <action>
    改 server/api/admin/models.py，不新增表/迁移：
    1. 顶部加 from typing import Literal（若未 import）。
    2. ModelItem（lines 15-28）字段强化：weight 改为 Field(ge=0, le=1, allow_inf_nan=False)；required_level 改为 Field(default=None, ge=1, le=5)；importance 改为 Literal["required", "preferred", "plus"] | None = None；years 改为 Field(default=None, ge=0, allow_inf_nan=False)。category 的 pattern="^(hard_skill|soft_skill|experience|qualification)$" 保留不动；std_name min_length=1 保留；gate/level_reason/occurrence/evidence 不动。allow_inf_nan=False 是 Pydantic v2 解析期拒绝 NaN/±inf 的正解（项目 pydantic 2.10.3 已实测）——绝不在 Σ 聚合级补救（sum([nan,...]) 恒绕过 abs>0.005）。
    3. update_model（lines 82-88）Σ=100% 校验之后、line 98 DELETE competency_item 之前，插入重复 std_name 校验：以 set[tuple[str, str]] 为 seen，遍历 body.items，key=(it.std_name, it.category)，命中 seen 则 raise HTTPException(status.HTTP_400_BAD_REQUEST, f"同一类目内能力项重复：{it.std_name}({it.category})")，否则 seen.add(key)。判重键 (std_name, category) 对齐 competency_item PRIMARY KEY(std_name, category) 与 diff_models 的 "std_name|category" 对齐键。Σ=100% 校验（lines 84-88）保持原样（容差 0.005）。
  </action>
  <acceptance_criteria>
    - grep -n "allow_inf_nan=False" server/api/admin/models.py 命中 weight 与 years 两处
    - grep -n "Field(ge=0, le=1" server/api/admin/models.py 命中 weight；grep -n "Field(default=None, ge=1, le=5)" 命中 required_level
    - grep -n "Literal\[\"required\", \"preferred\", \"plus\"\]" server/api/admin/models.py 命中 importance
    - grep -n "同一类目内能力项重复" server/api/admin/models.py 命中 update_model 内重复校验
    - cd server && python -m pytest test_phase4_model_edit.py -v 转绿
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_model_edit.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>update_model 拒绝 NaN/∞ weight、weight>1、required_level 越界、importance 非法枚举、years 负数、同 category 重复 std_name（422/400），Σ=100% 校验保留，合法编辑返回 200</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→PUT /models/{model_id} | 管理员编辑模型，body.items 为不可信输入（跨过 Pydantic 后仍可能含 NaN/越界/重复） |
| admin→GET /jds/orphan | 管理员访问孤儿 JD 列表（只读，无输入参数） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | models.py ModelItem.weight 提交 NaN/∞ 绕过 Σ=100% 污染 competency_item | mitigate | Field(ge=0, le=1, allow_inf_nan=False) Pydantic v2 解析期 422，绝不在 Σ 聚合级补救（ASVS V5） |
| T-04-05 | Tampering | models.py required_level 越界 / importance 非法枚举 / years 负数 | mitigate | Field(ge=1, le=5) / Literal["required","preferred","plus"] / Field(ge=0) 解析期 422（ASVS V5） |
| T-04-06 | Elevation of Privilege | GET /jds/orphan 与 PUT /models/{model_id}（admin 端点） | accept | 已在 router 级 dependencies=[Depends(require_admin)] 下（jds.py line 11 / models.py line 12），本 phase 不新增权限面（ASVS V4 现有控制） |

注：本 phase 无 npm/pip/cargo 新增依赖（复用 fastapi/pydantic 2.10.3/sqlite3/typing.Literal），Package Legitimacy Gate 不适用，无 [ASSUMED]/[SUS] 包、无 slopcheck 检查点。
</threat_model>

<verification>
- 新增测试：cd server && python -m pytest test_phase4_orphan.py -v 全绿 && python -m pytest test_phase4_model_edit.py -v 全绿
- 源码断言：jds.py 的 @router.get("/jds/orphan") 行号 < @router.get("/jds/{jd_id}") 行号；positions.py 无 orphan 路由；models.py 含 allow_inf_nan=False / le=1 / ge=1,le=5 / Literal / 重复校验
- 回归：cd server && python -m pytest test_phase4_binding.py -v 全绿 && python -m pytest test_phase4_fail_visible.py -v 全绿 && python -m pytest test_phase2_selection.py -v 全绿（确认 04-01 与 positions.py 共享文件无冲突）
</verification>

<success_criteria>
- /jds/orphan 返回孤儿 JD 列表（非 404），声明序先于 /jds/{jd_id}（REF-7.1）；字段口径已锁定【[04-009] 选项 B 现有实现】（字段子集 + status != 'failed'）
- 模型编辑 NaN/越界/非法枚举/重复 std_name 被拒（422/400），Σ=100% 保留（REF-7.2）
</success_criteria>

<output>
Create .planning/phases/04-question-bank-version/04-02-SUMMARY.md when done
</output>
