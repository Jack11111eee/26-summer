---
phase: 01-p0
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - server/db.py
  - server/services/readiness.py
  - server/services/question_bank.py
  - server/api/assessment.py
  - server/api/admin/models.py
  - server/api/admin/positions.py
  - server/test_p0_chain.py
  - web/src/views/assessment/PositionAssess.vue
autonomous: true
requirements: [REF-3.5, REF-8.5]
must_haves:
  truths:
    - "题库未就绪/生成中/模型不可测量时 POST /sessions 返回 409 + error_code（QUESTION_BANK_GENERATING / QUESTION_BANK_INCOMPLETE / MODEL_NOT_MEASURABLE），绝不创建 0 题会话（成功标准 3）"
    - "GET /api/admin/todos 返回新键 question_bank_not_ready（计数/列表），题库不就绪岗位进入管理员待办"
    - "confirm 模型时插入 question_bank_task 行（QUEUED），生成任务开始/结束更新自身行（RUNNING/SUCCEEDED/FAILED）"
    - "存量测试种子（直插 question_bank 行、无 task 行 + 题库足量）开考不误伤——m5/m6 回归全绿（Pitfall 3 兼容点）"
    - "候选人在 PositionAssess.vue 开考被 409 拒绝时看到可读中文提示（不显示 [object Object]）"
  artifacts:
    - path: "server/services/readiness.py"
      provides: "check_session_readiness() 全链骨架（§10.4）+ 三个失败状态名统一返回"
      contains: "def check_session_readiness"
    - path: "server/db.py"
      provides: "question_bank_task 表 DDL"
      contains: "question_bank_task"
    - path: "server/api/admin/positions.py"
      provides: "todos 聚合扩展 question_bank_not_ready"
      contains: "question_bank_not_ready"
    - path: "web/src/views/assessment/PositionAssess.vue"
      provides: "409 error_code 可读提示"
      contains: "error_code"
  key_links:
    - from: "server/api/assessment.py create_session"
      to: "server/services/readiness.py check_session_readiness"
      via: "INSERT 会话前预检，不通过 409"
      pattern: "check_session_readiness\\("
    - from: "server/api/admin/models.py confirm_model"
      to: "question_bank_task"
      via: "confirm 后 add_task 前插 QUEUED 行"
      pattern: "INSERT INTO question_bank_task"
    - from: "server/services/question_bank.py generate_question_bank"
      to: "question_bank_task"
      via: "开头 RUNNING / 结尾 SUCCEEDED / 异常 FAILED"
      pattern: "UPDATE question_bank_task"
    - from: "web/src/views/assessment/PositionAssess.vue onStart catch"
      to: "409 detail.error_code/message"
      via: "e.response?.data?.detail?.message || detail"
      pattern: "detail\\?\\.message"
---

<objective>
开考前可测量性检查（§10.4 全链骨架一次成型）+ question_bank_task 表落地：不通过阻止创建 session（409 + error_code）并产生管理员待办；confirm 触发生成时插 task 行、生成任务首尾更新自身行；前端 PositionAssess.vue 展示可读 409 提示。

Purpose: REF-3.5/REF-8.5——杜绝 0 题会话静默开考（宁可 409）；D-12 显式状态行区分"生成中/不完整/就绪"。
Output: readiness.py + question_bank_task DDL + create_session 预检 + todos 扩展 + 前端提示 + 三态测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0/01-CONTEXT.md
@.planning/phases/01-p0/01-PATTERNS.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

check_session_readiness 签名与返回（services/readiness.py，新文件）:
  def check_session_readiness(position_id: str) -> dict | None
  通过返回 None；不通过返回 {"error_code": "QUESTION_BANK_GENERATING|QUESTION_BANK_INCOMPLETE|MODEL_NOT_MEASURABLE", "detail": "<中文说明>"}
  §10.4 全链骨架（D-11，Phase 1 实现 1-5 项，6-7 为 no-op 占位）:
    1) position 存在且 status='active'——存在性与"无 confirmed 模型"仍走 create_session 既有 404 路径（不在此函数重复检查存在性）；active 复核语义写死（W-2，不留二义）：status != 'active' → 返回 MODEL_NOT_MEASURABLE，detail 如"该岗位当前未上架，不可开考"（不新增第 4 个状态名——D-11 四项承诺之一 position active 保持真实可查；复用 MODEL_NOT_MEASURABLE 作为"岗位不可开测"的语义载体）
    2) 模型 confirmed + items 非空（json_array_length(json_extract(model_json,'$.items'))==0 或全部 gate → MODEL_NOT_MEASURABLE，REF-8.5）
    3) 题库 readiness（question_bank_task 驱动 → GENERATING / INCOMPLETE，判定逻辑见下）
    4) required item 至少一题覆盖（缺 → INCOMPLETE）
    5) 配额可行——现行 CATEGORY_QUOTA 口径（question_selection.py:9 CATEGORY_QUOTA = {"hard_skill": 6, "soft_skill": 2, "experience": 2}；不足 → INCOMPLETE；勿用 config.CATEGORY_RATIO 旧口径，D-11）
    6) 综合题槽位（no-op：Phase 2-4 填函数体） 7) qualification 表单 schema（no-op：Phase 3 填函数体）
  三个失败状态名只在检查函数一处统一返回（D-11）

readiness 判定逻辑（D-12 + Pitfall 3 兼容——最关键设计点）:
  按 (position_id, model_id, model_version) 取最新 question_bank_task（created_at DESC LIMIT 1）：
    - 无 task 行：看实际可选题量（复用 select_questions_for_session 的 WHERE 口径：status='active' AND category=? AND (scope='general' OR (scope='position' AND position_id=?))）——足量（配额+required 覆盖）→ 就绪放行（兼容 m5/m6 直插题库种子，否则存量测试全部被 409 打挂）
    - status IN (QUEUED, RUNNING) → QUESTION_BANK_GENERATING
    - status = FAILED → QUESTION_BANK_INCOMPLETE（失败细节 Phase 4 REF-8.4 再做）
    - status = SUCCEEDED → 仍看实际可选题量（task 成功但配额/required 不足 → INCOMPLETE）

question_bank_task DDL（db.py _DDL 追加，列型惯例 TEXT/INTEGER、无 CHECK——N11）:
  task_id TEXT PRIMARY KEY, position_id TEXT NOT NULL REFERENCES position,
  model_id TEXT NOT NULL REFERENCES competency_model, model_version INTEGER NOT NULL,
  status TEXT NOT NULL (QUEUED/RUNNING/SUCCEEDED/FAILED 代码校验),
  created_at TEXT NOT NULL, started_at TEXT, finished_at TEXT, error_msg TEXT
  id 前缀 new_id("qbt")（Claude's Discretion 惯例）

插行/更新点:
  - admin/models.py confirm_model（:98-105）：conn.commit()（:102）之后、background.add_task（:105）之前插 QUEUED 行（new_id("qbt") + row["position_id"] + model_id + row["version"] + "QUEUED" + now_iso()），自身小事务 conn.commit()；new_id import 可顶部补或函数内局部（现文件 :70 已有局部 import new_id 先例）
  - services/question_bank.py generate_question_bank（:70-120）：函数开头（pos 行取得后）置 RUNNING + started_at；:73-75 pos is None 早退分支 task 行置 FAILED + error_msg（勿静默漏更）；结尾（:120 最后 commit 后）置 SUCCEEDED + finished_at；全函数包 try/except——异常置 FAILED + error_msg + finished_at 再静默退出（"现状失败静默改为至少落表"，D-12）；签名不变（models.py add_task 依赖）

create_session 接入（api/assessment.py:59-94）:
  位置：422 校验（:66-67）之后、模型查询（:69-73）之后、INSERT 会话（:77）之前——result = check_session_readiness(position_id)；if result: raise HTTPException(status.HTTP_409_CONFLICT, detail={"error_code": result["error_code"], "message": result["detail"]})（dict detail 实测可序列化进 r.json()["detail"]，测试只断言 error_code）
  既有 404"该岗位暂无已确认模型"（:74-75）保留在检查之前（无 confirmed 模型仍是 404 语义——本阶段不加新失败状态）

todos 扩展（admin/positions.py:11-28 get_todos）:
  返回 dict 增加键 "question_bank_not_ready": 计数（SELECT COUNT(DISTINCT position_id) c FROM question_bank_task WHERE status != 'SUCCEEDED'——按 position 去重；或等价聚合，plan 允许 executor 选其一，断言只查计数 >= 0 存在）；前端零破坏（Vue 对未知键安全，admin 页展示留 Phase 4，A1）

前端 409 提示（PositionAssess.vue:128-143 onStart catch——勘误（I-3，便于追溯）：D-13 字面写 Positions.vue，但经代码核实 `assessment.createSession` 的唯一调用点是 PositionAssess.vue:131（onStart；Positions.vue:76-78 只 router.push 不调 API），故主落点必须是 PositionAssess.vue，否则提示永不出现——01-PATTERNS.md 文件分类表与 RESEARCH Pattern 5 均已核实同结论）:
  现状 catch 只分 501；加 409 分支：const detail = e.response?.data?.detail；ElMessage.warning(detail?.message || detail || '当前岗位暂不可开考，请联系管理员')——detail 为 dict（{error_code, message}），勿直接展示否则 [object Object]（Pitfall 7）；detail 提取惯例参照 web/src/utils/sse.js:33-40

三态测试种子形态（test_p0_chain.py 追加）:
  - GENERATING：插 question_bank_task 行 status='QUEUED'（或 RUNNING）
  - INCOMPLETE：task 行 status='SUCCEEDED' 但题库只有 1 题（配额不满）；或 FAILED
  - MODEL_NOT_MEASURABLE：confirmed 模型 items 为空（model_json items=[]）
  - MODEL_NOT_MEASURABLE（其二，W-2 inactive 分支）：status='pending_review' 岗位 + confirmed 模型 + 足量题库（position CHECK 允许 pending_review，直插合法）
  - 通过种子（无 task 行 + 足量题库）→ 201 不误伤
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test_p0_chain.py 追加开考检查三态测试（先红）</name>
  <files>server/test_p0_chain.py</files>
  <read_first>
    - server/test_p0_chain.py（01-03 Task 1 已建——本任务在其后追加）
    - server/test_m5_backend.py（:43-127 种子——直插 question_bank 无 task 行的形态即"存量兼容"用例素材）
    - server/api/assessment.py（:59-94 create_session——现状空题集也建会话）
    - .planning/phases/01-p0/01-RESEARCH.md（Code Examples "开考检查失败矩阵" 骨架）
  </read_first>
  <behavior>
    - test_question_bank_generating_blocks_session：种子岗位+confirmed 模型+题库，另插 question_bank_task 行 status='QUEUED' → POST /sessions 返回 409，r.json()["detail"]["error_code"] == "QUESTION_BANK_GENERATING"，且 _q("SELECT COUNT(*) c FROM assessment_session WHERE position_id=?") == 0（绝不建会话）
    - test_question_bank_incomplete_blocks_session：task 行 status='SUCCEEDED' 但题库仅 1 题（远低于配额）→ 409 + error_code == "QUESTION_BANK_INCOMPLETE"，不建会话
    - test_model_not_measurable_blocks_session：confirmed 模型 model_json items 为空 → 409 + error_code == "MODEL_NOT_MEASURABLE"，不建会话（REF-8.5）
    - test_inactive_position_blocks_session：种 status='pending_review' 岗位 + confirmed 模型 + 足量题库 → POST /sessions 返回 409 + error_code == "MODEL_NOT_MEASURABLE"，且不建会话（W-2 status != 'active' 分支的红/绿断言；position 表 CHECK 允许 pending_review）
    - test_legacy_seed_without_task_row_passes：无 task 行 + 题库足量（m5 种子形态）→ POST /sessions 返回 201（Pitfall 3 存量兼容——硬性）
    - test_admin_todos_includes_question_bank_not_ready：存在非 SUCCEEDED task 行的岗位时 GET /api/admin/todos（admin headers）返回 json 含 "question_bank_not_ready" 键且 >= 1
  </behavior>
  <action>在既有 server/test_p0_chain.py 追加 4-5 个测试函数（同文件同进程）。种子自建：复用文件内既有 position/model 种子 helper，按 <behavior> 分别构造（GENERATING 直插 task 行 QUEUED；INCOMPLETE 用 task SUCCEEDED + 少量题；MODEL_NOT_MEASURABLE 两个用例——其一建一个 items=[] 的 confirmed 模型，其二种一个 status='pending_review' 岗位、其余照常配 confirmed 模型 + 足量题库，直插即可命中 W-2 inactive 分支）。admin 断言用 m7 的 _ensure_admin 模式种子 admin 用户（TestClient 登录取 headers）。不建会话的断言用 _q 计数。测试函数无参。本 Task 提交时红（check_session_readiness 与 question_bank_task 尚不存在）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_p0_chain.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_p0_chain.py 含三态 409 + error_code 断言（三个状态名精确字符串各至少一次；MODEL_NOT_MEASURABLE 同时覆盖 items 空模型与 inactive 岗位（status='pending_review'）两种种子形态——W-2 分支）
    - 每个失败态断言均含"不创建会话"的 _q 计数断言（COUNT == 0）
    - 含无 task 行 + 足量题库 → 201 的存量兼容断言（Pitfall 3）
    - 含 GET /api/admin/todos 的 question_bank_not_ready 键断言
    - 本 Task 提交时新增测试红，Task 2+3 后转绿
  </acceptance_criteria>
  <done>开考检查三态测试就位，红测证明现状 0 题会话静默开考缺陷</done>
</task>

<task type="auto">
  <name>Task 2: question_bank_task 表 + check_session_readiness + create_session 接入 + todos 扩展</name>
  <files>server/db.py, server/services/readiness.py, server/api/assessment.py, server/api/admin/positions.py</files>
  <read_first>
    - server/db.py（_DDL :9-215——追加位置在末尾；report 表 :189-196 列型惯例 analog）
    - server/services/question_selection.py（全文 66 行——CATEGORY_QUOTA :9 与 _pick_category :23-27 的 WHERE 口径为 readiness 实际题量判定的照抄源）
    - server/api/assessment.py（:59-94 create_session——接入点 :66-77；:22-28 list_positions 的 json_array_length 写法）
    - server/api/admin/positions.py（:11-28 get_todos 现状三键）
    - design/final-design/总设计文档.md（§10.4 :270-278）
    - .planning/phases/01-p0/01-PATTERNS.md（01-04 readiness 节 + question_bank_task 维护节）
  </read_first>
  <action>(1) server/db.py：_DDL 末尾追加 question_bank_task 表（<interfaces> 契约 9 列，分节注释注 SSOT §10.4/D-12；无 CHECK——N11）。(2) 新建 server/services/readiness.py：模块 docstring（中文，引 SSOT §10.4）+ 公开函数 check_session_readiness(position_id) -> dict | None——按 <interfaces> 七步骨架：函数首步查 position status（SELECT status FROM position WHERE position_id=?；status != "active" → 返回 MODEL_NOT_MEASURABLE，detail 如"该岗位当前未上架，不可开考"——W-2 写死的 inactive 分支，勿留占位或放行）；随后取最新 confirmed 模型（ORDER BY version DESC LIMIT 1，复用 create_session :69-73 同款查询）；json_array_length(json_extract(model_json,'$.items')) 判 items 空 → MODEL_NOT_MEASURABLE（detail 如"该岗位模型无可测能力项，无法开考"）；最新 task 行判定（GENERATING/FAILED→INCOMPLETE 语义按 <interfaces> 判定逻辑表）；无 task 行或 SUCCEEDED 时看实际可选题量——按 category 分组计数（照抄 question_selection.py :23-27 的 WHERE 口径：status='active' AND category=? AND (scope='general' OR (scope='position' AND position_id=?))），对照 CATEGORY_QUOTA（from .question_selection import CATEGORY_QUOTA）不足或 required item 无题 → QUESTION_BANK_INCOMPLETE（detail 含缺口说明）；required item 覆盖判定取 competency_item 中 importance='required' 且 gate=0 的项与题库 std_name 对照；第 6/7 步写 no-op 注释占位（"# 综合题槽位：Phase 2-4 填充" / "# qualification 表单 schema：Phase 3 填充"）。conn = get_conn() per-call（全 services 惯例）；SQL 全 ? 参数化。(3) server/api/assessment.py create_session：模型查询（:69-73）之后、INSERT（:77）之前调 result = check_session_readiness(position_id)；if result: raise HTTPException(status.HTTP_409_CONFLICT, detail={"error_code": result["error_code"], "message": result["detail"]})；import 按 server.api 惯例 from ..services.readiness import check_session_readiness。(4) server/api/admin/positions.py get_todos：返回 dict 增加 "question_bank_not_ready" 键（SELECT COUNT(DISTINCT position_id) c FROM question_bank_task WHERE status != 'SUCCEEDED'），计数查询照抄同函数既有 SELECT COUNT(*) 风格。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_p0_chain.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/db.py _DDL 含 CREATE TABLE IF NOT EXISTS question_bank_task（9 列，无 CHECK）
    - server/services/readiness.py 存在，含 def check_session_readiness(，文件内三个状态名字符串 QUESTION_BANK_GENERATING / QUESTION_BANK_INCOMPLETE / MODEL_NOT_MEASURABLE 各出现（统一返回点）
    - readiness.py import CATEGORY_QUOTA（现行口径，非 config.CATEGORY_RATIO）
    - 无 task 行 + 足量题库 → None（放行）路径存在（判定逻辑含该分支）
    - create_session 在 INSERT 会话前调 check_session_readiness 且不通过 raise HTTPException(409)（grep 到 check_session_readiness( 调用）
    - get_todos 返回 dict 含 "question_bank_not_ready" 键
    - test_p0_chain.py 三态 + 存量兼容 + todos 断言转绿
  </acceptance_criteria>
  <done>开考检查全链骨架落地：三态 409 阻断 + 存量种子不误伤 + 管理员待办数据就位</done>
</task>

<task type="auto">
  <name>Task 3: task 行生命周期维护（confirm 插行 + 生成首尾更新）+ 前端 409 提示</name>
  <files>server/api/admin/models.py, server/services/question_bank.py, web/src/views/assessment/PositionAssess.vue</files>
  <read_first>
    - server/api/admin/models.py（:85-107 confirm_model——:102 commit 后 :105 add_task 前为插行点；:9 import 段与 :70 局部 import 先例）
    - server/services/question_bank.py（:70-120 generate_question_bank——:73-75 pos is None 早退分支、:120 结尾 commit）
    - server/services/pipeline.py（:14-19 new_id/now_iso）
    - web/src/views/assessment/PositionAssess.vue（:127-143 onStart catch——501 分支现状）
    - web/src/utils/sse.js（:30-42 detail 提取惯例）
    - .planning/phases/01-p0/01-PATTERNS.md（01-04 节插行点/更新锚点 + 前端节）
  </read_first>
  <action>(1) server/api/admin/models.py confirm_model：在 conn.commit()（:102）之后、from ...services.question_bank import / background.add_task（:104-105）之前，插 question_bank_task 行：conn.execute("INSERT INTO question_bank_task(task_id, position_id, model_id, model_version, status, created_at) VALUES(?,?,?,?,?,?)", (new_id("qbt"), row["position_id"], model_id, row["version"], "QUEUED", now_iso())) + conn.commit()（自身小事务）；now_iso 已在顶部 import，new_id 按文件既有先例顶部补 import 或函数内局部 import 二选一。(2) server/services/question_bank.py generate_question_bank：函数体包 try/except；开头（pos 行取得后、pos is None 早退之前或之后按 :73-75 结构）把该 (position_id, model_id) 最新 QUEUED task 行置 status='RUNNING', started_at=now_iso()——pos is None 早退分支该行置 status='FAILED', error_msg='岗位不存在', finished_at=now_iso()（勿静默漏更）；正常结尾（:120 最后一次 conn.commit() 之后）置 status='SUCCEEDED', finished_at=now_iso()；except Exception 分支置 status='FAILED', error_msg=str(e)[:200], finished_at=now_iso() 再静默退出（保持"失败不抛"总语义，但落表）。所有 task 更新 conn.commit() 收尾。签名 generate_question_bank(position_id, model_id) 不变（models.py add_task 依赖）。task 行查找 SQL 用 ? 参数化。(3) web/src/views/assessment/PositionAssess.vue onStart catch：在 501 分支后加 409 分支——if (e.response?.status === 409) { const detail = e.response?.data?.detail; ElMessage.warning(detail?.message || detail || '当前岗位暂不可开考，请联系管理员'); return }（detail 为 dict 形态 {error_code, message}，勿直接展示 detail 字符串化；Pitfall 7）；else 分支保持现状。不动 Positions.vue（只跳转不调 API）。</action>
  <verify>
    <automated>set -o pipefail && cd server && python -m pytest test_p0_chain.py -v 2>&1 | tail -8 && python -m pytest test_m5_backend.py -v 2>&1 | tail -4 && python test_m6_backend.py 2>&1 | tail -3 && python -m pytest test_p0_security.py -v 2>&1 | tail -4 && cd ../web && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - models.py confirm_model 含 INSERT INTO question_bank_task（new_id("qbt") + "QUEUED"），位于 confirmed UPDATE 的 commit 之后、add_task 之前
    - question_bank.py generate_question_bank 含 RUNNING / SUCCEEDED / FAILED 三处 UPDATE question_bank_task，函数含 try/except（异常落表后静默）
    - pos is None 早退分支 task 行置 FAILED（不静默漏更）
    - generate_question_bank 函数签名不变（grep 签名行核对）
    - PositionAssess.vue catch 含 409 分支且取 detail?.message || detail（grep 到 detail?.message）
    - test_p0_chain.py / test_m5_backend.py / test_m6_backend.py 全绿（三态 + 存量兼容 + 主链回归）
    - cd web && npx vite build 成功
  </acceptance_criteria>
  <done>题库生成任务状态真实可查（QUEUED→RUNNING→SUCCEEDED/FAILED）；候选人开考被拒时看到可读中文提示；全部回归绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API 层 create_session → readiness 预检 | 0 题会话（业务完整性攻击面）在此边界被 409 阻断 |
| admin confirm → question_bank_task | task 行由代码维护，候选人不可触达（require_admin 边界既有） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering（0 题会话完整性） | create_session 空题集静默开考 | mitigate | check_session_readiness 三态 409 + 不建会话 _q 断言（REF-3.5/8.5，成功标准 3），Task 2 |
| T-01-19 | Information Disclosure（业务状态暴露） | 409 detail 形态 | mitigate | dict detail 仅含 error_code + 中文 message，无内部阈值/堆栈 |
| T-01-20 | DoS（误伤合法开考） | readiness 对存量种子的判定 | mitigate | "无 task 行 + 题库足量 → 放行"兼容路径 + test_legacy_seed_without_task_row_passes 证明（Pitfall 3） |
| T-01-21 | Repudiation（生成失败不可见） | question_bank_task FAILED 行 | mitigate | 异常落表（status=FAILED + error_msg + finished_at），todos 聚合计入待办（D-12/D-13）；UI 可见性属 Phase 4 REF-8.4（A1） |
| T-01-22 | Tampering | task 行 status 枚举 | mitigate | 代码校验 QUEUED/RUNNING/SUCCEEDED/FAILED（N11 无 DB CHECK 的补偿） |
</threat_model>

<verification>
1. `cd server && python -m pytest test_p0_chain.py -v` — 三态 409 + 不建会话 + 存量兼容 + todos 新键 + 主链串行（01-03 部分同文件）全绿
2. `cd server && python -m pytest test_m5_backend.py -v && python test_m6_backend.py` — 全套件回归不误伤
3. `cd web && npx vite build` — 前端改动编译通过
4. 手动（可选，演示前）：uvicorn 重启加载新 DDL + npm run build 反映 router/提示改动（Runtime State Inventory 收尾项）
</verification>

<success_criteria>
- 成功标准 3 达成：题库未就绪/模型不可测量时创建 session 返回 409 + 明确 error_code 并产生管理员待办，绝不创建 0 题会话
- D-11（§10.4 骨架分层 + 现行 CATEGORY_QUOTA 口径 + 三状态名统一返回）、D-12（question_bank_task 显式状态行）、D-13（todos 扩展 + 409 error_code + 前端可读提示）落地
- 存量测试种子不被误伤（m5/m6 全绿——兼容路径是本方案最关键设计点）
- 前端提示主落点在 PositionAssess.vue（CONTEXT 集成点勘误后），无 [object Object]
</success_criteria>

<output>
Create `.planning/phases/01-p0/01-04-SUMMARY.md` when done
</output>
