---
phase: 06-migration-test-closure
plan: "06-02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - server/test_m6_backend.py
  - server/test_question_bank.py
  - server/requirements.txt
  - .github/workflows/ci.yml
autonomous: true
requirements: [REF-7.4, REQ-e2e-demo-deliverables]

must_haves:
  truths:
    - "`python -m pytest server/ -q` 一次性收集全部测试文件，无「test_* 带参被误判 fixture」类收集错误（question_bank 3 errors 消除）"
    - "test_question_bank.py 带参函数改名 `check_*`（`__main__` runner 保留）；test_m6_backend.py `_test_*` → `test_*`（`__main__` runner 保留）"
    - "server/requirements.txt 新增 pytest>=8；.github/workflows/ci.yml 存在，跑 `pytest server/` + `npm ci && npm run build`（REF-7.4 必测项 CI 化）"
    - "test_m6_backend.py 的 9.5/24.5 断言数值保持不动（D-72：mock=3 确定性，口径正确）"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions 测试闭环"
      contains: "pytest"
    - path: "server/requirements.txt"
      provides: "pytest 依赖声明"
      contains: "pytest"
  key_links:
    - from: "server/test_question_bank.py __main__ runner"
      to: "check_generation/check_idempotent/check_selection"
      via: "改名后调用点同步"
      pattern: "check_"
    - from: ".github/workflows/ci.yml"
      to: "pytest server/"
      via: "pip install -r server/requirements.txt → python -m pytest server/"
      pattern: "pytest"
---

<objective>
在 06-01 已落地 `conftest.py` mock 三件套的前提下，统一测试收集（D-69）：改造两个既有测试模块，消除 pytest 收集期的 3 个「带参 test_* 被误判 fixture」错误；并把 `server/requirements.txt` 补上 pytest、`.github/workflows/ci.yml` 落地 GitHub Actions（REF-7.4）。

Purpose: 现有脚本式测试（`python test_x.py`）无法被 pytest 统一收集；DB_PATH import 时读取冲突已由 06-01 conftest 解决；本计划只负责收集兼容改造 + CI 配置补全（D-70）。
Output: 两个测试模块收集兼容改造 + requirements.txt(pytest) + ci.yml。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-migration-test-closure/06-CONTEXT.md
@.planning/phases/06-migration-test-closure/06-RESEARCH.md
@.planning/phases/06-migration-test-closure/06-PATTERNS.md

<interfaces>
<!-- 本计划改造对象 + 复用契约（全部 file:line 已核实） -->

From server/test_question_bank.py（收集期 3 errors 源）:
- `test_generation(pid, mid)`（71 行）
- `test_idempotent(pid, mid)`（117 行）
- `test_selection(pid, mid, model)`（125 行）
- `test_prompts()`（181 行，无参——保留 test_* 名）
- `__main__` runner（229-237 行）——改名后须同步更新调用点

From server/test_m6_backend.py（收集期 `_test_*` 永不被收集）:
- `_test_dual_scoring`（148）/ `_test_aggregation`（190）/ `_test_report`（228）/ `_test_feedback_api`（269）
- `_seed_full_chain`（35-145）——保留原名
- `__main__` runner（292-300）——改名后须同步更新
- 数值断言 9.5（200 行）/ 24.5（217 行）——保持不动（D-72）

From server/requirements.txt:
- 现有 fastapi/uvicorn/openai/python-jose/passlib/bcrypt/pydantic/pypinyin/python-multipart/httpx——pytest 缺失（须补）

From server/conftest.py（06-01 已落地）:
- 已在 import 前注入 mock 三件套（temp DB + LLM_PROVIDER=mock + JWT_SECRET=test-secret）——本计划复用，不再重复建 conftest
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 消除收集期 3 errors——test_question_bank 带参函数改名 + test_m6 `_test_*` 改名（REF-7.4）</name>
  <files>server/test_question_bank.py, server/test_m6_backend.py</files>
  <read_first>
    - server/test_question_bank.py（71/117/125/181/229-237 行）
    - server/test_m6_backend.py（148/190/228/269/292-300 行）
    - .planning/phases/06-migration-test-closure/06-PATTERNS.md（test_m6_backend 转换 shape）
    - .planning/phases/06-migration-test-closure/06-VALIDATION.md（06-02 收集验证）
  </read_first>
  <action>在 server/test_question_bank.py：把带参函数 `test_generation`→`check_generation`、`test_idempotent`→`check_idempotent`、`test_selection`→`check_selection`（无参的 `test_prompts` 保留）。同步更新 `__main__` runner（229-237）里对这三者的调用点（改为 check_*）。在 server/test_m6_backend.py：把 `_test_dual_scoring`→`test_dual_scoring`、`_test_aggregation`→`test_aggregation`、`_test_report`→`test_report`、`_test_feedback_api`→`test_feedback_api`（`_seed_full_chain` 保留）。同步更新 `__main__` runner（292-300）调用点。两个文件均保留 `__main__` runner（`python test_x.py` 仍可单跑），但 pytest 收集时不执行 runner。不改任何断言数值（test_m6 的 9.5/24.5 口径是正确基准，见 D-72/§24）。</action>
  <verify>
    <automated>cd server && python -m pytest --collect-only test_question_bank.py test_m6_backend.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def check_" server/test_question_bank.py` 命中 3 个改名函数
    - `grep -n "def test_" server/test_m6_backend.py` 命中 4 个 `test_*`（非 `_test_`）
    - 上条 collect-only 命令列出 test_question_bank 的 1 个 test + test_m6 的 4 个 test，无 ERROR/WARNING fixture 误判
    - `grep -c "_test_" server/test_m6_backend.py` 仅剩 `_seed_full_chain` 等非测试名（无 `_test_` 测试函数）
  </acceptance_criteria>
  <done>pytest 收集 test_question_bank.py 与 test_m6_backend.py 无 fixture 误判；既有 9.5/24.5 断言未改</done>
</task>

<task type="auto">
  <name>Task 2: requirements.txt 补 pytest + GitHub Actions CI 配置（REF-7.4 / D-70）</name>
  <files>server/requirements.txt, .github/workflows/ci.yml</files>
  <read_first>
    - server/requirements.txt（现有 9 行依赖）
    - web/package.json（scripts build 命令名）
    - .planning/phases/06-migration-test-closure/06-RESEARCH.md（D-70 CI 规格）
  </read_first>
  <action>在 server/requirements.txt 追加一行 `pytest>=8`（用 Edit，保留现有 9 行顺序，pytest 放末尾）。新建 .github/workflows/ci.yml（GitHub Actions，ubuntu-latest）。jobs：`backend`（`actions/checkout@v4` → `actions/setup-python@v5` with python-version 3.11 → `pip install -r server/requirements.txt` → `cd server && python -m pytest . -q`）；`frontend`（`actions/checkout@v4` → `actions/setup-node@v4` with node-version 20 → `cd web && npm ci && npm run build`）。两个 job 独立（无 needs），并行。不要在 ci.yml 里写 watch 模式或 `--ignore`。</action>
  <verify>
    <automated>grep -n "pytest\|npm run build\|setup-python\|setup-node" .github/workflows/ci.yml && grep -n "pytest" server/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pytest" server/requirements.txt` 命中（`pytest>=8`）
    - `grep -n "python -m pytest" .github/workflows/ci.yml` 命中
    - `grep -n "npm run build" .github/workflows/ci.yml` 命中
    - `grep -n "setup-python" .github/workflows/ci.yml` 命中（python-version 3.11）
    - `grep -n "setup-node" .github/workflows/ci.yml` 命中（node-version 20）
  </acceptance_criteria>
  <done>requirements.txt 含 pytest；ci.yml 存在，backend pytest + frontend build 两 job 并行</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → 源码 | GitHub Actions 拉取源码执行测试；测试用临时库与 mock（06-01 conftest），不触真实数据 |
| 测试进程 → config env | 06-01 conftest 在 import 前注入 mock/test-secret/temp-DB，防测试误连 data/app.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | .github/workflows/ci.yml | mitigate | `pip install -r`/`npm ci` 走 lockfile/requirements 锁定；pytest 官方框架，无 [SLOP] 风险 |
| T-06-06 | Info Disclosure | .github/workflows/ci.yml | mitigate | CI 不注入真实 secret；测试走 mock 三件套（06-01 conftest） |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | ci.yml `pip install`/`npm ci` 走 lockfile/requirements 锁定；pytest 为官方测试框架，无 [SLOP] 风险 |
</threat_model>

<verification>
全量收集：`cd server && python -m pytest . --collect-only -q 2>&1 | tail -30`（应无 fixture 收集 error）。全量执行：`cd server && python -m pytest . -q 2>&1 | tail -30`（06-03/06-04/06-05 新测试未落前，仅需既有模块全绿或收集干净——已知 test_m6/test_question_bank 全绿；test_m5/m7 等靠 06-01 conftest session 库仍绿）。CI 干跑不可在本地执行，以 yml 静态 grep + 前端本地 `npm run build` 等价验证。
</verification>

<success_criteria>
requirements.txt(pytest) + ci.yml 落地；test_question_bank 3 errors 与 test_m6 `_test_*` 未收集问题消除；`pytest server/` 收集干净、既有断言（9.5/24.5）不变；REF-7.4 测试统一 + CI 配置兑现。
</success_criteria>

<output>
Create `.planning/phases/06-migration-test-closure/06-02-SUMMARY.md` when done
</output>
