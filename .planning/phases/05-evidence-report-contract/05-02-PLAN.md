---
phase: 05-evidence-report-contract
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - server/services/aggregation.py
  - server/services/report.py
  - server/test_phase5_report.py
autonomous: true
requirements: [REF-5.4, REF-5.5, REF-5.6]

must_haves:
  truths:
    - "item 最终等级由内存统一测量记录 item_measurement + adjudicate 裁决（不再按题数均分）"
    - "重大冲突取较低值并留 human_review 人工复核标记（阈值常量一处定义）"
    - "缺失普通 item 按 r = Σ w_i·s_i / Σ w_i（s_i=(score−1)/4）补算并标 IMPUTED + 展示观察覆盖率"
    - "O=∅ → 不能补算 → NO_VALID_OBSERVATION + HUMAN_REVIEW_REQUIRED"
    - "required 缺失 → report 语义 PROVISIONAL + HUMAN_REVIEW_REQUIRED（不触发补测）"
  artifacts:
    - path: "server/services/aggregation.py"
      provides: "item_measurement 内存记录 + adjudicate + _impute_r + _observed_items（question→item 归约）+ _normalize_score + required 缺失判定"
      contains: "adjudicate"
    - path: "server/services/report.py"
      provides: "消费裁决结果与 PROVISIONAL/review_status 语义（状态落库在 05-03，本计划只透传 agg 字段）"
      contains: "review_status"
  key_links:
    - from: "server/services/aggregation.py"
      to: "_load_model_items"
      via: "补 importance 列（required 缺失判定依赖）"
      pattern: "ci.importance"
    - from: "server/services/aggregation.py adjudicate"
      to: "item_scores"
      via: "替换 sum(finals)/len(finals)"
      pattern: "adjudicate\\("
    - from: "server/services/aggregation.py _impute_r"
      to: "_observed_items"
      via: "question-level item_measurement 归约为 per-item {item_id, weight, score}"
      pattern: "_observed_items\\(measurements"
---

<objective>
把模块三聚合从「按题数均分 actual = sum(finals)/len(finals)」升级为「item_measurement 统一裁决」：普通题转统一测量记录后由 adjudicate 裁决 item_final_level（不按来源加权、不按题数重复乘 item.weight、重大冲突取低留人工标记）；缺失普通 item 走 r 比例补算 IMPUTED（特殊标记 + 覆盖率展示）；required 缺失 → PROVISIONAL + HUMAN_REVIEW_REQUIRED；O=∅ → NO_VALID_OBSERVATION + HUMAN_REVIEW_REQUIRED。

Purpose: REF-5.4 明令废弃按题数均分；D-014/D-015 裁决与补算语义；D-003 LLM 不碰数字（r 比例全代码算）。
Output: aggregation.py 内 item_measurement/adjudicate/_impute_r/_observed_items/_normalize_score 纯函数 + required 缺失判定；report.py 消费 agg 的 PROVISIONAL/review_status/coverage 字段（状态落库属 05-03，本计划只透传）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-evidence-report-contract/05-CONTEXT.md
@.planning/phases/05-evidence-report-contract/05-RESEARCH.md
@.planning/phases/05-evidence-report-contract/05-PATTERNS.md
@.planning/phases/05-evidence-report-contract/05-VALIDATION.md

<interfaces>
<!-- 本计划使用的既有契约 -->

From server/services/aggregation.py:
- `_load_model_items(session_id) -> dict[item_id -> {std_name, category, required_level, weight, gate, years}]`（aggregation.py:18-29；当前 SELECT 缺 importance 列——本计划补 ci.importance）
- `aggregate_session_scores(session_id) -> dict`（aggregation.py:90-216；返回 total_score/item_scores/gate_items/refusals/missing_warnings/strengths/weaknesses）
- 三路分流 + `_EXCLUDED_STATES = ("INVALIDATED","INCOMPLETE","INSUFFICIENT_EVIDENCE","NOT_ADMINISTERED")`（aggregation.py:110）
- 废弃对象 `actual = sum(finals) / len(finals)`（aggregation.py:173）
- 门槛分支 `_gate_row`/`_gate_check`（aggregation.py:48-87）不动

From server/services/report.py:
- `generate_report(session_id) -> dict`（report.py:89-153；agg = aggregate_session_scores(session_id) 后直接消费 agg["item_scores"]/agg["total_score"]；本计划只让它透传 agg 新增字段，不做状态机落库）

From server/test_m5_backend.py（三件套照抄形态）:
- 头部 11-29 行 + `_q` 只读助手（32-38 行）；`_seed_position_with_confirmed_model`（43-73）造 confirmed 模型 + competency_item

From 05-01 产出（本计划依赖）:
- server/services/trace_link.py link_entity（可选用，本计划不强制消费）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 测试脚手架（test_phase5_report.py 三件套 + adjudicate/IMPUTED/required 断言）</name>
  <files>server/test_phase5_report.py</files>
  <read_first>
    - server/test_m5_backend.py（1-38 行三件套 + _q 助手；43-73 行 _seed_position_with_confirmed_model 造模型）
    - .planning/phases/05-evidence-report-contract/05-VALIDATION.md（REF-5.4/5.5/5.6 测试名 + Edge Cases：IMPUTED r 浮点 < 1e-6、weight 全 0 不除零、单观察 r=自身归一化）
    - server/services/aggregation.py（90-216 行 aggregate_session_scores 现有结构——测试直调基准）
  </read_first>
  <behavior>
    - test_adjudicate_conflict_lower：measurements 两个 observed_level 一个 2 一个 5（差 3 ≥ 阈值）→ adjudicate 返回 (2.0, True)（取低 + human_review）；一致场景 [4,4] → (4.0, False)；空列表 → (None, False)
    - test_impute_r_math：observed=[{weight:0.5, score:5},{weight:0.5, score:1}] → r = (0.5*1.0 + 0.5*0.0)/1.0 = 0.5（s_i=(score−1)/4），断言 abs(r-0.5)<1e-6；weight 全 0 → den=0 返回 None；单观察 [{weight:0.3, score:3}] → r=((3-1)/4)=0.5
    - test_impute_no_valid_observation：observed=[] → _impute_r 返回 None → 聚合层标 observation_status="NO_VALID_OBSERVATION" + review_status="HUMAN_REVIEW_REQUIRED"
    - test_required_missing_provisional：造含 required item 但该 item 无 SCORED 行的 session → 聚合结果该 item 标 provisional，聚合顶层 review_status="HUMAN_REVIEW_REQUIRED" + provisional 标记（未落库状态机的本计划仅断言 agg 层字段）
  </behavior>
  <action>新建 server/test_phase5_report.py：头部照抄 test_m5_backend.py 1-29 行三件套（tempfile DB_PATH + LLM_PROVIDER=mock + JWT_SECRET + init_db + TestClient）。定义 _q 只读助手。测试 import：from server.services.aggregation import adjudicate, _impute_r, aggregate_session_scores, _normalize_score。按 <behavior> 四条写断言；adjudicate/_impute_r 为纯函数直调（unit，入参 per-item {weight, score}），required 缺失/O=∅ 用 _seed 直插 competency_item + assessment_session + question_score（无 SCORED 行）后调 aggregate_session_scores 断言 agg 层字段。逐文件命令 `cd server && python -m pytest test_phase5_report.py -v`（禁止同进程多文件）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py -v 2>&1 | grep -E "ImportError|ModuleNotFoundError|failed" ; echo "RED 预期：adjudicate/_impute_r/_normalize_score 不存在导致收集失败"</automated>
  </verify>
  <acceptance_criteria>
    - test_phase5_report.py 存在且三件套头部 grep 可确认
    - 4 个 test_* 函数名与 05-VALIDATION.md 一致：test_adjudicate_conflict_lower / test_impute_r_math / test_impute_no_valid_observation / test_required_missing_provisional
    - pytest 因 adjudicate/_impute_r 不存在而 RED（非语法错误）
  </acceptance_criteria>
  <done>test_phase5_report.py 落盘，4 条断言骨架可被 pytest 收集目标识别，当前 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: item_measurement + adjudicate + _normalize_score 纯函数（REF-5.4 + 归一化张力隔离）</name>
  <files>server/services/aggregation.py</files>
  <read_first>
    - server/services/aggregation.py（90-216 行 aggregate_session_scores；110 行 _EXCLUDED_STATES；173 行废弃对象 sum/len）
    - design/final-design/总设计文档.md（511-556 行 §19/§20.1/§20.3：adjudicate 裁决规则 / IMPUTED 公式 / 总分公式）
    - .planning/phases/05-evidence-report-contract/05-RESEARCH.md（Pattern 3/4 + Assumptions A4/A7：冲突阈值差 ≥2；归一化公式张力）
  </read_first>
  <behavior>
    - adjudicate 冲突：max(levels) − min(levels) >= 2 → (min(levels), True)；否则 round(mean, 2) + (…, False)；levels 空 → (None, False)
    - _normalize_score：source="imputed" 分支 s_i=(score−1)/4；source="observed" 分支返回 (score−1)/4（关口 A 已裁决：统一 (score−1)/4，作废 score/5 两尺度混用）
  </behavior>
  <action>在 server/services/aggregation.py 顶部加常量与纯函数（模块级，不含 DB 调用）：(1) ADJUDICATE_CONFLICT_THRESHOLD = 2（冲突量化阈值一处定义，注释「§19 重大冲突取低，实施期可调」）；(2) NORMALIZE_IMPUTED = "imputed" / NORMALIZE_OBSERVED = "observed" 两个 source 标签常量；(3) _normalize_score(score, source) -> float：source==NORMALIZE_IMPUTED 返回 (score - 1) / 4.0；source==NORMALIZE_OBSERVED 返回 (score - 1) / 4.0（关口 A 已裁决：统一 (score−1)/4）；(5) IMPUTE_RATIO_THRESHOLD = 0.2（§31-3 补算复核阈值，关口 A 已裁决）；(4) adjudicate(measurements: list[dict]) -> tuple[float | None, bool]：收集 observed_level 非 None 值列表 levels；空 → (None, False)；max−min >= ADJUDICATE_CONFLICT_THRESHOLD → (float(min(levels)), True)；否则 (round(sum(levels)/len(levels), 2), False)。measurements 元素 dict 键用 observed_level（item_measurement 内存记录字段命名）。不改 aggregate_session_scores 主循环（Task 3 才接）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py::test_adjudicate_conflict_lower test_phase5_report.py::test_impute_r_math -v</automated>
  </verify>
  <acceptance_criteria>
    - aggregation.py 存在 ADJUDICATE_CONFLICT_THRESHOLD / NORMALIZE_IMPUTED / NORMALIZE_OBSERVED / _normalize_score / adjudicate（grep 确认）
    - test_adjudicate_conflict_lower 绿（冲突取低 + human_review，一致场景 mean）
    - _normalize_score 的 imputed 分支 = (score-1)/4；observed 分支 = (score-1)/4（统一归一化，grep -n "score / 5\|/ 5.0" server/services/aggregation.py 无命中）
  </acceptance_criteria>
  <done>adjudicate/_normalize_score 纯函数可独立测试，归一化张力被隔离进单一函数 + 占位常量</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _impute_r + _observed_items 归约 + aggregate 主循环接入（IMPUTED 补算 + required 缺失 PROVISIONAL + O=∅）（REF-5.5/REF-5.6）</name>
  <files>server/services/aggregation.py, server/services/report.py</files>
  <read_first>
    - server/services/aggregation.py（90-216 行主循环；110-128 行三路分流；160-186 行 finals 为空分支与 actual=sum/len 替换点）
    - server/services/report.py（100-142 行 agg 消费点：report_data 组装）
    - design/final-design/总设计文档.md（524-556 行 §20.1/20.2/20.3）
  </read_first>
  <behavior>
    - 缺失普通 item（非 required/qualification、无 SCORED 行）→ 先归约 per-item observed 再 _impute_r，标 IMPUTED + no_data=False + imputed=True；observed 为空（O=∅）→ 不补算，标 NO_VALID_OBSERVATION
    - required 缺失（importance=="required" 且无 SCORED 且补算不适用）→ item 标 provisional，聚合顶层 review_status="HUMAN_REVIEW_REQUIRED"
    - IMPUTED 参与 total_score 与 radar（display level = r*4+1 映射回 1-5 级）；覆盖率字段覆盖 observed_count/imputed_count/total_measureable/coverage_ratio/missing_reasons
  </behavior>
  <action>在 aggregation.py 加纯函数 _impute_r(observed: list[dict]) -> float | None：空返回 None；num = Σ o["weight"]*_normalize_score(o["score"], NORMALIZE_IMPUTED)；den = Σ o["weight"]；den==0 返回 None；返回 num/den（入参 observed 为 per-item {item_id, weight, score} 列表，非 question-level）。新增纯函数 _observed_items(measurements: list[dict], model_items: dict) -> list[dict]：把 question-level item_measurement（键 observed_level，无 weight）按 item_id 分组，逐 item 调 adjudicate(该 item 的 measurements 列表) 得 item_final_level，item_final_level 非 None 才加入返回列表 {item_id, weight: (model_items[item_id].get("weight") or 0.0), score: item_final_level}；返回仅含已裁决 item 的 per-item observed 列表（weight 来源 = model_items[item_id].weight，即 D-59 r 公式 w_i）。改 _load_model_items SELECT 补 ci.importance（返回 dict 键 importance）。改 aggregate_session_scores：把三路分流收成 item_measurement 内存记录（question_id/item_id/observed_level/evidence_refs 空列表占位/measurement_source="ordinary"）——SCORED 行 observed_level=r["score_final"]；替换 finals 分支：有 measurement 的 item 调 adjudicate(measurements) 得 (item_final_level, human_review)，actual_level=item_final_level，score=weight*_normalize_score(item_final_level, NORMALIZE_OBSERVED)*100（关口 A 已裁决：统一 (score−1)/4，作废 (level/5)*100 旧口径）；无 measurement 的 item：importance in ("required","qualification") → required 缺失标 provisional + 顶层 review_status="HUMAN_REVIEW_REQUIRED"（qualification 不补算但亦不标 PROVISIONAL，仅记 missing_warnings）；普通 preferred/plus 缺失 → 先把第 1 步收成的全量 item_measurement 记录归约为 per-item observed（`observed_items = _observed_items(measurements, model_items)`，measurements=全量 item_measurement 列表、model_items=_load_model_items 返回），再 `r = _impute_r(observed_items)` 补算，r 非 None → actual_level=round(r*4+1, 2)、imputed=True、no_data=False、human_review = (coverage_ratio > IMPUTE_RATIO_THRESHOLD)（IMPUTE_RATIO_THRESHOLD=0.2，关口 A 已裁决）；r None → no_data=True + observation_status="NO_VALID_OBSERVATION" + review_status="HUMAN_REVIEW_REQUIRED"。聚合返回 dict 新增键：coverage（{observed_count, imputed_count, total_measureable, coverage_ratio, missing_reasons}）、review_status、observation_status、provisional（bool）。report.py generate_report 的 report_data 透传 agg 新增字段（review_status/observation_status/provisional/coverage 挂进 report_data——状态机落库属 05-03，本计划只透传不写 report_status 列）。</action>
  <verify>
    <automated>cd server && python -m pytest test_phase5_report.py -v</automated>
  </verify>
  <acceptance_criteria>
    - _load_model_items 返回含 importance 键；_impute_r 存在且 r=(score−1)/4 加权均值（test_impute_r_math 绿）
    - _observed_items 存在且把 question-level item_measurement 归约为 per-item {item_id, weight, score}（weight=model_items[item_id].weight、score=adjudicate 后 item_final_level；grep "_observed_items" 于 aggregation.py）
    - aggregate_session_scores 不再含 `sum(finals) / len(finals)`（grep 确认废弃对象已移除），改走 adjudicate
    - 缺失 required → 聚合顶层 review_status="HUMAN_REVIEW_REQUIRED" + provisional（test_required_missing_provisional 绿）
    - O=∅ → observation_status="NO_VALID_OBSERVATION" + review_status="HUMAN_REVIEW_REQUIRED"（test_impute_no_valid_observation 绿）
    - report.py report_data 透传 coverage/review_status/observation_status/provisional 字段
    - test_phase5_report.py 4 条全绿
  </acceptance_criteria>
  <done>item_measurement 统一裁决 + _observed_items question→item 归约 + IMPUTED 补算 + required 缺失 PROVISIONAL + O=∅ NO_VALID_OBSERVATION 全接入，test_phase5_report.py 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→服务端 | 聚合裁决与补算全在服务端代码，不信任任何外部数字 |
| 服务层→DB | IMPUTED/裁决结果透传 report_data，状态语义由 05-03 状态机落库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | server/services/aggregation.py _impute_r | mitigate | r 比例全代码算（D-003），den==0 不除零返回 None，浮点断言 < 1e-6 |
| T-05-05 | Tampering | server/services/aggregation.py adjudicate | mitigate | 冲突阈值常量一处定义，取低留人工标记（D-014） |
| T-05-06 | Tampering | 补算阈值未配置 | mitigate | §31-3 config 占位 + 覆盖率展示 + 超阈值 PROVISIONAL（见 Open Items） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
逐文件跑：`cd server && python -m pytest test_phase5_report.py -v` 全绿。既有 test_m6_backend.py 总分数值断言（31.4）在 05-03 版本化/状态机后统一改写——本计划不改 test_m6。
</verification>

<success_criteria>
item 最终等级由 item_measurement 统一裁决（adjudicate 替换按题数均分，冲突取低留人工标记）；缺失普通 item 先经 _observed_items 归约 per-item 再 IMPUTED r 比例补算 + 覆盖率展示；O=∅ → NO_VALID_OBSERVATION；required 缺失 → PROVISIONAL + HUMAN_REVIEW_REQUIRED；归一化张力隔离进 _normalize_score。test_phase5_report.py 4 条全绿。
</success_criteria>

## Open Items for Hard Gate A

本计划触及两处 MUST-NOT-DECIDE 开放项，全部以单一命名函数/常量 + `[OPEN — 关口包呈报]` 注释实现，不静默代决：

1. **总分归一化公式（SSOT §20.1 vs §20.3）— 已裁决：统一 `(score−1)/4`**（关口 A 用户裁决「选择 A」）。§20.1 锁 IMPUTED `s_i=(score−1)/4`（range [0,1]）；§20.3「normalized_item_score」原无公式；现行 aggregation 观测项用 `score/5`。已统一：`_normalize_score(score, source)` 两分支均返回 `(score−1)/4`，主循环观测项 `weight*_normalize_score(item_final_level, NORMALIZE_OBSERVED)*100` 取代 `(level/5)*100`。IMPUTED 参与总分/雷达的 display level = `r*4+1` 映射回 1–5。此裁决附带 SSOT §20.3 补公式（`normalized_item_score = (actual_level−1)/4`）——SSOT 变更另走 §14 原子 commit。

2. **补算复核阈值（SSOT §31-3）— 已裁决：`0.2`**（关口 A 用户裁决）。IMPUTED 比例 `coverage_ratio > 0.2` → 临时报告 + 人工复核。本计划落 `IMPUTE_RATIO_THRESHOLD = 0.2` 常量一处定义 + 覆盖率字段 `coverage.coverage_ratio` 展示。

<output>
Create `.planning/phases/05-evidence-report-contract/05-02-SUMMARY.md` when done
</output>
