# 临时讨论稿·第十轮 — 开放参数与未决事务收敛清单

> 状态：**裁决已定稿（2026-09-06，非权威）**。本文件是收敛闭合的勾选工作底稿，不作为实施依据，也不构成任何修改授权。
> 生成：2026-09-06，全仓库四路扫描（设计文档 / .planning / server / web）+ 定向核实归并。
> 权威：一切以 `design/final-design/总设计文档.md`（SSOT）为准。
> 已核实基线：`cd server && python -m pytest -q` → **229 passed / 4 warnings（260s）**，后端全绿。
> 分支：`feature/m5-assessment`（355 ahead / 0 behind main）**未并入主干**；工作树另有 11 改 + 1 未跟踪（`server/test_admin_pagination.py`）未提交。
>
> **2026-09-06 裁决记录**：§1.1 八占位、§1.2/§1.3 诸项、§2 #16 方案一、§3 #22 回写授权等已由用户拍板，详见各表「裁决（2026-09-06）」列。SSOT 修订稿在 `design/临时讨论稿-第十轮-SSOT修订稿.md`（待用户核查后授权写入）。

图例：`[裁决]`=需用户拍板取值/做不做；`[修复]`=代码缺口需修；`[回写]`=文档/记录滞后需同步；`[确认不做]`=刻意的范围裁切，闭合时显式勾销即可。

---

## 1. 开放参数裁决（config/模块级占位 None/[] —— 需逐项取值或明示"演示期不启用"）

> 处理选项 A：裁决取值 → 写入并接线；选项 B：明示演示期不启用 → 从 config 移除 + 注释改为 by-design。

### 1.1 config.py 占位（8 项）
| # | 常量 | 位置 | 消费现状 | SSOT 出处 | 裁决（2026-09-06） |
|---|---|---|---|---|---|
| 1 | `BAD_CASE_DIVERGENCE_THRESHOLD` | config.py:65 | None 时 `_detect_bad_case_divergence` 整段跳过 | §23「配置阈值」/REF-5.11 | **A：取 2 并接线**（1–5 整数刻度下 \|diff\|≥2 才是真信号；消费链已齐、永不自动改分，取值风险低） |
| 2 | `MAX_JD_LENGTH` | config.py:71 | 消费方 `validate_jd_length`，None 放行 | §25 / REF-6.3 | **A：取 10000 字符**（真实 JD 鲜超 5000，宽裕上限；主要意义是接入 deepseek 后控单请求输入成本） |
| 3 | `MAX_JD_FILE_LINES` | config.py:72 | **零消费**（import_file 无限行逻辑） | §25 / REF-6.3 | **A：取 500 并接线**（import_file 约 5 行改动；防误传大文件的无上限后台成本） |
| 4 | `MAX_PAGINATION_LIMIT` | config.py:73 | clamp no-op（None 原样放行），默认 page_size=20 不受钳制 | §25（分页上限） | **A：取 100**（钳制器已全面接线，标准值；消除 page_size 任意大全表吐出） |
| 5 | `TRACE_RETENTION_DAYS` | config.py:75 | **零消费**（无清理任务） | §31-5 | **B：演示期不启用，摘除**（单人演示无合规留存要求；trace 全量保留利于验收审计。§31-5 注记闭合） |
| 6 | `TRACE_DESENSITIZE` | config.py:76 | **零消费**（admin trace 查看未脱敏） | §31-5 | **B：演示期不启用，摘除**（#40 真实 LLM 观察层验证需管理员直读全量 trace，脱敏为负收益） |
| 7 | `IDEMPOTENCY_CLEANUP_THRESHOLD` | config.py:78 | **零消费**（无清理任务/接口） | §31-6 | **B：演示期不启用，摘除**（demo 量级永远到不了阈值；清理需净新增逻辑。§31-6 注记闭合） |
| 8 | `TITLE_CLEAN_WORDS=[]` | config.py:85 | **零消费**（清洗走各自 NOISE_HEADERS/_SUFFIXES 硬编码表） | §31-4 | **B：摘除**（与 NOISE_HEADERS/_SUFFIXES 职责重复，空词表是第二权威源。§31-4 词表部分一并闭合） |

### 1.2 裁决/注释与 SSOT 需收敛（2 项）
| # | 事项 | 位置 | 现状 | 裁决（2026-09-06） |
|---|---|---|---|---|
| 9 | `IMPUTE_RATIO_THRESHOLD=0.2` | aggregation.py:28 | 注释自称"关口 A 已裁决 0.2"，但 SSOT §31-3/§20.1 仍列「补算人工复核阈值实施期定」 | **确认 0.2 转正**（.planning 05-DECISIONS:37 记录用户 2026-09-05 关口 A 已裁决——非新裁决，纯滞后回写）→ 回写 SSOT §31-3 关闭 + §20.1 + §14 |
| 10 | `ADJUDICATE_CONFLICT_THRESHOLD=2` | aggregation.py:23 | §19「重大冲突取低」的极差阈值，模块级常量，未进 config | **转正进 SSOT §19 登记数值 2，并挪入 config.py**（极差 ≥2 级取低是保守正确值；消除全项目唯一"影响裁决行为的阈值不在 config"的孤儿） |

### 1.3 散落硬编码值（未列 §31，收敛到 config 一处即可）
| # | 事项 | 位置 | 现状 | 裁决（2026-09-06） |
|---|---|---|---|---|
| 11 | 客观题命中 5 / 未中 1 | scoring.py:68 | 与 `(score−1)/4` 归一化口径需一致 | **[确认] 既定规则不动**；建议常量集中（scoring.py 内常量化即可） |
| 12 | `_MAX_KEY_LEN=512` / `MAX_ANSWER_LEN=64*1024` | scoring.py:38-39 | WR-07 已公开设定 | **[确认]** 不动 |
| 13 | `evidence_quote = answer_text[:60]` | scoring.py:192 | 引文截断无出处 | **转正为固定展示规则，不进 config**（展示截断约定非行为参数；scoring.py 内常量化 `_OBJECTIVE_QUOTE_LEN=60` + SSOT 登记口径） |
| 14 | 三档难度权重阈值（weight>0.10…） | question_bank.py:24-28 | 07 §6.2 值，未进 SSOT §31 | **确认非开放参数**——题库生成结构规则，生成后随题库存量固化。SSOT 登记出处标注即可，不进 §31 |
| 15 | CORS `allow_origins=["http://localhost:5173"]` | main.py:52（勘误：清单原写 api/main.py 有误，实际在 server/main.py） | 硬编码无 env | **env 化**（2 行：`CORS_ORIGINS` env，默认不变；credentials 模式下不能开 \*，换机/局域网演示即断） |

### 1.4 已裁决（**不用再定**，仅供对照）
历史已决：`ORDINARY_PLAN_N=10`[02-007]、`MAX_CONTEXT_TOKENS=8000`[03-007]、`REFINE_MIN_TOKENS=500`、`FOLLOWUP_MAX=2`、`SESSION 40/20/6`（SSOT §15 注明非开放参数）、`DICT_MATCH_THRESHOLD=0.5`（2026-09-06 在途 diff 已裁决并接线）。
本轮已决（2026-09-06）：#1 `BAD_CASE_DIVERGENCE_THRESHOLD=2`、#2 `MAX_JD_LENGTH=10000`、#3 `MAX_JD_FILE_LINES=500`（需接线）、#4 `MAX_PAGINATION_LIMIT=100`、#9 `IMPUTE_RATIO_THRESHOLD=0.2` 转正、#10 `ADJUDICATE_CONFLICT_THRESHOLD=2` 转正进 config、#13 引文截断 60 固定规则、#14 非开放参数仅登记；#5–#8 明示演示期不启用、摘除（§31-5/§31-6/§31-4 词表部分随之闭合）。

---

## 2. 确证的功能缺口（真阻断，需代码修复）

| # | 事项 | 证据 | 裁决（2026-09-06） |
|---|---|---|---|
| 16 | **前端 `start` 接线缺失 [03-010]** | 服务端唯一 `PENDING_START→ACTIVE` = `POST /sessions/{id}/start`（assessment.py:246）；`get_session` 仅 `phase in (None,'ACTIVE')` 派发首题（:157）；`web/src/api/index.js` 无 start 封装、`web/src` 全库无 `/start`；Chat.vue 只 get_session → PENDING_START 下 `current_question=null`、`canAnswer=false`（"当前没有可回答的题目"），40min 计时亦不起算。Phase 6 E2E 仅 API 层（TestClient 直调 /start）。 | **✅ 方案一：Chat.vue 入场确认按钮**（api/index.js 加 `startSession` 约 3 行；Chat.vue 在 `phase==='PENDING_START'` 渲染居中"开始测评"按钮替代输入区，点击 → startSession → 重新 load()；409 SESSION_ALREADY_ACTIVE 按幂等处理直接 load）。方案二（create 后自动 start）否决——架空 PENDING_START 的 §15 计时起算语义（测量从考生显式确认起算，SC-5）；方案三（get_session 隐式激活）否决——写操作藏进只读 GET 且破坏 §13.1 同事务契约。顺带删 PositionAssess.vue:139 的 501 死文案分支 |
| 17 | readiness §10.4 第 6/7 项仍 no-op | readiness.py:51,58-59,178-179 注释写"Phase 2-4 / Phase 3 填充"，但表单链已实现——分支可能漏检 | **纯注释澄清，不动逻辑**。第 7 项（qualification 表单 schema）：表单链为模型 items 数据驱动生成 schema（form_instance.schema_snapshot，D-30 池耗尽渲染），无"schema 缺失"失败模式，已被第 2 项（items 非空）隐式覆盖 → 注释改「已由 items 检查覆盖，恒过（by-design）」；第 6 项（综合题槽位）：综合题不排期（§4.1 #33）→ 注释改「综合题本期不排期，恒过（by-design）」。SSOT §10.4 注记同步 |
| 18 | `_stable_evidence` 完整裁决未实现 | assessment.py:802 docstring 自认"Phase 5 完整裁决留白"，现为 sufficient_in_row≥2 轻量版（§11.3） | **轻量口径转正 by-design**。(a) 唯一消费者是难度状态机升档，只可能"不升档"（保守向），错向不碰分数/报告/聚合；(b) §11.3 完整判据（target 覆盖/锚点一致性/矛盾不平均）依赖 P-interviewer 结构化输出重构（#31 Prompt 块），现在实现完整版会被 Prompt 重构翻掉；(c) 证据链正式归宿在终局评分侧（evidence_spans/quote_hash 已实现，D-003）。完整裁决登记 backlog 不排期；SSOT §11.3 加轻量口径注记 + §14 |
| 19 | `ObservationDims.required_points_covered / source_span_available` 无人消费 | schemas.py:83-84，classify_observation 明确不消费 | **确认预留 by-design，不接线**（schemas.py:71 与 interview.py:169 注释已如实标注"Phase 5 证据链强化维度，保留不消费"；删除破坏已落 trace 的 observation JSON 兼容性；接线自然钩子是 P-interviewer 重构随 #31 排期）。勾销 |
| 20 | scoring 枚举位 `INSUFFICIENT_EVIDENCE/NOT_ADMINISTERED/INCOMPLETE` 未产出 | scoring.py:11-12,25-32 标"供 Phase 5 生产"，Phase 5 后仍未产出 | **明确本期不产出（by-design）**。现终态覆盖：拒答→REFUSED、题库无效→INVALIDATED、未答/缺失→聚合层 IMPUTED/PROVISIONAL/NO_VALID_OBSERVATION + missing_warnings（§20.1/§20.2 语义等价落地）；NOT_ADMINISTERED 的潜在场景（派发未答实例进 finalize）现走 §20.1 缺失路径，区分它需动聚合层分母规则——不值得。保留校验位（D-28 口径集）注释准确；SSOT §18/§12.4 加注记闭合 |

---

## 3. 文档/记录滞后（统一收口，多数代码已修）

| # | 位置 | 过期内容 | 现况 / 动作 |
|---|---|---|---|
| 21 | `design/final-design/模块三设计-立体人才画像.md` §8 | 优势/短板 gap 符号与排序键仍旧口径（优势=gap≥0、短板=gap<0、`|gap|×weight`） | 与 SSOT §21 [06-013] 反转**直接冲突**，`[回写]` 同步 + 注依据（随 SSOT 修订一并做，分模块稿仅摘录） |
| 22 | SSOT `总设计文档.md` §6 | 写"共 21 张表" | 代码实际 26 表（含 schema_version）/25（不含），用户已裁决改 §6 + §14：**授权已给**，取口径"25 业务表 + schema_version 注册表 = 26 物理"。`[回写]` |
| 23 | `06-VERIFICATION.md` 头部 | gaps_found 7/8、SC5-c ✗ partial、Re-verification: No | 代码已闭合（isolated 6/6 绿），`[回写]` 补 re-verify |
| 24 | `.planning/STATE.md:30` | "全量回归 3 failed / 220 passed" | 现已 229 passed，`[回写]` |
| 25 | STATE Deferred 表 | [04-011] 13 文件 / [04-016] WR-01~06 仍列"待处置" | 已修（1002d2f / 8fa15e4），`[回写]` 勾销 |
| 26 | `06-migration-test-closure/deferred-items.md` | 5 条"既有失败待设计裁决"标 defer | 均已修（f827ceb / fbc4aac / 1002d2f），`[回写]` 逐条标 CLOSED |
| 27 | `03-sse/deferred-items.md` | 2 条 | 已由 06-01 conftest + [06-011] 修，`[回写]` |
| 28 | `01-VERIFICATION.md` Anti-Patterns | 仍列已修问题 | 与 01-REVIEW-FIX(20/20) 矛盾，`[回写]` 以 REVIEW-FIX/代码为准 |
| 29 | `design/技术方案概述.md` §3.3 | LLM 打 0–10 分并参与权重 | 上游文档 vs SSOT（1–5、LLM 不碰数字）冲突；实施按 SSOT，`[确认]` 是否在文档加注 |
| 30 | `06-VALIDATION.md` frontmatter | wave_0_complete:false / sign-off pending | pre-execution 占位未回写，`[回写]` |

---

## 4. 刻意延后 / 登记不排期（显式"确认不做/排期"或保持登记）

### 4.1 需显式确认的延后
| # | 事项 | 出处/决策 | 裁决（2026-09-06） |
|---|---|---|---|
| 31 | Prompt 全量内容：P-question-gen(综合)/P-report/P-form-facts/P-integrated-score/P-virtual-candidate 待写；P-interviewer/P-refine 待重构 | SSOT §26 + 附录 A | **维持**：按约定用户拟初稿后展开（无裁决内容） |
| 32 | 等值备用题组 | REF-3.8 | **维持登记不排期** |
| 33 | 综合题槽位（生成 Prompt） | REF-3.9 / D-030 | **维持登记不排期**（前置 = #31；readiness 第 6 项随之 by-design 恒过） |
| 34 | Tools 白名单 | REF-4.11 / D-030 | **维持登记不排期**（随 Prompt 模块） |
| 35 | JWT HttpOnly cookie 迁移 | D-76 [06-009] | **维持锁方向、本期零改**（防线仍在：启动期 fail-closed + admin 权限隔离） |
| 36 | 随机 secret 生成持久化 / Token 撤销轮换 / 登录限速 | §25 / 06-DISCUSSION | **维持不排期** |
| 37 | 断线续传 cursor / 事件回放 | D-021 | **维持本期不要求** |
| 38 | ABANDONED 恢复 | D-022 | **维持本期不可恢复，勾销** |
| 39 | 进程重启任务恢复 / 持久化 job 表 | D-005 / 06-DISCUSSION | **维持接受内存执行，勾销**（SSOT §5 已有表述） |
| 40 | 真实 LLM（deepseek）观察层/注入质量验证 | D-027 | **验收项非开发项**：mock 全绿 ≠ 提示词质量达标。验收清单单列，届时需用户供 key + 放行出网 |

### 4.2 by-design 不做（勾销即可）
黄金集 / 浏览器插件 / 真实 JD 数据集（M4、D-029）；公平性离线评估（D-031，仅留记录）。

### 4.3 polish / 质量债 backlog（不阻断）
报告版本历史浏览/对比 UI、admin 完整复核 UI、Report.vue 轮询 2min 上限、11 处 body:dict 端点 Pydantic 化、chain 层④排序键退化（[02-VERIFICATION]）、题目质量真实指标、bad case 聚类摘要/复核辅助摘要（可选）。

---

## 5. 前端文案 / 重复枚举债（低优先）
- 死文案：`PositionAssess.vue:139`「测评功能尚未上线（模块二）」（501 分支，模块已实现）；`TestCenter.vue` P8/M7 标签、assessment 页脚 CF 标签。
- 6+ Vue 文件重复硬编码枚举→中文映射且已漂移（模型状态 stalled 三套：`stalled 模型`/`裁决滞留`/`滞留`；category/importance/source_type/role/callTypes 等），建议集中到共享常量或后端 dict 端点。
- `ItemTable.vue:10` `Lv{{ row.required_level }}` 无 null 兜底；`PositionAssess.vue` 404 空态文案缺失。

---

## 6. 收口动作清单（2026-09-06 裁决后更新，建议执行序）

1. ~~本清单过目 → 逐条裁决~~ **✅ 已完成**（2026-09-06 用户裁决启用 §1/§2/§4 各项，#16 = 方案一）。
2. **SSOT 修订**：修订稿已起草于 `design/临时讨论稿-第十轮-SSOT修订稿.md`（§6 表数、§31-1/-3/-4/-5/-6 关闭、§19/§20.1/§17/§18/§10.4/§11.3/§12.1 注记、§14 变更日志）→ **用户核查 → 授权后原子 commit 写入 SSOT**（含 #21 模块三摘录稿同步）。
3. §1 裁决值落地 config + 接线（#1=2、#2=10000、#4=100 直接填值；#3=500 需接线 import_file；#5–#8 摘除；#10 挪 config；#13 常量化；#15 env 化）。
4. 修复真阻断：#16 前端 start 接线方案一（api/index.js `startSession` + Chat.vue 入场确认按钮，409 幂等处理；顺带删 501 死文案）。
5. #17/#18/#19/#20 注释/by-design 落注（readiness.py 两处注释改写 + 后续代码注释见各裁决）。
6. 回写滞后记录：§3 #23–#30 各条。
7. 分支收口：commit 工作树（含 DICT 0.5 + 分页 + 本轮改动）→ 全量回归（基线 229 绿）→ merge/PR 入 main → 更新 STATE。
8. 勾销 by-design / backlog 项（#38/#39 勾销；#32–#37/#40 维持登记显式标注）。
