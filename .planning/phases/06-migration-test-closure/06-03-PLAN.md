---
phase: 06-migration-test-closure
plan: "06-03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - server/test_m1_regression.py
  - server/config.py
  - server/test_m5_backend.py
  - server/test_m6_backend.py
  - server/test_m7_backend.py
  - server/test_p0_chain.py
  - server/test_p0_security.py
  - server/test_phase2_difficulty.py
  - server/test_phase2_interview.py
  - server/test_phase2_scoring.py
  - server/test_phase3_forms.py
  - server/test_phase3_sse.py
  - server/test_phase3_timer.py
  - server/test_phase3_idempotency.py
  - server/test_phase3_misc.py
autonomous: true
requirements: [REF-7.5, REF-8.6]

must_haves:
  truths:
    - "M1 回归八项全部锁定：清洗边界 / 抽取异常 / 消歧 / 权重尾差 Σ=1（生成器精确 + 编辑器 ±0.005 容差）/ 冲突 stalled / confirmed 不可静默覆盖 / 版本 diff / 管理员权限"
    - "mock interviewer 固定 3 分（_mock_score 返回 {'score': 3}）以确定性记档为已知局限（REF-8.6），不增强 mock、不改变该行为"
    - "13 个会话类测试文件直插 question_bank 的 INSERT 全部补齐 model_id + model_version（消费侧 Phase 4 收紧后全绿）"
    - "config.py 落地 §31-4 词典匹配阈值 + 清洗词表占位常量（gate-package，标 `# 实施期校准 — 待用户裁决`，不臆造默认值）"
  artifacts:
    - path: "server/test_m1_regression.py"
      provides: "M1 八项回归锁 + mock=3 记档 docstring"
      contains: "test_"
    - path: "server/config.py"
      provides: "§31-4 占位常量"
      contains: "实施期校准"
  key_links:
    - from: "server/test_m1_regression.py"
      to: "server/services/aggregation.py _gate_check / adjudicate"
      via: "import 后断言 stalled 分支与冲突阈值=2 行为"
      pattern: "_gate_check"
    - from: "server/test_m1_regression.py"
      to: "server/services/aggregate.py _compute_weights"
      via: "断言 Σ=1（编辑器 ±0.005 容差）"
      pattern: "_compute_weights"
    - from: "13 个会话类测试文件 question_bank INSERT"
      to: "model_id/model_version 列"
      via: "INSERT 列清单补两列并写值"
      pattern: "model_id"
---

<objective>
M1 回归收口（D-71/REF-7.5）：新建 `server/test_m1_regression.py` 锁死模块一八个脆弱点（清洗/抽取/消歧/权重尾差/冲突/confirmed 覆盖/版本 diff/权限），并把 mock interviewer 固定 3 分的确定性行为以 docstring 记档（D-72/REF-8.6，不增强）。同步把 13 个会话类测试文件直插 `question_bank` 的 INSERT 补齐 `model_id` + `model_version`（[04-011] 消费侧收紧后失败）。§31-4 词典匹配阈值 + 清洗词表作为 gate-package 占位常量落地 config.py，标「待用户裁决」，禁止臆造默认值。

Purpose: Phase 1 修复后 M1 脆弱点无人守护；13 个测试文件因 Phase 4 消费侧双列匹配收紧而失败（[04-011] 延期项）；mock=3 属已知局限须记档而非掩盖。
Output: test_m1_regression.py（八项 + mock 记档）+ config.py §31-4 占位 + 13 文件 model_id/model_version 补齐。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-migration-test-closure/06-CONTEXT.md
@.planning/phases/06-migration-test-closure/06-RESEARCH.md
@.planning/phases/06-migration-test-closure/06-PATTERNS.md

<interfaces>
<!-- 本计划复用契约（全部 file:line 已核实） -->

From server/services/aggregation.py（M1 裁决层）:
- `ADJUDICATE_CONFLICT_THRESHOLD=2`（23 行）
- `IMPUTE_RATIO_THRESHOLD=0.2`（28 行）
- `_gate_check`（139-160 行）——conflict stalled 分支
- `adjudicate`（40 行）/ `aggregate_session_scores`（163 行）

From server/services/aggregate.py（M1 权重）:
- `_compute_weights`（81-105 行）——drift absorption 在 100-105；`run_aggregate`（108 行）

From server/services/scoring.py（mock 记档对象）:
- `_mock_score`（80-81 行）返回 `{"score": 3, ...}`——记档为已知局限，不改
- `MAX_ANSWER_LEN = 64*1024`（39 行）

From server/config.py（§31-4 占位落点）:
- 现有常量区块 10-59 行——追加占位常量到区块末尾

13 个会话类测试文件（[04-011] 消费侧收紧失败源，直插 question_bank 不写 model_id/model_version）:
test_m5_backend.py, test_m6_backend.py, test_m7_backend.py, test_p0_chain.py, test_p0_security.py, test_phase2_difficulty.py, test_phase2_interview.py, test_phase2_scoring.py, test_phase3_forms.py, test_phase3_sse.py, test_phase3_timer.py, test_phase3_idempotency.py, test_phase3_misc.py

消费侧收紧契约（Phase 4 D-47/D-49/D-50）:
- `_insert_question` 写 model_id/model_version/item_id/rubric_version='v1'，判重键 = (model_id, model_version, std_name, category, difficulty)
- readiness 三处 count/tier WHERE + selection `_load_candidate_rows` 均加 model_id+model_version 谓词
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_m1_regression.py 八项回归锁 + mock=3 记档（REF-7.5 / REF-8.6）</name>
  <files>server/test_m1_regression.py</files>
  <read_first>
    - server/services/aggregation.py（23/28/40/139-160/163 行）
    - server/services/aggregate.py（81-105/108 行）
    - server/services/scoring.py（80-81 行 _mock_score）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-71 M1 八项 + D-72 mock=3 记档）
    - .planning/phases/06-migration-test-closure/06-RESEARCH.md（Pattern 2 懒导入 + 测试纯函数）
  </read_first>
  <behavior>
    - 八项回归锁（每项一条 test_*，函数懒导入，避免 import 副作用）：
      1) 清洗边界：clean_jd 对空 JD/纯标点/超长 JD 不抛异常返回可处理结构
      2) 抽取异常：normalize_title 对无标题/纯数字标题不抛异常
      3) 消歧：无 LLM 时消歧降级路径不抛异常（mock 下确定性）
      4) 权重尾差 Σ=1：生成器路径 _compute_weights 精确 ==1.0；编辑器路径 ±0.005 容差（drift absorption 100-105 行）
      5) 冲突 stalled：_gate_check 在 conflict 计数 ≥ 2 时返回 stalled 分支
      6) confirmed 不可静默覆盖：adjudicate 对已 confirmed 项不覆盖其 score
      7) 版本 diff：模型 version 不同视为不同模型（不共享判重键）
      8) 管理员权限：require_admin 拒绝非 admin 身份
    - mock=3 记档：模块 docstring 写明「mock interviewer `_mock_score` 恒返回 score=3，故 b 一致性方差恒 0、c 靠客观题区分度；此为已知局限，不增强 mock（D-72）」
  </behavior>
  <action>新建 server/test_m1_regression.py。顶部写模块 docstring 记录 mock=3 确定性局限（见 behavior）。所有被断言函数一律在测试函数体内 `from server.services.aggregate import _compute_weights` 式懒导入（避免 import 期副作用，照 RESEARCH Pattern 2）。八条 `test_*` 函数逐条对应 behavior 八项，用现有 helper 风格（`_q` 只读查询 / 直接调纯函数）。断言数值用相对容差而非绝对（编辑器权重用 `pytest.approx(1.0, abs=0.005)`）。第 8 项管理员权限用 TestClient 或直接调 `require_admin` 依赖注入验证拒绝路径。mock=3 记档只写 docstring，不改 `server/services/scoring.py` 的 `_mock_score`。</action>
  <verify>
    <automated>cd server && python -m pytest test_m1_regression.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_" server/test_m1_regression.py` 输出 8（或 8 条 test_* 函数）
    - `grep -n "mock" server/test_m1_regression.py` 命中 docstring 记档（含「3」或「score=3」）
    - `grep -n "_compute_weights\|_gate_check\|adjudicate\|require_admin" server/test_m1_regression.py` 命中对应懒导入
    - 上条 pytest 命令 8 passed
  </acceptance_criteria>
  <done>M1 八项回归锁全绿；mock=3 确定性局限以 docstring 记档（未改 _mock_score）</done>
</task>

<task type="auto">
  <name>Task 2: §31-4 词典匹配阈值 + 清洗词表占位常量（gate-package，禁止臆造默认值）</name>
  <files>server/config.py</files>
  <read_first>
    - server/config.py（现有常量区块 10-59 行，看追加位置）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（开放参数处理约定）
  </read_first>
  <action>在 server/config.py 现有常量区块末尾（59 行 MAX_CONTEXT_TOKENS 之后）追加两个占位常量，值留空占位、每行末尾标注 `# 实施期校准 — 待用户裁决`（§31-4）：`DICT_MATCH_THRESHOLD = None  # 实施期校准 — 待用户裁决（词典候选 top10 匹配阈值）` 与 `TITLE_CLEAN_WORDS: list[str] = []  # 实施期校准 — 待用户裁决（清洗标题词表）`。不臆造任何阈值数值或词表内容——这是硬关口：开放参数只能由用户裁决。本任务只落占位与注释，不接线消费（消费侧在用户裁决后于后续计划接入）。</action>
  <verify>
    <automated>grep -n "实施期校准\|DICT_MATCH_THRESHOLD\|TITLE_CLEAN_WORDS" server/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DICT_MATCH_THRESHOLD" server/config.py` 命中，值为 None（占位）
    - `grep -n "TITLE_CLEAN_WORDS" server/config.py` 命中，值为 `[]`（占位）
    - 两行均含 `实施期校准 — 待用户裁决` 注释
    - 无任何臆造的阈值数值（None/空列表即为占位）
  </acceptance_criteria>
  <done>config.py 含 §31-4 两占位常量（None/[]），标注待用户裁决，未接线</done>
</task>

<task type="auto">
  <name>Task 3: 13 个会话类测试文件补齐 question_bank INSERT 的 model_id/model_version（[04-011]）</name>
  <files>server/test_m5_backend.py, server/test_m6_backend.py, server/test_m7_backend.py, server/test_p0_chain.py, server/test_p0_security.py, server/test_phase2_difficulty.py, server/test_phase2_interview.py, server/test_phase2_scoring.py, server/test_phase3_forms.py, server/test_phase3_sse.py, server/test_phase3_timer.py, server/test_phase3_idempotency.py, server/test_phase3_misc.py</files>
  <read_first>
    - server/db.py（question_bank DDL 中 model_id/model_version 列定义，确认 NOT NULL 与默认值）
    - 每个目标文件各自的 `INSERT INTO question_bank` 语句（grep 定位）
    - .planning/phases/06-migration-test-closure/06-PATTERNS.md（Phase 4 消费侧双列收紧契约 + 已有补列样例）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-47/D-49/D-50 双列匹配）
  </read_first>
  <behavior>
    - 每个文件里所有 `INSERT INTO question_bank` 语句的列清单补齐 `model_id` 与 `model_version` 两列，并在 VALUES 中写一个该文件已创建的 model 的 id 与 version=1（与文件内现有 model 创建逻辑对齐）
    - 若文件内没有 model 创建逻辑，须先补一个 model 行（照 Phase 4 `_seed_position_with_confirmed_model` 返回 pid,mid 的既有 helper 模式），再在 INSERT 引用其 mid
    - 不改 question_bank 其他列、不改断言数值、不改测试语义——只补两列
  </behavior>
  <action>对 13 个文件逐个：`grep -n "INSERT INTO question_bank" server/test_*.py` 定位所有直插点。每个直插点的列清单补 `model_id, model_version`，VALUES 对应位置写「该文件已创建的 model 的 mid」与 `1`（若文件内已有 `_seed_position_with_confirmed_model` 式 helper 返回 mid，直接引用；否则按同模式补 model 行）。注意：test_m6_backend.py 的 `_seed_full_chain`（35-145 行）也在其列（06-02 已改名 `_test_*`→`test_*`，本任务只动 INSERT 不冲突）。逐文件改完后跑该文件确认消费侧 WHERE model_id+model_version 不再返回 0 行。不要改动与 model_id 无关的断言。</action>
  <verify>
    <automated>cd server && grep -L "model_id" test_m5_backend.py test_m6_backend.py test_m7_backend.py test_p0_chain.py test_p0_security.py test_phase2_difficulty.py test_phase2_interview.py test_phase2_scoring.py test_phase3_forms.py test_phase3_sse.py test_phase3_timer.py test_phase3_idempotency.py test_phase3_misc.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 上条 `grep -L "model_id"` 对 13 个文件返回空（每个文件都含 model_id）
    - 13 个文件均含 `model_version`（`grep -L "model_version"` 空）
    - `cd server && python -m pytest test_m5_backend.py test_m6_backend.py test_m7_backend.py -q` 仍绿（m5/m6/m7 代表，确认消费侧双列匹配不破坏既有断言）
    - 无任何文件改了与 model_id 无关的断言数值
  </acceptance_criteria>
  <done>13 个会话类测试文件 question_bank 直插全部补齐 model_id/model_version；Phase 4 消费侧收紧后回归仍绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 → question_bank | 13 文件直插 question_bank 绕过 `_insert_question`，须手动补双列以对齐消费侧收紧 |
| config → 运行期常量 | §31-4 开放参数占位，值未经用户裁决不得生效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | server/config.py §31-4 占位 | mitigate | 值留 None/[] + 标注待用户裁决，禁止臆造默认值（红线） |
| T-06-08 | Tampering | 13 文件 question_bank INSERT | mitigate | 补 model_id/model_version 对齐消费侧双列匹配；不改测试语义与其他断言 |
| T-06-09 | Repudiation | mock=3 记档 | mitigate | docstring 记档 mock 恒 3 分的已知局限，防 b/c 评测方差恒 0 被误读 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
逐文件：`cd server && python -m pytest test_m1_regression.py -q` 8 passed；`cd server && python -m pytest test_m5_backend.py test_m6_backend.py test_m7_backend.py -q` 仍绿（双列补齐后消费侧不再返 0 行）。全量收集：`cd server && python -m pytest . --collect-only -q 2>&1 | tail -20`（06-02 已收集干净，本计划新增 test_m1_regression 应可收集）。
</verification>

<success_criteria>
M1 八项回归锁 + mock=3 记档落地；§31-4 占位常量（待用户裁决，无臆造值）；13 文件双列补齐后消费侧收紧回归全绿；REF-7.5（M1 回归）+ REF-8.6（mock 恒 3 记档）兑现。
</success_criteria>

<output>
Create `.planning/phases/06-migration-test-closure/06-03-SUMMARY.md` when done
</output>
