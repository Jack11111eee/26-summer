---
phase: 06-migration-test-closure
plan: "06-05"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - eval/consistency_test.py
  - eval/virtual_candidates.py
  - server/api/admin/eval.py
  - server/db.py
  - server/services/report.py
  - server/config.py
  - server/main.py
  - server/test_bad_case.py
  - server/test_secret_gate.py
  - server/test_input_limits.py
  - server/test_eval_isolation.py
autonomous: false
requirements: [REF-5.11, REF-6.1, REF-6.2, REF-6.3, REF-8.8, REQ-data-compliance, REQ-iterative-loop]

must_haves:
  truths:
    - "eval 三脚本（consistency/virtual_candidates + admin eval 路由）改用临时库运行，不再直接操作 data/app.db（REF-8.8 独立/临时库改造），结果经 eval_results 写回业务库"
    - "b 一致性（固定 transcript 复跑 score 分差 ≤ 1）与 c 虚拟考生（strong > medium > weak 客观题区分度）在隔离临时库上可复现（mock 下 b 方差恒 0 记档）"
    - "bad case 候选：score_live/score_final 双分背离 ≥ 阈值自动 INSERT bad_case_candidate（status=pending），永不自动改分（REF-5.11/D-031）"
    - "输入限额按类型配置（REF-6.3）：JD 长度 / 文件行数 / 回答长度 / prompt max_tokens / 分页 limit 五类集中 config.py，接口层校验；已决值（MAX_ANSWER_LEN=64K / MAX_CONTEXT_TOKENS=8000）沿用，未决值占位待裁决"
    - "生产 secret 启动校验锁定：main.py 现 fail-closed-always（更严于 D-77），测试锁定此行为（REF-6.2）"
    - "gate-package 开放参数（§31-5 trace 保留期/脱敏、§31-6 幂等清理阈值、REF-5.11 bad-case 背离阈值、REF-6.3 输入限额值）全部 config.py 占位，标「实施期校准 — 待用户裁决」，禁止臆造默认值"
  artifacts:
    - path: "server/db.py"
      provides: "bad_case_candidate 表（加入 _DDL）"
      contains: "bad_case_candidate"
    - path: "server/config.py"
      provides: "输入限额常量 + §31-5/§31-6/REF-5.11/REF-6.3 占位常量"
      contains: "实施期校准"
    - path: "server/test_bad_case.py"
      provides: "双分背离建候选 + 永不改分断言"
      contains: "test_"
  key_links:
    - from: "eval/consistency_test.py / virtual_candidates.py"
      to: "server.db.set_db_path"
      via: "运行前 set_db_path(temp) 指向隔离临时库，结束复位"
      pattern: "set_db_path"
    - from: "server/services/report.py generate_report"
      to: "bad_case_candidate"
      via: "评分完成后扫描 score_live vs score_final 背离，INSERT 候选，不改 score"
      pattern: "bad_case_candidate"
    - from: "server/config.py 限额常量"
      to: "接口层校验"
      via: "JD 导入 / 分页 limit 读取处引用常量做上限检查"
      pattern: "MAX_"
---

<objective>
Phase 6 收口最后一块：eval 隔离（REF-8.8）、bad case 候选（REF-5.11）、安全收尾（REF-6.1 JWT 方向 / REF-6.2 secret 校验 / REF-6.3 输入限额）。eval 三脚本从「直接操作业务库」改为「临时库 + 写回 eval_results」；评分链路落双分背离候选；config.py 集中输入限额 + 开放参数占位；secret 启动校验测试锁定。

Purpose: 三块均为 Phase 6 遗留收口项——eval 违反 §23 隔离、bad case 无人抓、输入限额与 secret 校验缺收口。
Output: eval 隔离改造 + bad_case_candidate 表与检测 + config 限额/占位常量 + 3 个新测试（bad_case/secret_gate/input_limits/eval_isolation 共 4 个）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-migration-test-closure/06-CONTEXT.md
@.planning/phases/06-migration-test-closure/06-RESEARCH.md
@.planning/phases/06-migration-test-closure/06-PATTERNS.md

<interfaces>
<!-- 本计划复用契约（全部 file:line 已核实） -->

From eval/consistency_test.py（直接 get_conn 违规源）:
- `test_scoring_consistency(session_id, runs=3)` —— 直接 get_conn() 操作业务库
From eval/virtual_candidates.py（直接 get_conn 违规源）:
- `test_virtual_candidates(position_id)` / `_get_or_seed_bank` / `_run_one_tier` —— 直接 get_conn()
From server/api/admin/eval.py:
- `_save_result`（21-32 行）/ `_run`（35-42 行）/ routes consistency（49-58）/ virtual-candidates（65-74）/ results（77-90）

From server/services/scoring.py:
- `_mock_score`（80-81 返回 {"score": 3}）/ `score_session`（240-335）/ `_score_objective`（43）/ `MAX_ANSWER_LEN = 64*1024`（39 行，已决限额）

From server/services/report.py:
- `REPORT_STATUSES`（22）/ `REVIEW_STATUSES`（24）/ `_insert_report_row`（50）/ `_mock_report`（86）/ `generate_report`（160-269）

From server/config.py:
- 常量区块 10-59 行（`MAX_CONTEXT_TOKENS = 8000` 59 行已决）；追加限额/占位常量到区块末尾

From server/main.py:
- `_INSECURE_JWT_DEFAULTS = {"", "change-me-in-.env"}`（16 行）；`_startup`（59-67）fail-closed `raise RuntimeError`

From server/db.py（06-01 已改造）:
- `set_db_path`（06-01 新增）/ `_DDL`（9-409，24 表，本计划追加 bad_case_candidate 表）
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: 安全方向硬关口决策（D-76 JWT cookie / REF-6.2 secret 严格度）</name>
  <decision>两个安全方向决策，已在计划审查硬关口由用户裁决（2026-09-05，见下方 resolution）</decision>
  <context>SSOT 对这两项均标「方向/实施期决定，非 P0」，但 Phase 6 收口需锁定方向。推荐默认值见下。**后续后果（对齐 checker I1）**：若选择 jwt-cookie-migrate，则 REF-6.1 本期仅完成「方向锁定」，实际迁移为 Phase 6 之外的后续计划（触及 server/core/security.py、server/api/auth.py、web/src/utils/sse.js、web/src/api/index.js 四处），本计划不排该迁移工作、不改这些文件。</context>
  <options>
    <option id="jwt-cookie-migrate">
      <name>D-76 JWT：迁移 HttpOnly cookie（推荐默认）</name>
      <pros>防 XSS 窃 token；SSOT 标「方向」；符合 ASVS L1 会话管理</pros>
      <cons>需改 auth.py/security.py/sse.js/index.js 四处；本期文件范围不含这些文件 → 若确认则列为后续计划</cons>
    </option>
    <option id="jwt-bearer-keep">
      <name>D-76 JWT：维持 Bearer + localStorage（暂缓迁移）</name>
      <pros>零改动；当前所有测试/前端已通</pros>
      <cons>token 暴露于 XSS 面；SSOT 方向未落地</cons>
    </option>
    <option id="secret-failclosed-keep">
      <name>REF-6.2 secret：维持 fail-closed-always（推荐默认）</name>
      <pros>main.py:59-67 已实现且更严于 D-77；测试锁定现状零生产改动</pros>
      <cons>mock 环境启动需设 test-secret（conftest 已兜底）</cons>
    </option>
    <option id="secret-warn-mock">
      <name>REF-6.2 secret：放宽为 warn-in-mock / fail-in-real（D-77 原案）</name>
      <pros>本地 mock 演示免设 secret</pros>
      <cons>需改 main.py 逻辑；弱化现有更严防线</cons>
    </option>
  </options>
  <resume-signal>选择：jwt-cookie-migrate 或 jwt-bearer-keep（D-76）+ secret-failclosed-keep 或 secret-warn-mock（REF-6.2）</resume-signal>
  <resolution date="2026-09-05" ref="[06-009]">
    - D-76 JWT 方向 = jwt-cookie-migrate（迁移 HttpOnly cookie）——本期仅锁方向，实际迁移为 Phase 6 外后续计划（触 server/core/security.py、server/api/auth.py、web/src/utils/sse.js、web/src/api/index.js 四处，本计划不改这些文件）
    - REF-6.2 secret 严格度 = secret-failclosed-keep（维持 main.py:59-67 fail-closed-always 现状）——Task 3 按推荐默认执行，无需修订
    - 5 开放参数 = 维持占位默认（None/[]，实施期校准）
  </resolution>
</task>

<task type="auto" tdd="true">
  <name>Task 1: eval 隔离——临时库运行 + 写回 eval_results（REF-8.8）</name>
  <files>eval/consistency_test.py, eval/virtual_candidates.py, server/api/admin/eval.py, server/test_eval_isolation.py</files>
  <read_first>
    - eval/consistency_test.py（全文，重点 test_scoring_consistency 与 get_conn 调用）
    - eval/virtual_candidates.py（全文，重点 _get_or_seed_bank/_run_one_tier）
    - server/api/admin/eval.py（21-90 行 _save_result/_run/routes）
    - .planning/phases/06-migration-test-closure/06-RESEARCH.md（Pattern 4 eval DB override）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-74 eval 隔离 temp DB）
  </read_first>
  <behavior>
    - eval/consistency_test.py 与 eval/virtual_candidates.py 运行前 `set_db_path(tempfile.mkdtemp()/eval.db)` 指向隔离临时库，`init_db()` 建表 + 播种，跑完后 `set_db_path(None)` 复位
    - 结果（consistency 各 run 的 score、virtual_candidates 各 tier 得分）经 `_save_result` 写回业务库 eval_results（业务库连接独立于临时库）
    - server/api/admin/eval.py 的 `_run` 在触发 eval 前对子进程/函数调用注入 set_db_path(temp)，结束复位
    - test_eval_isolation.py：断言 eval 运行期间业务库 data/app.db 的 session 表无新增行（隔离生效）、eval_results 有写回且新写回行 status='completed'（非 failed）——b/c 得分契约（b 分差≤1 / c 强>中>弱）只经 eval/*.py CLI 脚本跑、不走 pytest，故用 eval_results 写回 status 兜底断言（对齐 checker W5）
  </behavior>
  <action>在 eval/consistency_test.py 与 eval/virtual_candidates.py：模块级或函数入口处用 `from server.db import set_db_path, init_db`，运行前 `set_db_path(os.path.join(tempfile.mkdtemp(prefix="gsd-eval-"), "eval.db"))` + `init_db()`（必要时播种 position/question_bank/model），运行结束后 `set_db_path(None)`。业务库写回经 `server.api.admin.eval._save_result`（该函数连业务库连接，勿用 set_db_path 覆盖的临时库）。server/api/admin/eval.py 的 `_run`（35-42）改为：先 `set_db_path(temp)` 跑 eval，`finally: set_db_path(None)`，再 `_save_result` 写回。新建 server/test_eval_isolation.py：用 06-01 conftest session 库作业务库基准，断言 `set_db_path(temp)` 隔离后业务库 assessment_session 表行数不增、eval_results 行数增且新写回行 status='completed'（非 failed）。可选：import `eval.assertions` 的 `assert_score_consistency`/`assert_tier_ordering`，在播种好的临时库上直接调用断言 b 分差≤1 / c 强>中>弱（把 CLI-only 的得分契约纳入 pytest 断言面，对齐 checker W5）。注意 SQLite 单写者纪律：eval 与写回分两个连接，先 commit 再切库。</action>
  <verify>
    <automated>cd server && python -m pytest test_eval_isolation.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "set_db_path" eval/consistency_test.py eval/virtual_candidates.py server/api/admin/eval.py` 三文件均命中
    - `grep -n "set_db_path(None)" eval/consistency_test.py eval/virtual_candidates.py` 命中复位
    - test_eval_isolation.py 断言「业务库 session 行数不变 + eval_results 行数增 + 新写回行 status='completed'（非 failed）」
    - 上条 pytest 命令 passed
  </acceptance_criteria>
  <done>eval 三脚本经 set_db_path 隔离临时库运行，结果写回 eval_results（status='completed'）；test_eval_isolation 绿（隔离 + 写回 status 兜底断言）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bad case 候选——双分背离检测 + bad_case_candidate 表（REF-5.11）</name>
  <files>server/db.py, server/services/report.py, server/config.py, server/test_bad_case.py</files>
  <read_first>
    - server/db.py（_DDL 24 表结构；bad_case 相关列在 question_score/feedback）
    - server/services/report.py（160-269 generate_report；22/24 状态常量）
    - server/services/scoring.py（240-335 score_session 写 score_live/score_final）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-75 bad_case_candidate + admin review + D-031 永不改分）
  </read_first>
  <behavior>
    - bad_case_candidate 表（追加到 _DDL）：candidate_id TEXT PK / session_id TEXT NOT NULL / item_id TEXT NOT NULL / question_id TEXT / score_live REAL / score_final REAL / divergence REAL / status TEXT NOT NULL DEFAULT 'pending'（pending/reviewed/dismissed 校验）/ detected_at TEXT NOT NULL / review_note TEXT / reviewed_by TEXT / reviewed_at TEXT
    - 检测函数 `_detect_bad_case_divergence(conn, session_id)`：扫描 question_score 中 score_live 与 score_final 均非 NULL 的行，|score_live - score_final| >= BAD_CASE_DIVERGENCE_THRESHOLD 时 INSERT bad_case_candidate（status='pending'），永不 UPDATE question_score 的 score 字段
    - 在 generate_report（160-269）评分聚合后调用检测（先 commit 再检测，遵守 SQLite 单写者）
    - config.py 追加 `BAD_CASE_DIVERGENCE_THRESHOLD = None  # 实施期校准 — 待用户裁决`（占位）
  </behavior>
  <action>在 server/db.py 的 `_DDL` 字符串末尾（trace_link 表后）追加 `bad_case_candidate` 表 DDL（列见 behavior；status 用 CHECK(status IN ('pending','reviewed','dismissed'))；外键 session_id → assessment_session、item_id → competency_item）。在 server/services/report.py 新增 `_detect_bad_case_divergence(conn, session_id)`（从 config import BAD_CASE_DIVERGENCE_THRESHOLD；若阈值为 None 则直接 return 不检测——占位待裁决，不臆造数值），并在 `generate_report` 聚合评分后、写 report 行前调用（conn 已 commit 评分后）。新建 server/test_bad_case.py：手工 INSERT 一对 question_score（score_live=5.0, score_final=1.0, divergence=4.0）与一对（score_live=3.0, score_final=2.5, divergence=0.5），monkeypatch config.BAD_CASE_DIVERGENCE_THRESHOLD=2.0 后调 `_detect_bad_case_divergence`，断言前者建候选（status=pending）、后者不建、且 question_score 的 score_final/score_live 未被修改（D-031）。config.py 追加 `BAD_CASE_DIVERGENCE_THRESHOLD = None  # 实施期校准 — 待用户裁决（双分背离阈值）`（append-only，勿重写区块）。</action>
  <verify>
    <automated>cd server && python -m pytest test_bad_case.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bad_case_candidate" server/db.py` 命中表 DDL
    - `grep -n "_detect_bad_case_divergence" server/services/report.py` 命中定义 + generate_report 内调用
    - `grep -n "BAD_CASE_DIVERGENCE_THRESHOLD" server/config.py` 命中，值为 None（占位）
    - test_bad_case.py 断言「建候选 + 不改分」；上条 pytest 命令 passed
  </acceptance_criteria>
  <done>bad_case_candidate 表 + 双分背离检测落地；背离≥阈值建候选且永不改分；阈值占位待裁决</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 安全收尾——输入限额 + secret 校验锁定 + 开放参数占位（REF-6.2/REF-6.3 + §31-5/§31-6）</name>
  <files>server/config.py, server/main.py, server/test_secret_gate.py, server/test_input_limits.py</files>
  <read_first>
    - server/config.py（10-59 常量区块；追加位置）
    - server/main.py（16 行 _INSECURE_JWT_DEFAULTS；59-67 行 _startup fail-closed）
    - server/services/scoring.py（39 行 MAX_ANSWER_LEN=64K，已决）
    - .planning/phases/06-migration-test-closure/06-CONTEXT.md（D-78 输入限额按类型 + 开放参数处理约定）
  </read_first>
  <behavior>
    - config.py 追加输入限额常量：`MAX_JD_LENGTH = None`（JD 文本长度上限，占位）、`MAX_JD_FILE_LINES = None`（JSONL 行数上限，占位）、`MAX_PAGINATION_LIMIT = None`（分页 limit 上限，占位）；已决值沿用 `MAX_ANSWER_LEN = 64*1024`（scoring.py）与 `MAX_CONTEXT_TOKENS = 8000`（config.py:59）
    - config.py 追加开放参数占位：`TRACE_RETENTION_DAYS = None`（§31-5 保留期）、`TRACE_DESENSITIZE = None`（§31-5 脱敏开关）、`IDEMPOTENCY_CLEANUP_THRESHOLD = None`（§31-6 幂等清理阈值），均标「实施期校准 — 待用户裁决」
    - secret 校验锁定（承接 Task 0 决策，实现推荐默认 secret-failclosed-keep）：test_secret_gate.py 断言 `config.JWT_SECRET` 为默认值（"" 或 "change-me-in-.env"）时 `_startup` raise RuntimeError（fail-closed）；test-secret 时通过。**条件性**：若 Task 0 用户选择 secret-warn-mock，本任务须修订为断言 warn-in-mock / fail-in-real 并改 main.py 逻辑；以下动作按 secret-failclosed-keep 编写
    - test_input_limits.py：monkeypatch 限额常量为小值后，断言超限输入被拒（结构校验逻辑正确），不提交生产默认值
  </behavior>
  <action>前提（承接 Task 0 决策，对齐 checker W4）：本任务实现 REF-6.2 推荐默认 secret-failclosed-keep（锁定 main.py:59-67 现状 fail-closed-always，零生产改动）；若 Task 0 用户选择 secret-warn-mock，本任务须修订为 warn-in-mock/fail-in-real 断言并改 main.py。config 追加为 append-only（06-03 在其后追加 §31-4 占位，勿重写区块，对齐 checker W3）。在 server/config.py 常量区块末尾（59 行后）追加 6 个占位常量，全部标 `# 实施期校准 — 待用户裁决`：`MAX_JD_LENGTH = None`、`MAX_JD_FILE_LINES = None`、`MAX_PAGINATION_LIMIT = None`（REF-6.3 未决值）、`TRACE_RETENTION_DAYS = None`、`TRACE_DESENSITIZE = None`（§31-5）、`IDEMPOTENCY_CLEANUP_THRESHOLD = None`（§31-6）。不臆造任何数值或开关值。新建 server/test_secret_gate.py：`from server import config; from server.main import _startup, _INSECURE_JWT_DEFAULTS`，用 pytest 断言 `config.JWT_SECRET in _INSECURE_JWT_DEFAULTS` 时 `_startup` raise RuntimeError（用 try/except 或 pytest.raises）；`config.JWT_SECRET="test-secret"` 时不 raise（mock 三件套由 conftest 保证 test-secret）。新建 server/test_input_limits.py：`import server.config as C`，monkeypatch `C.MAX_JD_LENGTH=100` 后断言超 100 字符 JD 被校验函数拒绝、`C.MAX_PAGINATION_LIMIT=10` 后断言 limit=100 被钳到 10 或拒绝（结构校验，不锁生产默认值）。校验落点若需新增小型纯函数（如 `validate_jd_length(text)` / `clamp_pagination_limit(limit)`）则放 server/services/ 或 api 层就近，保持 surgical。</action>
  <verify>
    <automated>cd server && python -m pytest test_secret_gate.py test_input_limits.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAX_JD_LENGTH\|MAX_JD_FILE_LINES\|MAX_PAGINATION_LIMIT\|TRACE_RETENTION_DAYS\|TRACE_DESENSITIZE\|IDEMPOTENCY_CLEANUP_THRESHOLD" server/config.py` 六常量均命中且值 None（占位）
    - 六行均含 `实施期校准 — 待用户裁决` 注释
    - test_secret_gate.py 断言 fail-closed raise + test-secret 通过
    - test_input_limits.py 用 monkeypatch 断言超限拒绝/钳制
    - 上条 pytest 命令全 passed
  </acceptance_criteria>
  <done>输入限额按类型占位 + 已决值沿用；secret fail-closed 锁定（按 Task 0 secret-failclosed-keep 默认）；§31-5/§31-6 占位；测试全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval 脚本 → 业务库 | eval 不得直接操作 data/app.db（§23 隔离） |
| 候选人输入 → API | JD/回答/分页等输入超限须拒绝（REF-6.3） |
| 启动路径 → JWT secret | 默认 secret 须 fail-closed（REF-6.2） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | eval/consistency_test.py, eval/virtual_candidates.py | mitigate | set_db_path 隔离临时库 + finally 复位；写回经独立业务库连接（REF-8.8） |
| T-06-14 | Tampering | server/services/report.py _detect_bad_case_divergence | mitigate | 只 INSERT bad_case_candidate（status=pending），永不 UPDATE score（D-031） |
| T-06-15 | DoS | server/config.py 输入限额 | mitigate | 按类型限额常量集中 + 接口层校验（REF-6.3）；未决值占位不生效，已决值沿用 |
| T-06-16 | Info Disclosure | server/main.py JWT secret | mitigate | 默认 secret fail-closed（现更严于 D-77）；test_secret_gate 锁定 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新包，无供应链风险面 |
</threat_model>

<verification>
逐文件：`cd server && python -m pytest test_eval_isolation.py test_bad_case.py test_secret_gate.py test_input_limits.py -q` 全绿。全量收集：`cd server && python -m pytest . --collect-only -q 2>&1 | tail -20`（06-02 后无 fixture error，本计划 4 新测试可收集）。全量回归：`cd server && python -m pytest . -q 2>&1 | tail -30`（Phase 6 收口全绿）。
</verification>

<success_criteria>
eval 三脚本隔离临时库 + 写回 eval_results（REF-8.8）；bad_case_candidate 表 + 双分背离检测 + 永不改分（REF-5.11）；输入限额按类型 + 开放参数占位（REF-6.3/§31-5/§31-6）；secret fail-closed 锁定（REF-6.2）；D-76 JWT 方向经硬关口用户确认（REF-6.1）；4 个新测试全绿。
</success_criteria>

<output>
Create `.planning/phases/06-migration-test-closure/06-05-SUMMARY.md` when done
</output>
