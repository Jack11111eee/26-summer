---
phase: 04-question-bank-version
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/question_bank.py
  - server/services/readiness.py
  - server/services/question_selection.py
  - server/api/admin/positions.py
  - server/test_phase4_binding.py
  - server/test_phase4_fail_visible.py
  - server/test_phase2_selection.py
autonomous: true
requirements: [REF-2.5, REF-3.4, REF-8.4]
user_setup: []

must_haves:
  truths:
    - "generate_question_bank 落库的每一行 question_bank 携带 model_id/model_version/item_id/rubric_version='v1'（measurement_target/evidence_requirement 留 NULL）"
    - "模型升版后（v2）旧版题库（v1）不再被新会话选中；v2 题库未生成时开考被 readiness 阻止（QUESTION_BANK_INCOMPLETE）"
    - "题库生成失败时 readiness 返回 QUESTION_BANK_INCOMPLETE 且 detail 含 error_msg[:200] 截断（不静默 pass）"
    - "管理员待办 get_todos 返回 question_bank_failed 明细列表（position_id/model_id/model_version/error_msg），question_bank_not_ready 保持 int —— 硬关口 A 呈报口径 D-51"
  artifacts:
    - path: "server/services/question_bank.py"
      provides: "_insert_question 写 model_id/model_version/item_id/rubric_version + 4 处 exists 判重键升级"
    - path: "server/services/readiness.py"
      provides: "三处 count/tier WHERE 加 model_id+model_version 过滤 + task 查询取 error_msg + FAILED 分支"
    - path: "server/services/question_selection.py"
      provides: "_load_candidate_rows 强制 model_id=? AND model_version=?（去 NULL 放行）"
    - path: "server/api/admin/positions.py"
      provides: "get_todos 新增 question_bank_failed 明细"
  key_links:
    - from: "generate_question_bank"
      to: "question_bank.model_id/model_version/item_id/rubric_version"
      via: "_insert_question INSERT 列清单"
      pattern: "model_id, model_version, item_id, rubric_version"
    - from: "check_session_readiness / _load_candidate_rows"
      to: "question_bank.model_id/model_version"
      via: "AND model_id=? AND model_version=?"
      pattern: "model_id=\\? AND model_version=\\?"
    - from: "check_session_readiness"
      to: "question_bank_task.status='FAILED'"
      via: "QUESTION_BANK_INCOMPLETE + error_msg[:200]"
      pattern: "QUESTION_BANK_INCOMPLETE"
---

<objective>
把题库从「按岗位复用、一次生成终身有效」演进为「绑定 confirmed 模型版本」：question_bank 行的 model_id/model_version/item_id 列在本 phase 真正填充（rubric_version 落常量 "v1"），消费侧（readiness + selection）把 Phase 2 的「版本近似放行」收紧为「强制 model_id + model_version 匹配」，实现「升版须重建题库否则阻止开考」（REF-3.4）；同时把生成失败从「落表但不对外可见」升级为「readiness 带 error_msg + 管理员待办含失败明细」（REF-8.4）。

Purpose: 兑现 SSOT §9.2「有效题目 = status='active' 且 model/version 匹配」与 §10.4「题库绑定当前 model/version 且已生成完成」，杜绝升版后旧题库被误用与生成失败静默开考。
Output: 落库侧 + 消费侧四文件改造；新增 test_phase4_binding.py / test_phase4_fail_visible.py；test_phase2_selection.py 种子绑定 model_id/model_version。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

设计权威（只读，不改）：@design/final-design/总设计文档.md §9.2（question_bank 演进 +「有效题目 = active 且 model/version 匹配」）与 §10.4（开考检查三态 + 管理员待办）

改造对象（必读现状）：@server/services/question_bank.py、@server/services/readiness.py、@server/services/question_selection.py、@server/api/admin/positions.py、@server/db.py（question_bank DDL 已含 model_id/model_version/item_id/rubric_version 列，无需新迁移）

测试 analog：@server/test_phase2_selection.py（三件套 + _seed_question_bank 种子）、@server/test_question_bank.py（generate_question_bank 种子 + 判重断言）

<interfaces>
<!-- 执行者直接使用的既有契约（已逐行核对源码，无需再探索） -->

server/services/question_bank.py:
  - def generate_question_bank(position_id: str, model_id: str) -> None  # 签名保持不动（A3）
  - def _insert_question(conn, *, scope, position_id, item, difficulty, qtype, stem, answer_key, rubric, chain_key, chain_seq) -> None  # 需加 model_id/model_version/item_id 三 keyword-only 参
  - 4 处幂等 exists 查询（scope='position' 两分支 / scope='general' 两分支）现按 position_id 或 std_name+category 判重，缺 model_id/model_version 谓词

server/services/readiness.py:
  - def _question_count_by_category(conn, position_id) -> dict[str, int]  # 需加 model_id/model_version
  - def _covered_std_names(conn, position_id) -> set[str]  # 需加 model_id/model_version
  - def check_session_readiness(position_id, model=None) -> dict | None  # model 行含 model_id/version/model_json
  - tier LEFT JOIN（lines 132-141）WHERE 现无 qb.model_id/qb.model_version 谓词
  - task 查询（lines 95-100）现 SELECT status（缺 error_msg），FAILED 分支为注释占位

server/services/question_selection.py:
  - def _load_candidate_rows(conn, position_id, model_id) -> list[dict]  # 需加 model_version；line 233 现为「版本近似放行」子句
  - 调用点 _select_next_question_locked line 299: session 已取 model_version（line 284）

server/api/admin/positions.py:
  - def get_todos() -> dict  # 现返回 pending_positions/stalled_models/orphan_jds/question_bank_not_ready
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 新建 test_phase4_binding.py + test_phase4_fail_visible.py（先红）</name>
  <files>server/test_phase4_binding.py, server/test_phase4_fail_visible.py</files>
  <read_first>
    - server/test_phase2_selection.py（三件套头 lines 13-52 + _seed_position_with_confirmed_model lines 57-90 + _q helper lines 46-52）
    - server/test_question_bank.py（generate_question_bank 种子 lines 34-68，含 experience/qualification + evidence）
    - server/services/question_bank.py（_insert_question lines 56-67 / generate_question_bank lines 88-182 / 4 处 exists 判重 lines 122-149）
    - server/services/readiness.py（check_session_readiness lines 40-169 / task 查询 lines 95-105 / tier LEFT JOIN lines 132-141）
    - server/services/question_selection.py（_load_candidate_rows lines 219-244）
    - server/api/admin/positions.py（get_todos lines 11-33）
  </read_first>
  <behavior>
    - test_phase4_binding.py 覆盖 REF-2.5：种子 confirmed 模型（version=1）+ competency_item（带 item_id）→ 调 generate_question_bank → 断言 question_bank 每行 model_id=mid、model_version=1、item_id=对应 item_id、rubric_version='v1'、measurement_target IS NULL、evidence_requirement IS NULL
    - test_phase4_binding.py 覆盖 REF-3.4 判重键：种子 v1 生成题库 → 再 confirmed v2（新 model_id + version=2）→ generate_question_bank → 断言 v2 行（model_version=2）存在且非空（不因 v1 active 行跳过）
    - test_phase4_binding.py 覆盖 REF-3.4 消费侧：建 v1 题库 + v2 confirmed（未生成）→ check_session_readiness(v2 model) 返回 QUESTION_BANK_INCOMPLETE；selection 只取 model_version 匹配行
    - test_phase4_fail_visible.py 覆盖 REF-8.4：种子 confirmed 模型 + question_bank_task 行 status='FAILED' error_msg='LLM 解析异常堆栈...' → check_session_readiness 返回 error_code='QUESTION_BANK_INCOMPLETE' 且 detail 含截断 error_msg；GET /api/admin/todos 返回 question_bank_failed 列表含 position_id/model_id/model_version/error_msg 且 question_bank_not_ready 为 int
  </behavior>
  <action>
    新建两个测试文件，严格沿用 test_phase2_selection.py 三件套：文件顶部在 import server 之前设 os.environ["DB_PATH"]=tempfile 路径、os.environ["LLM_PROVIDER"]="mock"、os.environ["JWT_SECRET"]="test-secret"，sys.path.insert 仓库根，import TestClient/config/init_db/get_conn/app/new_id/now_iso，显式 init_db()（TestClient 不触发 startup）。写 _q 只读查询 helper（开连接→读→close，防 SQLite 单写锁）。种子函数：active position + confirmed competency_model（model_json 含 position_id/version/items）+ competency_item 行，复用 test_question_bank.py 的 _seed_model 模式。断言目标按 behavior 列出的列名/error_code/响应键（model_id/model_version/item_id/rubric_version/measurement_target/evidence_requirement/QUESTION_BANK_INCOMPLETE/question_bank_failed）写死。此文件当前运行必红（现有落库不写 model_id/model_version/item_id/rubric_version、readiness 不按版本过滤、get_todos 无 question_bank_failed），不得为求绿而放宽断言。test_phase4_binding.py 与 test_phase4_fail_visible.py 分属两文件、各自独立三件套（禁止同进程收集）。
  </action>
  <acceptance_criteria>
    - 两文件存在，且每个文件顶部含 tempfile + os.environ["DB_PATH"] + os.environ["LLM_PROVIDER"]="mock" + init_db() 三件套（grep 断言）
    - grep test_phase4_binding.py 含 "rubric_version" 与 "model_version" 与 "item_id" 断言
    - grep test_phase4_fail_visible.py 含 "QUESTION_BANK_INCOMPLETE" 与 "question_bank_failed"
    - cd server && python -m pytest test_phase4_binding.py -v 与 python -m pytest test_phase4_fail_visible.py -v 均收集成功且为 FAILED（红，非 collection error）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_binding.py test_phase4_fail_visible.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>两个测试文件存在、pytest 可收集、当前为红（断言失败而非 import/collection error），单文件单进程三件套齐全</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: question_bank.py 落库填充 model_id/model_version/item_id/rubric_version + 判重键升级</name>
  <files>server/services/question_bank.py</files>
  <read_first>
    - server/services/question_bank.py（全文——_insert_question lines 56-67 / generate_question_bank lines 88-182 / 4 处 exists lines 122-149）
    - server/db.py（question_bank DDL lines 125-152 确认列名；_migrate_question_bank_v2 lines 433-467 确认列已落）
    - server/test_phase4_binding.py（Task 1 刚建，落库绑定 + 判重键断言）
  </read_first>
  <behavior>
    - _insert_question 签名新增 keyword-only model_id: str、model_version: int | None、item_id: str | None
    - INSERT 列清单加入 model_id, model_version, item_id, rubric_version，值依次 model_id、model_version、item["item_id"]、常量 "v1"（measurement_target/evidence_requirement 不落列，留 NULL）
    - 4 处 exists 判重 WHERE 均含 model_id=? AND model_version=?（scope='position' 用 model_id/model_version 替换 position_id 谓词；scope='general' 追加 model_id/model_version）
    - generate_question_bank 签名保持 (position_id, model_id) 不变，顶部补取 model_version = SELECT version FROM competency_model WHERE model_id=?
  </behavior>
  <action>
    改 server/services/question_bank.py，不改表结构（列已由 _migrate_question_bank_v2 落位）：
    1. generate_question_bank（line 88）在 _update_task_status(conn, position_id, model_id, "RUNNING") 之前补取 model_version：conn.execute("SELECT version FROM competency_model WHERE model_id=?", (model_id,)).fetchone()，model_version = model_row["version"] if model_row else None。签名保持 (position_id: str, model_id: str) 不变（confirm_model/retry_question_bank_task 两处调用点只传这两参，A3）。
    2. _insert_question（line 56）签名加 keyword-only model_id、model_version、item_id 三参；INSERT 列清单在 scope, position_id 之后加 model_id, model_version, item_id, rubric_version（照 DDL 顺序），VALUES 对应位置传 model_id、model_version、item["item_id"]、"v1"（rubric_version 常量）。
    3. 调用点（line 165-171）传 model_id=model_id、model_version=model_version、item_id=item["item_id"]。
    4. 4 处 exists 判重键（lines 122-149）升级：scope='position' 两个分支把 WHERE 中的 position_id=? 替换为 model_id=? AND model_version=?（参数由 position_id 换为 model_id, model_version）；scope='general' 两个分支在 WHERE 追加 AND model_id=? AND model_version=?（参数追加 model_id, model_version）。四处的 WHERE 最终都含 model_id=? AND model_version=? 且保留 std_name/category/status='active'（position 分支含 difficulty，general 无难度维度分支不含）。
    保持逐 item conn.commit()（line 172）不动——「先 commit 再调 LLM」模式不破坏，不新增 commit 点、不新增 LLM 调用。
  </action>
  <acceptance_criteria>
    - grep -n "model_id, model_version, item_id, rubric_version" server/services/question_bank.py 命中 _insert_question 的 INSERT 列清单（grep -v '^#' 过滤注释）
    - grep -c "model_id=? AND model_version=?" server/services/question_bank.py 为 4（4 处 exists 判重，grep -v '^#'）
    - grep -n "model_version = model_row" 命中 generate_question_bank 顶部取 version
    - cd server && python -m pytest test_phase4_binding.py -v 中「落库绑定」与「判重键升级」断言转绿（其余消费侧断言可仍红，属 Task 3）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_binding.py -v 2>&1 | tail -40</automated>
  </verify>
  <done>generate_question_bank 落库每行写 model_id/model_version/item_id/rubric_version="v1"，4 处 exists 判重键含 model_id+model_version，binding 测试的落库/判重断言转绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 消费侧收紧（readiness + selection）+ 失败明细（positions）+ test_phase2_selection.py 种子绑定</name>
  <files>server/services/readiness.py, server/services/question_selection.py, server/api/admin/positions.py, server/test_phase2_selection.py</files>
  <read_first>
    - server/services/readiness.py（两 count helper lines 16-37 / task 查询 lines 95-105 / tier LEFT JOIN lines 132-141 / 调用点 lines 108-109）
    - server/services/question_selection.py（_load_candidate_rows lines 219-244 / 调用点 line 299）
    - server/api/admin/positions.py（get_todos lines 11-33）
    - server/test_phase2_selection.py（_seed_question_bank lines 93-125 / test_required_exception 的 _add lines 374-383）
    - server/test_phase4_binding.py + server/test_phase4_fail_visible.py（Task 1 断言）
  </read_first>
  <behavior>
    - readiness 两 count helper 与 tier LEFT JOIN 三处 WHERE 均加 AND model_id=? AND model_version=?（参数同源取自 model["model_id"] / model["version"]）
    - readiness task 查询 SELECT status 改为 SELECT status, error_msg，新增 FAILED 分支：status='FAILED' → 返回 QUESTION_BANK_INCOMPLETE + detail 附 error_msg[:200]
    - _load_candidate_rows 签名加 model_version，line 233 子句收紧为 b.model_id=? AND b.model_version=?（去 NULL 放行），调用点补 session["model_version"]
    - get_todos 保留 question_bank_not_ready（int），新增 question_bank_failed 明细列表
    - test_phase2_selection.py 种子写 model_id + model_version 绑定 confirmed 模型
  </behavior>
  <action>
    改三处源码 + 一处回归测试：
    1. server/services/readiness.py：
       a. _question_count_by_category（line 16）与 _covered_std_names（line 30）签名各加 model_id: str, model_version 两参；WHERE 在 status='active' 邻位加 AND model_id=? AND model_version=?，参数补 model_id, model_version。
       b. 调用点（lines 108-109）改为 _question_count_by_category(conn, position_id, model["model_id"], model["version"]) 与 _covered_std_names(conn, position_id, model["model_id"], model["version"])。
       c. tier LEFT JOIN（lines 132-141）WHERE 加 AND qb.model_id=? AND qb.model_version=?（放在 ci.model_id=? 邻位），参数由 (model["model_id"], category, position_id) 改为 (model["model_id"], model["version"], category, position_id)。
       d. task 查询（line 96）SELECT status 改为 SELECT status, error_msg；在 QUEUED/RUNNING 分支（lines 101-103）之后新增显式 FAILED 分支：task 非 None 且 task["status"] == "FAILED" → detail = "该岗位题库生成失败，不可开考"，若 task["error_msg"] 非空则 detail += f"（{task['error_msg'][:200]}）"，返回 {"error_code": "QUESTION_BANK_INCOMPLETE", "detail": detail}。替换 line 104 的注释占位。
    2. server/services/question_selection.py：_load_candidate_rows（line 219）签名改为 (conn, position_id, model_id, model_version)；line 233 子句 "(b.model_id IS NULL OR ? IS NULL OR b.model_id=?)" 收紧为 "b.model_id=? AND b.model_version=?"，参数由 (model_id, position_id, model_id, model_id) 改为 (model_id, position_id, model_id, model_version)。调用点（line 299）改为 _load_candidate_rows(conn, session["position_id"], model_id, session["model_version"])。
    3. server/api/admin/positions.py get_todos：保留 question_bank_not_ready int（lines 25-27 不动）；新增 question_bank_failed 查询——conn.execute("SELECT position_id, model_id, model_version, error_msg FROM question_bank_task WHERE status='FAILED'").fetchall()，转为 [dict(r) for r in rows]；返回 dict 加 "question_bank_failed" 键（error_msg 保持 task 行原值，入库时已 str(e)[:200] 截断，此处不额外截断、不去重、不滤空）。前端 Positions.vue 零改动（D-51 对未知键安全）。
    4. server/test_phase2_selection.py 回归：_seed_question_bank（line 93）签名加 mid 参并向下传，_add 的 INSERT 加 model_id, model_version 两列（值 mid 与 1），所有调用点 _seed_question_bank(pid) 改为 _seed_question_bank(pid, mid)；test_required_exception 的 _add（line 374）INSERT 列加 model_version（值传当前模型 version=1，cm_other 排除案例改传 model_version 为另一值保持「他人版本候选排除」语义）。不改任何断言语义。
    禁止：在 readiness 里改旧题 status 实现升版失效（D-48 旧题保持 active 靠过滤）；禁止只加 model_id 不加 model_version（D-50 两列都要）；禁止改动 main.py。
  </action>
  <acceptance_criteria>
    - grep -c "model_id=? AND model_version=?" server/services/readiness.py 为 3（两 count + tier LEFT JOIN，grep -v '^#'）
    - grep -n "SELECT status, error_msg" server/services/readiness.py 命中 task 查询；grep -n "QUESTION_BANK_INCOMPLETE" server/services/readiness.py 命中 FAILED 分支
    - grep -n "def _load_candidate_rows" server/services/question_selection.py 签名含 model_version；line 233 附近不再含 "IS NULL" 版本放行（grep -v '^#'）
    - grep -n "question_bank_failed" server/api/admin/positions.py 命中 get_todos 返回
    - cd server && python -m pytest test_phase4_binding.py test_phase4_fail_visible.py -v 全绿
    - cd server && python -m pytest test_phase2_selection.py -v 全绿（回归）
    - cd server && python test_question_bank.py 全 PASS（脚本式回归，判重键升级后不因同版本重跑跳过）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huaxinzhang/Desktop/trifles/26-summer-sem/server && python -m pytest test_phase4_binding.py test_phase4_fail_visible.py test_phase2_selection.py -v 2>&1 | tail -50 && echo "=====test_question_bank=====" && python test_question_bank.py 2>&1 | tail -30</automated>
  </verify>
  <done>四地消费侧 WHERE（readiness 三处 + selection 一处）全部 AND model_id=? AND model_version=? 且参数同源；FAILED 分支显式返回 QUESTION_BANK_INCOMPLETE + error_msg[:200]；get_todos 含 question_bank_failed；新增 + 既有 test_phase2_selection/test_question_bank 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→get_todos | 管理员调用待办端点，返回 question_bank_failed 明细（含 error_msg） |
| candidate→create_session | 候选创建会话时 readiness detail 可携带题库生成 error_msg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | readiness.py FAILED detail / positions.py question_bank_failed error_msg | mitigate | 复用 generate_question_bank 已落库的 str(e)[:200] 截断口径；readiness detail 只拼接 task["error_msg"][:200]，不附原始异常对象/堆栈/内部路径 |
| T-04-02 | Elevation of Privilege | get_todos（admin 端点） | accept | 已在 router 级 dependencies=[Depends(require_admin)] 下（positions.py line 8），本 phase 不新增权限面、不改权限模型（ASVS V4 现有控制） |
| T-04-03 | Tampering | 消费侧 model_version 过滤被绕过（NULL 放行残留） | mitigate | D-50 锁定四地 WHERE 全部 AND model_id=? AND model_version=? 且去 NULL 放行；grep 断言无 "IS NULL" 版本放行残留 |

注：本 phase 无 npm/pip/cargo 新增依赖（纯 Python 改造，复用 fastapi/pydantic 2.10.3/sqlite3 已装），Package Legitimacy Gate 不适用，无 [ASSUMED]/[SUS] 包、无 slopcheck 检查点。
</threat_model>

<verification>
- 新增测试：cd server && python -m pytest test_phase4_binding.py test_phase4_fail_visible.py -v 全绿
- 回归：cd server && python -m pytest test_phase2_selection.py -v 全绿；python test_question_bank.py 全 PASS
- 源码断言：grep -c "model_id=? AND model_version=?" readiness.py==3、question_selection.py==1、question_bank.py==4（grep -v '^#'）

回归面声明（供硬关口 A 呈报，非本 plan 修复项）：消费侧收紧后，以下既有的「直插 question_bank 且不写 model_id/model_version 种子」的会话类测试会因 readiness 计数归零而失败，属 Phase 4 收紧的自然涟漪，需硬关口 A 裁决是「追加 04-03 种子绑定回归计划」还是「并入 Phase 6 M1 回归/测试收口」：test_m5_backend.py、test_m6_backend.py、test_m7_backend.py、test_p0_chain.py、test_p0_security.py、test_phase2_difficulty.py、test_phase2_interview.py、test_phase2_scoring.py、test_phase3_forms.py、test_phase3_sse.py、test_phase3_timer.py、test_phase3_idempotency.py、test_phase3_misc.py（其 _seed_question_bank / 直插 question_bank 语句需写 model_id/model_version 绑定 confirmed 模型）。
</verification>

<success_criteria>
- question_bank 每行携带 model_id/model_version/item_id/rubric_version="v1"（落库侧）
- 升版后旧题库失配、新版本未生成时 readiness 返回 QUESTION_BANK_INCOMPLETE 阻止开考（REF-3.4）
- selection 只取 model_version 匹配题（去 NULL 放行）
- 生成失败 readiness 返回 QUESTION_BANK_INCOMPLETE + error_msg[:200]；get_todos 含 question_bank_failed 明细（REF-8.4）
</success_criteria>

<output>
Create .planning/phases/04-question-bank-version/04-01-SUMMARY.md when done
</output>
