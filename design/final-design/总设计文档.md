# AI 驱动的岗位胜任力测评与人才画像系统 — 总设计文档

- 版本：**v2.0**（2026-09-02，基于 checkpoint《项目全貌与设计收敛状态》§21–25 与第八~十轮讨论确认全面重写）
- 状态：**单一事实来源（SSOT）**。本文是全系统设计、重构、实施与验收的唯一权威。
- 取代关系：本文取代 2026-08-30 版总设计文档（已移入 `design/final-design/历史档案/`，仅供追溯）。与旧版冲突处一律以本文为准。
- 实施方式：**基于现有代码重构演进，不重写**。旧版中与本文不冲突的已实现细节（模块一流水线、报告五段式、部署形态等）继续有效；实施时不需要查阅任何旧文档。
- 分模块文档：本文按模块拆分为四份分块摘录（同目录《模块一设计…》~《模块四设计…》），聚焦单模块阅读；变更一律以本文为准。
- 上游需求：《需求文档-胜任力测评与人才画像系统》《技术方案概述》。
- 维护规则：任何设计变更、范围调整、接口改动，**先更新本文（正文 + §14 变更日志），再动代码**。

---

# 第一部分 总览与全局约束

## 1. 系统定位与端到端链路

「AI 驱动的岗位胜任力测评与人才画像系统」：把非结构化 JD 转化为可测量的**岗位胜任力模型**（尺子），基于尺子对候选人进行**有界动态测评**，产出**立体人才画像**（读数），并通过**测试闭环**保证全链可审计、可回溯、有评测标准。

```
JD 文本 ──► 胜任力模型（模块一）──► 有界动态测评（模块二）──► 人才画像/评分（模块三）
                                                                      │
                                            测试闭环（模块四）◄────────┘
                                       可审计 / 反馈可回溯 / 测评标准（可迁移）
```

## 2. 系统范围（v2.0 收敛，不可扩张解读）

**系统做：**

- 候选人测评的人才画像报告；
- 各能力项评分与证据展示；
- 测评过程、状态与异常记录；
- 必要的人工复核支持。

**系统不做：**

- 最终录用判断；
- 最终录用排序；
- 自动通过/淘汰；
- 代替企业作出最终招聘决定。

任何界面、报告文案、Prompt 不得出现"录用结论""排名"等表述。qualification gate 只表达"资格事实核验结果"，报告警告只表达"测量完整性/资格信息提示"。

## 3. 四条强约束（全系统不可谈判）

| # | 约束 | 落点 |
|---|---|---|
| ① | **LLM 不碰数字** | 权重、分数算术、配额、题量、聚合、补算全部由代码计算 |
| ② | **人工是唯一权威** | confirmed 模型不被静默覆盖；评分可异议但绝不自动改分；报告发布必须人工明确点击 |
| ③ | **一切留痕** | 每道工序中间产物落库；每次 LLM 调用 trace 落库；答案原文不可变归档；状态事件 append-only |
| ④ | **代码是唯一状态机** | 难度升降、追问上限、题量、finish、工具授权等关键迁移全部由代码执行；LLM 只提供结构化观察与建议 |

## 4. 有界测评循环（模块二运行时总纲）

不采用自由 ReAct。每轮执行：

```
Observation → Policy/Plan → Act → Evaluation → Persist
```

- **Observation**：LLM 可参与回答状态分类、证据观察（结构化输出，代码裁决）；
- **Policy/Plan**：代码依据状态、配额、难度护栏、题量裁决下一步；
- **Act**：受限话术或允许的交互动作（表单、脚手架等）；
- **Evaluation**：代码判断证据是否充分、是否覆盖 measurement target；
- **Persist**：状态、证据、版本、事件同事务持久化。

LLM 永远不能决定：题量、追问上限、finish、难度最终迁移、权重、综合题数量、最终分数、报告发布。

## 5. 技术栈与部署形态

| 层 | 选型 |
|---|---|
| 后端 | Python 3.11+ / FastAPI / Uvicorn，单进程 |
| LLM 客户端 | `openai` SDK（base_url 可切 DeepSeek 等 OpenAI 兼容端点），JSON 模式 + 内部 function-call 式 adapter |
| 存储 | SQLite 单文件（单机演示/单实例），DDL + 迁移内嵌 `server/db.py`，含 schema_version 演进 |
| 鉴权 | python-jose(JWT HS256) + passlib[bcrypt]；单一 secret；HttpOnly/SameSite cookie 方向 |
| 前端 | `web-next/`（唯一活跃）：Vue 3 + Vite + Vue Router + Pinia + axios；UI 组件（ui/）与报告雷达图为自研（SVG），不依赖 Element Plus / ECharts。旧 `web/`（Element Plus + ECharts）已退役留档，不再改动 |
| 部署 | vite build 产物（`web-next/dist`）由 FastAPI 挂静态文件，单进程 uvicorn；mock 模式离线可跑通全流程 |

**本期目标为演示上线**：单机、单实例、单进程。接受 `BackgroundTasks` 内存执行，不要求进程重启后任务恢复（后续优化记录：未来有并发/可靠性需求再评估持久化 job 表、worker 或队列）。迁移/回滚/备份恢复功能需要开发，但不作为本期上线硬门槛。

## 6. 全局数据库清单

全库 **28 张物理表 = 27 张业务表 + 1 张 schema_version 迁移注册表**（以 `server/db.py` 全部 CREATE TABLE 实测为准，2026-09-06 收口盘点 26 张；2026-09-07 新增 `aggregate_task`（§8.4）与 `evidence_exclusion`（§8.5）后为 28，实施随代码落地；迁移事务内临时表 `llm_trace_new / feedback_new` 重命名后不残留，不计）：

- 模块一（10）：`user / position / position_alias / jd_record / competency_model / competency_item / competency_dict / llm_trace / aggregate_task / evidence_exclusion`
- 模块二（13）：`assessment_session / question_bank / question_bank_task / assessment_question / assessment_message / context_raw / form_submission / form_instance / assessment_state_event / question_score / session_time_intervals / idempotency_record / bad_case_candidate`
- 模块三（2）：`report / feedback`
- 模块四（1）：`eval_results`
- 公共（1）：`trace_link`
- 迁移注册表（1）：`schema_version`

合计 10+13+2+1+1+1 = 28。详细字段见 §7（模块一沿用现状）、§8.4（aggregate_task）、§8.5（evidence_exclusion）、§9.5（question_bank_task）、§12（模块二三演进）、§13.4（事件与 trace）。

## 7. 权限模型（P0 上线阻断项）

- 全部候选人资源接口（session/report/form/feedback）必须做**资源级所有权校验**：`WHERE resource.user_id = current_user.id`，不能只查 ID 存在；
- candidate 只能访问本人资源；admin 为最高权限（本期不分级），可读取候选人数据与完整 trace；
- 角色限制必须在后端执行，不能依赖前端 route guard；
- 测评入口统一要求：position `active` + 模型 `confirmed`；
- 越权测试矩阵（candidate↔candidate 读写、admin 边界）为上线阻断测试。

---

# 第二部分 模块一：JD 解析与胜任力模型构建（已实现，重构时保持）

## 8. 流水线（六工位，现状沿用）

```
①接入(粘贴/JSONL) → ②清洗(纯规则) → ③抽取(LLM#1) → ④归一消歧(词典+LLM#2)
→ ⑤聚合(代码频次+LLM#3裁决+代码算权重) → ⑥人审(审核页确认升版本)
```

- 状态机：`imported → parsing → parsed/failed → aggregating → draft/stalled → confirmed`；confirmed 后新增 JD/重解析走 diff 审阅流升 v{n+1}；
- 标题源优先：JSONL 行内 `job_title`（兼容 `position` 键名）入库即存 `jd_record.job_title`；pipeline 与 reparse 不覆写已存标题，仅库内标题为空时以 LLM#1 抽取兜底；
- 归岗：取标题（上述源标题优先）→ 规范化（去空格/大小写/常见后缀）→ position 精确匹配（`COLLATE NOCASE`，大小写不敏感；存量岗位名不迁移）→ 别名表 → 未命中建 `pending_review` 岗位（人工审激活；不聚合、不对测评端可见）；
- 异步：导入/聚合后台任务 + 前端轮询（聚合任务进度反馈见 §8.4）；聚合自动触发钩子独立于解析异常处理。

## 8.1 工序实现约束（沿用并需补测试）

| 工序 | 要点 |
|---|---|
| ② 清洗 | 纯规则切段；要求块空或 <30 字 → `low_confidence=1` 但继续流程 |
| ③ 抽取 | LLM#1 JSON 强 Schema；校验失败带错误重试 ×2 → failed |
| ④ 消歧 | 词典候选 top10（编辑距离/拼音）→ LLM#2 裁决；失败重试 ×2 → 降级代码去重；词典为空跳过 LLM#2 |
| ⑤ 聚合 | 代码频次（r/req/occ——req 为**条件口径**：「标 required 的 JD 数 ÷ 该能力**出现**的 JD 数」，绝对口径废弃）→ importance 三档映射：`required ⇔ 条件req ≥ REQ_THRESHOLD 且 r ≥ REQ_MIN_OCCURRENCE_RATIO 且 出现 JD 数 ≥ REQ_MIN_OCCURRENCE`（仅 hard_skill 类目可判 required，soft_skill 上限 preferred——2026-09-07 裁决）；`preferred ⇔ 未达 required 且 出现 JD 数 ≥ PREFERRED_MIN_OCCURRENCE`；否则 `plus` → level 冲突交 LLM#3（无自动众数后门）→ 权重纯代码；LLM#3 重试 ×2 仍败 → `stalled`。required 三重判据各职责：条件 req 修正单 JD 内 LLM#1 措辞泛滥（实测单 JD 80 %+ 项标 required，绝对口径下被稀释至 22 岗均值 3.5 项）；r≥0.25 保证岗位共识而非孤证；occ≥3 是统计证据下限。裸条件口径（仅换分母）已实测否决——图像算法岗 271/306 项 required 泛滥。`_collect_items` 滤除 evidence_exclusion active 行（**全排除的 JD-项不计 r/req 分子**，分母不变——§8.5）。preferred occ 基准（2026-09-08 裁决）：绝对 r 阈值与样本量耦合致 34 模型中 15 个 preferred=0（低样本岗 r 恒不可达 0.5，中段 [0.25,0.5) 全被判 required 无剩余人口）——改 occ≥3 统计语义与 required 双门槛同基，15→11 模型消除、SLAM 10/0/153→10/10/143（三档 17/10/74,Σw 9.9%）、算法岗 plus 池 Σw 97.8%→65.8%；R_THRESHOLD 退役（required 侧 r≥0.25 保留，职责不重叠）。**gate qualification facet 打标与断言等价合并（2026-09-08，§16.1 下游渲染消费）**：分组产物中的 gate qualification items 先做 facet 分类（确定性纯函数：关键词词表 + 内嵌数字解析，无 LLM），打标落 `competency_item.facet_key / facet_params_json`（两 nullable 列）；随后对**有序 facet 且派生参数一致**的组做断言等价合并（一组一行，std_name 取更窄措辞，evidences/jds/req_jds 并集；复合断言如「211硕士及以上学历」横跨学历+院校两轴、专业范围类、长尾未分类项**不参与**），合并先于 LLM#3 分档（被合并项不进其视野）。语义边界：只合「同断言不同措辞」（「本科及以上学历」与「全日制本科学历及以上」），不处理「跨 JD 词面近重复」（词典第 3 层管辖，冻结裁决有效）；存量模型 facet_key 为 NULL → 渲染端整组降级勾选组（§16.1 fallback），已 confirmed 模型不回填不重打标，岗位重聚合刷版本自然带上 |
| ⑥ 人审 | PUT 编辑草稿 → confirm 升版本；编辑 stalled 模型后自动转 draft 并清 stall_reason。证据可标记排除/恢复（语句粒度软排除+留痕，§8.5；前端仅 draft/stalled 可操作） |

**M1 回归测试为后续动态测评实施前的硬前置**：清洗边界、抽取 schema 异常、消歧、权重尾差（Σ=1，尾差由权重最大项吸收）、等级冲突 stalled、confirmed 不可静默覆盖、版本 diff、管理员权限。

## 8.2 权重口径（v2.0 关键修正：普通题类目比例改为 7:3）

**第一层（类目间，模块一聚合时使用）：**

```
hard_skill : soft_skill : experience : qualification = 0.70 : 0.30 : 0 : 0（评分能力项）
experience/qualification 走表单事实采集，不占类目权重池，gate 二值判定
```

普通评分能力项满足：

```
Σ hard_skill item.weight = 0.70
Σ soft_skill item.weight = 0.30
```

若某大类无有效能力项，现有大类归一到 1.00；若大类有 item 但无合法题库 → 阻止开考（见 §10.4），不静默转移权重。

**第二层（类内）：** 各项按 importance 系数分摊（required 1.0 / preferred 0.6 / plus 0.3），合成结果存 `competency_item.weight`。模块三算总分**直接复用 item.weight，不再二次乘大类比例**。

**tier 语义（v2.0 确认）：** required/preferred/plus 影响原始重要性、题量配额和覆盖优先级；**不再额外乘最终分数**。

## 8.3 关键原则

- gate 项（qualification 全部 + experience 年限项）二值判定，不进 1–5 评分；
- 人工唯一权威：confirmed 模型不被静默覆盖；分数是历史事实；
- 每道工序中间产物（raw_items/std_items/cleaned_text）落 jd_record 可查；
- 能力词典独立管理；被引用条目停用/合并而非删除。

## 8.4 聚合任务可观测（2026-09-07 新增）

聚合为后台任务，过程中**无中间状态落库**（模型行在循环结束后一次性写入），前端原先只能轮询「模型 404→200」二态感知，存在卡点不可见、超时误报问题。本节定义聚合任务的**进度反馈与全程可追溯**契约。

**表 `aggregate_task`**（聚合任务记录，每次触发一行，不清理）：

| 字段 | 类型 | 说明 |
|---|---|---|
| task_id | TEXT PK | `agg_task_` 前缀 |
| position_id | TEXT NOT NULL | 归属岗位 |
| status | TEXT NOT NULL | `RUNNING / SUCCEEDED / FAILED`（不设 QUEUED：BackgroundTasks 触发即执行、无排队阶段） |
| trigger_source | TEXT NOT NULL | `manual / retry / auto:jd-parse`（三条触发路径可区分） |
| total / done | INTEGER | 能力项总数（循环前写入）/ 已处理数（循环内逐项 +1） |
| llm_total / llm_done | INTEGER | 需 LLM#3 裁决的冲突项数（纯代码预计算）/ 已裁决数 |
| current_item | TEXT | 正在处理的 `(std_name, category)`（liveness 信号） |
| model_id | TEXT | 聚合产物 competency_model id（模型行落库时回填——含 stalled 产出行，保溯源链完整） |
| error | TEXT | FAILED 时停滞/异常原因 |
| created_at / started_at / finished_at | TEXT | 触发/起/止时间 |

**进度端点**：`GET /admin/positions/{position_id}/aggregate/progress`——按岗位查**最新一条任务行**，无记录 404。响应含上表全部进度字段。

**行为规则**：

- 记录位置在 `run_aggregate` 内部（非 endpoint 层），pipeline 内联自动聚合路径一并覆盖；
- **双计数进度**：done/total 是条目进度（非冲突项毫秒级、可能跳变），llm_done/llm_total 与真实耗时同步——前端应同时展示两者；
- stalled 并入 `FAILED + error`（进度记录不复刻模型级 stalled 语义，失败行永久可查）。设计差异登记：§8 状态机的 `stalled` 指**模型产物**状态（模型已落库但标记不完整），本表 FAILED 指**任务运行**状态，两者并存不冲突；
- 进程重启 → 启动时将 RUNNING 残留行一次性置 FAILED（error=进程重启中断）；BackgroundTasks 后台任务同死于重启，语义一致。
- 前端停机判定依据 progress 端点心跳（行不变 N 轮报停滞），替换现行 3 分钟硬超时误报。

**溯源链**（迭代修复）：`aggregate_task(task_id) → competency_model(model_id) → llm_trace(ref_id=model_id, call_type=aggregate_level)`——从失败聚合可一路钻取到当时 LLM#3 逐次裁决原文。

**前端联动**：`ModelReview.vue` 触发后轮询对象改 progress 端点；进入页面时若已有 RUNNING 行即认领恢复（任务生命周期与页面组件解耦——离开页面不中断、不报错，后台照常跑完）。展示形态：进度条 + `done/total` + `llm_done/llm_total` + 当前项名。

---

## 8.5 证据排除（可标记、可恢复、重聚合生效，2026-09-07 新增）

管理员在模型审核页（工序⑥）审查聚合证据时，可将单条证据摘录标记为**已排除**（软标记，不删数据，操作人/时间/原因留痕，可解除再恢复）。排除是**源头治理**动作：作用于「岗位 × JD × (std_name, category) × 证据原文」粒度，独立于模型版本持久存在，重聚合不丢失，对新版本生效。

**表 `evidence_exclusion`**（证据排除记录，岗位维度持久，不随聚合重建）：

| 字段 | 类型 | 说明 |
|---|---|---|
| position_id / jd_id | TEXT NOT NULL | 归属岗位 / 证据来源 JD |
| std_name / category | TEXT NOT NULL | 消歧后标准名 + 类目（判重口径与 competency_item 对齐） |
| text | TEXT NOT NULL | 被排除的证据摘录原文（语句粒度判重键成分） |
| reason | TEXT | 排除原因（管理员填写，可空） |
| excluded_by / excluded_at | TEXT NOT NULL | 排除操作人 / 时间 |
| status | TEXT NOT NULL | `active`（生效中）/ `lifted`（已解除）——解除不删行，审计可查 |
| lifted_by / lifted_at | TEXT | 解除操作人 / 时间（可空） |
| PRIMARY KEY | | `(position_id, jd_id, std_name, category, text)` |

**生效规则**（三条，`run_aggregate` 的 `_collect_items` 单点收口，下游 LLM#3/occurrence/权重全部继承）：

1. **证据级**：被排除摘录不进入新版本的模型快照 evidence、LLM#3 等级裁决 prompt、题库生成岗位背景摘录；
2. **频次级（2026-09-07 用户裁决：全排除才影响）**：某 JD 对某 (std_name, category) 的**当期证据全部**被排除（原有证据非空且无剩余）→ 该 JD 不计入该项 r/req 分子（分母 total_jds 口径不变）→ importance 可能降档 → 类内权重重算；部分排除不影响 r/req；原有证据为空的项无可排除对象、行为不变；
3. **消项级**：某能力项在全部 JD 上的证据均被排除 → 该项从聚合产物消失，权重并入其余项（Σ=100% 不变）。

**读取与交互**：`GET /admin/positions/{id}/model` 对 draft/stalled 模型**读取时实时合并**被排除条目（每条附 `excluded: true` + 留痕字段，前端灰显/划线/可恢复，合并非版本历史快照——历史以表内审计列为准）；confirmed 版本返回入库快照不合并；`PUT /admin/models/{id}` 收到含 excluded 标记的 evidence 由后端剥离落库（存储不变量：模型存储只含未排除证据）。

**端点**（岗位作用域，require_admin）：

- `POST /admin/positions/{id}/evidence-exclusions`——body `{jd_id, std_name, category, text, reason?}`；目标须为本岗位 parsed JD 的现存证据摘录（400 校验）；幂等 upsert，最后写入者覆盖 reason/操作人；
- `DELETE /admin/positions/{id}/evidence-exclusions`——body `{jd_id, std_name, category, text}`；置 `lifted` 不删行，幂等；
- `GET /admin/positions/{id}/evidence-exclusions`——全量列表（含已解除），每行附 `evidence_matched` 实时标志：JD 重解析或词典合并改名后失配的排除据此可见，不做自动迁移。

**行为边界**：后端不因当前模型 confirmed 拒绝标记/解除（排除是版本无关的岗位级治理）；前端仅在 draft/stalled 暴露标记/恢复按钮，confirmed 只读；标记不自动触发重聚合（管理员手动「重新聚合」生效）；全排除致能力项从模型消失后，恢复经由「排除记录」列表（GET 端点）完成闭环。

---

# 第三部分 模块二：有界动态测评

## 9. 题库

### 9.1 分类树（v2.0 确认）

普通对话题库只有两个大类，每类下三层 tier：

```
hard_skill ── required / preferred / plus
soft_skill ── required / preferred / plus
```

`experience` / `qualification` **不进入普通对话题库**：qualification 只走结构化表单；experience 通过表单/简历检测；两者不占普通主问题配额。能力项以 `measurement_mode` 区分（ordinary_question / form / resume），该字段来自 confirmed 模型版本，不可由前端或 LLM 修改。**2026-09-07 裁决收口（生成侧同步）**：题库生成不为 experience/qualification 产生任何 question_bank 行——两类信息唯一采集通道为 §16 表单链；历史 scope=general 通用题为存量遗留（选题白名单本就隔离），不迁移不删除。

### 9.2 question_bank 表（演进后）

```sql
question_bank(
  question_id TEXT PRIMARY KEY,
  position_id TEXT NOT NULL REFERENCES position,
  model_id TEXT NOT NULL, model_version INTEGER NOT NULL,   -- v2.0：题库必须绑定模型版本
  item_id TEXT NULL REFERENCES competency_item,             -- 普通题必填；综合题 NULL
  question_type TEXT NOT NULL CHECK(question_type IN ('ordinary','integrated')),
  measurement_stage TEXT NOT NULL CHECK(measurement_stage IN ('ordinary','integrated_final')),
  category TEXT NULL, tier TEXT NULL,                       -- 普通题必填
  difficulty TEXT NULL CHECK(difficulty IN ('easy','medium','hard')),  -- 综合题 NULL
  qtype TEXT NOT NULL CHECK(qtype IN ('objective','subjective')),
  stem TEXT NOT NULL, answer_key TEXT,
  rubric TEXT, rubric_version TEXT NOT NULL,
  measurement_target TEXT NOT NULL,                        -- v2.0：测量目标
  evidence_requirement TEXT,                                -- v2.0：证据要求
  observable_level_max INTEGER NOT NULL,                    -- v2.0：默认 easy=3/medium=4/hard=5
  observable_level_min INTEGER NOT NULL,                    -- v2.0：默认 easy=2/medium=3/hard=4
  equivalence_group_id TEXT NULL,                           -- v2.0：人工批准等值组
  integrated_bindings_json TEXT NULL,                       -- v2.0：综合题多 item 绑定快照
  chain_key TEXT, chain_seq INTEGER,
  source TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active',
  created_at TEXT NOT NULL
)
```

- **题目属于岗位 + confirmed 模型版本**；模型升版必须生成/绑定新题库，否则阻止开考；
- 综合题 `question_type='integrated'`，`integrated_bindings_json` 为结构化绑定列表，每项含 `item_id / measurement_target / rubric_mapping / evidence_requirement`，在题库发布与实例化时校验并存不可变快照；本期不建关系表；
- 综合题不编码为 `difficulty=integrated`（正交两字段：question_type + ordinary_difficulty）；
- **有效题目定义**：`status='active'` 且 model/version 匹配、难度题型合法、未标记无效。
- **旧版题库归档**（2026-09-08 新增）：`status` 增枚举值 `'archived'`（与 `'active'`、eval 工具占位题 `'eval_seed'` 并存，代码校验、不加 DB CHECK）——语义为「被同岗位**更新 confirmed 模型**的题库取代、保留行以支撑追溯」。规则：①归档单元 = 模型（model_id），谓词为「同岗位存在更新 confirmed 模型」；②归档在新模型 confirm 事务内执行（helper 单点：`archive_superseded_banks(position_id)`）；③`model_id` 被 `in_progress` 会话引用时**豁免**（动态派题不断粮——豁免只认 in_progress，已终态会话的追溯不依赖 active，见下条）；④会话到终态（completed/abandoned，含 6h sweep）时补刀归档；⑤retry 端点对「岗位已存在更新 confirmed 模型」的 FAILED 任务返回 409（防归档模型重跑复活 active 行）。**archived 不改变任何按 `bank_question_id` 直连的 join 语义**：评分、报告、追问、答卷展示均不读 status，已归档版本的在途答卷、评分、报告、追溯不受影响（by-design 不变量，回归测试守恒）。归档**不延迟、不依赖**新库生成结果——新会话可用性由 model/version 绑定与 §10.4 readiness 决定，与归档正交。

### 9.3 等值备用题（v2.0 确认）

第一阶段只支持**人工批准的显式等值组**：同组强制一致 `item_id / difficulty / measurement_target / rubric_version / evidence_requirement / 权重语义`；配 `equivalence_status(pending/approved/rejected) + approved_by + approved_at`。不同难度的替代题标记为 `difficulty_alternative`，不入等值组；综合题与普通题之间不建立等值关系。

### 9.4 难度与 1–5 能力等级映射（v2.0 最终确认）

| 难度 | observable_level_max | rubric 最低锚点（level_min） | 锚点区间 |
|---|---:|---:|---|
| easy | 3 | 2 | [2, 3] |
| medium | 4 | 3 | [3, 4] |
| hard | 5 | 4 | [4, 5] |

评分语义：

- 作答达到最低锚点：在区间内评分；
- 有效作答但低于最低锚点：支撑**等级 1**（区别于拒答/缺证据）；
- 未形成有效观察（拒答、缺证据、题目无效、系统错误、模型不确定）：**不产生能力等级证据**；
- rubric 可将单题上限配低，不允许配超过所属难度默认上限；
- 等级 5 只能由 hard 题 5 级锚点、证据完整且稳定的测量支撑；
- `required_level` 只用于难度路径决策与报告达标比较（gap=required−actual 可解释），不改变 item.weight、不改变评分；
- **难度不构成最终分数第三层权重**。

### 9.5 题库生成任务可观测（2026-09-08 新增）

题库生成同为 BackgroundTasks 后台任务，现行实现任务行只有入口 RUNNING 与终态两个写入点：生成中无中间状态落库、失败仅存 `str(e)[:200]` 单值截断错误、无任务查询端点（前端可见性仅 `/admin/todos` 聚合计数与最新一条失败摘要）、进程重启后 RUNNING/QUEUED 残留行无收尸（且 retry 仅接受 FAILED、confirm 对已确认模型恒 409，残留即该岗位永久锁死「生成中」）。本节为 `question_bank_task`（§6 既有登记，D-12 代码先行，本节补字段级契约）定义进度反馈与全程可追溯，形态对齐模块一 §8.4 先例。

**表 `question_bank_task` 增列**（存量库启动自动补列，幂等迁移；既有列 task_id / position_id / model_id / model_version / status / created_at / started_at / finished_at / error_msg 不变）：

| 新列 | 类型 | 说明 |
|---|---|---|
| total / done | INTEGER | 计划生成的（item × 难度档）总数（循环前写入）/ 已完成数（含幂等跳过的档，循环内逐档 +1） |
| current_item | TEXT | 正在处理的 `(std_name, category, difficulty)`（liveness 信号） |

与 §8.4 `aggregate_task` 的**有意差异**（登记）：①保留 QUEUED 态——confirm 事务先插行再调度后台任务（三态在后台异常前可查；聚合触发即执行故不设）；②已有 model_version 列（题库单元 = 岗位 + confirmed 模型版本，§9.2）；③触发路径仅 confirm / retry 两路，不设 trigger_source。

**端点契约**（admin）：

| 端点 | 语义 |
|---|---|
| `GET /admin/question-bank-tasks` | 按（岗位 × 模型）最新任务行的列表：岗位名、model_version、status、done/total/current_item、error 摘要、题量（关联 question_bank 计数）+ `archived_count`（该模型 archived 行计数——归档后主计数归零仍可辨明「未丢题」）、`bank_status`（派生字段：使用中/已归档，按该 task 行 model 旧版题库行状态）、时间戳、task_id；服务端分页 + status 过滤 + `bank_status` 过滤 |
| `GET /admin/question-bank-tasks/{task_id}` | 单任务详情：全字段 + 同（岗位 × 模型）历史任务行（retry 保留旧行审计）+ llm_trace 时间窗调用列表；该模型已整体归档时 item × difficulty 覆盖统计按不过滤 status 口径重算（归档版本照常可看） |
| `GET /admin/question-bank-tasks/{task_id}/questions` | 该题库题目表：std_name / category / difficulty / qtype / stem 预览，分页；该模型已整体归档时 queryset 放开 status 过滤（active+archived 全量分页）、题目行增 status 列 |

**行为规则**：

- 进度更新在 `generate_question_bank` 内部：循环前写 total，循环内逐档 UPDATE done/current_item、与该档题目写入同事务 commit（逐次 commit 防轮询连接长持锁——§8.4 已论证同型问题）；
- 启动 sweep：进程重启时 `init_db` 将 RUNNING/QUEUED 残留行一次性置 FAILED（error=「进程重启中断」），与 §5「不要求进程重启后任务恢复」一致——不自动重启，人工 retry 补跑；
- error_msg 截断放宽 `str(e)[:200]` → `[:2000]`；存量短行不回填；
- llm_trace 关联不改表：按任务时间窗（created_at～finished_at）× `call_type='question_gen'` × 该模型 item 集（ref_id=item_id）过滤，prompt / response / error / attempt 逐次可溯；
- 列表以任务行驱动：「有题无任务行」的 D-12 前遗留组合不入列表（2026-09-08 核验当前库为 0，不迁移不补挂）。

**前端联动**（web-next）：左侧栏新增「题库状态」（`/admin/qbank`）。列表按岗位 × 模型版本一行，status 过滤（全部/生成中/已落库/失败）+ `bank_status` 筛选（全部/使用中/已归档，已归档行弱化视觉）；列表行展示 `question_count` 旁并列 `archived_count`；生成中行显示进度条 + done/total + 当前项名（3s 间隔轮询列表端点、进页认领 RUNNING 行，任务生命周期与页面解耦——§8.4 同型）；点行开抽屉详情——已落库：item × 难度覆盖统计 + 题目表（题干预览，分页；归档版本后端自动放开 active+archived 全量、题目行带 status 列，无手动 toggle 交互）；失败：error 全文 + 历史任务行 + llm_trace 逐次调用（默认折叠、按次展开）+ retry 按钮接线既有端点（列表/详情均含 task_id，为 retry 提供取值来源；岗位已有更新 confirmed 模型时 retry 得 409，前端提示冲突原因）。

## 10. 题量与配额

### 10.1 三类主问题分别计数

```
ordinary_plan_count       岗位级策略配置的普通题计划数 N
ordinary_exception_count  required 刚性例外新增普通题数 E
integrated_plan_count     综合题独立槽位数 I（0 ≤ 实际 ≤ 2）
followup_count            追问轮次，单独统计，不计主问题
main_question_count = N + E + 实际综合题数
```

- `N` 由管理员在岗位测评策略中配置，系统不设全局上下限，默认值实施期结合 40 分钟体验测定；
- 综合题不占 N；required 例外不改变 item.weight。

### 10.2 大类 7:3 整数分配（最大余数法）

```
raw_hard = 0.70 × N；raw_soft = 0.30 × N
先取整数部分，余下名额按小数部分大小分配；小数部分相等时归 hard_skill
```

| N | hard | soft |
|---:|---:|---:|
| 9 | 6 | 3 |
| 10 | 7 | 3 |
| 11 | 8 | 3 |
| 15 | 11 | 4 |

### 10.3 类内 tier 配额（v2.0 用户公式）

以各大类实际题量为总体，按 `required 0.8 : preferred 0.6 : plus 0.3`（和 1.7）分配：

```
required_target  = ceil(category_quota × 0.8 / 1.7)
preferred_target = ceil(category_quota × 0.6 / 1.7)
plus_target      = category_quota − required_target − preferred_target
```

实际分配遵守 `required > preferred > plus` 优先级：题量不足时先保 required 再保 preferred，plus 用剩余名额；向上取整不得使目标超过该大类总量（如 soft 仅 2 题：required=1、preferred=1、plus=0）。

### 10.4 开考前可测量性检查（不通过 → 阻止创建 session 并报告管理员）

```
confirmed model → question bank readiness → quota feasibility → form schema readiness → session 可创建
```

检查项：position active；模型 confirmed；题库绑定当前 model/version 且已生成完成；每个有效 required item 至少一条合法普通题（**普通类目 hard/soft——experience/qualification 不生成题、不参与题库覆盖检查，2026-09-07 裁决，见 §9.1**）；hard/soft 配额可满足（**有 item 但题库不足 → 不允许转移名额，阻止开考**）；综合题槽位有合法题目（若策略 I>0。**本期 I=0 且综合题不排期 → 恒过，by-design，2026-09-06**）；qualification 表单 schema 可用（**现为模型 items 数据驱动生成（form_instance.schema_snapshot），其存在性已被"items 非空"检查隐式覆盖 → 恒过，by-design，2026-09-06**）。

不通过时返回明确状态：`QUESTION_BANK_GENERATING / QUESTION_BANK_INCOMPLETE / MODEL_NOT_MEASURABLE`，并产生管理员待办，而非创建 0 题 session。

### 10.5 required 刚性例外（v2.0 确认）

普通计划结束后检查 required 覆盖；未获有效普通测量的 required item：

- 同一 item **最多一次例外**，新增一条普通主问题（计入 E）；
- 例外题**只允许 medium**；无可用 medium 题时选 **hard**（不走 easy 起始）；
- 不使用综合题；
- 例外仍受题库版本、session 状态、计时约束；
- 耗尽后 item 状态 `REQUIRED_UNMEASURED` → 带警告临时报告 + 人工复核。

### 10.6 动态选题执行顺序（代码执行，可审计）

```
第一层 合法性过滤：版本/阶段/未用实例/当前题已封存/路径合法
第二层 硬约束：未覆盖 required 优先（无候选 → 触发例外或不完整）
第三层 配额：category/tier 剩余配额
第四层 排序：chain 后继（条件满足才继续，可让位 required）→ item.weight → 题目质量 → 稳定随机种子
```

- 每轮 `action=next` 时由代码选题；LLM 只做岗位化/口语化轻包装，question_id 落库绑定；
- chain 不能挤掉未覆盖 required、不能改变题量槽位、不能绕过难度护栏。

## 11. 会话、对话与追问

### 11.1 实例模型（v2.0 确认）

- 每个实际呈现的**不同题面**创建新 `assessment_question` 实例（动态实例化方案 A）；
- `followup` 是当前实例内部子轮次：`question_id` 不变、不建新实例、不增主问题数、不占综合题槽位、**每题最多 2 次**（config 硬约束）；
- 同一 item 的 easy/medium/hard 不同题面 = 三个实例，同属该 item 的 `difficulty_path`；
- 一个实例结束才进下一个主问题实例。

### 11.2 难度路径状态机（v2.0 确认）

```
easy → medium：一次充分证据
medium → hard：充分且稳定的证据；hard 仅对 target_level > 4 的 item 开放
降级触发（仅统计有效候选人证据失败）：
  - 跳过；同 item 同难度连续两道有效题未达最低锚点；
  - followup 后仍模糊/错误/不足；当前非最低难度才可降级
降级后恢复：连续两次充分证据或一次稳定证据（滞回原则）
一次实例内不升降级；路径变更在实例封存后由下一实例承载
不设 item 级路径振荡次数上限
```

**不计入普通失败**（不得触发降级）：技术故障、无障碍问题、题目无效、模型不确定、合理流程质疑、明确拒答、攻击性事件、单纯紧张/停顿/表达风格。

**跳级**：默认禁止运行时静默跳级；仅当模型/rubric 明确配置允许且报告记录实际路径时才可；否则 `PATH_UNAVAILABLE` → 人工/不完整。

### 11.3 证据判定（v2.0 确认：结构化观察 + 代码裁决）

**`evidence_sufficient`** 由 LLM/规则提供结构化观察维度（relevance / required_points_covered / specificity / attribution / source_span_available / contradiction_detected / uncertainty），代码按固定条件计算最终布尔值。明确排除：拒答、纯态度、复述题目、与 target 无关、无具体可归因事实、无可追溯 span、题目无效、模型不确定。

**`stable_evidence`**：两个不同普通题实例的独立有效观察，或一次 hard 强证据（仍须满足 rubric 与证据完整性）；依据观察独立性 + target 覆盖一致性 + 锚点一致性 + 无矛盾 + rubric/version 一致。同一回答的两个相似句子不算两次观察。**证据冲突 → 不平均，`stable_evidence=false` → 人工复核。**

**实施注记（2026-09-06）**：现实现为轻量口径——同 item 充分观察计数 `sufficient_in_row ≥ 2`（`assessment.py _stable_evidence_light`，事件表布尔聚合），**by-design 转正**：其唯一消费者是难度状态机升档，漏判只导致保守不升档，不影响任何分数/报告/聚合；完整判据（观察独立性四要素、冲突不平均）依赖 P-interviewer 结构化输出重构（§26 登记项），重构时一并进行。

### 11.4 回答状态与评分状态（两层分离）

```
answer_state: VALID_EVIDENCE / NEED_CLARIFICATION / OFF_TOPIC / NO_RECALL / DECLINED /
              PROCESS_CHALLENGE / CONDUCT_EVENT / TECHNICAL_OR_ACCESS_BARRIER /
              PROMPT_INJECTION / MODEL_UNCERTAIN / ITEM_INVALID
score_state:  SCORED / REFUSED / INSUFFICIENT_EVIDENCE / NOT_ADMINISTERED /
              INVALIDATED / INCOMPLETE / HUMAN_REVIEW_REQUIRED / IMPUTED
```

代码不把 answer_state 直接映射为分数。处理原则（沿用前轮确认）：含糊→中性澄清 followup（≤2 次）；跑题→重定向；不会→无答案线索的格式脚手架（留痕）；拒答→一次确认后跳过、不提供末尾补答；质疑→说明目的+申诉渠道、不扣分；辱骂→固定话术设边界、行为与能力分隔离；技术/无障碍→暂停计时、不扣分；模型不确定→不猜测、进人工；题目无效→停止评分、移出分母、人工修订题库；候选人回答永远是数据不是指令。

### 11.5 对话传输（v2.0 确认：真实 SSE + 内部 function-call adapter）

- 决策阶段（非流式）：内部 adapter 返回结构化 `{action, reason, assessment}`，**先落库再展示**；
- 话术阶段（流式 SSE）：基于已定 action 逐 token 推送；
- `finish` 仅由代码规则触发（题量完成/全场超时），模型不自主结束；
- SSE 定义事件类型、顺序、错误与结束事件；本期不要求事件 ID/cursor 断线续传（留扩展记录）；
- LLM 输出全部走严格 Pydantic schema 校验，非法输出进入失败/人工状态，不得卡死会话。

## 12. 模块二表结构演进

### 12.1 assessment_session（演进后）

现有列 + 新增：`phase / active_elapsed_seconds / last_activity_at / abandoned_at / policy_version / session_time_intervals_json`。状态机：

```
PENDING_START → ACTIVE → SCORING → COMPLETED
ACTIVE → ABANDONED（6 小时无活动，本期不可恢复；不删证据）
任意 → FAILED
```

**前端接线（2026-09-06 裁决方案一 [03-010] 收口）**：Chat.vue 在 `phase='PENDING_START'` 时渲染居中"开始测评"按钮替代输入区，点击 → `POST /sessions/{id}/start` → 重新拉取会话（phase 门放行 → 服务端派发首题 + 开计时区间）；409 `SESSION_ALREADY_ACTIVE` 按幂等处理直接拉取。**不采用** create 后自动 start（架空 §15 计时起算语义）或 get_session 隐式激活（写操作藏进只读 GET）。

### 12.2 assessment_question（演进后）

现有列 + 新增：`question_type / measurement_stage / item_id / difficulty / status / activated_at / answered_at / closed_at / followup_count / seal_reason / selection_reason / selection_policy_version / path_state_snapshot / binding_snapshot_json`。`(session_id, sequence_no)` 唯一。

### 12.3 assessment_message（演进后）

现有列 + 明确分列：`raw_content / raw_hash / refined_content（NULL）/ action / reason / client_request_id / sequence_no`。原文不可变；精炼只影响 interviewer 上下文。

### 12.4 question_score（演进后）

统一 `score_final`（废弃重复列 `final_score`，迁移合并旧数据）。新增：`item_id / score_state / measurement_target / rubric_version / evidence_spans_json / scorer_version / human_override_score / human_override_state / override_reason / reviewer_id / reviewed_at`。综合题一题多 item：`(question_id, item_id)` 多记录合法。

**分母规则**：`SCORED` 进正常观察；`REFUSED` 不进能力等级分母（只进行为/完整度）；`INSUFFICIENT_EVIDENCE / NOT_ADMINISTERED / INVALIDATED / MODEL_UNCERTAIN / INCOMPLETE` 均不进正常分母，产生缺失/警告。系统错误与题目无效不得转成普通低分。

### 12.5 context_raw 与证据定位

- 原文 hash 复用**限制在单一 session 内**，不跨候选人复用；
- 证据引用结构化：`source_message_id / source_content_type(raw|refined) / start_offset / end_offset（Unicode code point）/ quote_hash`；
- 终局评分永远回捞原文；报告同时展示 quote 与来源定位。

## 13. 状态事件与 trace（v2.0 新增契约）

### 13.1 assessment_state_event（append-only）

```sql
assessment_state_event(
  id TEXT PRIMARY KEY, session_id TEXT NOT NULL, sequence_no INTEGER NOT NULL,
  assessment_question_id TEXT NULL, assessment_message_id TEXT NULL,
  event_type TEXT NOT NULL, from_state TEXT NULL, to_state TEXT NULL,
  actor_type TEXT NOT NULL, actor_id TEXT NULL, request_id TEXT NULL,
  idempotency_key TEXT NULL, policy_version TEXT NULL, model_version INTEGER NULL,
  question_bank_version TEXT NULL, correlation_id TEXT NULL, causation_event_id TEXT NULL,
  payload_json TEXT, created_at TEXT NOT NULL,
  UNIQUE(session_id, sequence_no)
)
```

- 状态迁移事件必填 from/to；动作/事实类事件允许为空；
- 禁止 UPDATE/DELETE，纠错走补偿事件；当前快照列与事件同事务更新；LLM 调用不持有长事务；
- 正常业务读快照列；回放（按 sequence_no 重放迁移事件）仅用于审计/恢复/修复/测试，与快照不一致时标记并进人工，不静默覆盖。

### 13.2 候选事件枚举（定稿时每个注明：必填字段/是否迁移/是否计题量/是否计时/是否需人工）

```
SESSION_* CREATED|STARTED|PAUSE_REQUESTED|PAUSED|RESUMED|ENTERED_SCORING|GLOBAL_TIMEOUT|COMPLETED|ABANDONED
QUESTION_* SELECTED|ACTIVATED|ANSWER_RECEIVED|FOLLOWUP_ASKED|SEALED|TIMEOUT|DIFFICULTY_RAISED|DIFFICULTY_LOWERED|DIFFICULTY_RESTORED|PATH_UNAVAILABLE|INVALID_MARKED|REQUIRED_EXCEPTION_GRANTED
MESSAGE_* STORED|REFINED
OBSERVATION_* CLASSIFIED|EVIDENCE_EVALUATED|STABILITY_UPDATED|UNCERTAINTY_RAISED
CONTROL_* SCAFFOLD_SHOWN|SUPPORT_SHOWN|CONDUCT_EVENT|INJECTION_DETECTED|TECHNICAL_RETRY|TECHNICAL_BARRIER
FORM_* RENDERED|SUBMITTED|VALIDATED|VALIDATION_FAILED|FACT_EXTRACTED|FACT_CONFLICT
GATE_* EVALUATED|OVERRIDDEN（人工覆盖需二次确认）
POLICY_* DECISION_RECORDED
TOOL_* REQUESTED|EXECUTED|FAILED_ESCALATED（失败→暂停并人工接管）
TASK_* QUEUED|STARTED|SUCCEEDED|FAILED
REVIEW_* REQUESTED|ACCESSED|DECIDED|REPORT_PUBLISH_CONFIRMED|FEEDBACK_RECEIVED
```

### 13.3 trace_link（统一关联表）

```sql
trace_link(id, trace_id, entity_type, entity_id,
           link_role /* input|output|caused_by|scored|reported|source */, created_at,
           UNIQUE(trace_id, entity_type, entity_id, link_role))
```

业务表不逐一加 trace 外键；report→session→model/version→question→message→score→trace 审计链通过 trace_link 闭合。

### 13.4 幂等

- 作用域：`session_id + endpoint + idempotency_key`；
- 答题另带 `question_instance_id / expected_question_revision / client_attempt_id`；
- 重复请求返回第一次持久化结果，不重复写消息/计 followup/扣题量/启任务；
- 数据库事务 + 乐观版本号防并发双写；
- 幂等记录长期保存，达阈值自动提醒清理，留管理员清理接口（策略实施期定）。**演示期注记（2026-09-06）：阈值与清理接口均不启用（见 §31-6）。**

## 14. 上下文管理

| 层 | 职责 |
|---|---|
| 原始证据层 | raw_content + raw_hash，不可覆盖，终局评分回捞 |
| 交互上下文层 | interviewer 滑窗；**以 Token 数控制（参数待定，留接口）**；最新回答不得重复拼接两次 |
| 导航摘要层 | item 覆盖/难度/chain/异常/待处理；结构化状态优先，LLM 摘要可选；失败回退数据库状态，不阻塞 |

P-refine：单条回答超阈值（config `REFINE_MIN_TOKENS`，数值实施期校准）触发压缩；原文与精炼分列保存；精炼承担注入防护。

## 15. 计时、暂停与恢复

- **全场 40 分钟**：从候选人确认开始且首题成功激活起算；
- **单题 20 分钟**：从题目成功激活并发送起算；followup 共用同一单题计时器；
- 服务端权威：保存 `session_time_interval(session_id, interval_type: active|paused, reason, started_at_server, ended_at_server)`；`active_elapsed = Σ active 区间`；客户端只展示；
- **所有暂停类型（候选人请求/技术故障/无障碍便利/管理）均不计入 40 分钟**，且必须写事件；便利原因等敏感信息不进入评分 Prompt；
- 短暂断线不自动暂停；仅显式 pause 或技术状态产生 paused 区间；
- **无活动 6 小时 → `ABANDONED`，本期不可恢复**（后续可开发可恢复，仅留记录，不细化不留接口）；检测 = 下次请求惰性判断 + 单进程周期扫描；ABANDONED 不删证据；
- 单题超时：封存当前题（seal_reason=timeout），继续下一题；全场超时：停止新增主问题，进入收尾评分；
- 时间不参与题目筛选优先级，不因剩余时间不足拒绝合法 chain/升降级。

## 16. 表单与 Tools

### 16.1 form_instance（v2.0 新增生命周期实体）

- schema 由代码定义 + 版本化；**instance 创建时存不可变 schema 快照**；
- `render_form` 由代码在资格核验阶段触发（LLM 只能请求）；响应含 `form_instance_id / schema_snapshot / form_status`；
- `GET /forms/{form_instance_id}` 候选人只读已激活 instance 的 schema，不暴露内部阈值；
- `submit_form` 携 `form_instance_id / schema_version / idempotency_key / payload`；服务端校验所有权、session 状态、类型/必填/枚举/长度；**重复提交返回第一次结果**；同一 instance 修订走**不可变 revision**；
- gate 由代码计算，独立结构化结果 `gate_result/gate_status/gate_reason/evaluated_schema_version/evaluated_at`；人工覆盖存 `automated_gate_result + human_override + override_reason + reviewer_id`（需二次人工确认）。
- **qualification 渲染 facet 化（快照 v1 → v2，2026-09-08）**：渲染时 gate qualification items 按 `competency_item.facet_key`（聚合落行时预打标，§8.1 工序⑤）分组收集——最高学历 / 院校档次 / 英语等级为**有序枚举单选**（阈值比较派生，学历类断言取措辞最低档为阈值：如「学士或硕士学位」→ 本科；985 满足 211 要求）；数值型断言（年龄、出勤天数等）为**数字输入**（std_name 内嵌数字解析阈值与量纲，比较派生）；专业范围类与未分类长尾项合并为**勾选组**（勾 = 是，不解析）。experience 年限项沿用 base 字段「与岗位相关的工作年限（年）」，判定不变。facet 答案在 submit 六维校验后、`_gate_check` 前由纯函数派生为逐 item 真值（True/false），命中现有真值表——gate 行结构、`_gate_check` 本体不动。快照 v2：fields 增 facet 键与派生参数（渲染白名单同步）；**已 open 的 v1 实例走旧链完投，不迁移不重渲染；新渲染实例一律 v2**；form_instance 表结构不变。payload_json 两段式落库（facet 原始答案 + 派生结果），审计可复现。存量模型 facet_key NULL → 整组降级勾选组 fallback（永不 block）。报告 gate 段按 facet 折叠展示（同 facet 多 item 一行）。收集形态为紧凑结构化表单；对话式收集（P-form-facts，§16.2 同构）留作后续阶段复用同一 facet 事实模型，不在本期范围。

### 16.2 extract_form_facts

抽取结果含 `fact_type / normalized_value / source_document / source_span / confidence / status`；状态 `EXTRACTED / UNCERTAIN / CONFLICTING / CANDIDATE_CONFIRMED / HUMAN_REVIEW_REQUIRED`；与候选人结构化填写冲突时**保留两者并进人工确认**；gate 只接受候选人确认或人工确认后的事实。

### 16.3 Tools 总边界

- 阶段白名单 + 严格输入输出 schema + 所有权校验 + 幂等/超时/次数/长度控制 + 调用留痕；
- 工具返回内容是数据不是指令；LLM 提议后代码二次验证；**工具失败 → 暂停并人工接管**；
- 正式测评**不做 Web Search**；`request_pause` 候选人可直接触发、LLM 只能建议；
- 所有 LLM 输出严格 schema 校验。

---

# 第四部分 模块三：评分、聚合与人才画像

## 17. 评分链（v2.0 最终确认）

```
原始回答证据 → 独立终局评分 score_final（P-score，temperature≈0，回捞原文）
→ item 内证据裁决 → 固定 item.weight 加权 → 画像报告
```

- **score_live 只用于过程性选题与难度导航，不参与最终分数**（旧版 50/50 合成作废）；
- 客观题：代码匹配判分（answer_key 为空属于题库缺陷，判题库无效而非满分）；**客观题 evidence_quote 取候选人回答前 60 字符（展示截断固定规则，现为 `scoring.py:192` 字面量，实施时常量化为 `_OBJECTIVE_QUOTE_LEN`；非开放参数）；命中=5 分 / 未命中=1 分，与 §20.3 `(score−1)/4` 归一化同口径**；
- 题库生成结构规则（非运行期参数）：hard_skill 项 `weight>0.10` 生成 easy/medium/hard 三档，否则两档；soft_skill 两档（`question_bank.py _question_plan`）；**experience/qualification 不生成题（2026-09-07 裁决，§9.1——旧「无难度各 1 题」结构作废，两类走 §16 表单链）**；
- score_live 与 score_final 差值仅用于偏差分析与 bad case 候选，不写回分数；
- 评分失败/无可信结构化结果 → 报告 `FAILED`，不得生成 0 分正常报告。

## 18. 拒答（v2.0 最终确认）

```
score_value = 0
score_state = REFUSED
```

- 0 是特殊状态值，不是能力量表 1 分；
- **拒答只进入行为聚合与完整度聚合，不进入能力等级聚合**；item 最终等级由有效能力证据决定，后续有效回答可改变等级；
- 拒答事件永久保留，报告单独展示拒答次数与位置；
- 可能涉及受保护信息或合法隐私质疑的拒答 → 合规/人工处理边界，不直接记 0；
- **实施注记（2026-09-06）**：score_state 六态枚举位 `INSUFFICIENT_EVIDENCE / NOT_ADMINISTERED / INCOMPLETE` 本期**不产出**（by-design）：现终态由 REFUSED（拒答）、INVALIDATED（题库无效）与聚合层 IMPUTED/PROVISIONAL/NO_VALID_OBSERVATION + missing_warnings（§20.1/§20.2）语义等价覆盖；NOT_ADMINISTERED 的潜在场景（派发未答实例进 finalize）走 §20.1 缺失路径，不做区分。枚举位保留供校验与后续生产（D-28 口径集）。

## 19. item 内多题合并与综合题（v2.0 最终确认）

普通题与综合题先转为统一测量记录：

```
item_measurement(question_id, item_id, observed_level, evidence_refs, measurement_source: ordinary|integrated)
item_final_level = adjudicate(ordinary_measurements + integrated_measurements)
```

裁决规则：普通最低测量资格先检查（综合题不能替代）；按 rubric/覆盖/稳定性/冲突裁决，**不按来源加权、不按题数重复乘 item.weight**；共享 evidence span 只影响证据解释、不折扣不自动复制；**重大冲突取较低值并留人工复核标记（判据：观测等级极差 ≥ `ADJUDICATE_CONFLICT_THRESHOLD = 2`，2026-09-06 转正登记，集中 `config.py`）**；综合题可作为最高等级证据来源（仍受其 rubric 约束）。

综合题第二题决策表：剩余合法联合 target 为主触发；第一题整体无效可触发；仍受最多 2 题、题库版本匹配、session 状态约束；全场超时不再激活。综合题整体 `ITEM_INVALID` → 所有绑定 item 综合结果无效、移出分母、不算有效测量；单 item 证据不足不影响其他 item 独立评分。

## 20. 缺失、补算与聚合

### 20.1 观察集合与补算（普通非 gate item）

```
O = 全部 score_state=SCORED 的 item 级测量
M = 缺失集合
r = Σ(i∈O) w_i×s_i / Σ(i∈O) w_i（s_i = normalized = (score−1)/4）
缺失普通 item 补算值 = r，标记 IMPUTED
```

- `O = ∅` → 不能补算 → `NO_VALID_OBSERVATION / HUMAN_REVIEW_REQUIRED`；
- required 与 qualification 不适用比例补算；
- `IMPUTED` 参与总分与雷达，但必须特殊视觉标记 + 展示观察覆盖率/真实观察数/缺失原因；
- 补算比例超阈值 → 临时报告 + 人工复核。**阈值已裁决 0.2**（`IMPUTE_RATIO_THRESHOLD`，2026-09-05 关口 A：IMPUTED 覆盖率 > 2 成 → PROVISIONAL + HUMAN_REVIEW_REQUIRED）。

### 20.2 required 缺失

```
report_status = PROVISIONAL；review_status = HUMAN_REVIEW_REQUIRED
```

不触发补测；人工确认后**可发布为正式报告，必须明确点击发布**；系统不做录用判断。

### 20.3 总分

```
normalized_item_score = (actual_level − 1) / 4（与 §20.1 s_i=(score−1)/4 同尺度：1→0、5→1）
总分 = Σ(item.weight × normalized_item_score) × 100
gap = required_level − actual_level
门槛项：达标拿满/不达标 0，UI 红绿标签单列
```

大类的 7:3 已体现在 item.weight 中，**聚合时不再二次乘大类比例**。

## 21. 报告（五段式沿用 + v2.0 发布契约）

①总分+门槛标签 ②雷达图（required 灰 vs actual 蓝；IMPUTED 特殊标记）③逐项明细（gap 着色、理由、逐行异议）④优势/短板/建议（代码排序：优势=gap≤0 中权重前 3；短板=gap>0 中 gap×weight 前 3；LLM 只写文字且只能基于给定短板项）⑤逐题回顾（score_final + 证据 quote + 来源定位）。

### 21.1 报告状态机与发布

```
report_status: GENERATING → PROVISIONAL | READY → PUBLISHED | FAILED
review_status: NONE | REQUIRED | IN_PROGRESS | CONFIRMED | CLOSED
```

**发布前一致性校验（代码，七项）**：数字可重算；weight 总和一致；引用 question/message 属于该 session；model/version 与快照一致；无效题/系统错误未进正常分母；IMPUTED/REFUSED/required 警告与结构化状态一致；文案无录用判断表述。

**流程**：评分完成 → 聚合与校验 → 生成（PROVISIONAL 或 READY）→ 有人工复核要求则等待 → 管理员复核后**明确点击发布** → PUBLISHED。人工复核字段：`review_request_reason / reviewer_id / review_note / review_outcome / reviewed_at / publish_confirmed_by / published_at`。报告生成不隐式代替评分；正常 UI 主链必须 score→report 串行（前端完成后由服务端执行，不依赖浏览器补调）。

**重复触发护栏（P0 成功标准 5）**：`POST /sessions/{id}/report` 在会话已存在报告且报告处于生成中（`GENERATING`）或终态成功（`PROVISIONAL`/`READY`/`PUBLISHED`）时返回 409（`REPORT_GENERATING` / `REPORT_ALREADY_GENERATED`），不重复触发评分/报告；报告为 `FAILED`（后台链失败）或无 report 行时允许重新入队（202），作为失败重试路径。

---

# 第五部分 模块四：测试闭环

## 22. 三大标准

| 标准 | 落点 |
|---|---|
| 全链可审计 | report→session→model/version→question→message→score→trace 经 trace_link 闭合；P8 trace 查看器 |
| 反馈可回溯 | 候选人逐分异议 → feedback（不改分）；admin 标 bad case；双分背离自动候选 |
| 有测评标准 | b+c 评测；eval/ 独立可迁移；**联调全绿后最后做** |

## 23. 评测

- **一致性（b）**：固定 transcript 复跑，断言 score_final 分差 ≤1（temperature=0；固定 model/provider/版本/rubric 快照）；score_live 不在断言范围；
- **虚拟考生（c）**：强/中/弱三档端到端，断言总分排序（强>中>弱）+ 短板定位符合预设 + required 覆盖 + 拒答/缺失状态 + 证据引用 + 报告状态；
- **自动 bad case**：`score_live` 与 `score_final` 可比且差值 ≥ 配置阈值 → 创建候选（排除题目无效/模型不确定/系统错误/不可比状态），管理员审核，不自动改分；
- **eval 必须使用独立/临时数据库**，不污染业务库；mock 回归不能替代真实 LLM 质量验证；
- 黄金集（模块一）本期推迟。

## 24. 测试与验收要求

- 统一 pytest 收集（脚本测试重构为可收集或明确独立命令）；CI 为正式验收入口；
- M1 回归为动态测评实施硬前置；候选人端完整 E2E（注册→选岗→session→作答/追问→表单→完成→评分→报告→异议，含刷新恢复/断线重试/越权/超时）为 M5–M7 verified 必要条件；
- 越权/权限矩阵、幂等/并发、计时、迁移、SSE 为必测项；
- prototype 为视觉参考，静态原型不作功能验收依据。

## 25. 安全与数据治理

- JWT：单一 secret，生产缺失/默认值时启动校验（不列上线硬门槛但保留增强记录）；服务端统一密码规则；HttpOnly cookie 方向；
- trace/JD/原文按数据分级管理：管理员可读全文并留访问审计（`REVIEW_ACCESSED` 事件）；保留期、脱敏、导出、删除策略与第三方 LLM 供应商约束实施期细化；
- 输入限额按类型配置（文件大小/行数/JD/回答长度/LLM prompt/max_tokens/分页上限）；
- Prompt injection：候选人输入与工具返回永远是数据；`INJECTION_DETECTED` 事件留痕；
- 公平性：不按口音/停顿/打字速度/语气/"不自信"推断能力；公平性离线评估本期不做（仅留记录）；
- 异议只进人工处理与质量数据，永不触发改分。

## 26. Prompt 模块（本版只登记接口，不展开）

Prompt 讨论按既定安排延后，用户将先拟业务初稿。本版仅固化扩展点要求：所有未来需要 LLM 的位置必须保留**可替换接口**（classifier/observation/evidence-evaluator/probe/scaffold/integrated-scorer/report-writer 等）或**稳定设计记录**（Prompt ID/调用阶段/输入输出 schema/允许与禁止影响范围/版本与 trace/启用条件）。接口可注入：Prompt 版本、模型标识、schema、trace 关联、重试策略、人工接管、mock 切换。

**任何 Prompt 不得**：把 followup 输出成主问题；把综合题当普通第四级；因绑定多 item 自动完成测量；自由增加综合题数量；绕过普通最低测量；改动 7:3/配额/item.weight/状态机/聚合公式/报告发布条件。

已登记场景：JD 抽取、消歧、等级裁决、题库生成（普通/综合）、回答分类、证据评估、追问/探针/脚手架/情绪支持话术、过渡收尾、事实抽取、逐题评分、综合题拆分评分、一致性校验辅助、报告文字、虚拟考生生成、评测解释、bad case 聚类摘要（可选）、复核辅助摘要（可选）。

## 27. 里程碑状态口径（四维）

每个里程碑记录：`implemented（已有代码）/ contract_complete（满足本文契约）/ verified（通过测试验收）/ production_ready（满足上线要求）`。当前 M1–M3 大体 implemented+verified 不足；M5–M7 主体 implemented、contract_complete=false、verified=false。

## 28. 修复与重构待办（来自差距登记，实施顺序）

1. **P0**：资源所有权校验（§7）；正常 UI score→report 串行（§21.1）；开考可测量性检查（§10.4）；状态事件表落地（§13）；
2. 动态选题四层结构替换一次性预选（§10.6）；难度路径状态机（§11.2）；非末题 finish 护栏；回答状态分类完整化；
3. 表单 instance/schema/幂等链（§16）；SSE 真实化（§11.5）；幂等与并发（§13.4）；计时区间（§15）；
4. 题库 model/version 绑定与生成失败可见（§9.2）；`/jds/orphan` 路由顺序修复；模型编辑字段校验（NaN/范围/重复）；
5. 证据 span/trace_link 落地（§12.5/§13.3）；报告发布校验（§21.1）；feedback 补 user_id/note/审计字段；报告版本化（覆盖生成改为不可变版本，防 feedback 外键断裂）；
6. 迁移体系（schema_version + 备份 + 回滚 + 迁移测试）；测试重构与 CI；M1 回归；E2E；eval 隔离。

---

# 第六部分 文档治理

## 29. SSOT 与文档身份

- 全仓库**唯一 SSOT 标记** = 本文（`design/final-design/总设计文档.md`）；
- 其余文档只有三种身份：**从属模块稿**（本目录分模块摘录）/ **临时讨论稿**（design/ 临时讨论稿-*）/ **历史档案**（含旧版总文档、原 04/05/06）；
- 变更日志维护在本文 §14（沿用旧版模式），条目格式：日期 / 变更内容 / 原因 / 影响面；
- 旧顶层 `design/总设计文档.md` 已随目录整理移入历史档案，靠 git 管历史，不删除。

## 30. 与旧版（2026-08-30 版）的关键差异登记

| # | 旧版 | 新版 | 依据 |
|---|---|---|---|
| N1 | 类目 55/20/20/5 | 评分类目 7:3 + gate 事实核验不占权重 | 第八轮 |
| N2 | score_live 50/50 合成 | score_live 仅导航，不进最终分 | 第五轮 |
| N3 | 14 张表 | 18→21 张（新增事件表/trace_link/form_instance） | 第八轮 |
| N4 | 题量 hard6/soft2/exp2 固定 | 岗位级 N + 7:3 最大余数 + tier 0.8/0.6/0.3 公式；exp/qual 走表单 | 第八轮 |
| N5 | 一次性预选题 | 动态实例化 + 四层选题结构 | 第四五轮 |
| N6 | 难度递进按类目 | easy→medium→hard 路径状态机 + 3/4/5 上限映射 | 第八十轮 |
| N7 | 24h abandoned | 6h 无活动 abandoned 且本期不可恢复 | 第九轮 |
| N8 | 拒答未明确 | REFUSED=0，只进行为/完整度聚合不进能力等级 | 第八九轮 |
| N9 | SSE"待切" | 真实 SSE 为最终方向，本期实现 | 第八轮 |
| N10 | 范围含录用表述 | 明确不做录用判断/排序 | 第一轮 |
| N11 | CHECK 约束遗留争议 | 新表避免不可 ALTER 的 CHECK，用代码校验枚举 | 实施约束 |
| N12 | 里程碑"完成" | 四维状态（implemented/contract_complete/verified/production_ready） | 第八轮 |

## 31. 开放问题（实施期定）

1. `N` 默认值已裁决 10[02-007]；**余项：40 分钟体验校准（随真实 LLM 验收）**；
2. 滑窗 Token 已裁决 8000[03-007]；`REFINE_MIN_TOKENS=500` 已定默认；**余项：deepseek 接入后的实测校准**；
3. ~~补算人工复核阈值~~ **已裁决 0.2**（2026-09-05 关口 A；IMPUTED 覆盖率 > 0.2 → PROVISIONAL + 人工复核，`aggregation.py IMPUTE_RATIO_THRESHOLD`）；
4. ~~词典候选匹配阈值、清洗标题词表~~ 匹配阈值 **已裁决 0.5**（2026-09-06，`config.py DICT_MATCH_THRESHOLD`）；清洗走 pipeline 内置 NOISE_HEADERS/_SUFFIXES 硬编码表，**配置词表不启用（2026-09-06 演示期裁决，`TITLE_CLEAN_WORDS` 占位摘除）**；
5. ~~trace 保留期/脱敏~~ **演示期不启用**（2026-09-06 裁决：trace 全量保留利于验收审计，脱敏妨碍观察层验证；`TRACE_RETENTION_DAYS / TRACE_DESENSITIZE` 占位摘除）。生产 PII 治理（保留期/脱敏/供应商数据约束）随真实上线再开；
6. ~~幂等清理阈值与策略~~ **演示期不启用**（demo 量级永远到不了阈值，无清理任务/接口 by-design；`IDEMPOTENCY_CLEANUP_THRESHOLD` 占位摘除）。生产清理策略随真实上线再开。

---

# 附录 A Prompt 清单（登记，不展开内容）

| 编号 | 用途 | 状态 |
|---|---|---|
| P-extract / P-disambiguate / P-aggregate-level | 模块一 LLM#1/#2/#3 | 已实现（v1，重构时按 §26 接口化） |
| P-question-gen（普通/综合） | 题库生成 | 已实现 v1；综合题生成待 Prompt 模块讨论 |
| P-interviewer | 面试官系统指令（分类/追问/话术/建议） | 已实现 v1；按 §26 重构为结构化观察输出 |
| P-refine | 长输入精炼 + 注入防护 | 已实现 v1 |
| P-score | 终局逐题评分（锚点+证据引用） | 已实现 v1 |
| P-report | 报告文字（绑证据约束） | 待 Prompt 模块讨论 |
| P-form-facts | 表单/简历事实抽取 | 待 Prompt 模块讨论 |
| P-integrated-score | 综合题多 item 拆分评分 | 待 Prompt 模块讨论 |
| P-virtual-candidate | 虚拟考生生成 | 待 Prompt 模块讨论 |

# 附录 B 已完成里程碑存档

- M1 鉴权 + 单 JD 解析链；M2 聚合 + 人审；M3 外围页面（2026-08-30 完成）
- M5 题库/session/对话核心、M6 评分报告、M7 测试闭环：主体代码已落地（2026-08-30），按 §27 四维口径当前 contract_complete=false
- M4（保留不做）：黄金集、浏览器插件、真实 JD 数据集

# 附录 C 文档关系

- 本文：唯一 SSOT；
- 本目录《模块一~四设计》：本文分块摘录；
- `design/` 临时讨论稿（第一~十轮）：收敛过程记录，不作为实施依据；
- 历史档案（本目录 `历史档案/` + design/ 原 04/05/06）：仅追溯用，冲突处以本文为准。

---

# §14 变更日志

| 日期 | 变更 | 原因 | 影响面 |
|---|---|---|---|
| 2026-09-02 | **总文档 v2.0 创建**：基于 checkpoint §21–25 与第八~十轮讨论全面重写。关键落定：7:3 类目比例、岗位级 N + tier 0.8/0.6/0.3 配额公式、动态实例化与四层选题、难度 3/4/5 上限映射与路径状态机、score_live 仅导航、REFUSED 特殊观察值（不进能力等级聚合）、统一 item_measurement 裁决、IMPUTED 补算、assessment_state_event/trace_link/form_instance 三新表、6h abandoned、真实 SSE、报告发布校验与人工发布、权限 P0、演示上线范围、四维里程碑口径、§30 差异登记 N1–N12 | 非 Prompt 设计收敛完成，用户授权成文 | 全文 |
| 2026-09-05 | **§20.3 补全总分归一化公式**：`normalized_item_score = (actual_level − 1) / 4`，与 §20.1 `s_i=(score−1)/4` 同尺度，统一 observed/imputed 归一化（作废旧实现 `score/5` 隐式口径） | 用户关口 A 裁决「统一 (score−1)/4」；旧实现观测项 `score/5` 与补算 `(score−1)/4` 两尺度混用 | §20.3、Phase 5 计划 05-02（aggregation.py `_normalize_score` + 主循环总分） |
| 2026-09-06 | **§21 优势/短板 gap 符号约定反转**：`gap = required_level − actual_level` 不变；优势=`gap≤0`（actual≥required，达标/超配）、短板=`gap>0`（actual<required，低于要求），短板排序键 `|gap|×weight` 改 `gap×weight`（gap>0 无需取绝对值） | 用户裁决 [06-013] 选项 A：原「短板=gap<0」把低于要求的项归入优势，与「短板=低于要求」的自然语义相反 | §21、aggregation.py 优势/短板筛选、test_m6_backend 断言、eval/virtual_candidates 短板断言 |
| 2026-09-06 | **§21.1 补报告重复触发护栏**：`POST /report` 在报告处于 `GENERATING`/`PROVISIONAL`/`READY`/`PUBLISHED` 时返回 409（`REPORT_GENERATING`/`REPORT_ALREADY_GENERATED`），`FAILED` 或无行允许 202 重试 | 修复 P0 成功标准 5 既有设计级失败（终态报告重复触发未 409，`test_completed_session_guardrail`）；消除 D-08 串行链端点语义与 P0 护栏的分歧 | §21.1、`api/assessment.py request_report` |
| 2026-09-06 | **§31-4 词典候选匹配阈值裁决**：`DICT_MATCH_THRESHOLD=0.5`（difflib 编辑距离 ratio + 子串包含，归一化 lowercase/strip），每能力项取同 category 词典相似度≥阈值的前 top10 候选送 LLM#2；替换原「全量 active 词条」实现 | 修复 §8.1「top10（编辑距离/拼音）」实现缺口（原实现无 top10/阈值，成本 O(n²) 且消歧质量随词典增长恶化）；拼音未启用（干净语料收益低，避免 pypinyin 依赖） | §31-4、config.py `DICT_MATCH_THRESHOLD`、services/pipeline.py `_dict_candidates` |
| 2026-09-06 | **管理员列表接口服务端分页**：`GET /admin/positions`、`/admin/positions/pending`、`/admin/jds/orphan`、`/admin/dict`、`/admin/users` 新增 `page`/`page_size` 查询参数（默认 `page=1`、`page_size=20`），返回结构由 `list[dict]` 改为 `{items, total}`；新增 `GET /admin/positions/options` 轻量选项接口（`[{position_id, name}]` 全量不分页，供改归下拉/详情页名称查找） | 用户要求「岗位库三表 / 能力词典 / 用户管理」列表加分页（每页 20 条）；原接口返回全量无翻页 | 后端 `api/admin/{jds,positions,dict,users}.py`、前端 `web/src/views/admin/{Positions,Dict,Users,PositionDetail}.vue`、`test_phase4_orphan.py`、新增 `test_admin_pagination.py` |
| 2026-09-06 | **第十轮收口·开放参数与未决事务裁决**：①§6 表数修正 21→26（25 业务+schema_version）；②§31-3 补算复核阈值裁决 0.2、§31-4 清洗词表不启用、§31-5 trace 保留期/脱敏演示期不启用、§31-6 幂等清理演示期不启用（四个 config 占位摘除）；③§19 重大冲突极差阈值登记 `ADJUDICATE_CONFLICT_THRESHOLD=2`（进 config）；④§20.1 补算复核阈值 0.2 登记；⑤§17 客观题引文截断 60 字符 + 命中 5/未中 1 口径登记；⑥§10.4 检查项 6/7 注记 by-design 恒过；⑦§11.3 stable_evidence 轻量口径 by-design；⑧§18 三枚举位本期不产出注记；⑨§12.1 前端 start 接线方案一（Chat.vue 入场确认按钮）；⑩§13.4 幂等清理演示期不启用注记 | 用户 2026-09-06 对第十轮收敛清单逐项裁决（BAD_CASE_DIVERGENCE_THRESHOLD=2、MAX_JD_LENGTH=10000、MAX_JD_FILE_LINES=500、MAX_PAGINATION_LIMIT=100 同日裁决，代码接线随实施不含本次文档写入） | §6/§10.4/§11.3/§12.1/§13.4/§17/§18/§19/§20.1/§31/§14；`server/config.py`、`server/services/{aggregation,scoring,readiness,report}.py`、`server/api/assessment.py`、`web/src/api/index.js`、`web/src/views/assessment/Chat.vue` |
| 2026-09-07 | **模块一·源标题优先归岗**：①JSONL 导入行内 `job_title`（兼容 `position` 键）透传入库 `jd_record.job_title`（原导入丢弃源标题、LLM#1 无条件覆写，致 905/1000 标题漂移、岗位碎片化）；②pipeline 归岗改源标题优先——库内非空不覆写（reparse 同语义），为空才以 LLM#1 兜底；③`normalize_title` 补 `.lower()`（docstring 声明兑现）+ position/别名匹配改 `COLLATE NOCASE`（存量 129 行含大写 ASCII 岗位名不迁移，匹配层解决，避免 MIGRATIONS 未审阅自动改业务库）；④新增回填脚本 `scripts/backfill_jd_source_title.py`（回写源标题+重算归岗+清空孤儿岗，dry-run 默认，须用户审阅后手动 `--apply` 执行） | 1000 条 rocxu-ai 语料导入后源标题丢弃、正文开头多为技能串致 LLM#1 无信号；归岗大小写敏感令仅大小写差异的 13 个标题族分裂（443 position/352 单 JD 岗/27 个 pending_review 孤儿岗）；归岗输入口径变化的 SSOT 授权随 2026-09-07 修理工单计划批准取得 | §8/§14；《模块一设计》§2/§3/§9；`server/api/admin/jds.py`、`server/services/{pipeline,assign}.py`、`server/test_m1_source_title.py`（新增）、`scripts/backfill_jd_source_title.py`（新增）；data/app.db 回填由用户另行手动执行，不在本次代码变更内 |
| 2026-09-07 | **模块二·exp/qual 不进题库收口**：①§9.1 补生成侧裁决——题库生成不为 experience/qualification 产生任何 question_bank 行，表单链（§16）为两类信息唯一采集通道；选题白名单 `ORDINARY_CATEGORIES` 自 Phase 2 起本就隔离历史通用题，本次收口生成侧；历史 scope=general 通用题为存量遗留，不迁移不删除；②§10.4 检查项 4 同步——required 题库覆盖检查限定普通类目 hard/soft（无年限 experience 常为 required+gate=0，若不限定则停生成后被误阻断开考）；③§17 题库生成结构规则作废「experience/qualification 无难度各 1 题」；④`_question_plan` 不再含 exp/qual 分支，scope=general 生成路径随之移除（题库生成从此只有 scope=position） | 消除 SSOT §9.1（不进普通对话题库）与 §17 旧注记（exp/qual 各 1 题）的内部矛盾——用户裁决「experience/qualification 不进题库，用表单获取」（2026-09-07）；exp/qual 通用题自选题白名单落地以来恒不被选中，纯为死数据与浪费 LLM 调用 | §9.1/§10.4/§17/§14；《模块二设计》§1.1/§1.4；`server/services/question_bank.py`（_question_plan 跳过 exp/qual + scope=general 分支移除）、`server/services/readiness.py`（检查 4 限定 ORDINARY_CATEGORIES）、`server/test_question_bank.py`、`server/test_phase4_binding.py`、`server/test_m5_backend.py` 断言同步 |
| 2026-09-07 | **模块一·聚合任务可观测（§8.4 新增）**：新增 `aggregate_task` 表（RUNNING/SUCCEEDED/FAILED + trigger_source + total/done + llm_total/llm_done + current_item + model_id + error + 时间戳，不清理），进度端点 `GET /admin/positions/{id}/aggregate/progress` 按岗位查最新任务行；记录落在 `run_aggregate` 内部（pipeline 内联自动聚合同覆盖）；stalled 并入 FAILED+error（任务态与模型态并存不冲突）；进程重启启动时将 RUNNING 残留行置 FAILED（error=进程重启中断）；前端轮询对象改 progress 端点，进度条+双计数+当前项名，心跳停滞判定替换 3 分钟硬超时误报，进页面认领 RUNNING 行（任务生命周期与页面解耦）。触发响应不返回 task_id（响应返回时后台任务可能未起跑，查询以岗位维度定位）；不设 QUEUED 态（BackgroundTasks 触发即执行） | 用户裁决（2026-09-07）：聚合过程需进度反馈（混合形态——进度条+当前项），且聚合历史、失败须可事后追溯（「一切都可溯源」系统原则）；现行实现聚合完成前无任何中间状态落库，前端只能 404→200 二态轮询且 3 分钟超时误报 | §6（表数 26→27）/§8/§8.4/§14；《模块一设计》§2/§5/§7/§10；实施随代码落地：`server/db.py`、`server/services/aggregate.py`、`server/api/admin/models.py`、`web/src/views/admin/ModelReview.vue` |
| 2026-09-07 | **模块一·聚合 importance 混合口径**：§8.1 工序⑤ 的 req 从绝对口径（标 required 的 JD 数 ÷ 岗位 JD 总数）改为条件口径（÷ 该能力出现的 JD 数），加双支撑门槛（r ≥ REQ_MIN_OCCURRENCE_RATIO=0.25 且出现 JD 数 ≥ REQ_MIN_OCCURRENCE=3）方可判 required，且仅 hard_skill 可判 required（soft_skill 上限 preferred）；preferred/plus 判据不变（r≥R_THRESHOLD=0.5）。常量 `REQ_THRESHOLD=0.5` 语义随之变更（条件口径阈值），新增两常量挂模块一分册 §6 | 三个测试岗（SLAM/NLP/视觉）聚合产出「必备项过少、长尾严重」（required 数 2/2/1，权重坍塌至本池 6%）+ 第四岗（图像算法）验证 + 22 岗全量模拟（n≥7 段 1–3→7–11 项稳定修复；n∈[4,6] 段 4.2→3.4 保守降档 by-design，occ≥3 不放宽——用户裁决）；裸条件口径实测否决（271/306 required 泛滥）；软技能排除 required、P3 证据面板展示条件 req 三组成数（required JD 数/出现 JD 数/岗位总数）均为用户裁决 | §8.1/§14；《模块一设计》§3/§6；实施随代码落地：`server/config.py`、`server/services/aggregate.py`（`_map_importance` 混合判据 + `_collect_items` 增记 occ）、`competency_item.occurrence_json` 增 `occ` 键（存量兼容）、`server/test_m1_regression.py` 断言同步、P3 审核页证据面板（`web/src/views/admin/ModelReview.vue`） |
| 2026-09-07 | **模块一·jd_record.position_id 索引补建**：新增 `idx_jd_position`（DB 迁移 #16，`_DDL` 尾部同步登记；新库嗅探跳过、存量库启动自动补建，CREATE INDEX IF NOT EXISTS 幂等）。不涉及表结构/枚举/接口契约变更，纯性能修复 | 管理端列表接口（`GET /admin/positions`、`/positions/pending`）的 jd_count 相关子查询按 position_id 查 jd_record，无索引时全表 SCAN——ORDER BY temp B-tree 令子查询对全部岗位行执行（与 LIMIT 无关），957 岗实测每请求 ~0.7-1s；孤儿计数/详情页 JD 清单/聚合 parsed 计数同病（索引后实测主列表 0.93s→0.002s，420×） | §14；`server/db.py`（_DDL + 迁移 #16）、`server/test_migration.py`（断言 15→16 + 存量库补建用例）、`server/test_admin_pagination.py`（EXPLAIN QUERY PLAN 守护测试）；data/app.db 已执行 ANALYZE 刷新统计 |
| 2026-09-07 | **模块一·证据排除与审核页可下钻（§8.5 新增）**：①新增 `evidence_exclusion` 表（(position_id, jd_id, std_name, category, text) 语句粒度 + reason/excluded_by/excluded_at + status active/lifted 解除留痕）——排除为源头治理级软标记：不删数据、岗位维度持久、重聚合不丢失；②聚合 `_collect_items` 单点滤除：排除摘录不进快照/LLM#3/题库背景；**全排除的 JD-项不计 r/req 分子**（分母不变，用户裁决「全排除才影响频次」）→ importance/权重重算；全排除项从产物消失；③读侧 `GET /admin/positions/{id}/model`（draft/stalled）实时合并 `excluded: true` 条目，`PUT /admin/models/{id}` 剥离排除条目落库，question_gen 背景取第一条未排除证据；④新端点 POST/DELETE/GET `/admin/positions/{id}/evidence-exclusions`（幂等 upsert / lift 不删行 / 列表含 evidence_matched 失配标志）；⑤web-next 模型审核页：evidence 行 jd_id 可下钻 JD 工序留档抽屉（复用 PositionDetail 模式）、行级排除/恢复（仅 draft/stalled 可操作）、「显示已排除」开关、排除记录列表（全排除项恢复闭环）；web-next 证据面板 `String(raw)` 渲染 [object Object] 缺陷一并修复 | 管理员排查可疑证据（误抽取/治理合并带入/低质摘录）时只能整体删除能力项或容忍污染：硬删不可溯源、重聚合即漂回，且证据与 JD 留档断链无法核对（用户 2026-09-07 裁决：留痕软标记 / 源头治理级 / 语句级全排除才影响 r/req / 以 web-next 为准单端实施） | §6（表数 27→28）/ §8 / §8.1 / §8.5 / §14；实施：`server/db.py`、`server/services/{aggregate,question_bank}.py`、`server/api/admin/models.py`、`server/test_evidence_exclusion.py`（新增）+ `server/test_migration.py`、`web-next/src/views/admin/ModelReview.vue` + `web-next/src/api/index.js` |
| 2026-09-08 | **前端基线切换（登记性收口，不改接口/范围）**：`web-next/` 正式定为唯一活跃前端（Vue 3 + Vite + Vue Router + Pinia + axios；自研 `src/components/ui/` 组件与报告 SVG 雷达，不依赖 Element Plus / ECharts；dev 端口 5174、`/api` 代理 ：8000，`vite.config.js` 以 `API_TARGET` 可覆盖）。`web/`（Element Plus + ECharts 旧实现）正式退役留档：不再改动、不作为任何实施对象、CI 不再构建。§5 前端行同步改写实态。历史变更日志中 `web/src/...` 路径为当时实施位置记录，按写入时事实保留，不回写 | 用户 2026-09-07 指示 web-next 为准、web 弃用仅留档，2026-09-08 重申「web/ 正式退役、web-next/ 正式启用」并要求文档作说明以便后续调试 | §5/§14；`README.md`、`README_EN.md`（目录树/快速开始/CI/约定同步）；代码面接线（`server/main.py` 静态挂载路径、`.github/workflows/ci.yml` 构建目录、CORS 默认 Origin）另行走修理工单，不在本登记内 |
| 2026-09-08 | **模块二·题库生成任务可观测（§9.5 新增）**：`question_bank_task` 增列 total / done / current_item（存量库启动自动补列，幂等迁移；与 §8.4 差异登记：保留 QUEUED 态、已有 model_version、不设 trigger_source——触发路径仅 confirm / retry）；`generate_question_bank` 循环前写 total、循环内逐档推进 done/current_item（与该档题目写入同事务 commit）；`init_db` 启动将 RUNNING/QUEUED 残留行置 FAILED（error=进程重启中断）；error_msg 截断 200→2000。新增查询端点：`GET /admin/question-bank-tasks`（岗位×模型最新任务行列表，服务端分页 + status 过滤，含题量与 task_id）/ `…/{task_id}`（详情 + 同岗位历史任务行 + llm_trace 时间窗调用列表——不改表，按 ref_id=item_id + 任务时间窗关联）/ `…/{task_id}/questions`（题目表 + item×难度覆盖统计）。web-next 左侧栏新增「题库状态」页（`/admin/qbank`）：列表岗位×模型版本一行、生成中进度条 + 3s 轮询 + 进页认领 RUNNING、失败详情 error 全文 + retry 接线既有端点（列表/详情含 task_id 为其取值来源）；已落库详情 v1 只做统计概览 + 题目表（题干预览）。列表以任务行驱动，D-12 前有题无任务行遗留不入（当前库核验为 0） | 用户 2026-09-08 提出题库状态前端可见性单薄（仅 `/admin/todos` 聚合计数 + 页脚单条截断 error_msg），经讨论六项裁决：rubric list 绑定故障修复独立先行（不入本节）、进度方案取显式进度列、llm_trace 纳入详情、列表粒度岗位×模型版本、已落库详情 v1 只做统计概览+题目表、error 截断放宽 2000；议题与证据链见《临时讨论稿-题库状态页与生成可观测-20260908》。现行生成中间零状态落库、无任务查询端点（retry 端点前端从未接线）、重启 RUNNING 残留 + retry 仅认 FAILED + confirm 恒 409 致岗位永久锁死「生成中」 | §6/§9.5/§14；实施随代码落地：`server/db.py`（增列迁移）、`server/services/question_bank.py`（进度推进 + error 放宽）、`server/api/admin/`（任务查询端点）、`web-next/src/`（AdminShell + router + api/index.js + 新题库状态页，新分支实施）；模块二分册同步摘录未随本次写入（另行文档收口） |
| 2026-09-08 | **模块一·聚合 preferred 判据改 occ 基准**：§8.1 工序⑤ 的 preferred 从 `未达 required 且 r ≥ R_THRESHOLD(0.5)` 改为 `未达 required 且 出现 JD 数 ≥ PREFERRED_MIN_OCCURRENCE(3)`；R_THRESHOLD 退役（required 侧 r≥0.25 保留）。required 判据与其余不变 | SLAM算法岗人审发现「词条分布」preferred 恒为 0（4 版本全 10/0/153）——绝对 r 阈值与样本量耦合：jd=14 时 r≥0.5 ⇔ occ≥7（中段 [0.25,0.5) 8 项全判 required 无剩余人口），jd≤5 岗 r 恒不可达 0.5；全库 34 模型 15 个 preferred=0。全量模拟裁决（用户 2026-09-08）：P=3 平衡点——15→11 模型消除、SLAM v4 10/0/143→10/10/143、算法岗(131 JD) plus 池 Σw 97.8%→65.8%；P=2 否决（occ=2 噪声 + 软技能权重反超畸变） | §8.1/§14；《模块一设计》§3/§6；`server/config.py`（PREFERRED_MIN_OCCURRENCE 新增、R_THRESHOLD 删除）、`server/services/aggregate.py` `_map_importance`、`server/test_m1_regression.py` 断言同步 |
| 2026-09-08 | **模块二·旧版题库归档标记（§9.2 补规则 + §9.5 契约扩展）**：①`question_bank.status` 枚举扩展 `'archived'`（语义：被同岗位更新 confirmed 模型的题库取代、保留行支撑追溯；与 `'active'`/eval 占位 `'eval_seed'` 并存，代码校验不加 DB CHECK，无迁移）；②归档单元 = 模型（model_id），谓词「同岗位存在更新 confirmed 模型」，触发点两处——新模型 confirm 事务内（helper `archive_superseded_banks(position_id)` 单点）+ 会话到终态（completed/abandoned 含 6h sweep）补刀；③在途豁免：model_id 被 in_progress 会话引用时不归档（动态派题不断粮；已终态会话追溯不依赖 active——按 bank_question_id 直连的评分/报告/追问/答卷 join 均不读 status，为登记不变量，回归测试守恒）；④归档不依赖新库生成结果（新会话可用性由 model/version 绑定 + §10.4 readiness 决定，与归档正交）；⑤retry 护栏：岗位已存在更新 confirmed 模型时其 FAILED 任务 retry 返回 409（防已归档模型重跑复活 active 行）；⑥§9.5 列表端点增 `bank_status` 派生字段（使用中/已归档）+ `archived_count` 旁列 + `bank_status` 过滤参数，详情 coverage 与题目表在模型整体归档时后端自动放开 status 过滤（active+archived 全量，题目行带 status 列，无手动 toggle）；前端题库状态页同步（bank_status 筛选/标签/已归档行弱化）。不实施范围：不删行、不改选题/评分/报告逻辑、无 unarchive 入口、不迁移存量（豁免清零后由触发点自然归档，当前库实况首跑预期 0 行） | 用户 2026-09-08 提出议题「同岗位新题库落库后旧题库何为」——现状为无标记休眠（旧版行 status 恒 'active'，全库无 UPDATE 路径），且若直接作废旧版行将在途会话动态派题断粮、管理端无法区分使用中/已归档；四项裁决（在途豁免两段式接受、retry 409 同意、归档版题目表自动放开、回填无需决策）+ R1–R7 全稿认可，裁决链见《临时讨论稿-旧版题库归档标记-20260908》；登记不修的现存缺陷：eval virtual_candidates 题目查询不带 model/version 绑定（多版本并存时串版本，与本次正交） | §9.2/§9.5/§14；《模块二设计》分册同步摘录另行收口；实施随代码落地：`server/services/question_bank.py`（helper + confirm 接线）、`server/api/admin/models.py`（confirm 事务调用 + retry 409）、`server/api/admin/question_bank_tasks.py`（bank_status/archived_count/筛选/放开）、`server/services/timer.py` + `server/api/assessment.py`（终态补刀）、`web-next/src/views/admin/QbankStatus.vue` + `web-next/src/api/index.js`、新增归档测试（`server/test_bank_archive.py`——confirm 归档/在途豁免/终态与 sweep 补刀/retry 409/归档版全链路不回归/管理端契约/eval_seed 不受影响） |
| 2026-09-08 | **模块二·资格核验表单 facet 化 + 模块一·gate qualification 上游归并（§16.1 / §8.1 工序⑤）**：①**渲染层**——gate qualification 表单从「每 item 一个二值下拉」改为「facet 分组收集」：最高学历/院校档次/英语等级有序枚举单选（阈值比较派生，学历取措辞最低档宽读、985 满足 211）、数值断言数字输入（内嵌数字解析阈值量纲）、专业范围类与长尾未分类项勾选组（勾=是，不解析）；experience base 字段标签改「与岗位相关的工作年限（年）」判定不变；facet 答案在 submit 六维校验后、`_gate_check` 前由纯函数派生为逐 item 真值（True/false 命中现有真值表），gate 行结构/`_gate_check` 本体/aggregation 消费不动；快照 v1→v2（fields 增 facet 键与派生参数，白名单同步；已 open 的 v1 实例走旧链完投不迁移不重渲染，新渲染一律 v2）；payload_json 两段式（facet 原始答案 + 派生结果）审计可复现；报告 gate 段按 facet 折叠展示。②**聚合产物层**——facet 分类打标前移至 item 生成时（`competency_item` 增 `facet_key`/`facet_params_json` 两 nullable 列，无表结构破坏）；聚合工序⑤新增断言等价合并：同有序 facet 且派生参数一致的组合一为一行（std_name 取更窄措辞，evidences/jds/req_jds 并集），先于 LLM#3 分档；复合断言/专业类/长尾不参与。**语义边界**：只合「同断言不同措辞」（「本科及以上学历」与「全日制本科学历及以上」），不处理「跨 JD 词面近重复」（词典第 3 层管辖，冻结裁决有效）；存量 confirmed 模型不回填，重聚合刷版本自然带上。③**不做清单**：表单分段控件与提交摘要回显砍掉（保最小改动面）；专业类近重复管理端建议合并——缓 | 实测评暴露表单下拉无选项 bug（前端按 {value,label} 消费后端纯字符串数组 options，已独立先行修复 commit eb87edd）＋全库 gate qualification std_name 约 150 个、场均 12.1 个二值卡（最高 61），同一事实面（学历/院校/英语/专业范围/年龄/出勤）大量近重复措辞，候选人被迫逐条作答同质断言；用户两轮裁决（2026-09-08，裁决链见《临时讨论稿-资格核验表单facet化-20260908》）：第一轮——紧凑表单（非对话式抽取）/专业类勾选组不解析/长尾降级勾选组永不 block/experience 只改标签/P0 bug 允许独立先修；第二轮——A 类自动合并同意/打标前移（两 nullable 列）同意/B 类词面近重复缓/报告 facet 折叠保留 | §6（competency_item 两列，表数不变）/§8.1 工序⑤/§16.1/§14；《模块二设计》§10 同步摘录；实施随代码落地：`server/db.py`（两 nullable 列迁移）、`server/services/aggregate.py`（facet 分类纯函数 + 断言等价合并）、`server/services/forms.py`（facet 渲染 + 派生管线 + 快照 v2 + 白名单 facet 键）、`web-next/src/components/FormCard.vue`（facet 控件渲染，v1/v2 双形态兼容）、`web-next/src/views/assessment/Report.vue`（gate 段 facet 折叠）+ 相应测试（v2 渲染/派生五类/v1 旧链兼容/聚合合并/报告折叠） |
