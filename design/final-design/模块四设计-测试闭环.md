# 模块四设计：测试闭环、反馈与审计

> 本文档为《design/final-design/总设计文档.md》（唯一 SSOT）**第五部分的分块摘录**，聚焦模块四阅读。
> 状态：**功能已实现并通过 mock 回归**（统一 pytest 收集 + CI、eval 隔离、自动 bad case、短板断言均已接线，全量回归 237 绿，2026-09-07）；真实 LLM 质量验证（deepseek 放量）为余项。
> 维护规则：任何设计变更，先更新《总设计文档.md》（正文 + §14 变更日志），再动代码。
> 2026-09-07 与 SSOT v2.0（含 §14 变更日志至 2026-09-06）全量核对同步。

---

## 1. 三大标准落点

| 标准 | 落点 | 实现 |
|---|---|---|
| ①全链可审计 | report → session → model/version → question → message → score → trace，经 `trace_link` 统一关联表闭合 | trace 查看器（admin）+ trace_link |
| ②反馈可回溯 | 候选人逐分异议 → feedback（不改分）；admin 审核标记 bad case；双分背离自动候选 | feedback 表 + bad case 流 |
| ③有测评标准 | b+c 评测；eval/ 目录独立可迁移 | 评测运行器 + eval_results |

## 2. 评测契约

### 2.1 一致性测试（b）

- 固定 transcript 复跑，断言 **score_final 分差 ≤ 1**；
- 固定条件：model/provider/版本、temperature=0、rubric 与模型快照、（若适用）随机种子；
- score_live 属过程值，不在断言范围。

### 2.2 虚拟考生测试（c）

- 强/中/弱三档脚本端到端；
- 断言：总分排序（强 > 中 > 弱）**+ 短板定位符合预设 + required 覆盖 + 拒答/缺失状态 + 证据引用 + 报告状态**——短板断言已接入（`assert_weakness_identified`，报告生成纳入链路复用五子项结构断言；短板定位依赖 SSOT §21「短板=gap>0」约定，06-013 反转已同步）。

### 2.3 自动 bad case 候选

```
score_live 与 score_final 可比 且 |live − final| ≥ 阈值
（BAD_CASE_DIVERGENCE_THRESHOLD = 2，2026-09-06 裁决，config.py）
→ 创建 bad_case_candidate（排除：题目无效 / 模型不确定 / 系统错误 / 状态不可比）
→ 管理员审核确认，不自动改分（永不 UPDATE question_score 的 score 字段）
```

幂等：同 (session_id, item_id, question_id) 已有 pending 候选则跳过（报告版本化重复生成不重复建）。

### 2.4 数据隔离

- **eval 已使用独立临时数据库**（业务库只读快照 → gsd-eval- 临时库，结果经业务库连接写回 eval_results；后台任务异常落 failed 显式可见）；
- eval 运行留 eval_results；runs 参数需边界校验（当前 `runs: int = 3` 无显式上限钳制，**留待加固**）。

### 2.5 黄金集（a）

本期推迟（真实数据收集后另行排期），不作为本期验收项。

## 3. 测试与验收要求

- ~~统一 pytest 收集~~ **已落地**：`server/conftest.py` 统一注入 mock 三件套 + session 级临时库，全部测试 pytest 可收集；GitHub Actions CI（backend: `cd server && python -m pytest . -q`；frontend: build）为正式验收入口；
- **M1 回归为动态测评实施硬前置（已落地为 `test_m1_regression.py`）**（清单见《模块一设计》§8）；
- **候选人端完整 E2E 为 M5–M7 verified 必要条件**：注册→登录→选岗→session→start（PENDING_START 入场确认）→首题→作答/追问→表单→完成→评分→报告→异议，另测刷新恢复 / 断线重试（幂等）/ 超时 / 越权——已落地为 `test_e2e_full_chain.py`（TestClient 脚本化），浏览器端到端冒烟随真实 LLM 验收；
- 越权权限矩阵（candidate↔candidate 读写 session/report/form/feedback、admin 边界）为上线阻断测试（已落地为 `test_p0_security.py`）；
- 必测补充：幂等与并发、计时、迁移、SSE、畸形 JWT、上传限额、路由冲突（/jds/orphan）——均有专项测试文件覆盖；
- mock 回归只做工程回归，**不能替代真实 LLM 质量验证**（deepseek 接入后的放量验证为当前余项）；
- prototype 为视觉参考，静态原型不作功能/API 验收依据。

## 4. trace 与审计

- trace_link 关联角色：input / output / caused_by / scored / reported / source；唯一约束 (trace_id, entity_type, entity_id, link_role)；
- 旧 trace 的自由文本 ref_id 已在迁移时导入 trace_link（按 call_type 逐候选实体表探测，命中即拆 entity_type/entity_id 导为 link_role='source'，幂等 INSERT OR IGNORE；命不中保留原值不拆——弱关联不强造）；
- 管理员查看 trace 全文（含 prompt/response、候选人原文）须留 `REVIEW_ACCESSED` 访问审计；
- trace 演示期全量保留、不做脱敏（2026-09-06 裁决：利于验收审计；生产 PII 治理随真实上线再开——SSOT §31-5）。

## 5. 里程碑状态口径（四维）

每个里程碑记录 `implemented / contract_complete / verified / production_ready`。当前 M5–M7 主体 implemented、契约层大项已落地（全量回归 237 绿）；**verified 的最后缺口 = 真 LLM 质量验证 + 浏览器端 E2E 冒烟**；production_ready 待上线项（迁移/备份演练等，SSOT §5）。

## 6. 重构注意（模块四部分——2026-09-07 核对后更新）

- ~~自动 bad case 候选逻辑未实现~~ **已落地**（`_detect_bad_case_divergence`，报告生成钩子，阈值 2，幂等去重）；
- ~~eval 隔离~~ **已落地**（gsd-eval- 临时库 + `set_db_path` 切换 + 结果写回 eval_results）；
- ~~feedback 补审计字段~~ **已落地**（user_id / review_note / reviewer_id / reviewed_at；admin review 的 note 不再被丢弃）；
- eval 触发参数校验：runs 上限钳制**留待加固**；任务失败显式可见已落地（异常落 failed 行，异常字符串截断入库）；
- ~~后台评测任务静默~~ **已落地**（失败显式 failed 状态，不静默）。

## 7. 本文依据

《总设计文档.md》§5、§13.3、§22–§25、§27–§28、§31；变更日志条目 2026-09-06；差异登记见总文档 §30。
