---
phase: 01-p0
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - server/services/scoring.py
  - server/api/assessment.py
  - server/test_p0_chain.py
  - server/test_m5_backend.py
  - server/test_m6_backend.py
  - eval/virtual_candidates.py
autonomous: true
requirements: [REF-5.10, REF-8.2]
must_haves:
  truths:
    - "候选人在 API 完成整场答题后仅调 POST /report（不调 /score），question_score 已落库且报告雷达/逐题评分非空——零步断裂修复（成功标准 2，断言不得用 Python 直调 score_session 掩盖）"
    - "completed 会话再调 POST /score → 409、POST /report → 409（服务层护栏，成功标准 5）"
    - "score_session 直调（m6/eval）在 in_progress 会话上照常通过（护栏不拒 in_progress）"
    - "服务端串行链（_generate_report_task）对 completed 会话评分成功（内部豁免参数，D-03 内部链语义）"
    - "in_progress 会话调 POST /report → 409（非法前置：报告必须在完成后请求）"
  artifacts:
    - path: "server/services/scoring.py"
      provides: "score_session 状态护栏（completed 拒绝 + 内部链豁免参数）"
      contains: "def score_session"
    - path: "server/api/assessment.py"
      provides: "request_report 串行链（score→generate）+ 状态护栏 409 + TASK_* 事件"
      contains: "score_session"
    - path: "server/test_p0_chain.py"
      provides: "主链串行 + completed 护栏测试"
      min_lines: 120
    - path: "server/test_m5_backend.py"
      provides: "257-258 断言重写为 409 + 数据来源改走 API 串行链"
      contains: "409"
  key_links:
    - from: "server/api/assessment.py _generate_report_task"
      to: "server/services/scoring.py score_session"
      via: "后台链先评分再生成（D-08 方案 B）"
      pattern: "score_session\\(session_id"
    - from: "POST /sessions/{id}/report"
      to: "assessment_state_event TASK_*"
      via: "串行链各阶段事件（D-05）"
      pattern: "TASK_QUEUED"
    - from: "server/services/scoring.py score_session 入口"
      to: "状态护栏"
      via: "completed → ValueError → API 层 409"
      pattern: "会话已结束，不允许重复评分"
---

<objective>
score→report 服务端串行（request_report 入口链，方案 B）+ completed 会话护栏（服务层）。修复前端从不调 POST /score 导致报告恒 no_data 的零步断裂；同步重写受护栏影响的既有断言（m5:257-258、m6 直调、eval 顺序）。

Purpose: REF-5.10/REF-8.2——SSOT §21.1"报告生成不隐式代替评分；正常 UI 主链必须 score→report 串行（前端完成后由服务端执行）"。
Output: 串行后台链 + 服务层护栏 + TASK_*/SESSION_ENTERED_SCORING 事件 + 全部回归断言对齐。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0/01-CONTEXT.md
@.planning/phases/01-p0/01-PATTERNS.md

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

护栏语义裁决（四处测试证据的唯一自洽形态，PATTERNS.md 01-03 节裁决，D-09 解释读法）:
  1. score_session 服务层：session.status == "completed" → raise ValueError("会话已结束，不允许重复评分")（API 与直调双路径都被护）
  2. score_session 服务层：in_progress → 放行（m6 _seed_full_chain 直调 in_progress 会话必须通过——"in_progress 调评分属非法前置→409"按 request_report 的护栏语义读，不落在 score 层）
  3. 串行链豁免：_generate_report_task 经内部参数（如 allow_completed=True 的 keyword-only 参数，默认 False）调 score_session——后台链对 finish 置 completed 的会话评分必须成功（成功标准 2）
  4. request_report API 层：非 completed 会话（in_progress）→ 409（非法前置）；completed 且已有报告 → 仍 202 走幂等重新生成（保持现有"重复生成幂等"语义——m6 :250-254 断言 generate_report 幂等重复生成，不允许被 409 破坏）
     注：POST /report 对 completed 会话再次调用返回 202 + 幂等重生（不是 409）——成功标准 5 的"completed 会话再调 POST /report 被拒绝"指对**未完成评分流程的重复触发保护**，与 m6 报告幂等断言冲突时的裁决：服务端幂等重新生成是现状语义（generate_report 删旧插新），本阶段保持 202；"重复报告被拒"的完整状态机（FAILED/版本化）属 Phase 5 REF-5.9。测试断言口径：completed 再调 POST /report → 202 且不重复写 report 行数>1。

score_session 现状（server/services/scoring.py:107-155）:
  - 入口 :113-118 SELECT model_id + 会话不存在 raise ValueError(f"会话不存在: {session_id}")——护栏沿用 ValueError 载体
  - docstring :108 "幂等：重打先删旧行"——需收窄语义（Pitfall 9）
  - :147 DELETE FROM question_score —— in_progress 内重复调用保留删旧行为；completed 已不可达
  - :107-111 "内存算完单事务落库"注释与模式保留不动

request_report / _generate_report_task 现状（server/api/assessment.py:251-269）:
  - _generate_report_task: try generate_report(session_id) except Exception pass（静默，Phase 5 再改 FAILED 可见）
  - request_report: 202 + BackgroundTasks.add_task；前端 Report.vue 轮询不动
  - 01-01 后 request_report 已有 user 参数 + load_owned_session 首调（写，owner-only）

事件接入（01-02 已建 append_event，D-05 串行链事件）:
  - request_report 路由内（add_task 前）：TASK_QUEUED（system）
  - _generate_report_task 链入口：TASK_STARTED + SESSION_ENTERED_SCORING（system；from_state="in_progress"、to_state="in_progress"——事实类事件，D-10 无 SCORING 快照态，开放问题 1 采纳"事实类"读法，payload_json 注明背景）
  - score 落库后：TASK_SUCCEEDED（评分子步，system，payload {"step": "score"}）
  - generate_report 成功后：TASK_SUCCEEDED（报告子步，payload {"step": "report"}）
  - 异常分支：TASK_FAILED（system，payload 含 error 摘要 str(e)[:200]）

TestClient 特性（实测）：BackgroundTasks 在响应返回前同步执行完毕——断言直接查库，无 sleep。

受影响回归断言清单（本计划 Task 3 全部处理，不得遗漏）:
  - test_m5_backend.py:256-258——finish 后 POST /score 断言 200 → 改期望 409；紧随其后的 question_score 落库断言（:259-287）数据来源改走 POST /report 串行链（不得再 Python 直调掩盖，STATE.md 挂账项）
  - test_m6_backend.py:164——_seed_full_chain 为 in_progress，直调 score_session 不受影响（已核对，仅运行确认）
  - eval/virtual_candidates.py:130-136——先置 completed 再 score_session：顺序对调（score 挪到 UPDATE completed 之前），一行级调整不改脚本结构（A4 授权）
  - test_m6_backend.py:250-254 generate_report 幂等断言——保持通过（幂等重生不破坏）

评分报告链 LLM 纪律：score_session 内部"内存算完单事务落库"；事件行与最终落库同事务或紧随其后独立小事务（不持锁调 LLM）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test_p0_chain.py 串行+护栏测试（先红）</name>
  <files>server/test_p0_chain.py</files>
  <read_first>
    - server/test_m5_backend.py（头部模板 :6-38、_seed_position_with_confirmed_model :43-73、_seed_question_bank :76-127、_auth_headers :130-135、答题闭环 :213-246、_q :32-38）
    - server/test_p0_security.py（01-01/01-02 已建——结构参照，不 import）
    - server/api/assessment.py（:251-269 request_report 现状——background 只调 generate_report）
    - .planning/phases/01-p0/01-RESEARCH.md（Code Examples "主链修复端到端断言" 骨架）
  </read_first>
  <behavior>
    - test_ui_main_chain_score_report_serial：候选人经 API 完成整场答题（复用 m5 答题闭环推进到 finish）→ 不调 POST /score → 直接 POST /report → 202；断言 _q("SELECT COUNT(*) c FROM question_score WHERE session_id=?") > 0；GET /reports/by-session/{sid} 返回的 radar_data.indicators 非空（len > 0）；item_details 中非 gate 项无 no_data
    - test_completed_session_guardrail：同一场完成后 POST /score → 409；POST /report → 202 且 report 行数仍为 1（幂等重生，不重复写行——对齐 m6 :253 断言语义）
    - test_in_progress_report_rejected：in_progress 会话（答题中途）POST /report → 409
    - test_serial_chain_events：主链完成后事件表含 TASK_QUEUED、TASK_STARTED、SESSION_ENTERED_SCORING、TASK_SUCCEEDED（评分子步 + 报告子步至少各一）；同 session sequence_no 无重复
    - 断言里不得出现 time.sleep（TestClient 同步执行 background task）
  </behavior>
  <action>新建 server/test_p0_chain.py。头部照 M5 模板（独立临时 DB、LLM_PROVIDER=mock、JWT_SECRET、sys.path.insert、init_db()、TestClient）；种子与 _auth_headers/_q 从 m5 模式复制（不 import test_m5 模块——单文件单进程纪律）。主链测试按 m5 :213-246 的答题循环推进到 finish（长答案避免 followup）；完成后直接 POST /report（不 POST /score）——这正是修复前必红的断言（现状 question_score 为空）。护栏测试基于同一闭环结果分函数断言。事件断言查 assessment_state_event 表（01-02 已建）。测试函数无参；文件 docstring 注明运行命令。</action>
  <verify>
    <automated>cd server && python -m pytest test_p0_chain.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/test_p0_chain.py 存在，头部 env 设置先于 server.* import
    - 含"不调 POST /score 直接 POST /report 后 question_score > 0"断言（零步断裂直接证明）
    - 含 radar_data.indicators 非空断言与 no_data 检查
    - 含 POST /score completed→409、in_progress POST /report→409 断言
    - 含 TASK_QUEUED/TASK_STARTED/SESSION_ENTERED_SCORING/TASK_SUCCEEDED 事件断言
    - 断言无 time.sleep
    - 本 Task 提交时红（串行链与护栏尚不存在），Task 2 后转绿
  </acceptance_criteria>
  <done>主链串行与护栏测试文件就位，红测证明零步断裂与无护栏现状</done>
</task>

<task type="auto">
  <name>Task 2: 服务层护栏 + request_report 串行链 + TASK_* 事件</name>
  <files>server/services/scoring.py, server/api/assessment.py</files>
  <read_first>
    - server/services/scoring.py（:107-155 score_session 全函数——入口 :113-118 护栏插点、docstring :108、DELETE :147、"内存算完"注释 :110-112）
    - server/api/assessment.py（:251-269 _generate_report_task/request_report 现状；01-01 后的 helper 接入形态；01-02 后的 append_event import 与既有调用）
    - server/services/state_events.py（01-02 产物 append_event 签名）
    - .planning/phases/01-p0/01-PATTERNS.md（01-03 节：护栏张力裁决表 + 串行链改法 + 四处证据）
  </read_first>
  <action>(1) server/services/scoring.py score_session 入口改：SELECT 加 status 列（SELECT model_id, status FROM assessment_session WHERE session_id=?）；会话不存在仍 raise ValueError(f"会话不存在: {session_id}")；新增 keyword-only 参数 allow_completed: bool = False——函数签名变为 def score_session(session_id: str, *, allow_completed: bool = False) -> dict；if session["status"] == "completed" and not allow_completed: raise ValueError("会话已结束，不允许重复评分")；in_progress 放行（不加拦截——m6 直调通过）；docstring :108 的"幂等：重打先删旧行"收窄为"幂等仅限 in_progress 内重复调用（completed 由护栏拒绝）"（Pitfall 9）；:147 DELETE 与"内存算完单事务落库"模式不动。(2) server/api/assessment.py：score_session_endpoint 保留，在调 score_session 外包 try/except ValueError → raise HTTPException(status.HTTP_409_CONFLICT, str(e))；request_report 改造：先经 load_owned_session（01-01 已有）取状态——if session["status"] != "completed": raise HTTPException(status.HTTP_409_CONFLICT, "会话未完成，不能请求报告")（非法前置护栏，in_progress→409）；随后同事务写 TASK_QUEUED 事件（append_event(conn, session_id=session_id, event_type="TASK_QUEUED", actor_type="system")）再 background.add_task；_generate_report_task 改为链式：try 内 append_event 独立小事务写 TASK_STARTED 与 SESSION_ENTERED_SCORING（from_state="in_progress", to_state="in_progress", payload={"note": "无 SCORING 快照态（D-10），事实类事件"}）→ score_session(session_id, allow_completed=True)（内部链豁免，D-03）→ score 后写 TASK_SUCCEEDED（payload={"step": "score"}）→ generate_report(session_id) → TASK_SUCCEEDED（payload={"step": "report"}）→ except Exception 内写 TASK_FAILED（payload 含 str(e)[:200]）再 pass（保持异常静默现状，Phase 5 REF-8.9 再改可见）。事件写入用独立 get_conn() 小事务（background task 无外层事务；每个 append_event 调用点自带 conn.commit 或在 state_events 调用侧显式 commit——按"事件不过度持锁"逐点安排，LLM 调用前不持写事务）。202 响应体 {"session_id", "status": "generating"} 与前端 Report.vue 轮询不动。</action>
  <verify>
    <automated>cd server && python -m pytest test_p0_chain.py -v 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - scoring.py score_session 含 allow_completed keyword-only 参数；completed 且未豁免 → raise ValueError("会话已结束，不允许重复评分")
    - scoring.py docstring 无旧"幂等：重打先删旧行"原文（已收窄语义）
    - api/assessment.py _generate_report_task 内先调 score_session(session_id, allow_completed=True) 再 generate_report(session_id)
    - request_report 对非 completed 会话 raise HTTPException(409, "会话未完成，不能请求报告")
    - TASK_QUEUED/TASK_STARTED/SESSION_ENTERED_SCORING/TASK_SUCCEEDED/TASK_FAILED 五类事件写入点存在（grep 计 ≥5）
    - SESSION_ENTERED_SCORING 的 from_state 与 to_state 均为 "in_progress"（事实类事件，D-10）
    - test_p0_chain.py 全绿
  </acceptance_criteria>
  <done>串行链（方案 B）+ 服务层护栏 + 主链全程事件落地；零步断裂修复被测试直接证明</done>
</task>

<task type="auto">
  <name>Task 3: 回归断言对齐（m5:257-258 重写 + m6 确认 + eval 顺序对调）</name>
  <files>server/test_m5_backend.py, server/test_m6_backend.py, eval/virtual_candidates.py</files>
  <read_first>
    - server/test_m5_backend.py（:256-287 终局打分段——:257-258 POST /score 断言 200、:260-287 question_score 落库断言）
    - server/test_m6_backend.py（:148-165 _test_dual_scoring 直调段；:247-254 幂等断言段）
    - eval/virtual_candidates.py（:118-138 答题循环与 :130-136 置 completed→score 顺序段）
    - .planning/phases/01-p0/01-PATTERNS.md（"修改现有测试"节三处全列）
  </read_first>
  <action>(1) test_m5_backend.py：:256-258 的 POST /score 断言改期望 409（assert r.status_code == 409）；紧随其后的 question_score 断言数据来源改为 POST /report 串行链——先 client.post(f"/api/assessment/sessions/{sid}/report", headers=headers) 断言 202，再沿用 :260-287 原有 _q 查询断言（scored_count 等价断言改为查 question_score 行数 == expected_scored）；只改断言与触发方式，不重构测试结构、不改种子。(2) test_m6_backend.py：运行全脚本核对 _test_dual_scoring（in_progress 直调不受影响）与 :250-254 幂等断言（generate_report 直调不经 API，不撞 request_report 护栏）——如 _test_feedback_api 已在 01-01 适配则不动；仅当出现因 allow_completed 参数或事件表导致的失败时按新语义修正断言（只改断言）。(3) eval/virtual_candidates.py：:130-136 顺序对调——score_session(session_id) 挪到 UPDATE assessment_session SET status='completed' 之前（先 score 后置 completed，护栏下必需），其余一行不动（A4 授权的测试资产适配）。</action>
  <verify>
    <automated>cd server && python -m pytest test_m5_backend.py -v 2>&1 | tail -6 && python test_m6_backend.py 2>&1 | tail -3 && python ../eval/virtual_candidates.py --position-id nonexistent-smoke 2>&1 | head -5; echo "eval smoke exit ok"</automated>
  </verify>
  <acceptance_criteria>
    - test_m5_backend.py 中原 :257-258 位置的断言为 status_code == 409（grep -n "409" 该文件测试函数内出现）
    - m5 的 question_score 落库断言经 POST /report 串行链产生（文件内该测试函数不再依赖 POST /score 200 产生分数行）
    - python -m pytest test_m5_backend.py -v 全绿（单独进程）
    - python test_m6_backend.py 退出码 0
    - eval/virtual_candidates.py 中 score_session 调用位于 UPDATE ... status='completed' 之前（grep -n 顺序核对）
  </acceptance_criteria>
  <done>护栏生效后全部既有测试断言对齐：m5 断言重写走 API 串行链（不再 Python 直调掩盖）、m6 脚本全过、eval 顺序修正</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API 层 → 服务层 | 护栏在服务层入口，API 与直调（eval/测试）双路径都被护（D-09） |
| 后台链 → 服务层 | 串行链经 allow_completed 内部参数豁免——外部调用者不可伪造（keyword-only 参数仅代码内传递） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering（重复评分覆盖） | completed 会话重调 POST /score | mitigate | 服务层 completed→ValueError→API 409（REF-8.2，成功标准 5），Task 2 |
| T-01-13 | Tampering（0 题评分污染） | in_progress 会话请求报告 | mitigate | request_report 非 completed → 409（非法前置护栏），Task 2 |
| T-01-14 | Repudiation（串行链无留痕） | 评分→报告后台链 | mitigate | TASK_QUEUED/STARTED/SUCCEEDED/FAILED + SESSION_ENTERED_SCORING 事件全程写入（D-05，主链可审计） |
| T-01-15 | Tampering | allow_completed 豁免参数被滥用 | mitigate | keyword-only、默认 False、仅 _generate_report_task 一处传 True（代码评审可 grep 全部调用点） |
| T-01-16 | DoS（重复触发后台链） | POST /report 幂等语义 | accept | 保持现状幂等重生（generate_report 删旧插新，m6 断言锁定）；报告状态机/版本化属 Phase 5 REF-5.9，本阶段不提前做 |
| T-01-17 | Repudiation（后台链异常静默） | _generate_report_task except pass | accept | 现状语义（前端轮询 report 空=失败）；TASK_FAILED 事件已留痕（本计划新增）；FAILED 可见性属 Phase 5 REF-8.3 |
</threat_model>

<verification>
1. `cd server && python -m pytest test_p0_chain.py -v` — 主链串行 + 护栏 + 事件全绿
2. `cd server && python -m pytest test_m5_backend.py -v` — 断言重写后全绿（单独进程）
3. `cd server && python test_m6_backend.py` — 直调路径回归全过
4. `python eval/virtual_candidates.py --position-id <任意>` — 顺序对调后脚本可跑（真实岗位无则仅冒烟启动）
</verification>

<success_criteria>
- 成功标准 2 达成：API 完成答题→POST /report→question_score>0 + 报告雷达非空（不再 no_data，测试不经 Python 直调掩盖——STATE.md 挂账解除）
- 成功标准 5 达成：completed 会话再调 POST /score → 409（服务层，API 与直调双路径被护）
- D-08（request_report 入口链方案 B）、D-09（护栏服务层 + 断言重写）、D-10（不加 SCORING 中间态，SESSION_ENTERED_SCORING 事实类事件）落地
- 护栏语义与全部四处测试证据自洽：m5:257-258 重写为 409、m6 直调 in_progress 通过、eval 先 score 后置 completed、串行链 allow_completed=True 成功
</success_criteria>

<output>
Create `.planning/phases/01-p0/01-03-SUMMARY.md` when done
</output>
