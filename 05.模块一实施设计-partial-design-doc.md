# 模块一实施设计文档（Partial Design Doc）

- 创建日期：2026-08-30
- 状态：实施中（随迭代持续更新）
- 范围：模块一「岗位 JD 解析与胜任力模型构建」的**可落地实施设计**，含鉴权与词典管理
- 上游文档：《04.模块一设计-岗位JD解析与胜任力模型构建.md》（概念设计），本文档是其实施版
- 下游：模块二（测评生成）、模块三（画像评估）只消费本文档定义的输出契约

> **维护规则（重要）**
> 1. 任何设计变更、想法调整、范围增减，**先更新本文档（正文 + 变更日志），再实施**；
> 2. 变更日志条目格式：日期 / 变更内容 / 原因 / 影响面；
> 3. 与 04 概念设计的差异必须登记在 §2.2 差异表，答辩时用于说明取舍。

---

## 1. 背景与目标

将 04 的概念设计落地为**有可视化界面的可运行原型**：管理员完成「导入 JD → 自动解析 → 聚合 → 人审确认 → 发布胜任力模型」全流程，测评用户（candidate）在 P5 选择岗位进入测评（测评选段由模块二接管，本期预留接口）。

**本期交付边界**：模块一完整闭环 + 鉴权 + 词典管理 + 用户管理。模块二/三不实现，仅预留接口与占位页。

---

## 2. 已确认决策（2026-08-30 对齐拍板）

### 2.1 范围决策

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 浏览器插件 | **砍掉**。导入渠道 = 手动粘贴 + JSONL 文件上传；`source_type` 枚举保留 `plugin` 占位 |
| 2 | 归岗 LLM 判别称 | **简化**。job_title 未命中岗位/别名 → 直接创建 `pending_review` 新岗位；别名靠人审改归时积累，不做第 4 次 LLM 调用 |
| 3 | 黄金集 / 迭代闭环评估 | **本期不做**，原型完成后补充；预留 `/api/admin/eval` 接口位 |
| 4 | 权重 vs 评分 | **权重本期完整实现**（代码计算：类间 5.5:2:2:0.5 × 类内 1.0/0.6/0.3 归一，Σ=100%）；**给候选人打分属模块三**，本期只存 `gate` 标记与年限/学历数据，评分规则留接口 |
| 5 | 能力词典 | **做独立管理界面**（P6）；编辑即确认（`llm_pending` → `human`），不做审批流；被引用条目只能停用不能删除 |
| 6 | 鉴权 | **做完整鉴权**：JWT 双角色；**开放注册**（注册即 candidate）；admin 由种子脚本创建；用户管理界面本期实现 |
| 7 | JD 演示数据 | 本期先用临时样例，真实数据集后续共同收集 |
| 8 | 里程碑 | M1 鉴权+解析链 → M2 聚合+人审 → M3 岗位库/P5/词典/diff/用户管理；M4（黄金集、插件）保留后续做 |

### 2.2 与 04 概念设计的差异登记

| # | 04 原设计 | 本文档 | 原因 |
|---|---|---|---|
| D1 | 三渠道含浏览器插件 | 仅粘贴/上传 | 范围收敛，见决策 1 |
| D2 | 归岗未命中 → 轻量 LLM 判别称/新岗 | 直接建新岗位 pending_review | 见决策 2 |
| D3 | `llm_trace_json` 为 competency_model 表的一列 | 独立 `llm_trace` 表 | 单 JD 解析链（LLM#1/#2）的 trace 也要落库，统一一张表便于检索、答辩展示与未来黄金集比对 |
| D4 | competency_dict 无排除项、无状态列 | 增加 `exclusions_json`、`status(active/disabled)` | 词典管理页要编辑排除项；被模型引用的条目只能停用（决策 5） |
| D5 | 未涉及鉴权 | JWT + 双角色 + 用户表 | 两类操作者（管理员/测评用户）的落地，见决策 6 |
| D6 | competency_model 无确认人字段 | 增加 `confirmed_by`、`confirmed_at` | 人审留痕，「人工是唯一权威」的审计依据 |

### 2.3 原封不动继承 04 的部分

- 六步流水线（①接入 ②清洗 ③抽取 ④归一消歧 ⑤聚合 ⑥人审）与状态机
- 三次 LLM 调用职责契约（#1 抽取 / #2 消歧 / #3 等级裁决）
- importance 聚合双比率阈值（req≥50%→required 等，配置项）
- 权重计算公式（决策 4）
- 5 级 Dreyfus 锚点、门槛项二值判定语义
- 「人工是唯一权威」、confirmed 不被静默覆盖、diff 审阅流
- 每道工序中间产物全部落库

---

## 3. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.11+ / FastAPI / Uvicorn | 04 既定 |
| LLM 客户端 | `openai` SDK（`base_url` 切换 DeepSeek/通义），JSON 模式 | DeepSeek 注意 prompt 含 "json" 字样 |
| 默认模型 | DeepSeek（备选通义） | key 走环境变量 `.env` |
| 存储 | SQLite（单文件 `data/app.db`） | 不上图谱（04 已决策） |
| 鉴权 | `python-jose`(JWT) + `passlib[bcrypt]` | HS256，过期 12h |
| 前端 | Vue 3 + Vite + Element Plus + Vue Router + Pinia + axios | ECharts 本期备用（雷达图属模块三） |
| 中文匹配 | `pypinyin`（拼音首字母）+ `difflib`（编辑距离） | 词典候选 top10 用 |
| 部署形态 | `vite build` 产物由 FastAPI 挂载静态文件，单进程 `uvicorn` 起服务 | 答辩一条命令演示 |

---

## 4. 项目结构

```
26-summer-sem/
├── 02~05 文档（md）
├── server/                  # FastAPI 后端
│   ├── main.py              # 应用入口、路由注册、静态文件挂载
│   ├── config.py            # 环境变量、阈值/权重等可配置常量
│   ├── db.py                # SQLite 连接、建表
│   ├── core/
│   │   └── security.py      # JWT 签发/校验、角色依赖注入
│   ├── api/
│   │   ├── auth.py          # 注册/登录/me
│   │   ├── admin/           # /api/admin/*（需 admin 角色）
│   │   │   ├── positions.py # 岗位库、审核、聚合、版本/diff
│   │   │   ├── jds.py       # JD 导入、重解析、人工补录、改归
│   │   │   ├── dict.py      # 能力词典 CRUD/合并/停用
│   │   │   └── users.py     # 用户管理
│   │   └── assessment.py    # /api/assessment/*（登录即可）
│   ├── services/
│   │   ├── llm.py           # LLM 客户端封装（JSON 模式+重试+trace）
│   │   ├── pipeline.py      # 工序②清洗 ③抽取 ④消歧 编排
│   │   ├── assign.py        # 归岗逻辑
│   │   ├── aggregate.py     # 工序⑤ 频次统计+等级裁决+权重
│   │   └── prompts/         # 提示词模板（.py 常量，迭代留痕靠 llm_trace）
│   └── schemas.py           # Pydantic 模型（含 LLM 输出强 Schema）
├── web/                     # Vue 3 前端
│   └── src/
│       ├── api/             # axios 封装（token 拦截器）
│       ├── router/          # 路由 + 角色守卫
│       ├── stores/          # Pinia（auth store）
│       ├── views/
│       │   ├── Login.vue / Register.vue
│       │   ├── admin/       # P1 岗位库 P2 岗位详情 P3 模型审核
│       │   │                # P4 版本diff P6 词典 P7 用户管理
│       │   └── assessment/  # P5 岗位列表 + 模块二占位页
│       └── components/
├── data/                    # app.db（gitignore）
├── scripts/
│   └── seed_admin.py        # 首个 admin 种子脚本（读环境变量）
└── .env                     # LLM key、JWT secret、admin 初始口令（gitignore）
```

`.gitignore` 增补：`data/`、`.env`、`node_modules/`、`__pycache__/`、`web/dist/`。

---

## 5. 数据库设计（DDL）

在 04 六表基础上，增 `user` 表与 `llm_trace` 表，词典表加列（差异 D3/D4/D6）。

```sql
-- 用户（鉴权）
user(
  user_id      TEXT PRIMARY KEY,
  username     TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role         TEXT NOT NULL CHECK(role IN ('admin','candidate')),
  is_active    INTEGER NOT NULL DEFAULT 1,
  created_at   TEXT NOT NULL
)  -- user_id 预留为模块三测评记录的外键

-- 岗位与 JD（同 04）
position(position_id TEXT PRIMARY KEY, name TEXT NOT NULL,
         status TEXT NOT NULL CHECK(status IN ('pending_review','active')),
         created_at TEXT NOT NULL)
position_alias(alias_id TEXT PRIMARY KEY, position_id TEXT NOT NULL REFERENCES position,
               alias TEXT UNIQUE NOT NULL)

jd_record(
  jd_id TEXT PRIMARY KEY, position_id TEXT REFERENCES position,  -- 可归空=待归属
  job_title TEXT, company TEXT,
  source_type TEXT NOT NULL CHECK(source_type IN ('paste','file','plugin')),
  raw_text TEXT NOT NULL, cleaned_text TEXT,
  raw_items_json TEXT,          -- 工序③产物落库
  std_items_json TEXT,          -- 工序④产物落库
  low_confidence INTEGER NOT NULL DEFAULT 0,   -- 清洗时要求块为空/过短
  status TEXT NOT NULL CHECK(status IN ('imported','parsing','parsed','failed')),
  error_msg TEXT,               -- failed 时记录原因
  created_at TEXT NOT NULL
)

-- 模型（版本化快照）+ 明细
competency_model(
  model_id TEXT PRIMARY KEY, position_id TEXT NOT NULL REFERENCES position,
  version INTEGER NOT NULL,
  status TEXT NOT NULL CHECK(status IN ('draft','confirmed','stalled')),
  model_json TEXT NOT NULL,
  confirmed_by TEXT REFERENCES user, confirmed_at TEXT,   -- 差异 D6
  created_at TEXT NOT NULL,
  UNIQUE(position_id, version)
)
competency_item(
  item_id TEXT PRIMARY KEY, model_id TEXT NOT NULL REFERENCES competency_model,
  std_name TEXT NOT NULL,
  category TEXT NOT NULL CHECK(category IN ('hard_skill','soft_skill','experience','qualification')),
  required_level INTEGER,              -- 1~5，门槛项为 NULL
  importance TEXT CHECK(importance IN ('required','preferred','plus')),
  weight REAL, years REAL,             -- years 仅 experience 年限项
  gate INTEGER NOT NULL DEFAULT 0,     -- 门槛项标记（模块三二值判定依据）
  level_reason TEXT,                   -- LLM#3 裁决理由
  occurrence_json TEXT,                -- {"r":0.9,"req":0.63}
  evidence_json TEXT                   -- [{"jd_id":"...","text":"..."}]
)

-- 能力词典（差异 D4）
competency_dict(
  std_name TEXT NOT NULL, category TEXT NOT NULL,
  definition TEXT, exclusions_json TEXT,   -- 排除项列表（消歧锚点）
  aliases_json TEXT,                       -- 别名列表
  created_by TEXT NOT NULL CHECK(created_by IN ('llm_pending','human')),
  status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','disabled')),
  created_at TEXT NOT NULL, updated_at TEXT NOT NULL,
  PRIMARY KEY(std_name, category)
)

-- LLM 调用留痕（差异 D3）
llm_trace(
  trace_id TEXT PRIMARY KEY,
  call_type TEXT NOT NULL CHECK(call_type IN ('extract','disambiguate','aggregate_level')),
  ref_id TEXT NOT NULL,        -- jd_id 或 model_id
  attempt INTEGER NOT NULL,    -- 第几次尝试
  prompt TEXT NOT NULL, response TEXT,
  success INTEGER NOT NULL, error TEXT,
  created_at TEXT NOT NULL
)
```

---

## 6. 鉴权设计

| 项 | 设计 |
|---|---|
| 角色 | `admin`（全部 /api/admin/* + /api/assessment/*）；`candidate`（仅 /api/assessment/*） |
| 注册 | **开放注册**，`POST /api/auth/register` 固定写入 `role=candidate`，接口层杜绝注册出 admin |
| 首个 admin | `python -m scripts.seed_admin`，读 `ADMIN_USERNAME` / `ADMIN_PASSWORD` 环境变量，幂等（已存在则跳过） |
| Token | JWT HS256，`sub=user_id, role, exp=12h`；前端存 localStorage，axios 请求拦截器注入 `Authorization: Bearer` |
| 前端守卫 | Vue Router `meta.role` 校验；未登录 → /login；角色不符 → 各自首页 |
| 后端校验 | FastAPI 依赖注入：`require_admin` / `require_login`，401/403 语义区分 |
| 停用账号 | admin 在用户管理页置 `is_active=0`；校验 token 时同步检查 |

---

## 7. API 契约

### 7.1 认证（公开）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/auth/register | 开放注册 candidate。body: `{username, password}` |
| POST | /api/auth/login | → `{token, user:{username, role}}` |
| GET | /api/auth/me | 当前用户信息（前端刷新后恢复会话） |

### 7.2 管理端（/api/admin/*，需 admin）

**岗位库 / 审核（P1）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/admin/positions | 岗位列表：名称/状态/JD 数/当前模型版本与状态；响应带 `todos`（待审岗位数、stalled 数、待归属 JD 数） |
| POST | /api/admin/positions/{id}/review | 新岗位审核：`{action:"approve"}` → active；`{action:"reject"}` → 岗位撤销，其下 JD 归 NULL 进待归属 |
| POST | /api/admin/jds/{id}/reassign | 待归属 JD 手动改归：`{position_id}` |

**JD 导入与单 JD 链路（P2）**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/admin/jds/import | 粘贴导入。body: `{jd_text, company?}`；**不要求选岗位**（归岗全自动） |
| POST | /api/admin/jds/import-file | JSONL 批量上传：`{"id","position"?,"company","jd_text"}` 每行一条 |
| GET | /api/admin/positions/{id}/jds | 该岗位 JD 列表（来源/时间/状态/low_confidence） |
| GET | /api/admin/jds/{id} | 单 JD 详情：原文/清洗结果/raw_items/std_items/错误信息（工序留档查看） |
| POST | /api/admin/jds/{id}/reparse | failed 重跑解析链 |
| POST | /api/admin/jds/{id}/manual-items | failed 人工补录 std_items |

**聚合 / 模型 / 版本（P3/P4）**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/admin/positions/{id}/aggregate | 触发聚合（生成新 draft；已有 confirmed 时不覆盖，走 diff） |
| GET | /api/admin/positions/{id}/model | 当前生效模型（draft 优先，否则最新 confirmed） |
| PUT | /api/admin/models/{id} | 人审编辑草稿：整份 model_json 替换（改名/调级/调权/增删项/改类间配比） |
| POST | /api/admin/models/{id}/confirm | 确认 → status=confirmed，version=该岗位 max+1，记 confirmed_by/at |
| POST | /api/admin/positions/{id}/retry-level | stalled 处理：`{action:"retry"}` 重试 LLM#3 或 `{action:"manual", levels:{...}}` 手动定级 |
| GET | /api/admin/positions/{id}/versions | 版本历史 |
| GET | /api/admin/models/{new_id}/diff?against={old_id} | 逐项 diff：`[{std_name, change:"added/removed/field", detail}]`，供 diff 审阅页 |

**能力词典（P6）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/admin/dict | 列表/筛选（类目、来源、状态、关键字） |
| POST | /api/admin/dict | 新增标准名（created_by=human） |
| PUT | /api/admin/dict/{std_name}/{category} | 编辑定义/排除项/别名；保存即 `created_by=human`（编辑即确认）；别名校验不与标准名/别名冲突 |
| POST | /api/admin/dict/merge | 合并：`{from:{std_name,category}, to:{std_name,category}}`，from 的标准名并入 to 的别名 |
| DELETE | /api/admin/dict/{std_name}/{category} | 被 competency_item 引用 → 仅允许 `status=disabled`；无引用才可真删 |

**用户管理（P7）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/admin/users | 用户列表 |
| PATCH | /api/admin/users/{id} | 启用/停用、重置密码 |
| POST | /api/admin/users | 管理员手动建号（补注册之外的通道） |

### 7.3 测评端（/api/assessment/*，登录即可）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/assessment/positions | 仅 `active` 且存在 confirmed 模型的岗位 |
| GET | /api/assessment/positions/{id}/model | confirmed 模型快照（模块二的输入契约，本期用于占位页展示） |

> 预留：`POST /api/assessment/sessions`（模块二创建测评会话）——本期返回 501 + 文档说明，接口位保留。

---

## 8. 解析流水线实现要点

### 8.1 异步策略

LLM 调用耗时秒级~十秒级，**不在请求内同步执行**：

- 导入接口落库（status=imported）后立即返回，解析链交给 `asyncio` 后台任务；
- 前端列表轮询（5s）刷新状态：`imported → parsing → parsed / failed`；
- 聚合同理：触发后立即返回，模型状态 `draft/stalled` 由轮询更新。

### 8.2 工序实现约束

| 工序 | 要点 |
|---|---|
| ② 清洗 | 纯规则：按常见标题词（"公司介绍/福利待遇/加入我们"等）切段，职责块/要求块分离；要求块为空或 <30 字 → `low_confidence=1` 但**继续流程**；无 LLM 兜底 |
| ③ 抽取 | LLM#1，JSON 模式 + 强 Schema（items[]：name/category/required_level/importance/evidence/years?/job_title）；校验失败带错误信息重试 ×2 → failed；四条硬约束（原子化/抄录证据/三档措辞映射/1–5 级措辞映射）写入 prompt |
| 归岗 | job_title 规范化（去空格/大小写/常见后缀）→ position 精确匹配 → 别名表 → 未命中建 `pending_review` 岗位（决策 2）；`pending_review` 岗位不聚合、不对测评端可见 |
| ④ 消歧 | 词典候选 = 同类目过滤 + 编辑距离/拼音首字母 top10 → LLM#2 裁决同义/包含/重合；校验失败重试 ×2 → 降级代码精确去重；新标准名写词典 `llm_pending`；词典为空时跳过 LLM#2 |
| ⑤ 聚合 | 纯代码频次（r、req）→ importance 阈值映射（配置项）→ level 冲突交 LLM#3（无自动取众数后门）→ 权重纯代码（决策 4）；LLM#3 重试 ×2 仍败 → 模型 `stalled`，P1 待办 |
| ⑥ 人审 | PUT 编辑草稿 → confirm 升版本；confirmed 后再聚合出新 draft → diff 审阅流，逐项「保留人工值/采用新值/再编辑」 |

### 8.3 提示词管理

- prompt 模板集中放 `server/services/prompts/`，每个模板带版本注释；
- 每次调用的 prompt + response 落 `llm_trace`（答辩「提示词工程迭代」素材）；
- LLM#1 的 few-shot 示例在 M1 实现时起草，定稿后**追加到本文档附录 A**。

### 8.4 可配置常量（config.py）

`CATEGORY_RATIO = 5.5:2:2:0.5`、`IMPORTANCE_COEF = {required:1.0, preferred:0.6, plus:0.3}`、`REQ_THRESHOLD = 0.5`、`R_THRESHOLD = 0.5`、`LLM_RETRY = 2`、`CLEAN_MIN_REQ_LEN = 30`。

---

## 9. 前端页面

| 路由 | 页面 | 角色 | 说明 |
|---|---|---|---|
| /login, /register | 登录/注册 | 公开 | Element Plus 表单，登录后按角色跳转 |
| /admin/positions | P1 岗位库 | admin | 顶部待办条（新岗位审核/stalled/待归属）+ 岗位卡片列表 + [导入JD] 弹窗（粘贴/文件两个 Tab） |
| /admin/positions/:id | P2 岗位详情 | admin | JD 列表（状态轮询）、单 JD 工序留档抽屉、[重新聚合] |
| /admin/positions/:id/review | P3 模型审核 | admin | 左右双栏：左证据面板（JD 原文高亮 + 出现率 + LLM#3 理由），右模型树（类间配比可拖动、项可编辑/删除）+ Σ=100% 校验 + [确认模型] |
| /admin/positions/:id/versions | P4 版本历史/diff | admin | 版本列表 + diff 审阅（逐项标记 新增/删除/字段变更，逐项三选一） |
| /admin/dict | P6 能力词典 | admin | 表格（标准名/类目/定义/排除项/别名/来源标签/状态）+ 编辑抽屉 + 合并对话框；`llm_pending` 标签醒目 |
| /admin/users | P7 用户管理 | admin | 用户表格 + 建号/停用/重置密码 |
| /assessment/positions | P5 岗位列表 | 登录 | 卡片式岗位选择（仅 active + confirmed） |
| /assessment/positions/:id | 测评占位页 | 登录 | 展示该岗位 confirmed 模型快照 + 「测评功能由模块二提供」占位 |

视觉基调：Element Plus 默认主题 + 统一页头（Logo/角色/退出）+ 卡片式布局；状态色约定——pending_review 橙、failed 红、stalled 红、draft 蓝、confirmed 绿。

---

## 10. 里程碑

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1** 鉴权 + 单 JD 解析链 | 项目骨架、建表、注册/登录/守卫/种子 admin、①~④ + LLM#1/#2 + trace、P2 雏形（导入 + JD 列表轮询） | 注册登录可用；粘贴一条真实 JD → 看到 std_items，failed 有错误信息 |
| **M2** 聚合 + 人审 | ⑤（统计/LLM#3/权重）、⑥ P3 审核页、状态机全流转、stalled 处理 | 同岗位多条 JD → 草稿 → 编辑 → 确认 v1；LLM#3 失败能 stalled 并手动定级恢复 |
| **M3** 外围页面 | P1 岗位库、P5 测评视角 + 占位页、P6 词典管理、P4 版本 diff、P7 用户管理 | 管理员全流程 + candidate 注册选岗位；词典编辑影响下次消歧 |
| M4（保留，本期不做） | 黄金集 eval、浏览器插件、真实 JD 数据集收集导入 | 原型验收后另行排期 |

---

## 11. 开放问题（实现阶段定）

1. LLM#1 few-shot 具体示例（M1 起草，定稿入附录 A）；
2. 聚合自动触发时机（当前：JD parsed 后若岗位 active 自动触发一次 + P2/P3 手动按钮；若演示节奏不顺再调）；
3. 词典候选 top10 的匹配阈值（编辑距离与拼音权重，M1 调参）；
4. 清洗标题词表（用真实 JD 样本迭代，M1 先给基础版）。

---

## 12. 变更日志

| 日期 | 变更 | 原因 | 影响面 |
|---|---|---|---|
| 2026-08-30 | 文档创建；登记决策 1~8 与差异 D1~D6 | 实施前对齐 | 全文 |
